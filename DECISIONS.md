# บันทึกการตัดสินใจ

> **ไฟล์นี้สำคัญกว่าที่คิด** เพราะ `PROJECT.md` บอกว่า *ทำอะไร*
> แต่ไฟล์นี้บอกว่า *ทำไมถึงไม่ทำอย่างอื่น* ซึ่งเป็นส่วนที่หายง่ายที่สุด
>
> เมื่อโปรเจคเสร็จ ไฟล์นี้จะกลายเป็นหัวข้อ **"ทำไมถึงเลือกแบบนี้"** ใน README
> ซึ่งเป็นส่วนที่คนสัมภาษณ์ให้ค่าสูงกว่าตัวโค้ด

รูปแบบ: ตัดสินใจอะไร / ปฏิเสธอะไร / เพราะอะไร / อะไรทำให้เปลี่ยนใจได้

---

## D01 — ใช้ PostgreSQL ไม่ใช่ MySQL

**ปฏิเสธ:** MySQL / MariaDB

**เหตุผล:** ตลาดงานไทยมีทั้งสองพอๆ กัน จำนวนงานตัดสินไม่ได้
ตัวตัดสินคือ **PostgreSQL ให้ ground truth ที่โปรเจคนี้ต้องใช้**
`pg_stat_activity` มี state `idle in transaction` ตรงๆ, `pg_locks` ดูห่วงโซ่การรอได้,
`EXPLAIN (ANALYZE, BUFFERS)` ละเอียดกว่า

**เปลี่ยนใจเมื่อ:** เล็งบริษัทที่ใช้ MySQL เป็นหลักแบบเจาะจงแล้ว

**หมายเหตุ:** หลักการ (isolation level, lock ordering, MVCC) ย้ายข้าม RDBMS ได้
รายละเอียดย้ายไม่ได้ — ต้องพูดให้ชัดตอนสัมภาษณ์ว่ายังไม่เคยใช้ Oracle

---

## D02 — Docker ไม่ใช่ติดตั้งลงเครื่อง

**ปฏิเสธ:** ติดตั้ง PostgreSQL ลง Windows โดยตรง

**เหตุผล:** ต้องจงใจตั้งค่าให้ DB เปราะ และต้องพัง/สร้างใหม่บ่อยมาก
ที่สำคัญกว่า: **คนอื่นต้องทำซ้ำผลได้** ไม่งั้นตัวเลขที่รายงานไม่มีน้ำหนัก

---

## D03 — ข้อมูล generate เอง ไม่ใช่ข้อมูลจริง (เฉพาะชั้น fault)

**ปฏิเสธ:** ใช้ข้อมูลจริงตั้งแต่ต้น

**เหตุผล:** fault injection ต้อง**คุมตัวแปรได้** ถ้าคุมความเบ้ไม่ได้
จะพิสูจน์ไม่ได้ว่า planner ประมาณผิดเพราะอะไร
ข้อมูลชุดนี้คือ **อุปกรณ์ทดลอง** ไม่ใช่ตัวแทนข้อมูลจริง

**ข้อควรระวัง:** ปกติ "seed data ปลอม" เป็นสัญญาณไม่ดีในพอร์ตโฟลิโอ
ต้องเขียนเหตุผลนี้ไว้ใน README ให้ชัด ไม่งั้นคนอ่านเข้าใจผิด

**ข้อมูลจริงยังต้องมี** แต่ไปอยู่ชั้นค้นหาภาษาไทย (OpenStreetMap / data.go.th)
เพราะชั้นนั้นวัด "ผลลัพธ์ที่มนุษย์พอใจ" ซึ่งข้อมูลปลอมแทนไม่ได้

---

## D04 — ทดสอบลึก 1 ตัว + พิสูจน์ข้ามตัว ไม่ใช่ทดสอบ 5 ตัวเท่าๆ กัน

**ปฏิเสธ:** วัด recall เต็มรูปแบบกับ Qdrant, Milvus, Weaviate, Pinecone, FAISS

**เหตุผล:** 5 container 5 API = โปรเจคไม่จบ และได้แค่บทความเทียบยี่ห้อที่หมดอายุใน 3 เดือน

**ทางออก 3 ชั้น:**
1. pgvector — fault ครบ 12 ข้อ
2. Qdrant — รันเฉพาะ Q01 เพื่อพิสูจน์ว่า recall collapse ไม่ใช่บั๊กเฉพาะยี่ห้อ
3. ที่เหลือ — ตารางจับคู่จากเอกสารทางการ ไม่วัด

ชั้น 2 คือคำตอบของ **"นี่มันบั๊กของ pgvector หรือเปล่า"** ซึ่งจะโดนถามแน่นอน

---

## D05 — ตัด Pinecone ออกจากชั้นที่วัด

**เหตุผลที่ถูกต้อง:**
1. **fault ชั้นติดตั้งหายไปทั้งหมด** — I01, I02, I04, I05 เกิดตอนสร้าง index เอง
   พอเขาจัดการให้ ก็ไม่มีอะไรให้ฉีด
2. **ทำซ้ำไม่ได้** — เขาอัปเดตระบบเมื่อไหร่ ผลเปลี่ยนโดยเราไม่รู้ตัว
3. ต้องจ่ายเงิน คนอื่นทำซ้ำฟรีไม่ได้

**เหตุผลที่ผมเคยให้แล้วผิด:** เคยบอกว่า "ตรวจข้างในไม่ได้ = ไม่มี ground truth"
ผิด — ground truth มาจาก vector ของเราเอง คำนวณ exact search ข้างนอกได้

**ข้อสังเกตที่มีค่า:** กับบริการแบบเช่าใช้ เรา "ปิด index เพื่อหาเฉลย" ไม่ได้
แปลว่าผู้ใช้ไม่มีทางรู้ว่า recall ตัวเองเท่าไหร่ →
**ความล้มเหลวเงียบไม่หายไปเพราะจ่ายเงิน แต่ตรวจจับยากขึ้น**
ประโยคนี้ควรอยู่ในบทนำของรายงาน

---

## D06 — เลื่อนการใช้ embedding มาจัดกลุ่ม error ออกไป

**ปฏิเสธ (ชั่วคราว):** ใช้ vector similarity จัดกลุ่ม error log

**เหตุผล:** ตอนนี้มี fault 12 ข้อที่รู้เฉลยและจับด้วยกฎได้ **ไม่มีอะไรให้จัดกลุ่ม**
ใส่ตอนนี้จะอ่านออกมาเป็น "ใส่ AI เพราะอยากใส่"

**ลำดับที่ถูก:** normalize + hash ก่อน (ลบ pid ลบตัวเลข ลบชื่อตาราง)
→ วัดว่าพลาดตรงไหน → **ถ้าไม่พลาด ก็ไม่ต้องใช้ embedding เลย**
ถ้าพลาด จะมีเหตุผลจับต้องได้ว่าทำไมต้องใช้

---

## D07 — ตรวจ bug ของ vector search เอง ≠ ใช้ AI มาช่วยวินิจฉัย

**บันทึกไว้เพราะเคยเข้าใจผิดกันในบทสนทนา**

- ใช้ embedding **เป็นเครื่องมือวินิจฉัย** → ยังไม่จำเป็น (ดู D06)
- ตรวจ **bug ของ ANN search เอง** → ทำ และเป็นแกนของโปรเจค

ถ้าโปรเจคอ้างว่าตรวจระบบข้อมูล**ปัจจุบัน** แล้วไม่แตะ vector search
ก็เท่ากับตรวจระบบของปี 2020

---

## D08 — ใช้ buffers เป็นตัวชี้วัดหลัก ไม่ใช่เวลา

**เหตุผล:** buffers นิ่ง เวลาแกว่งตามเครื่องและตามภาระงานขณะนั้น

**หลักฐานจากการทดลองจริง:** planner คิดว่าการใช้ parallel worker คุ้ม
(cost 4785.95 vs 4780.08) แต่เวลาจริงเท่ากัน (14.2 vs 14.5 ms)
→ **อย่าเชื่อ cost ให้เชื่อ actual — และ actual ที่นิ่งที่สุดคือ buffers**

---

## D09 — เกณฑ์วัดผลต้อง commit ก่อนมีตัวเลข

**เหตุผล:** ถ้าตั้งเกณฑ์ทีหลัง = เลือกเกณฑ์ที่เข้าข้างผลตัวเอง
ชุด query 200 ข้อ + seed + สูตร recall ต้องอยู่ใน repo ก่อนรันวัดครั้งแรก

---

## D10 — ชื่อโปรเจค QuietFail

**ปฏิเสธ:** `pgvector-bench`, `pg-fault-lab` (ชื่อเดิม)

**เหตุผล:** ตั้งชื่อตามวิทยานิพนธ์ ไม่ใช่ตามเทคโนโลยี
ชื่อที่ผูกกับเครื่องมือจะล้าสมัยทันทีที่เปลี่ยนเครื่องมือ

**ยังไม่ได้ทำ:** ค้น GitHub / npm ว่าซ้ำใครไหม

---

# ความผิดพลาดที่เกิดขึ้นจริง — เก็บไว้เตือน

> ส่วนนี้มีค่าสองอย่าง: กันพลาดซ้ำ และเป็นวัตถุดิบของหัวข้อ
> **"สิ่งที่ลองแล้วไม่ได้ผล"** ใน README ซึ่งหายากมากในพอร์ตนิสิต

## E01 — "มองหา Seq Scan" หยาบเกินไป

ผู้ช่วย AI เคยแนะนำให้ "มองหา Seq Scan" ซึ่งทำให้เข้าใจว่า Seq Scan = ไม่ดี

**ของจริง:** `merchant_id = 1` คืน 12% ของตาราง → Seq Scan **คือทางเลือกที่ถูกต้อง**
การไล่ index แล้วกระโดดหยิบ 23,724 ครั้งจะช้ากว่า

**เกณฑ์ที่ถูก:** Seq Scan เป็นปัญหาเมื่ออ่านเยอะเพื่อได้น้อย
(เช่น 2,280 buffers เพื่อ 18 แถว) ไม่ใช่เพราะเป็น Seq Scan

## E02 — ทายจำนวนแถวผิด

ผู้ช่วยทายว่า `merchant_id = 4999` จะได้ราว 10 แถว **ของจริง 18** (พลาด 1.8 เท่า)

คำนวณจากสูตร `power(random(), 4)` ได้ แต่ค่าจริงขึ้นกับการสุ่ม
→ ยืนยันว่าต้องวัด ไม่ใช่คำนวณบนกระดาษ

## E03 — บอกเหตุผลตัด Pinecone ผิด

ดู D05 — เคยอ้างว่าไม่มี ground truth ที่พิสูจน์ได้ ซึ่งไม่จริง

## E04 — ตอบผิดประเด็นเรื่อง AI clustering

ผู้ใช้ถามถึง **bug ของ ANN** แต่ผู้ช่วยตีความว่าจะเอา AI **มาเป็นเครื่องมือวินิจฉัย**
แล้วเถียงว่ายังไม่จำเป็น → ผู้ใช้ถูก ผู้ช่วยผิด (ดู D07)

## E05 — ให้คำสั่ง bash/make กับผู้ใช้ Windows

เสียเวลาไปหลายรอบ ตอนนี้มี `WINDOWS.md` แล้ว
**คำสั่งทุกอันต้องเป็น PowerShell**

## E06 — ไม่ได้เตือนเรื่องโครงสร้างโฟลเดอร์ตั้งแต่แรก

ผู้ใช้ดาวน์โหลดไฟล์ทีละอันจนกองแบนหมด ทำให้ Docker พยายามสร้างโฟลเดอร์เอง
แล้วชนสิทธิ์ของ `C:\Users\` → ได้ error `Access is denied` ที่ชี้ไปคนละเรื่อง

**บทเรียนที่ตรงกับโปรเจคพอดี:** error บอกว่า "ไม่มีสิทธิ์" แต่ต้นเหตุคือ "ไฟล์ไม่อยู่"

---

## D11 — ทบทวน CHANGELOG + README ของ pgvector (2026-07-26)

แหล่ง: github.com/pgvector/pgvector · ดูรายละเอียดเต็มใน `EVIDENCE.md`

- 0.8.0 (2024-10-30) เพิ่ม iterative index scans → กระทบ Q03
- 0.4.2 (2023-05-13) เพิ่ม notice ตอนสร้าง index ด้วยข้อมูลน้อย
  → I02 ไม่ใช่ "เงียบ" แต่เป็น "เตือนแล้วไม่มีใครอ่าน"
- README ระบุเองว่า approximate index **เปลี่ยนคำตอบ** ไม่ใช่แค่ความเร็ว
  → Q01 ได้การรับรองจากผู้พัฒนา ไม่ต้องพิสูจน์ว่ามีจริงอีก

**เปลี่ยนวิธีหา ground truth:** เลิกใช้ `DROP INDEX`
ใช้ `SET LOCAL enable_indexscan = off` ตามที่เอกสารแนะนำ เร็วกว่าและไม่เปลี่ยนสภาพ DB

---

## D12 — checker ต้อง LOAD 'vector' ก่อนอ่าน pg_settings

พบ 2026-07-26: GUC ของ pgvector ไม่ปรากฏใน `pg_settings`
จนกว่าไลบรารีจะถูกโหลดเข้า session (ไม่ได้อยู่ใน shared_preload_libraries)

**ผลกระทบ:** ตัวตรวจ Q04 จะได้ 0 rows แล้วรายงานว่าผ่าน = **false negative**

**ข้อบังคับ:** ทุก check ที่อ่าน GUC ต้อง `LOAD 'vector'` ก่อน
และถ้าหาสิ่งที่ควรมีไม่เจอ ต้องรายงานว่า "ตรวจไม่ได้" ไม่ใช่ "ผ่าน"

---

## D13 — ยืนยันค่า default ของ pgvector 0.8.5 (2026-07-26)

ทุกค่า `setting == boot_val` (สภาพติดตั้งใหม่ ไม่มีใครแตะ)

```
hnsw.ef_search 40 · hnsw.iterative_scan off · hnsw.max_scan_tuples 20000
hnsw.scan_mem_multiplier 1 · ivfflat.iterative_scan off
ivfflat.max_probes 32768 · ivfflat.probes 1
```

ผลต่อแผน:
- **Q03 รอด** — iterative scan มีมาตั้งแต่ 0.8.0 แต่ยังปิดเป็นค่าเริ่มต้นใน 0.8.5
  เปลี่ยนกรอบเป็น "ทางแก้มีมา 2 ปีแล้ว แต่ต้องรู้ว่าต้องเปิดเอง"
- **Q04 อัปเกรด** — `probes=1` คือค้นแค่ 1 กลุ่ม
  ที่ 2M แถว: lists≈1414 → probes ที่ควรใช้≈38 → ต่ำกว่าคำแนะนำ 38 เท่า
- **Q06 (ใหม่)** — `LIMIT` > `ef_search` (40) ยืนยันโดย FAQ ทางการ
- **V07 (ใหม่)** — NULL vector และ zero vector ไม่ถูก index → แถวหายเงียบ
- ผลข้างเคียง: เปิด iterative_scan แล้วจะไปชน `max_scan_tuples=20000`

---

## D14 — ล็อก image tag เป็น 0.8.5-pg17 (2026-07-26)

**ปฏิเสธ:** `pgvector/pgvector:pg17`

**เหตุผล:** tag `pg17` เท่ากับ `0.8.5-pg17` *ในตอนนี้* แต่มันขยับตามเวอร์ชันใหม่
ใครรัน `docker compose pull` ทีหลังจะได้คนละเวอร์ชันโดยไม่มีอะไรเตือน
→ ผลการทดลองทั้งหมดเทียบกันไม่ได้

**ประเด็นน่าจำ:** โปรเจคที่ศึกษาความล้มเหลวเงียบ มีความล้มเหลวเงียบอยู่ในไฟล์ตั้งค่าตัวเอง
เขียนเรื่องนี้ลง README

---

## E07 — ทำนายผล EXP01 ผิด (2026-07-26)

**ทำนาย:** `merchant_id=1` (12% ของตาราง) จะยังใช้ Seq Scan หลังมี index
**ผลจริง:** Index Only Scan, `Heap Fetches: 0`, buffers 2280 → 22

**สาเหตุที่ผิด:** เอากฎ "index ไม่คุ้มเมื่อคืนแถวเกิน 5-10%" มาใช้ผิดบริบท
กฎนั้นตั้งอยู่บนต้นทุนการสุ่มอ่าน heap ซึ่ง Index Only Scan ไม่มีเลย

**ผลจริง (Phase C ปิด parallel):**
```
merchant_id=1     2280 → 22 buffers (104x)
merchant_id=4999  2280 →  3 buffers (760x)
ช่องว่าง 7.3x
```

**ของแถมที่ไม่ได้ออกแบบไว้:**
1. cache warming — รอบแรกหลัง CREATE INDEX มี `read=21` รอบสอง `hit` ล้วน
   → เพิ่มเป็นกฎเหล็กข้อ 8
2. สถิติแม่นไม่เท่ากัน — ค่าที่พบบ่อยคลาด 1.006x ค่าที่พบน้อยคลาด 1.5x
   ค่าที่พบบ่อยอยู่ใน most-common-values list ส่วนค่าที่พบน้อยตกในถังประมาณรวม
   → กระทบการออกแบบ F10 เพราะความคลาดแย่สุดตรงจุดที่ plan สำคัญสุด

**ต่อยอด:** EXP01b ใช้ `sum()` แทน `count(*)` เพื่อบังคับให้ต้องอ่าน heap

**ค่าต่อโปรเจค:** นี่คือวัตถุดิบของหัวข้อ "สิ่งที่ลองแล้วไม่ได้ผล" ใน README
และสาธิตวิทยานิพนธ์ตรงๆ — คำแนะนำที่อ้างกฎถูกต้อง แต่ผิดบริบท
และไม่มีอะไรเตือนนอกจากการวัดจริง

---

## E08 — index ทำให้ช้าลง เพราะ lossy bitmap (2026-07-26, EXP01b)

**การทดลอง:** เปลี่ยนจาก `count(*)` เป็น `sum(total_satang)` เพื่อบังคับให้ต้องอ่าน heap
(แก้ข้อบกพร่องของ EXP01 ที่ทดสอบผิดวิธี — ดู E07)

**ผล merchant_id=1 (23,724 แถว จาก 200k) บน work_mem=64kB:**

```
ไม่มี index : Seq Scan          2280 buffers    8.47 ms
มี index    : Bitmap Heap Scan  2301 buffers   10.79 ms   <- แย่กว่า
```

**index ทำให้ช้าลง 27% และอ่านมากขึ้น โดยไม่มี error ใดๆ**

**สาเหตุ:** `work_mem` เล็กเกินกว่าจะเก็บ bitmap ระดับแถว → ลดชั้นเป็น lossy bitmap
```
Heap Blocks: exact=732 lossy=1548     (lossy = 68% ของตาราง)
Rows Removed by Index Recheck: 119,688
```
lossy = จำแค่ว่า "หน้านี้มีของ" ต้องอ่านทั้งหน้าแล้วคัดใหม่ทีละแถว

**จุดสำคัญ — planner ตัดสินใจผิดเอง:**
```
Seq Scan     cost 4839.65  →  8.47 ms จริง
Bitmap Heap  cost 4623.57  → 10.79 ms จริง
```
โมเดลต้นทุนบอกว่าถูกกว่า 4.5% ความจริงแพงกว่า 27%
**planner ประเมินการเกิด lossy bitmap ไม่ได้**

→ ยืนยัน D08 (อย่าเชื่อ cost เชื่อ actual) ด้วยหลักฐานที่ชัดกว่าเดิมมาก

**merchant_id=4999 (18 แถว):** ยังดีขึ้น 2280 → 19 buffers (120×) ตามปกติ

**Phase D — ยืนยันสมมติฐานสถิติจาก E07:**
```
n_mcv_tracked = 100 · n_distinct = 4677  → ติดตามละเอียดแค่ 2.1%
merchant_1_in_mcv    = true   → เดาคลาด 0.6%
merchant_4999_in_mcv = false  → เดาคลาด 50%
```
ความคลาดแย่ที่สุดตรงค่าที่พบน้อย ซึ่งเป็นจุดที่การเลือก plan สำคัญที่สุด
→ นี่คือฐานรากของ F10 ทั้งข้อ

**สถานะสมมติฐานเดิม:** E07 ทำนายว่า query ที่คืน 12% ของตารางจะไม่คุ้มกับ index
→ **ถูก** เมื่อทดสอบด้วยวิธีที่ถูกต้อง แต่ผลแรงกว่าที่ทำนาย
(ไม่ใช่แค่ "ไม่ช่วย" แต่ "ทำให้แย่ลง")

**fault ใหม่ที่ได้:** "เพิ่ม index แล้วช้าลง โดยระบบเป็นคนเลือกเอง"

**งานต่อ (ยังไม่ทำ):** รัน EXP01b ซ้ำบนโปรไฟล์ realistic (work_mem=4MB)
ถ้า lossy หายไป = fault นี้เข้ากลุ่ม "ปัญหาที่ config กลบไว้"
ซึ่งอันตรายกว่า เพราะจะโผล่ตอนโหลดหนักที่ work_mem ไม่พอ

---

## E09 — `make seed` ตั้งค่าเริ่มต้นเป็น 2M แต่ผลทุกชุดมาจาก 200k (2026-07-26)

**พบจาก:** การไล่ตรวจความสอดคล้องของเอกสารทั้ง repo
**ไม่ใช่**จากการไปชนเข้าเหมือนสามครั้งก่อนหน้า

`Makefile` ตั้ง `ROWS ?= 2000000` แต่ตัวเลข buffers ทุกตัวใน `README.md`
และ `CLAUDE.md` มาจาก dataset **200,000 แถว**

**อาการ:** ใครก็ตามที่ clone แล้วรัน `make seed` จะได้ข้อมูลใหญ่กว่า 10 เท่า
**ไม่มี error ไม่มีคำเตือน** แล้วตัวเลขที่วัดได้จะเทียบกับผลในรายงานไม่ได้เลย
โดยไม่มีอะไรบอกว่าทำไม

**แก้:** `ROWS ?= 200000` — ค่าเริ่มต้นต้องเป็นค่าที่ผลในรายงานใช้จริง

**ปฏิเสธ:** ลบ `Makefile` ทิ้ง (เพราะเครื่องพัฒนาเป็น Windows ไม่มี `make`)
เหตุผล: คนที่ clone บน Linux ยังใช้ได้จริง · ปัญหาอยู่ที่**ค่า default** ไม่ใช่ตัวไฟล์
ลบทิ้งคือแก้อาการไม่ใช่แก้เหตุ

**เปลี่ยนใจเมื่อ:** ย้ายไปใช้ตัวรันคำสั่งอย่างอื่นที่ใช้ได้ทั้งสอง OS

---

### ทำไม E09 มีค่ามากกว่าตัวมันเอง

นี่คือความล้มเหลวเงียบ**ครั้งที่ 4** ในโปรเจคที่ศึกษาความล้มเหลวเงียบ
แต่ต่างจากสามครั้งแรกตรง**วิธีที่เจอ**

| # | เจอเพราะ |
|---|---|
| 1 docker tag ขยับ | ไปอ่านเอกสารทางการแล้วบังเอิญเห็น |
| 2 ไม่ `LOAD 'vector'` | รันแล้วได้ 0 rows |
| 3 CRLF | สคริปต์พังในคอนเทนเนอร์ |
| **4 `ROWS` ไม่ตรง** | **ไล่ตรวจอย่างเป็นระบบ — ยังไม่เคยแสดงอาการเลย** |

สามข้อแรกมีอาการให้เห็นก่อน ข้อ 4 รออยู่เฉยๆ จนกว่าจะมีคนอื่นเอาไปรัน
→ **นี่คือสิ่งที่ `quietfail-check` ควรทำให้ผู้ใช้พอดี**
หาความไม่สอดคล้องที่ยัง**ไม่**แสดงอาการ ก่อนที่มันจะแสดง

---

## D15 — ล็อกชุด fault 12 ข้อ ปิดหนี้กฎเหล็กข้อ 4 (2026-07-26)

**ปัญหาที่ต้องปิด:** `PROJECT.md` กับ `EVIDENCE.md` นับได้ 12 ข้อเท่ากัน
แต่เป็นคนละชุด · และ D13 รับ Q06 กับ V07 เข้ามาโดยยังไม่ได้ตัดของเดิมออก
= ค้างชำระกฎเหล็กข้อ 4 มาตั้งแต่วันที่รับ

**ชุดที่ล็อกแล้ว:**

```
I01 I02 I03 I04 I05   Q01 Q02 Q03 Q04 Q06   V07 L02      = 12
```

**รับเข้า**

| ID | เหตุผล |
|---|---|
| **Q06** `LIMIT` > `ef_search` | FAQ ทางการยืนยันตรงๆ · ตรวจแบบ static ได้ → เข้า `quietfail-check` ได้ทันที |
| **V07** NULL / zero vector | เอกสารยืนยันสองที่ · เป็นปัญหา**คุณภาพข้อมูล** ทีมที่จูน index เก่งก็ไม่เจอ · ตรวจง่ายมาก (นับแถว) · zero vector เกิดจริงเมื่อ embedding ล้มเหลวแล้วโค้ดใส่ศูนย์แทน |

**ตัดออก**

| ID | เหตุผล | เปลี่ยนใจเมื่อ |
|---|---|---|
| **Q05** สลับ metric | เอกสารยืนยันได้แค่บางส่วน และอาการซ้อนทับกับ I01 ซึ่งยืนยันเต็มและตรวจจาก catalog ได้ | เจอกรณีที่ Q05 ให้อาการที่ I01 อธิบายไม่ได้ |
| **L01** พารามิเตอร์ไม่ scale | ต้นทุนเวลาสูงที่สุดในบรรดา fault ทั้งหมด — ต้องมีข้อมูล 4 ขนาดและสร้าง index ใหม่ทุกขนาด | เฟส 3 จบเร็วกว่าแผน หรือมีเครื่องที่รัน 5M ได้สบาย |
| **V08** multi-tenant | ต้องสร้างสถาปัตยกรรมใหม่ก่อนถึงจะฉีดได้ = สร้างระบบเพื่อทดสอบข้อเดียว | โปรเจคมีระบบ multi-tenant ด้วยเหตุผลอื่นอยู่แล้ว |

**ID ที่ตัดแล้วห้ามนำกลับมาใช้ซ้ำ** และห้ามเรียงเลขใหม่
เพราะ `results/` และบันทึกใน `DECISIONS.md` อ้างถึง ID เดิม
การเรียงใหม่จะทำให้หลักฐานเก่าชี้ไปคนละข้อโดยไม่มีอะไรเตือน
(ซึ่งเป็นความล้มเหลวเงียบอีกแบบหนึ่ง — เทียบ E09)

**ข้อสังเกตที่บันทึกไว้ตอนตัดสินใจ (ผู้ใช้รับทราบแล้วและยืนยันตามเดิม):**
ผู้ช่วยตั้งข้อสังเกตว่า L01 ถูกกว่าที่ประเมิน เพราะ `PROJECT.md` ข้อ 7
สั่งวัด 4 ขนาดอยู่แล้ว โครงสร้างจึงรองรับ L01 อยู่ก่อน — ต่างจาก V08 ที่ต้องสร้างของใหม่จริง
ผู้ใช้พิจารณาแล้วยืนยันตัด **เพื่อคุมขอบเขตให้จบทัน**
บันทึกไว้เพื่อให้รู้ว่านี่คือการเลือกที่รู้ตัว ไม่ใช่หลุดไปเพราะนับให้ครบ 12

**ผลข้างเคียงที่ยังไม่ตัดสิน:** `PROJECT.md` ข้อ 7 ยังสั่งวัด 4 ขนาด (100k/500k/2M/5M)
ซึ่งเดิมมีไว้รองรับ L01 เป็นหลัก ส่วนเฟส 2 ต้องการแค่ 2 ระดับ
→ ยังไม่แก้ ต้องตัดสินแยกว่าจะลดเหลือ 2 ขนาดหรือคงไว้เผื่อ L01 กลับมา

---

## หมายเหตุ — ภาษาไทยดูเพี้ยนใน PowerShell

`Get-Content` ของ PowerShell 5.1 อ่านไฟล์ด้วย codepage ระบบแทน UTF-8
ไฟล์ไม่ได้เสีย ใช้ `Get-Content ไฟล์ -Encoding UTF8` หรือดูบน GitHub

(อีกหนึ่งความล้มเหลวเงียบ — ข้อมูลดูพัง ทั้งที่ไม่พัง)
