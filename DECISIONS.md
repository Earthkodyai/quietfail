# บันทึกการตัดสินใจ

> **ไฟล์นี้สำคัญกว่าที่คิด** เพราะ `PROJECT.md` บอกว่า *ทำอะไร*
> แต่ไฟล์นี้บอกว่า *ทำไมถึงไม่ทำอย่างอื่น* ซึ่งเป็นส่วนที่หายง่ายที่สุด
>
> เมื่อโปรเจคเสร็จ ไฟล์นี้จะกลายเป็นหัวข้อ **"ทำไมถึงเลือกแบบนี้"** ใน README
> ซึ่งเป็นส่วนที่คนสัมภาษณ์ให้ค่าสูงกว่าตัวโค้ด

รูปแบบ: ตัดสินใจอะไร / ปฏิเสธอะไร / เพราะอะไร / อะไรทำให้เปลี่ยนใจได้

---

## D01 — ใช้ PostgreSQL ไม่ใช่ MySQL

**ปฏิเสธ:** MySQL / MariaDB

**เหตุผล:** ตลาดงานไทยมีทั้งสองพอๆ กัน จำนวนงานตัดสินไม่ได้
ตัวตัดสินคือ **PostgreSQL ให้ ground truth ที่โปรเจคนี้ต้องใช้**
`pg_stat_activity` มี state `idle in transaction` ตรงๆ, `pg_locks` ดูห่วงโซ่การรอได้,
`EXPLAIN (ANALYZE, BUFFERS)` ละเอียดกว่า

**เปลี่ยนใจเมื่อ:** เล็งบริษัทที่ใช้ MySQL เป็นหลักแบบเจาะจงแล้ว

**หมายเหตุ:** หลักการ (isolation level, lock ordering, MVCC) ย้ายข้าม RDBMS ได้
รายละเอียดย้ายไม่ได้ — ต้องพูดให้ชัดตอนสัมภาษณ์ว่ายังไม่เคยใช้ Oracle

---

## D02 — Docker ไม่ใช่ติดตั้งลงเครื่อง

**ปฏิเสธ:** ติดตั้ง PostgreSQL ลง Windows โดยตรง

**เหตุผล:** ต้องจงใจตั้งค่าให้ DB เปราะ และต้องพัง/สร้างใหม่บ่อยมาก
ที่สำคัญกว่า: **คนอื่นต้องทำซ้ำผลได้** ไม่งั้นตัวเลขที่รายงานไม่มีน้ำหนัก

---

## D03 — ข้อมูล generate เอง ไม่ใช่ข้อมูลจริง (เฉพาะชั้น fault)

**ปฏิเสธ:** ใช้ข้อมูลจริงตั้งแต่ต้น

**เหตุผล:** fault injection ต้อง**คุมตัวแปรได้** ถ้าคุมความเบ้ไม่ได้
จะพิสูจน์ไม่ได้ว่า planner ประมาณผิดเพราะอะไร
ข้อมูลชุดนี้คือ **อุปกรณ์ทดลอง** ไม่ใช่ตัวแทนข้อมูลจริง

**ข้อควรระวัง:** ปกติ "seed data ปลอม" เป็นสัญญาณไม่ดีในพอร์ตโฟลิโอ
ต้องเขียนเหตุผลนี้ไว้ใน README ให้ชัด ไม่งั้นคนอ่านเข้าใจผิด

**ข้อมูลจริงยังต้องมี** แต่ไปอยู่ชั้นค้นหาภาษาไทย (OpenStreetMap / data.go.th)
เพราะชั้นนั้นวัด "ผลลัพธ์ที่มนุษย์พอใจ" ซึ่งข้อมูลปลอมแทนไม่ได้

---

## D04 — ทดสอบลึก 1 ตัว + พิสูจน์ข้ามตัว ไม่ใช่ทดสอบ 5 ตัวเท่าๆ กัน

**ปฏิเสธ:** วัด recall เต็มรูปแบบกับ Qdrant, Milvus, Weaviate, Pinecone, FAISS

**เหตุผล:** 5 container 5 API = โปรเจคไม่จบ และได้แค่บทความเทียบยี่ห้อที่หมดอายุใน 3 เดือน

**ทางออก 3 ชั้น:**
1. pgvector — fault ครบ 12 ข้อ
2. Qdrant — รันเฉพาะ Q01 เพื่อพิสูจน์ว่า recall collapse ไม่ใช่บั๊กเฉพาะยี่ห้อ
3. ที่เหลือ — ตารางจับคู่จากเอกสารทางการ ไม่วัด

ชั้น 2 คือคำตอบของ **"นี่มันบั๊กของ pgvector หรือเปล่า"** ซึ่งจะโดนถามแน่นอน

---

## D05 — ตัด Pinecone ออกจากชั้นที่วัด

**เหตุผลที่ถูกต้อง:**
1. **fault ชั้นติดตั้งหายไปทั้งหมด** — I01, I02, I04, I05 เกิดตอนสร้าง index เอง
   พอเขาจัดการให้ ก็ไม่มีอะไรให้ฉีด
2. **ทำซ้ำไม่ได้** — เขาอัปเดตระบบเมื่อไหร่ ผลเปลี่ยนโดยเราไม่รู้ตัว
3. ต้องจ่ายเงิน คนอื่นทำซ้ำฟรีไม่ได้

**เหตุผลที่ผมเคยให้แล้วผิด:** เคยบอกว่า "ตรวจข้างในไม่ได้ = ไม่มี ground truth"
ผิด — ground truth มาจาก vector ของเราเอง คำนวณ exact search ข้างนอกได้

**ข้อสังเกตที่มีค่า:** กับบริการแบบเช่าใช้ เรา "ปิด index เพื่อหาเฉลย" ไม่ได้
แปลว่าผู้ใช้ไม่มีทางรู้ว่า recall ตัวเองเท่าไหร่ →
**ความล้มเหลวเงียบไม่หายไปเพราะจ่ายเงิน แต่ตรวจจับยากขึ้น**
ประโยคนี้ควรอยู่ในบทนำของรายงาน

---

## D06 — เลื่อนการใช้ embedding มาจัดกลุ่ม error ออกไป

**ปฏิเสธ (ชั่วคราว):** ใช้ vector similarity จัดกลุ่ม error log

**เหตุผล:** ตอนนี้มี fault 12 ข้อที่รู้เฉลยและจับด้วยกฎได้ **ไม่มีอะไรให้จัดกลุ่ม**
ใส่ตอนนี้จะอ่านออกมาเป็น "ใส่ AI เพราะอยากใส่"

**ลำดับที่ถูก:** normalize + hash ก่อน (ลบ pid ลบตัวเลข ลบชื่อตาราง)
→ วัดว่าพลาดตรงไหน → **ถ้าไม่พลาด ก็ไม่ต้องใช้ embedding เลย**
ถ้าพลาด จะมีเหตุผลจับต้องได้ว่าทำไมต้องใช้

---

## D07 — ตรวจ bug ของ vector search เอง ≠ ใช้ AI มาช่วยวินิจฉัย

**บันทึกไว้เพราะเคยเข้าใจผิดกันในบทสนทนา**

- ใช้ embedding **เป็นเครื่องมือวินิจฉัย** → ยังไม่จำเป็น (ดู D06)
- ตรวจ **bug ของ ANN search เอง** → ทำ และเป็นแกนของโปรเจค

ถ้าโปรเจคอ้างว่าตรวจระบบข้อมูล**ปัจจุบัน** แล้วไม่แตะ vector search
ก็เท่ากับตรวจระบบของปี 2020

---

## D08 — ใช้ buffers เป็นตัวชี้วัดหลัก ไม่ใช่เวลา

**เหตุผล:** buffers นิ่ง เวลาแกว่งตามเครื่องและตามภาระงานขณะนั้น

**หลักฐานจากการทดลองจริง:** planner คิดว่าการใช้ parallel worker คุ้ม
(cost 4785.95 vs 4780.08) แต่เวลาจริงเท่ากัน (14.2 vs 14.5 ms)
→ **อย่าเชื่อ cost ให้เชื่อ actual — และ actual ที่นิ่งที่สุดคือ buffers**

---

### แก้ไข 2026-07-26 หลัง EXP01c — ขยาย ไม่ใช่กลับคำ

D08 ยังถูกในสิ่งที่มันอ้าง: buffers นิ่งกว่าเวลาจริง **แต่มันวัดเฉพาะ I/O**

EXP01c พบ fault ที่ buffers มองไม่เห็นเลย — `work_mem` 6 ค่า
ทำให้เกิด lossy bitmap และ recheck 110,062 แถว
แต่ `buffers = 2501` **เท่ากันเป๊ะทั้ง 6 ค่า** เพราะงานที่เพิ่มขึ้น
เป็นการคัดแถวซ้ำในหน่วยความจำ ไม่ได้อ่าน page เพิ่มแม้แต่หน้าเดียว

ส่วนเวลาก็ยังเชื่อไม่ได้ตามเดิม: รอบที่ plan เหมือนกันเป๊ะแกว่ง 5.99–7.61 ms

**ตัวชี้วัดที่เพิ่มเข้ามา:** `Heap Blocks` (มี `lossy=` หรือไม่)
และ `Rows Removed by Index Recheck` — เป็นค่าเชิงโครงสร้างจาก plan
ไม่ใช่นาฬิกา จึงนิ่งพอๆ กับ buffers

→ เข้าเป็น **กฎเหล็กข้อ 7ก** ใน `CLAUDE.md` และเพิ่มแถวใน `PROJECT.md` ข้อ 7
**อนุมัติโดยเจ้าของโปรเจค 2026-07-26**

**บทเรียนที่กว้างกว่าตัวกฎ:** ตัวชี้วัดหลักที่เลือกไว้ตอนต้นโปรเจค
มองไม่เห็น fault บางชนิด และมันไม่ได้บอกเราเอง —
ต้องเจอ fault ที่มันพลาดก่อนถึงจะรู้ ซึ่งคือรูปแบบเดียวกับที่โปรเจคนี้ศึกษาอยู่

---

## D09 — เกณฑ์วัดผลต้อง commit ก่อนมีตัวเลข

**เหตุผล:** ถ้าตั้งเกณฑ์ทีหลัง = เลือกเกณฑ์ที่เข้าข้างผลตัวเอง
ชุด query 200 ข้อ + seed + สูตร recall ต้องอยู่ใน repo ก่อนรันวัดครั้งแรก

---

## D10 — ชื่อโปรเจค QuietFail

**ปฏิเสธ:** `pgvector-bench`, `pg-fault-lab` (ชื่อเดิม)

**เหตุผล:** ตั้งชื่อตามวิทยานิพนธ์ ไม่ใช่ตามเทคโนโลยี
ชื่อที่ผูกกับเครื่องมือจะล้าสมัยทันทีที่เปลี่ยนเครื่องมือ

**ยังไม่ได้ทำ:** ค้น GitHub / npm ว่าซ้ำใครไหม

---

# ความผิดพลาดที่เกิดขึ้นจริง — เก็บไว้เตือน

> ส่วนนี้มีค่าสองอย่าง: กันพลาดซ้ำ และเป็นวัตถุดิบของหัวข้อ
> **"สิ่งที่ลองแล้วไม่ได้ผล"** ใน README ซึ่งหายากมากในพอร์ตนิสิต

## E01 — "มองหา Seq Scan" หยาบเกินไป

ผู้ช่วย AI เคยแนะนำให้ "มองหา Seq Scan" ซึ่งทำให้เข้าใจว่า Seq Scan = ไม่ดี

**ของจริง:** `merchant_id = 1` คืน 12% ของตาราง → Seq Scan **คือทางเลือกที่ถูกต้อง**
การไล่ index แล้วกระโดดหยิบ 23,983 ครั้งจะช้ากว่า

**เกณฑ์ที่ถูก:** Seq Scan เป็นปัญหาเมื่ออ่านเยอะเพื่อได้น้อย
(เช่น 2,479 buffers เพื่อ 6 แถว) ไม่ใช่เพราะเป็น Seq Scan

## E02 — ทายจำนวนแถวผิด

ผู้ช่วยทายว่า `merchant_id = 4999` จะได้ราว 10 แถว **ของจริง 18** (พลาด 1.8 เท่า)

คำนวณจากสูตร `power(random(), 4)` ได้ แต่ค่าจริงขึ้นกับการสุ่ม
→ ยืนยันว่าต้องวัด ไม่ใช่คำนวณบนกระดาษ

## E03 — บอกเหตุผลตัด Pinecone ผิด

ดู D05 — เคยอ้างว่าไม่มี ground truth ที่พิสูจน์ได้ ซึ่งไม่จริง

## E04 — ตอบผิดประเด็นเรื่อง AI clustering

ผู้ใช้ถามถึง **bug ของ ANN** แต่ผู้ช่วยตีความว่าจะเอา AI **มาเป็นเครื่องมือวินิจฉัย**
แล้วเถียงว่ายังไม่จำเป็น → ผู้ใช้ถูก ผู้ช่วยผิด (ดู D07)

## E05 — ให้คำสั่ง bash/make กับผู้ใช้ Windows

เสียเวลาไปหลายรอบ ตอนนี้มี `WINDOWS.md` แล้ว
**คำสั่งทุกอันต้องเป็น PowerShell**

## E06 — ไม่ได้เตือนเรื่องโครงสร้างโฟลเดอร์ตั้งแต่แรก

ผู้ใช้ดาวน์โหลดไฟล์ทีละอันจนกองแบนหมด ทำให้ Docker พยายามสร้างโฟลเดอร์เอง
แล้วชนสิทธิ์ของ `C:\Users\` → ได้ error `Access is denied` ที่ชี้ไปคนละเรื่อง

**บทเรียนที่ตรงกับโปรเจคพอดี:** error บอกว่า "ไม่มีสิทธิ์" แต่ต้นเหตุคือ "ไฟล์ไม่อยู่"

---

## D11 — ทบทวน CHANGELOG + README ของ pgvector (2026-07-26)

แหล่ง: github.com/pgvector/pgvector · ดูรายละเอียดเต็มใน `EVIDENCE.md`

- 0.8.0 (2024-10-30) เพิ่ม iterative index scans → กระทบ Q03
- 0.4.2 (2023-05-13) เพิ่ม notice ตอนสร้าง index ด้วยข้อมูลน้อย
  → I02 ไม่ใช่ "เงียบ" แต่เป็น "เตือนแล้วไม่มีใครอ่าน"
- README ระบุเองว่า approximate index **เปลี่ยนคำตอบ** ไม่ใช่แค่ความเร็ว
  → Q01 ได้การรับรองจากผู้พัฒนา ไม่ต้องพิสูจน์ว่ามีจริงอีก

**เปลี่ยนวิธีหา ground truth:** เลิกใช้ `DROP INDEX`
ใช้ `SET LOCAL enable_indexscan = off` ตามที่เอกสารแนะนำ เร็วกว่าและไม่เปลี่ยนสภาพ DB

---

## D12 — checker ต้อง LOAD 'vector' ก่อนอ่าน pg_settings

พบ 2026-07-26: GUC ของ pgvector ไม่ปรากฏใน `pg_settings`
จนกว่าไลบรารีจะถูกโหลดเข้า session (ไม่ได้อยู่ใน shared_preload_libraries)

**ผลกระทบ:** ตัวตรวจ Q04 จะได้ 0 rows แล้วรายงานว่าผ่าน = **false negative**

**ข้อบังคับ:** ทุก check ที่อ่าน GUC ต้อง `LOAD 'vector'` ก่อน
และถ้าหาสิ่งที่ควรมีไม่เจอ ต้องรายงานว่า "ตรวจไม่ได้" ไม่ใช่ "ผ่าน"

---

## D13 — ยืนยันค่า default ของ pgvector 0.8.5 (2026-07-26)

ทุกค่า `setting == boot_val` (สภาพติดตั้งใหม่ ไม่มีใครแตะ)

```
hnsw.ef_search 40 · hnsw.iterative_scan off · hnsw.max_scan_tuples 20000
hnsw.scan_mem_multiplier 1 · ivfflat.iterative_scan off
ivfflat.max_probes 32768 · ivfflat.probes 1
```

ผลต่อแผน:
- **Q03 รอด** — iterative scan มีมาตั้งแต่ 0.8.0 แต่ยังปิดเป็นค่าเริ่มต้นใน 0.8.5
  เปลี่ยนกรอบเป็น "ทางแก้มีมา 2 ปีแล้ว แต่ต้องรู้ว่าต้องเปิดเอง"
- **Q04 อัปเกรด** — `probes=1` คือค้นแค่ 1 กลุ่ม
  ที่ 2M แถว: lists≈1414 → probes ที่ควรใช้≈38 → ต่ำกว่าคำแนะนำ 38 เท่า
- **Q06 (ใหม่)** — `LIMIT` > `ef_search` (40) ยืนยันโดย FAQ ทางการ
- **V07 (ใหม่)** — NULL vector และ zero vector ไม่ถูก index → แถวหายเงียบ
- ผลข้างเคียง: เปิด iterative_scan แล้วจะไปชน `max_scan_tuples=20000`

---

## D14 — ล็อก image tag เป็น 0.8.5-pg17 (2026-07-26)

**ปฏิเสธ:** `pgvector/pgvector:pg17`

**เหตุผล:** tag `pg17` เท่ากับ `0.8.5-pg17` *ในตอนนี้* แต่มันขยับตามเวอร์ชันใหม่
ใครรัน `docker compose pull` ทีหลังจะได้คนละเวอร์ชันโดยไม่มีอะไรเตือน
→ ผลการทดลองทั้งหมดเทียบกันไม่ได้

**ประเด็นน่าจำ:** โปรเจคที่ศึกษาความล้มเหลวเงียบ มีความล้มเหลวเงียบอยู่ในไฟล์ตั้งค่าตัวเอง
เขียนเรื่องนี้ลง README

---

## E07 — ทำนายผล EXP01 ผิด (2026-07-26)

**ทำนาย:** `merchant_id=1` (12% ของตาราง) จะยังใช้ Seq Scan หลังมี index
**ผลจริง:** Index Only Scan, `Heap Fetches` เกือบ 0, buffers 2479 → 25

**สาเหตุที่ผิด:** เอากฎ "index ไม่คุ้มเมื่อคืนแถวเกิน 5-10%" มาใช้ผิดบริบท
กฎนั้นตั้งอยู่บนต้นทุนการสุ่มอ่าน heap ซึ่ง Index Only Scan ไม่มีเลย

**ผลจริง (Phase C ปิด parallel) — รันใหม่ 2026-07-26 หลังแก้ E17:**
```
merchant_id=1     2479 → 25 buffers (99x)   · 23,983 แถว
merchant_id=4999  2479 →  3 buffers (826x)  · 6 แถว
ช่องว่าง 8.3x
```
(ค่าเดิมบน seed ที่พัง: 2280 → 22 / 2280 → 3 · ช่องว่าง 7.3x
 **ข้อสรุปไม่เปลี่ยน** — ทั้งคู่ยังเป็น Index Only Scan และช่องว่างยังกว้าง)

**ของแถมที่ไม่ได้ออกแบบไว้:**
1. cache warming — รอบแรกหลัง CREATE INDEX มี `read=21` รอบสอง `hit` ล้วน
   → เพิ่มเป็นกฎเหล็กข้อ 8
2. สถิติแม่นไม่เท่ากัน — ค่าที่พบบ่อยคลาด 1.006x ค่าที่พบน้อยคลาด 1.5x
   ค่าที่พบบ่อยอยู่ใน most-common-values list ส่วนค่าที่พบน้อยตกในถังประมาณรวม
   → กระทบการออกแบบ F10 เพราะความคลาดแย่สุดตรงจุดที่ plan สำคัญสุด

**ต่อยอด:** EXP01b ใช้ `sum()` แทน `count(*)` เพื่อบังคับให้ต้องอ่าน heap

**ค่าต่อโปรเจค:** นี่คือวัตถุดิบของหัวข้อ "สิ่งที่ลองแล้วไม่ได้ผล" ใน README
และสาธิตวิทยานิพนธ์ตรงๆ — คำแนะนำที่อ้างกฎถูกต้อง แต่ผิดบริบท
และไม่มีอะไรเตือนนอกจากการวัดจริง

---

## E08 — index ทำให้ช้าลง เพราะ lossy bitmap (2026-07-26, EXP01b)

**การทดลอง:** เปลี่ยนจาก `count(*)` เป็น `sum(total_satang)` เพื่อบังคับให้ต้องอ่าน heap
(แก้ข้อบกพร่องของ EXP01 ที่ทดสอบผิดวิธี — ดู E07)

**ผล merchant_id=1 (23,983 แถว จาก 200k) บน work_mem=64kB — รันใหม่หลังแก้ E17:**

```
ไม่มี index : Seq Scan          2479 buffers   20.53 ms
มี index    : Bitmap Heap Scan  2501 buffers   44.77 ms   <- แย่กว่า
```

**index ทำให้ช้าลง 118% และอ่านมากขึ้น โดยไม่มี error ใดๆ**

(ค่าเดิมบน seed ที่พัง: 2280/8.47ms → 2301/10.79ms = ช้าลง 27%
 **ผลใหม่แรงกว่าเดิม 4 เท่า** ข้อสรุปเดิมยืนและหนักแน่นขึ้น)

**สาเหตุ:** `work_mem` เล็กเกินกว่าจะเก็บ bitmap ระดับแถว → ลดชั้นเป็น lossy bitmap
```
Heap Blocks: exact=931 lossy=1548     (lossy = 62% ของตาราง)
Rows Removed by Index Recheck: 110,062
```
lossy = จำแค่ว่า "หน้านี้มีของ" ต้องอ่านทั้งหน้าแล้วคัดใหม่ทีละแถว

**จุดสำคัญ — planner ตัดสินใจผิดเอง:**
```
Seq Scan     cost 5038.97  → 20.53 ms จริง
Bitmap Heap  cost 4863.43  → 44.77 ms จริง
```
โมเดลต้นทุนบอกว่าถูกกว่า 3.5% ความจริงแพงกว่า 2.2 เท่า
**planner ประเมินการเกิด lossy bitmap ไม่ได้**

→ ยืนยัน D08 (อย่าเชื่อ cost เชื่อ actual) ด้วยหลักฐานที่ชัดกว่าเดิมมาก

**merchant_id=4999 (6 แถว):** ยังดีขึ้น 2479 → 8 buffers (310×) ตามปกติ

**Phase D — ยืนยันสมมติฐานสถิติจาก E07:**
```
n_mcv_tracked = 100 · n_distinct = 4695 (ค่าประมาณ ดู E30)  → ติดตามละเอียดแค่ 2.1%
merchant_1_in_mcv    = true   → ประมาณ 23,987 จริง 23,983 = คลาด 0.02%
merchant_4999_in_mcv = false  → ประมาณ 27     จริง 6      = คลาด 4.5 เท่า
```
ความคลาดแย่ที่สุดตรงค่าที่พบน้อย ซึ่งเป็นจุดที่การเลือก plan สำคัญที่สุด
→ นี่คือฐานรากของ F10 ทั้งข้อ

**สถานะสมมติฐานเดิม:** E07 ทำนายว่า query ที่คืน 12% ของตารางจะไม่คุ้มกับ index
→ **ถูก** เมื่อทดสอบด้วยวิธีที่ถูกต้อง แต่ผลแรงกว่าที่ทำนาย
(ไม่ใช่แค่ "ไม่ช่วย" แต่ "ทำให้แย่ลง")

**fault ใหม่ที่ได้:** "เพิ่ม index แล้วช้าลง โดยระบบเป็นคนเลือกเอง"

**งานต่อ (ยังไม่ทำ):** รัน EXP01b ซ้ำบนโปรไฟล์ realistic (work_mem=4MB)
ถ้า lossy หายไป = fault นี้เข้ากลุ่ม "ปัญหาที่ config กลบไว้"
ซึ่งอันตรายกว่า เพราะจะโผล่ตอนโหลดหนักที่ work_mem ไม่พอ

---

## E09 — `make seed` ตั้งค่าเริ่มต้นเป็น 2M แต่ผลทุกชุดมาจาก 200k (2026-07-26)

**พบจาก:** การไล่ตรวจความสอดคล้องของเอกสารทั้ง repo
**ไม่ใช่**จากการไปชนเข้าเหมือนสามครั้งก่อนหน้า

`Makefile` ตั้ง `ROWS ?= 2000000` แต่ตัวเลข buffers ทุกตัวใน `README.md`
และ `CLAUDE.md` มาจาก dataset **200,000 แถว**

**อาการ:** ใครก็ตามที่ clone แล้วรัน `make seed` จะได้ข้อมูลใหญ่กว่า 10 เท่า
**ไม่มี error ไม่มีคำเตือน** แล้วตัวเลขที่วัดได้จะเทียบกับผลในรายงานไม่ได้เลย
โดยไม่มีอะไรบอกว่าทำไม

**แก้:** `ROWS ?= 200000` — ค่าเริ่มต้นต้องเป็นค่าที่ผลในรายงานใช้จริง

**ปฏิเสธ:** ลบ `Makefile` ทิ้ง (เพราะเครื่องพัฒนาเป็น Windows ไม่มี `make`)
เหตุผล: คนที่ clone บน Linux ยังใช้ได้จริง · ปัญหาอยู่ที่**ค่า default** ไม่ใช่ตัวไฟล์
ลบทิ้งคือแก้อาการไม่ใช่แก้เหตุ

**เปลี่ยนใจเมื่อ:** ย้ายไปใช้ตัวรันคำสั่งอย่างอื่นที่ใช้ได้ทั้งสอง OS

---

### ทำไม E09 มีค่ามากกว่าตัวมันเอง

นี่คือความล้มเหลวเงียบ**ครั้งที่ 4** ในโปรเจคที่ศึกษาความล้มเหลวเงียบ
แต่ต่างจากสามครั้งแรกตรง**วิธีที่เจอ**

| # | เจอเพราะ |
|---|---|
| 1 docker tag ขยับ | ไปอ่านเอกสารทางการแล้วบังเอิญเห็น |
| 2 ไม่ `LOAD 'vector'` | รันแล้วได้ 0 rows |
| 3 CRLF | สคริปต์พังในคอนเทนเนอร์ |
| **4 `ROWS` ไม่ตรง** | **ไล่ตรวจอย่างเป็นระบบ — ยังไม่เคยแสดงอาการเลย** |

สามข้อแรกมีอาการให้เห็นก่อน ข้อ 4 รออยู่เฉยๆ จนกว่าจะมีคนอื่นเอาไปรัน
→ **นี่คือสิ่งที่ `quietfail-check` ควรทำให้ผู้ใช้พอดี**
หาความไม่สอดคล้องที่ยัง**ไม่**แสดงอาการ ก่อนที่มันจะแสดง

---

## E10 — เดาว่ามี `l2_norm(vector)` ทั้งที่ไม่มี (2026-07-26)

เขียน assertion ตรวจ zero vector ด้วย `l2_norm(embedding)` เพราะคิดว่าเป็นชื่อมาตรฐาน

**ของจริงใน pgvector 0.8.5:** `l2_norm` มีเฉพาะ `halfvec` และ `sparsevec`
ตัวที่รับ `vector` ชื่อ **`vector_norm`**

**จุดที่น่าสนใจ — ข้อความ error ชี้ผิดทาง:**
```
ERROR:  function l2_norm(vector) is not unique
HINT:   Could not choose a best candidate function.
```
อ่านแล้วเข้าใจว่า "มีหลายตัวเกินไป" ต้องไปใส่ cast
**ต้นเหตุจริงคือไม่มีตัวที่รับ `vector` เลยสักตัว** แล้ว PostgreSQL
ไปลอง cast ไป halfvec กับ sparsevec ได้ทั้งคู่ จึงกำกวม

ถ้าหลงทำตาม HINT จะไปใส่ `::halfvec` ซึ่ง**ลดความละเอียดของข้อมูลเงียบๆ**
เพื่อแก้ error ที่ไม่ได้เกิดจากเรื่องนั้น

**ตรงกับ E06 และวิทยานิพนธ์ของโปรเจค:** error บอกอย่าง ต้นเหตุอีกอย่าง
วิธีที่ถูกคือ `SELECT oid::regprocedure FROM pg_proc WHERE proname ~ 'norm'`
ให้ catalog ตอบ ไม่ใช่ให้ผู้ช่วยเดา (กฎเหล็กข้อ 1)

---

## E17 — 🔴 `seed.sql` สร้างค่าเดียวซ้ำ 200,000 แถว ตั้งแต่ commit แรก (2026-07-26)

**ร้ายแรงที่สุดในบรรดาบั๊กที่เจอ** เพราะอยู่ในตารางหลักที่ทุกการทดลองใช้

พบตอนวัดว่า query ของ F03b ช้าพอจะชน timeout ไหม แล้วเจอว่า
`created_at_naive` มีค่าเดียวทั้งตาราง

```
ก่อนแก้  created_at 1 · created_at_naive 1 · total_satang 1 · merchant_id 5000
หลังแก้  created_at 200000 · total_satang 157610 · merchant_id 5000
ช่วงเวลา  ก่อน: 2026-02-24 จุดเดียว → หลัง: 2025-02-01 ถึง 2026-07-26
```

**สาเหตุ: รูปแบบเดียวกับ E11 เป๊ะ**
```sql
FROM generate_series(1, :rows) AS g,
LATERAL ( SELECT ... random() ... ) AS r     -- ไม่อ้างถึง g เลย
```
`LATERAL` ที่ไม่อ้างถึงคอลัมน์ของฝั่งซ้าย **ไม่ถือว่า correlated**
planner จึงคำนวณครั้งเดียวแล้วใช้ซ้ำทุกแถว

`merchant_id` รอดเพราะคำนวณ inline ใน SELECT list ไม่ได้อยู่ใน `LATERAL`

**แก้:** เติม `g AS _force_correlated` เข้าไปใน subquery
และเพิ่ม assertion ตรวจ `count(DISTINCT ...)` ซึ่งควรมีตั้งแต่แรก

### ⚠️ ผลกระทบต่อผลเก่า — ต้องอ่านให้ครบ

**ยังใช้ได้:** EXP01 / EXP01b / EXP01c วัด selectivity ด้วย `merchant_id`
ซึ่งเป็นคอลัมน์ที่**ไม่ได้พัง** และ `sum(total_satang)` มีไว้บังคับให้แตะ heap
ซึ่งยังแตะจริง ข้อสรุปเชิงกลไก (lossy bitmap · buffers มองไม่เห็น · MCV 2.1%)
ไม่เปลี่ยน

**แต่ตัวเลขดิบเคลื่อนแล้ว** เพราะการแก้เปลี่ยนลำดับการเรียก `random()`
ทั้งชุด → การกระจายของ `merchant_id` ก็เปลี่ยนตาม

| | ก่อนแก้ | หลังแก้ |
|---|---|---|
| `merchant_id = 1` | 23,724 แถว | **23,983 แถว** |
| `merchant_id = 4999` | 18 แถว | **6 แถว** |
| `n_distinct` | 4,677 | **4,656** |

→ **ปิดหนี้แล้ว 2026-07-26** รัน EXP01/EXP01b/EXP01c ใหม่ทั้งสามตัว
และอัปเดตตัวเลขใน `CLAUDE.md`, `README.md`, E07, E08, EXP01c ครบแล้ว

**ผลของการรันใหม่ — ข้อสรุปเชิงกลไกไม่เปลี่ยนสักข้อ และสองข้อแรงขึ้น:**

| ข้อสรุป | ก่อน | หลัง |
|---|---|---|
| index ทำให้ query ช้าลง (E08) | ช้าลง 27% | **ช้าลง 118%** |
| ค่านอก MCV ประมาณคลาด | 50% | **4.5 เท่า** |
| เส้นแบ่ง lossy | 128–160kB | เท่าเดิม |
| buffers มองไม่เห็น lossy | 2301 ทั้ง 6 ค่า | **2501 ทั้ง 6 ค่า** |
| lossy ไม่ monotonic | มี | **ทำซ้ำไม่ได้ ถอนแล้ว** |

**บทเรียน:** E11 เจอรูปแบบนี้แล้วครั้งหนึ่งใน corpus ของ vector
แต่ไม่ได้ย้อนกลับไปตรวจว่า `seed.sql` ที่เขียนไว้ก่อนหน้ามีรูปแบบเดียวกันหรือเปล่า
**เจอบั๊กรูปแบบหนึ่งแล้ว ต้องไล่หาที่อื่นที่ใช้รูปแบบเดียวกันทันที**

---

## E18 — สเปก F03 ระบุ timeout 2s ซึ่งไม่มี query ไหนช้าถึง (2026-07-26)

`FAULTS.md` เขียนวิธีฉีด F03b ว่าใช้ `statement_timeout = '2s'` กับ
`SELECT count(*) FROM orders WHERE created_at_naive < '2020-01-01'`

วัดจริง: query นั้นใช้ **41ms** และหลังแก้ E17 ข้อมูลเริ่มที่ปี 2025
เงื่อนไขจึงตรง 0 แถว · ไล่วัดตัวเลือกอื่นทั้งหมดบนชุด 200k แถว
ไม่มีตัวไหนแตะ 2 วินาที

**แก้:** ใช้ `statement_timeout = 100ms` **เท่ากันทั้งสองเคส**
กับ query ที่วัดค่าต่ำสุดจาก 5 รอบได้ 331ms → margin 3.3 เท่า

| candidate | ค่าต่ำสุดจาก 5 รอบ |
|---|---|
| sort คอลัมน์เดียว | 240 ms |
| distinct 3 คอลัมน์ | 274 ms |
| **sort 4 คอลัมน์** | **331 ms** ← เลือก |

**เลือกจากค่าต่ำสุด ไม่ใช่ค่าเฉลี่ย** เพราะค่าต่ำสุดคือกรณีแย่สุดของ margin

**และให้สคริปต์ตรวจ margin เองก่อนเริ่มทุกครั้ง** — วัด query จริง
ถ้าไม่ถึง 2 เท่าของ timeout ให้ exit พร้อมบอกเหตุผล
พิสูจน์แล้วว่าจำเป็น: รอบหนึ่ง cache อุ่นทำให้เหลือ 151ms และ pre-flight ปฏิเสธที่จะรัน

---

## E19 — `pg_stat_activity` ถูก snapshot ครั้งเดียวต่อ transaction (2026-07-26)

ตัวเฝ้าดูของ F03 เป็นลูป plpgsql สุ่มตัวอย่างทุก 5ms เป็นเวลา 6 วินาที
รันแล้วเก็บได้ **0 แถว** ทั้งที่ยืนยันด้วยตาว่า victim โผล่ใน `pg_stat_activity` จริง

**สาเหตุ:** `DO $$ ... $$` ทั้งก้อนคือ transaction เดียว
และ `pg_stat_activity` ถูก snapshot **ครั้งเดียวต่อ transaction**
ลูปจึงเห็นภาพเดิมซ้ำ 1,200 รอบ — ภาพที่ถ่ายไว้ตั้งแต่ก่อน victim เริ่มทำงาน

**ไม่มี error ไม่มีคำเตือน แค่ได้ 0 แถว**

**แก้:** เรียก `pg_stat_clear_snapshot()` ทุกรอบของลูป

**ทำไมข้อนี้สำคัญเกินตัวมันเอง:** เครื่องมือเฝ้าระวังที่เขียนเป็นลูปใน
transaction เดียวจะรายงานว่า "ระบบปกติดี" ตลอดไป โดยไม่มีอะไรผิดพลาดให้เห็น
**นี่คือความล้มเหลวเงียบในเครื่องมือตรวจความล้มเหลวเงียบ**
ควรเขียนลง README

**ก่อนหน้านี้ยังเสียเวลาไปกับ:** ฝัง SQL ของตัวเฝ้าดูไว้ใน `.sh`
แล้ว quoting ของ bash ชนกับ dollar-quote ของ plpgsql จนมันตายเงียบ
→ ย้ายไปเป็น `sql/f03_watcher.sql` และเพิ่มตัวตรวจว่าตัวเฝ้าดูตายหรือเปล่า

---

## E20 — บรรทัด CONTEXT ต่างกัน แต่ไม่ได้ช่วยแยกสาเหตุ (2026-07-26)

`FAULTS.md` อ้างว่า F03 กับ F03b ให้ข้อความ error **เหมือนกันทุกตัวอักษร**
รันจริงแล้วพบว่าบรรทัด `ERROR:` เหมือนกันจริง แต่ F03 มีบรรทัดเสริม:

```
F03  : ERROR:  canceling statement due to statement timeout
       CONTEXT:  while updating tuple (0,1) in relation "orders"
F03b : ERROR:  canceling statement due to statement timeout
```

**แต่ CONTEXT ไม่ได้แยกสาเหตุ** — มันบอกว่าคำสั่งกำลัง update tuple
ซึ่งเป็นคุณสมบัติของ **ชนิดคำสั่ง** ไม่ใช่ของ **สาเหตุ**
`UPDATE` ที่ช้าเองก็ได้ CONTEXT แบบเดียวกัน จึงยังบอกไม่ได้ว่าต้องแก้ตรงไหน

**ปรับ assertion:** เทียบเฉพาะบรรทัด `ERROR:` และ**บันทึก CONTEXT ไว้เป็นข้อสังเกต**
ไม่ใช่ทำให้ assertion ตก · สิ่งที่ระบบ log และ client library ส่วนใหญ่โชว์คือบรรทัด ERROR

**ข้อสรุปแข็งขึ้นกว่าสเปกเดิม** เพราะตอนนี้พูดได้ว่า
"ต่อให้อ่าน CONTEXT ด้วยก็ยังแยกไม่ออก" ไม่ใช่แค่ "ข้อความเหมือนกัน"

---

## E29 — 🔴 ฆ่า parallel worker แล้ว PostgreSQL รีเซ็ตทั้งคลัสเตอร์ (2026-07-27)

ตัวเก็บกวาดของ I03 สั่ง `pg_terminate_backend()` ใส่ทุก session ที่ query ตรงกับ
`CREATE INDEX%qf_i03_idx%` ซึ่งรวม **parallel worker** ของ build ไปด้วย

**log ยืนยันลำดับเหตุการณ์ครบ ไม่ต้องเดา:**
```
13:27:26.762 [1905] FATAL: terminating background worker "parallel worker" due to administrator command
13:27:26.766 [1]    LOG: background worker "parallel worker" (PID 1904) exited with exit code 1
13:27:26.788 [1]    LOG: server process (PID 1902) exited with exit code 2
13:27:26.788 [1]    LOG: terminating any other active server processes
13:27:26.791 [1]    LOG: all server processes terminated; reinitializing
```

ฆ่า worker → leader (1902) ตายด้วย exit code 2 ซึ่งเป็นการตายผิดปกติ
→ postmaster ถือว่า crash → **ตัดทุก connection แล้วเข้า recovery ทั้งคลัสเตอร์**

**อาการที่เห็นชี้ไปคนละเรื่องอีกแล้ว:**
```
FATAL: the database system is not yet accepting connections
DETAIL: Consistent recovery state has not been yet reached.
```
คำสั่งถัดไปทุกตัวล้มเหลวด้วยข้อความที่ไม่มีอะไรเกี่ยวกับ "คุณฆ่า worker"
ถ้าไม่ไปอ่าน log จะไล่หาสาเหตุผิดทางแน่นอน

**แก้ 2 ชั้น**
1. เล็งเฉพาะ **leader** — `backend_type = 'client backend'` ห้ามแตะ `'parallel worker'`
2. ใช้ **`pg_cancel_backend()`** แทน `pg_terminate_backend()` —
   cancel ยกเลิก query ทั้งชุดรวม worker อย่างสะอาด ส่วน terminate ฆ่า process

ยืนยันหลังแก้: รันตัวพิสูจน์ 3 สถานะ 3 รอบติด DB ไม่ restart เลยสักครั้ง

### ทำไมข้อนี้สำคัญเกินกว่าจะเป็นบั๊กของ harness

**ข้อมูลไม่หายเพราะโชคดี** — corpus ยังครบ 100,000 แถวหลัง recovery
เพราะทุกอย่างที่เขียนไปแล้ว commit ครบ ถ้ามี transaction ค้างอยู่ตอนนั้น
งานทั้งวันอาจหายไป

**และมันคือความเสี่ยงจริงของเครื่องมือ fault injection ทุกตัว**
เครื่องมือที่ฉีด fault ด้วยการฆ่า process **ทำให้ระบบทั้งระบบล่มได้**
โดยที่ error ที่เห็นไม่ได้ชี้กลับมาที่เครื่องมือเลย
→ ต้องเขียนใน README ส่วนข้อจำกัด: QuietFail แตะ backend ของ PostgreSQL
**ห้ามรันบน production เด็ดขาด** — ไม่ใช่แค่เพราะ config เปราะ แต่เพราะตัวมันเองอันตราย

**เชื่อมกับ E28:** ทั้งสองข้อคือ "ตัวแก้/ตัวเก็บกวาดมี failure mode ของตัวเอง"
E28 = เพิ่ม `maintenance_work_mem` มากไปแล้ว build ล้ม
E29 = เก็บกวาดแรงไปแล้วคลัสเตอร์ล่ม
ทั้งคู่ error ชี้ไปคนละที่กับต้นเหตุ

---

## E33 — คำเตือนของ pgvector ออกกับกรณีที่ดีที่สุด และเงียบกับกรณีที่แย่ที่สุด (2026-07-27)

**เจอตอนทำ I02 · เป็นผลที่แรงที่สุดที่โปรเจคนี้วัดได้จนถึงตอนนี้**

ทะเบียนลงชื่อ I02 ไว้ว่า **"สร้าง IVFFlat ตอนตารางยังเล็ก"** ตามที่เอกสารบอก
และ D11 บันทึกว่า 0.4.2 เพิ่ม NOTICE เตือนตอนสร้างด้วยข้อมูลน้อย

วัด 4 เงื่อนไข ข้อมูลปลายทางชุดเดียวกันเป๊ะ 100,000 แถว เปลี่ยนแค่ตอนที่ build

| | build ตอนมี | กลุ่มที่เห็น | เตือนไหม | recall |
|---|---|---|---|---|
| A | 50 แถว | 50/50 | **เตือน** | **1.0000** |
| B | 1,000 แถว | 50/50 | เงียบ | 0.8798 |
| C | 100,000 แถว | 50/50 | เงียบ | 0.7682 |
| D | 1,000 แถว | **5/50** | **เงียบ** | **0.2278** |

**คำเตือนออกกับ A ซึ่งได้ recall สมบูรณ์ และเงียบสนิทกับ D ซึ่งผลหายไป 77%**

`DETAIL` ของ NOTICE เขียนว่า *This will cause low recall* — **วัดแล้วเป็นเท็จ**
บนชุดข้อมูลนี้ กรณีที่ถูกเตือนได้ recall เต็มทั้ง 5 รอบ ช่วงแกว่ง 0.0000

### ทำไมถึงออกผิดที่

เกณฑ์ของ pgvector คือ `rows < lists` (วัดเส้นแบ่งแล้ว: ออกที่ 50 ไม่ออกที่ 100)
คือ **จำนวนแถว** ซึ่งไม่ใช่ตัวแปรที่ทำให้พัง

ตัวแปรจริงคือ *ตัวอย่างตอน build เป็นตัวแทนของข้อมูลสุดท้ายหรือไม่*
50 แถวที่ครอบคลุมครบ 50 กลุ่ม ดีกว่า 1,000 แถวที่มาจาก 5 กลุ่ม **20 เท่า**
และความเป็นตัวแทนไม่มีอยู่ใน catalog ที่ไหนเลย จึงเตือนจากมันไม่ได้

### ความผิดพลาดในการออกแบบของผมเอง ที่เกือบทำให้สรุปผิด

รอบแรกทำแค่ A/B/C แล้วได้ผลกลับด้าน (A = 1.0000) **เกือบรายงานว่า
"เอกสารแนะนำผิด build เร็วดีกว่า"**

สาเหตุจริงคือ corpus เรียง id แบบสลับกลุ่ม **50 แถวแรกจึงมีครบ 50 กลุ่มพอดีกลุ่มละ 1**
= ตัวอย่างที่เป็นตัวแทนสมบูรณ์แบบ ซึ่งไม่ใช่สิ่งที่เกิดในงานจริง
A/B จึงวัด *"build เร็วบนตัวอย่างที่เป็นตัวแทน"* ไม่ใช่ความผิดพลาดที่ตั้งใจจะวัด

→ เพิ่มเงื่อนไข D (ข้อมูลชุดแรกเอียง) แล้วภาพถึงครบ

**บทเรียน:** ข้อมูลสังเคราะห์ที่ "สะอาด" ซ่อน fault ได้ · corpus ชุดนี้สร้างมาให้
กลุ่มกระจายสม่ำเสมอตาม id ซึ่งทำให้ตัวอย่างช่วงแรก**เป็นตัวแทนโดยบังเอิญ**
ในระบบจริงข้อมูลชุดแรกมักเอียง (ลูกค้ากลุ่มแรก · ภูมิภาคแรก · หมวดแรก)

### ข้อที่สามที่ชื่อเดิมถูกการวัดหักล้าง

| fault | ชื่อเดิมโทษ | ความจริง |
|---|---|---|
| **Q02** | เขียน `WHERE` แทน `ORDER BY` | `WHERE` ไม่ใช่ตัวปัญหา · การขาด `ORDER BY` + `LIMIT` ต่างหาก |
| **I04** | k-means ของ IVFFlat สุ่ม | HNSW ที่ไม่มี k-means ก็แกว่ง · ตัด parallel ออกแล้วยังแกว่ง |
| **I02** | สร้างตอนตารางยังเล็ก | ขนาดไม่ใช่ตัวแปร · **ความเป็นตัวแทน**ต่างหาก |

**สามในสิบสองข้อ ตั้งชื่อจากสมมติฐาน แล้วการวัดหักล้าง**
ทุกครั้งจับได้ด้วยกลุ่มควบคุม ไม่ใช่ด้วยการอ่านผลหลัก
→ **ยืนยันกฎเหล็กข้อ 1 ตรงตัว** — อย่าให้ผู้ช่วย AI (หรือชื่อที่ตั้งไว้ตอนวางแผน) เป็นคนตัดสิน

---

## E32 — ชื่อของ I04 โทษผิดตัว และกลุ่มควบคุมเป็นคนจับได้ (2026-07-27)

`FAULTS.md` และ `EVIDENCE.md` ลงทะเบียน I04 ไว้ว่า **"k-means ของ IVFFlat สุ่ม"**
มาตั้งแต่ต้น และ E27 กับ Q04 ก็เจอสัญญาณที่เข้ากันได้กับชื่อนั้น 2 ครั้ง

**ถ้าวัดแต่ IVFFlat ก็จะยืนยันชื่อเดิมได้อย่างสวยงาม** — ข้อมูลเข้ากันได้ทุกอย่าง

แต่กลุ่มควบคุม ข วัด **HNSW** ด้วย ซึ่งเป็นกราฟ **ไม่มี k-means อยู่ในนั้นเลย**

```
IVFFlat probes=1   เปลี่ยนคำตอบ 196 / 200 query
HNSW    ef=40      เปลี่ยนคำตอบ 132 / 200 query   ← ไม่มี k-means แต่ก็เปลี่ยน
```

สมมติฐานถัดไปที่สมเหตุสมผลที่สุดคือ parallel build ทำให้ลำดับใส่ข้อมูลไม่แน่นอน
(HNSW เป็นกราฟ กราฟขึ้นกับลำดับ) → ควบคุม ค ตั้ง
`max_parallel_maintenance_workers = 0` แล้ววัดใหม่ 5 build

```
ปิด parallel:  IVFFlat 200/200 เปลี่ยน  ·  HNSW 127/200 เปลี่ยน
```

**ยังแกว่งทั้งคู่ → ไม่ใช่เรื่อง parallel เช่นกัน**

**เปลี่ยนชื่อเป็น "สร้าง index ใหม่แล้วได้คำตอบไม่เหมือนเดิม"**
ซึ่งเป็นสิ่งที่**วัดได้** ส่วนกลไกของ HNSW ยังไม่รู้ และบันทึกไว้ว่ายังไม่รู้

**บทเรียน:** ชื่อ fault ที่ตั้งไว้ตอนวางแผน คือสมมติฐาน ไม่ใช่ข้อเท็จจริง
และสัญญาณที่ "เข้ากันได้" 2 ครั้ง (E27 · Q04) ก็ยังไม่พอยืนยันสาเหตุ
**กลุ่มควบคุมคือสิ่งเดียวที่จับได้** — ถ้าไม่วัด HNSW ด้วย โปรเจคนี้จะประกาศ
สาเหตุที่ผิดออกไปพร้อมตัวเลขสวยๆ รองรับ

รูปแบบเดียวกับ **Q02** ที่ชื่อเดิมโทษ `WHERE` แล้วพบว่า `WHERE` ไม่ใช่ตัวปัญหา
ต่างกันตรงที่ Q02 จับได้จากตัวแปรที่ 3 ในตารางเดียวกัน ส่วน I04 ต้องมีกลุ่มควบคุมแยก

> **กฎที่เพิ่ม:** fault ที่บอกว่า "เพราะกลไก X ของ index ชนิดหนึ่ง"
> ต้องวัด index อีกชนิดที่**ไม่มี**กลไกนั้นเป็นกลุ่มควบคุมเสมอ
> ไม่งั้นยืนยันได้แค่ว่า "เกิดขึ้นจริง" ไม่ใช่ "เกิดเพราะอะไร"

---

## E30 — 🔴 I05 มีตัวตรวจ แต่ไม่เคยพิสูจน์ว่ามันพลิกได้ (2026-07-27)

**เจอตอน audit ก่อนส่งงานให้ผู้ช่วยคนถัดไป ไม่ได้เจอตอนทำ I05**

I05 ผ่านครบทุกอย่างที่ตาเห็น: วัด build 3 ค่า · assertion ผ่าน 3/3 · 3 รอบติด ·
มี `groundtruth/i05.json` · มีสาขาใน `sql/score.sql` บรรทัด 309 · commit เรียบร้อย

**แต่ `results/i05_phase3_cycle.txt` ทั้ง 102 บรรทัด ไม่มีคำว่า DETECTED เลยสักครั้ง**
เพราะตัวฉีดของ I05 **ไม่เคยเรียก `score.sql`** — ตัวตรวจถูกเขียนแล้วไม่เคยถูกรัน

นี่ผิดสูตรข้อ 6 ใน `CLAUDE.md` ตรงๆ: *"ตัวนับที่ไม่มีวันตอบลบ = ไม่ได้วัดอะไรเลย"*

**ทำไมถึงหลุด:** I01 I03 Q02 Q04 Q06 V07 ล้วนมีไฟล์ `*_checker_states.sql` แยก
I05 ไม่มี และไม่มีอะไรในกระบวนการที่บังคับให้มี — ผ่านเพราะ**ไม่มีใครถาม**
รูปแบบเดียวกับ E26 (`2>/dev/null` กลบ) และ E17 (seed พังตั้งแต่ commit แรก):
**สิ่งที่ไม่ถูกตรวจ ไม่ได้แปลว่าถูกต้อง**

**แก้แล้ว:** `sql/i05_checker_states.sql` → `results/i05_checker_states.txt`

```
64MB  → DETECTED       (ความจุ 28,352 < 100,000)
256MB → NOT_DETECTED   (ความจุ 113,408 > 100,000)
225MB → DETECTED       (ความจุ 99,675  < 100,000)   ← ขอบ
226MB → NOT_DETECTED   (ความจุ 100,118 > 100,000)   ← พลิกที่ 1MB
```

ตัวตรวจพลิกตรงขอบที่คำนวณล่วงหน้าพอดี · ไม่ต้อง build สักครั้ง (ใช้เวลารวมไม่ถึงนาที
เทียบกับ 72 วินาทีต่อ build ตอนวัดจริง) · `qf_corpus` fingerprint ไม่เปลี่ยน ·
`maintenance_work_mem` กลับเป็น 64MB

**ยังพิสูจน์ไม่ได้ 2 ทาง** — CANNOT_CHECK จาก "ไม่มีตาราง" และ "มิติไม่ใช่ 384"
ต้อง DROP หรือ ALTER corpus ที่ล็อกไว้ → บันทึกว่ายังไม่ได้พิสูจน์ ไม่อ้างว่าครบ

**กฎที่เพิ่มจากข้อนี้:** fault จะนับว่าเสร็จได้ ต้องมีไฟล์ `*_checker_states.sql`
หรือมีผลรัน `score.sql` อยู่ใน `results/` **ห้ามนับว่าเสร็จจาก assertion อย่างเดียว**

---

## E31 — `n_distinct` เป็นค่าประมาณ แต่เอกสารสามที่จดเป็นค่าตายตัวคนละค่า (2026-07-27)

เจอตอน audit เดียวกับ E30

```
CLAUDE.md  บรรทัด 453  : n_distinct = 4695
DECISIONS.md E17 ตาราง : n_distinct = 4,656
วัดสดตอน audit         : n_distinct = 4679
```

ข้อมูลไม่ได้เปลี่ยนแม้แต่แถวเดียวระหว่างสามครั้งนี้

**ไม่ใช่บั๊ก** — `n_distinct` ใน `pg_stats` มาจากการ**สุ่มตัวอย่าง**ของ `ANALYZE`
จึงเปลี่ยนทุกครั้งที่ ANALYZE ใหม่ ต่างจาก `count(*)` ที่เป็นการนับจริง
(`merchant_id=1` = 23,983 และ `=4999` = 6 ตรงกันทุกครั้ง เพราะเป็นการนับ)

**ความผิดคือการจดค่าประมาณเหมือนเป็นค่าที่วัดได้** ซึ่งขัดกฎเหล็กข้อ 2 โดยอ้อม —
กฎบอกว่า "ห้ามใส่ตัวเลขที่ยังไม่ได้วัด" แต่ไม่ได้บอกว่า
**ตัวเลขที่วัดแล้วบางตัวก็ไม่คงที่**

**แก้แล้ว:** เขียนเป็น `~4,68x` พร้อมช่วงที่เคยวัดได้ทั้งสามค่า
**ข้อสรุปไม่กระทบ** — 100/4,656 · 100/4,679 · 100/4,695 ล้วนปัดเป็น **2.1%**
ฐานรากของ F10 จึงยังยืน

---

## E28 — ทางแก้ของ I05 มี failure mode ของตัวเอง และ error ชี้ผิดที่ (2026-07-27)

ตอนทำ I05 ตั้งใจไล่ `maintenance_work_mem` ถึง 1GB เพื่อเป็นกลุ่มควบคุม
build ล้มเหลวด้วย:

```
ERROR:  could not resize shared memory segment "/PostgreSQL.2192554940"
        to 1070633728 bytes: No space left on device
```

**อ่านแล้วเข้าใจว่าดิสก์เต็ม** — ของจริงคือ `/dev/shm` ของคอนเทนเนอร์เต็ม
`docker-compose.yml` ตั้ง `shm_size: 256mb` ส่วน HNSW build แบบขนาน
จอง dynamic shared memory ตามขนาด `maintenance_work_mem`
ที่ 1GB จึงขอ DSM ~1GB บน shm ที่มี 256MB

**ยืนยันกลไกด้วยการทดลอง ไม่ใช่เดา:**
```sql
SET maintenance_work_mem = '1GB';
SET max_parallel_maintenance_workers = 0;   -- ปิด parallel build
CREATE INDEX ... USING hnsw ...;            -- ผ่านทันที
```
ปิด parallel แล้วผ่าน → ตัวแปรคือ DSM ของ parallel build จริง
(`max_parallel_workers_per_gather = 0` ที่ตั้งไว้แล้วไม่ช่วย เพราะคุมคนละอย่าง —
มันคุม parallel ของ **query** ส่วน build ใช้ `max_parallel_maintenance_workers`)

### ทำไมข้อนี้มีค่าเกินกว่าจะเป็นแค่บั๊กของ harness

**ทางแก้ที่เอกสารแนะนำสำหรับ I05 คือ "เพิ่ม `maintenance_work_mem`"**
แต่เพิ่มมากไปในคอนเทนเนอร์ที่ `shm` เล็ก จะทำให้ `CREATE INDEX` **ล้มเหลวไปเลย**
จากเดิมที่แค่ช้า

| ค่า | ผลลัพธ์ |
|---|---|
| ต่ำเกิน | build ช้า + NOTICE เตือน (คือ I05) |
| พอดี | build เร็ว ไม่มี NOTICE |
| **สูงเกิน shm** | **build ล้มเหลวทั้งหมด · error ชี้ไปที่ดิสก์** |

**และข้อความ error ชี้ผิดที่** — พูดถึง "No space left on device"
ทั้งที่ดิสก์ว่าง คนที่เจอจะไปไล่ดู disk usage ก่อนเสมอ
เป็นตระกูลเดียวกับ **F03** (error เดียวกัน คนละสาเหตุ) และ **E10**
(`l2_norm ... is not unique` ที่ต้นเหตุคือไม่มี overload เลย)

**บทเรียนสำหรับรายงาน:** ความล้มเหลวเงียบไม่ได้แก้ด้วยการหมุนพารามิเตอร์ขึ้น
พารามิเตอร์ทุกตัวมีเพดานที่มาจากที่อื่นในระบบ และเพดานนั้นมักไม่อยู่ในเอกสารของ pgvector
เพราะมันเป็นข้อจำกัดของคอนเทนเนอร์ ไม่ใช่ของ pgvector

**ผลต่อการออกแบบ I05:** ใช้ `64MB / 128MB / 256MB` แทน
256MB ไม่มี NOTICE แล้ว จึงเป็นกลุ่มควบคุมที่ใช้ได้ และอยู่ในเพดาน shm
พร้อมให้สคริปต์แยก **"ชนข้อจำกัดสภาพแวดล้อม"** ออกจาก **"fault ไม่เกิด"** ให้ชัด

---

## E26 — `2>/dev/null` กลบความล้มเหลวเป็นครั้งที่สามในวันเดียว (2026-07-27)

harness ของ I01 รัน 4 รอบ assertion ผ่าน 4/4 ทุกรอบ
แต่**ตัวตรวจ static ขึ้น `CANNOT_CHECK` ทั้ง 12 ครั้ง** ทั้งที่ทดสอบมือแล้วมันแยกได้ครบ

**ต้นเหตุ:** ผมเขียน `CREATE INDEX` ฝังใน `bash -c` ที่ครอบด้วย single quote
แล้วพยายาม escape ด้วย `E'` ซึ่ง bash ไม่ตีความ
SQL จึงได้สตริงพัง → `CREATE INDEX` ล้มเหลว → ไม่มี index → ตัวตรวจตอบถูกแล้วว่าตรวจไม่ได้

**และ error ไม่โผล่เพราะผมใส่ `2>/dev/null` ไว้เอง**

**นี่คือครั้งที่สามในเซสชันเดียว:**

| ครั้ง | ที่ไหน | ผลที่ตามมา |
|---|---|---|
| 1 | IVFFlat build ใน Q01 | ชน `temp_file_limit` แล้วเงียบ |
| 2 | ทดสอบตัวตรวจ I01 ครั้งแรก | index ไม่ถูกสร้าง ตัวตรวจตอบ CANNOT_CHECK สามครั้งติด |
| 3 | harness 4 รอบของ I01 | ผล 12 ช่องผิดทั้งหมด |

**กฎที่ตั้งจากบทเรียนนี้:**
> **ห้ามใส่ `2>/dev/null` รอบคำสั่งที่เปลี่ยนสภาพระบบ** (`CREATE INDEX`, `INSERT`, `ALTER`)
> ใส่ได้เฉพาะรอบคำสั่งที่แค่อ่านค่า และแม้กระนั้นก็ควรหลีกเลี่ยง
>
> **ห้ามฝัง SQL ที่มี quote ซับซ้อนใน `bash -c`** — ย้ายไปเป็นไฟล์ `.sql`
> เคยเจอแล้วสองรอบ (E19 ตัวเฝ้าดูของ F03 · ครั้งนี้)

**แก้:** ย้ายไป `sql/i01_checker_states.sql` แล้วรันด้วย `psql -f` ตรงๆ
เจอบั๊กซ้อนอีกสองชั้นทันทีที่ error ไม่ถูกกลบ:
1. `score.sql` สร้าง `CREATE TEMP TABLE score_result` โดยไม่ `DROP` ก่อน
   → เรียกซ้ำในเซสชันเดียวไม่ได้ → แก้เป็น `DROP TABLE IF EXISTS` ก่อน
2. รอบที่ตายกลางคันทิ้ง index ค้างไว้ → รอบถัดไปเริ่มจากสภาพไม่สะอาด
   แล้วรายงาน `DETECTED` ในสถานะที่ควรเป็น `CANNOT_CHECK`
   → เพิ่ม `DROP INDEX IF EXISTS` ที่ต้นสคริปต์

หลังแก้: ตัวตรวจแยกได้ครบ 3 สถานะ **3 รอบติด**

---

## E27 — สัญญาณของ I04 โผล่มาเองระหว่างทำ I01 (2026-07-27) · ยังไม่ใช่ข้อสรุป

รัน I01 สามรอบด้วย IVFFlat opclass ถูก บน corpus ชุดเดียวกัน พารามิเตอร์เดียวกัน
ในเซสชันเดียวกัน ได้ recall@10 ต่างกันทุกรอบ:

```
0.7780 · 0.8020 · 0.7415      ช่วง 0.0605 = 8% ของค่ากลาง
```

(และถ้านับรอบทดสอบก่อนหน้ากับรอบ Q01 ด้วย จะได้ 0.8675 กับ 0.7870 เพิ่ม
รวม 5 ค่า ช่วง 0.7415–0.8675 = 17% — แต่สองค่านั้นมาจากคนละรอบการรัน
สภาพ cache ต่างกัน จึงคุมตัวแปรไม่เท่ากัน)

**เข้ากันได้กับ I04** — IVFFlat ใช้ k-means ซึ่งมีการสุ่มอยู่ข้างใน
ข้อมูลเดิม โค้ดเดิม สร้าง index ใหม่ได้คนละคำตอบ

**แต่ยังไม่ใช่ข้อสรุป** เพราะ:
- ไม่ได้ออกแบบมาเพื่อวัดข้อนี้ · 3 ตัวอย่างน้อยเกินไป
- ยังไม่ได้แยกว่าเป็นการสุ่มของ k-means จริง หรือเป็นผลข้างเคียงอย่างอื่น
- `EVIDENCE.md` ระบุว่า I04 ต้องพิสูจน์ด้วยการสร้าง index **5 รอบ** โดยเฉพาะ

→ บันทึกไว้เป็นสัญญาณ **ห้ามอ้างเป็นผลของ I04 จนกว่าจะทำ I04 จริง**
และเมื่อทำ I04 ต้องออกแบบให้คุมตัวแปรครบ ไม่ใช่เอาตัวเลขชุดนี้ไปใช้

---

## E25 — ตัวตรวจ "planner ใช้ index ไหม" ของผมเอง ตอบผิดทุกครั้ง (2026-07-27)

ตอนทำ I01 เขียนตัวตรวจว่า plan มีคำว่า `Index Scan` ไหม แล้ว assertion ตกทันที
`ข้อ 1 ตก: opclass ผิดแต่ planner ยังใช้ index`

**แต่ catalog ยืนยันชัดว่ามันใช้ไม่ได้:**
```
vector_l2_ops     รองรับ  <->(vector,vector)
vector_cosine_ops รองรับ  <=>(vector,vector)
```
`<=>` ไม่อยู่ใน `vector_l2_ops` เลย → planner ใช้ index l2 กับ `<=>` ไม่ได้แน่นอน

**ต้นเหตุมีสองชั้น ซ้อนกัน**

**ชั้นที่ 1 — query ที่เอาไป EXPLAIN มี index scan ของตารางอื่นปนอยู่**
```sql
ORDER BY embedding <=> (SELECT embedding FROM qf_queries WHERE id = 1)
```
subquery นั้นใช้ **primary key ของ `qf_queries`** ซึ่งเป็น `Index Scan`
ตัวตรวจที่ค้นคำว่า "Index Scan" ทั้ง plan จึงเจอมันทุกครั้ง
→ **ตอบว่า "ใช้ index" แม้ในกรณีที่ไม่ได้ใช้**

**ชั้นที่ 2 — พอแก้เป็นอ่าน JSON ก็ยังดูตื้นไป**
ดูเฉพาะลูกโดยตรงของ `Plan` แต่แผนของ Seq Scan คือ
`Limit → Sort → Seq Scan` — `qf_corpus` อยู่ลึกสองชั้น หาไม่เจอ
แล้ว fallback ไปคืน `Limit` ซึ่ง "บังเอิญ" ไม่มีคำว่า Index จึงตอบถูกโดยบังเอิญ

**แก้:** ใช้ `EXPLAIN` แบบ TEXT แล้ว match บรรทัดที่สแกน `qf_corpus` ตรงๆ
```
regexp_match(plan_txt, '([A-Z][A-Za-z ]*Scan)[^
]* on qf_corpus')
```
พร้อม **exception ถ้าหา node ไม่เจอ** — กฎเหล็กข้อ 10 หาไม่เจอ = ตรวจไม่ได้ ห้ามเดา

**บั๊กที่สาม ในวันเดียวกัน:** assertion ข้อ 3 เช็ค
`count(*) = 2 FROM pg_opclass WHERE opcname IN (...)` แล้วตก
เพราะ opclass ชื่อเดียวกัน**มีทั้งของ hnsw และ ivfflat** จึงได้ 4 ไม่ใช่ 2
→ แก้เป็น `count(DISTINCT opcname)`

**บทเรียนที่ตรงกับวิทยานิพนธ์:** ตัวตรวจที่ค้นคำในข้อความ (string matching)
เป็นตัวตรวจที่**ให้ false positive ได้เงียบๆ** — มันเจอสิ่งที่มองหา
แต่เจอในที่ที่ไม่เกี่ยวข้อง ตัวเลขที่ได้จึงดูสมเหตุสมผลเสมอ
`quietfail-check` ต้องอ่านโครงสร้าง (catalog / plan node ที่ระบุตาราง)
ไม่ใช่ค้นคำใน output

**ผลกระทบต่อผลเก่า:** `qf_q01_measure` ใน Q01 ใช้ query ที่มี subquery แบบเดียวกัน
แต่ใช้มันคำนวณ **buffers** เท่านั้น ไม่ได้ใช้ตัดสินว่า index ถูกใช้ไหม
buffers ของ query ตัวแทนจึงรวม page ของ `qf_queries` pkey ไปด้วยไม่กี่ block
(exact 20,002 · HNSW 979) — **ไม่กระทบตัวเลข recall และ speedup**
ซึ่งมาจากลูป 200 query กับ `qf_recall_at()` คนละทางกัน จึงไม่รันใหม่

---

## D18 — ลบ `PROJECT.md` ข้อ 7 และปิด F13 ถาวร (2026-07-27)

**เจ้าของโปรเจคสั่ง** ปิดหนี้ค้าง 2 รายการที่ audit เมื่อเช้าเปิดไว้

### ก) `PROJECT.md` ข้อ 7 — ลบเนื้อหา คงเลขข้อ

ข้อ 7 สั่งวัด **4 ขนาด (100k/500k/2M/5M)** ซึ่งมีไว้รองรับ **L01 ที่ D15 ตัดไปแล้ว**
ส่วนเกณฑ์ผ่านเฟส 2 (ข้อ 9) ต้องการแค่ **2 ระดับ** — เอกสารสั่งงานที่ไม่มีใครต้องทำแล้ว

และเนื้อหาที่เหลือซ้ำกับ `CLAUDE.md` เกือบทั้งหมด **แล้วเคลื่อนออกจากกันจริง**
เป็นรูปแบบเดียวกับที่ D15 แก้ไปแล้วครั้งหนึ่ง (รายการ fault เคยอยู่สามที่)

**คงหัวข้อ "## 7." ไว้เป็นป้ายหลุมศพ ไม่เรียงเลขใหม่**
เพราะ `CLAUDE.md` · `DECISIONS.md` · `sql/*.sql` อ้าง "ข้อ 8 / 9 / 10" อยู่
เรียงใหม่ = การอ้างอิงเลื่อนทั้งหมดโดยไม่มีอะไรเตือน — หลักการเดียวกับที่ D15
ห้ามเรียงเลข fault ใหม่ และเทียบ E09

**ย้ายของก่อนลบ:** "กฎการรันซ้ำ" เป็นชิ้นเดียวที่ไม่ได้อยู่ที่อื่น → ย้ายเข้า `CLAUDE.md`
พร้อม**เพิ่มข้อใหม่จากผลของ I04**: ต้องรายงานราย query ด้วยเมื่อเกี่ยวกับ recall
เพราะ I04 แสดงว่าค่าเฉลี่ยนิ่งได้ทั้งที่ query เกือบทุกข้อเปลี่ยนคำตอบ

**แก้การอ้างอิงที่จะค้าง:** `sql/qf10` · `qf11` (2 ที่) · `qf12` เคยอ้าง "ข้อ 7" → ชี้ไป `CLAUDE.md`
โดยเฉพาะ `qf12` ที่เขียนว่าขนาดคือ 100k/500k/2M/5M ซึ่งไม่จริงแล้ว

### ข) F13 — ลบส่วนขยาย เก็บแถวในทะเบียนบัคระหว่างทาง

F13 (index ทำให้ช้าลง 118% เพราะ lossy bitmap) มีอยู่ 2 ที่ใน `FAULTS.md`:
แถวเดียวในทะเบียนบัคระหว่างทาง และส่วนขยายยาวใต้ทะเบียน

ส่วนขยายมีข้อความ *"ยังไม่ตัดสินให้ เพราะเป็นการตัดสินใจของเจ้าของโปรเจค"*
→ **เจ้าของตัดสินแล้ว: อยู่ในทะเบียนบัคระหว่างทางถาวร ลบส่วนขยาย**

**ไม่มีหลักฐานหาย** — ตัวเลข EXP01c อยู่ใน `DECISIONS.md` (E08 · หัวข้อ *แก้ไข หลัง EXP01c*)
และ `results/exp01c_work_mem_threshold.txt` · ส่วนบทเรียนกลายเป็น **กฎเหล็กข้อ 7ก** ไปแล้ว

---

## D17 — ทำตัวตรวจ repo อัตโนมัติ เพื่อให้ `CLAUDE.md` เชื่อถือได้โดยไม่ต้องตรวจซ้ำ (2026-07-27)

**ปัญหา:** context window เต็มบ่อย ผู้ช่วยคนถัดไปต้องเลือกระหว่าง
เชื่อ `CLAUDE.md` แบบไม่มีหลักประกัน หรือไล่ตรวจเอง — ซึ่ง audit เต็มรอบหนึ่ง
กินพื้นที่ไปเกือบครึ่ง session แล้วเหลือที่ทำงานจริงน้อยมาก

**ทางเลือกที่ปฏิเสธ**

| ทางเลือก | ทำไมไม่เอา |
|---|---|
| เขียน "ตรวจแล้วนะ" ลง `CLAUDE.md` เฉยๆ | เป็นคำกล่าวอ้าง ไม่ใช่หลักฐาน · เก่าเงียบๆ ได้โดยไม่มีอะไรบอก |
| สคริปต์ PowerShell + bash คู่กัน | ตรรกะเดียวกันสองที่ = เคลื่อนออกจากกันแน่นอน (รูปแบบเดียวกับ D15) |
| ฝัง SQL ยาวใน `bash -c` | กับดักข้อ 2 ที่ละเมิดมาแล้ว 5 ครั้ง |

**ที่เลือก:** `scripts/audit.py` ไฟล์เดียว รันได้ทั้ง PowerShell และ Bash
เรียก `psql` ด้วย list ไม่ผ่าน shell → SQL ที่มี `'` ไม่ต้อง escape เลย
ค่าที่ใช้เทียบแยกไว้ใน `scripts/audit_baseline.json` ไม่ฝังในโค้ด

**ตอบ 3 อย่างเหมือน `score.sql`** — `PASS` / `FAIL` / `CANNOT_CHECK`
และ `CANNOT_CHECK` ทำให้ exit code ไม่ใช่ 0 (docker ไม่ขึ้น = ตรวจไม่ได้ ≠ ผ่าน)
**เป็นการเอากฎเหล็กข้อ 10 ของโปรเจคมาใช้กับตัวโปรเจคเอง**

**สามข้อที่ audit ครั้งแรกเจอ (E30 · E31 · หลักฐานไม่ถูก track)
กลายเป็นหัวข้อถาวรในตัวตรวจแล้ว** — บั๊กที่เคยหลุดหนึ่งครั้ง จะหลุดซ้ำไม่ได้อีก

### ตัวตรวจจับบั๊กของตัวเองได้ 3 อย่างในการรันครั้งแรก

1. เขียนรายงานทับที่เดิมทุกครั้ง → working tree สกปรกเสมอ → **ตัวเองไม่มีวันผ่าน**
   แก้ด้วยการยกเว้นรายงานของตัวเองออกจากการนับ
2. ตัดชื่อไฟล์จาก `git status --porcelain` ด้วยตำแหน่งคงที่ `[3:]` → ได้ `LAUDE.md`
   porcelain มีหลายรูปแบบ (`' M x'` `'M  x'` `'?? x'`) ต้องใช้ `split()`
3. **ค้นคำว่า "เหลือ" ทั้งไฟล์ → ไปโดนประโยค "จนเหลือที่ทำงานจริงน้อยลง"
   ที่ผมเพิ่งเขียนเองในย่อหน้าอื่น** แล้วรายงานว่าสถานะไม่ตรง
   → แก้เป็นเกาะบรรทัด `กำลังทำ :` เท่านั้น

ข้อ 3 **คือกฎเหล็กข้อ 6 ซ้ำอีกครั้ง** (ห้าม match ข้อความ ให้อ่านโครงสร้าง)
คราวนี้เกิดกับตัวตรวจของ repo แทนที่จะเป็นตัวตรวจ query plan — เทียบ E25

**สิ่งที่ตัวตรวจนี้ไม่ได้ตรวจ ต้องพูดให้ชัด:** ไม่ได้รัน fault ซ้ำ
ยืนยันแค่ว่า*หลักฐานมีอยู่และครบรูปแบบ* ไม่ใช่ว่าผลยัง reproduce ได้
และไม่ยืนยัน verdict ของ `score.sql` ว่าต้องเป็นค่าใด เพราะ Q02 อ่าน
`pg_stat_statements` ซึ่งเปลี่ยนตามประวัติการรัน — เกณฑ์ที่แกว่งจะทำลายความเชื่อถือ
มากกว่าจะสร้าง

---

## D16 — เฟส 2.5 ตอบอะไรได้ และตอบอะไรไม่ได้ (2026-07-27)

**คำถามที่เฟส 2.5 ตั้งไว้:** "recall collapse เป็นบั๊กของ pgvector หรือเป็นธรรมชาติของ ANN"

**วิธีตอบ:** รัน Q01 บน Qdrant 1.12.4 โดยคุมทุกอย่างให้เหมือนเดิม
corpus ส่งออกทีละแถวจาก `qf_corpus` · ชุด query fingerprint เดิม ·
**เฉลยใช้ `qf_truth` จาก exact search ของ PostgreSQL** · สูตร recall เดียวกัน
→ ตัวแปรเดียวที่ต่างคือ engine

**ตัวสอบทานที่ทำให้เทียบได้จริง:** Qdrant โหมด `exact` ได้ recall = 1.0000
เทียบกับเฉลยของ PostgreSQL — ถ้าข้อนี้ไม่ผ่าน ตัวเลขที่เหลือไม่มีความหมาย

| engine (สองแถวแรก ef เท่ากัน) | recall@10 | recall@100 |
|---|---|---|
| pgvector HNSW `ef_search=40` | 0.8565 | 0.3903 |
| Qdrant HNSW `hnsw_ef=40` | 0.9945 | 0.9895 |
| Qdrant ค่าเริ่มต้นของตัวเอง | 1.0000 | 0.9996 |

### ✅ เขียนลงรายงานได้

**recall loss ไม่ใช่บั๊กของ pgvector** — Qdrant ก็เสียเหมือนกันเมื่อความพยายามค้นหาเท่ากัน
`0.9945` ไม่ใช่ `1.0` · แย่สุดต่อ query = 0.90 · ที่ k=100 ได้ครบแค่ 69 จาก 200 query
**การค้นแบบประมาณทำผลลัพธ์หายจริงในทั้งสอง engine**

### ⛔ ห้ามเขียน — และนี่คือส่วนที่สำคัญกว่า

**ห้ามเขียนว่า "ANN ทุกตัวพังเท่ากัน"** ขนาดต่างกันมหาศาล
pgvector recall@100 = 0.39 ส่วน Qdrant ที่ ef เท่ากัน = 0.99

**สาเหตุที่ต่างคือพฤติกรรมเมื่อ `ef < k` ซึ่งเป็นเรื่องของแต่ละ engine**
- pgvector ปล่อยให้ `ef_search=40` ตัดคุณภาพของ `LIMIT 100` ทิ้ง = กลไกของ **Q06**
- Qdrant ตั้ง `hnsw_ef=40` กับ `limit=100` แล้ว recall ยัง 0.9895 = ไม่ปล่อย

**ประโยคที่ล็อกไว้ใช้ในรายงาน:**
> การค้นแบบประมาณทำให้ผลลัพธ์หายจริงในทั้งสอง engine — มันคือธรรมชาติของ ANN
> แต่ **ขนาด** ของความเสียหายถูกกำหนดโดยค่าเริ่มต้นที่ผู้พัฒนาเลือก
> และโดยพฤติกรรมเมื่อ `ef < k` ซึ่งเป็นเรื่องของแต่ละ engine ไม่ใช่ของ ANN

**ทำไมข้อสรุปแบบนี้ดีกว่าที่ตั้งใจไว้ตอนแรก:** แผนเดิม (D04) หวังว่าจะได้
"Qdrant ก็พังเหมือนกัน" เพื่อปิดคำถาม ผลจริงซับซ้อนกว่านั้น และ**ทำให้โปรเจค
พูดได้ทั้งสองด้าน** — ทั้งว่าปัญหาเป็นเรื่องทั่วไป และว่าการเลือกค่าเริ่มต้นมีผลมหาศาล
ซึ่งตรงกับข้อความหลักของ QuietFail พอดี: ปัญหาไม่ได้อยู่ที่ algorithm แต่อยู่ที่
**คนที่ deploy ไม่เคยวัดว่าค่าเริ่มต้นของตัวเองแลกอะไรไป**

**เปลี่ยนใจเมื่อ:** วัดที่ 500k บน Qdrant แล้วพบว่ามันตกแรงเหมือน pgvector
(ยังไม่ได้ทำ — upload 500k ผ่าน REST แพงกว่า 5 เท่า)

---

## E24 — Qdrant ที่ค่าเริ่มต้นล้วนๆ **ไม่สร้าง HNSW เลย** และขึ้นสถานะเขียว (2026-07-27)

เฟส 2.5 อัปโหลด corpus 100,000 จุดเข้า Qdrant 1.12.4 ด้วยค่าเริ่มต้นทั้งหมด
แล้ววัด recall ได้ **1.0000** ทั้งโหมด exact และโหมด "hnsw"

**assertion จับได้ทันที:** `ข้อ 2 ตก: Qdrant HNSW ได้ recall 1.0 — fault ไม่เกิด`

**สาเหตุ:** ไม่เคยมี HNSW เกิดขึ้นเลย
```
status: green · points_count: 100000 · indexed_vectors_count: 0 · segments: 8
```

Qdrant ตัดสินใจสร้าง HNSW **ต่อ segment** โดยเทียบกับ `indexing_threshold`
ซึ่งมีหน่วยเป็น **KB ไม่ใช่จำนวนจุด**

```
100,000 จุด แตกเป็น 8 segment → segment ละ 12,500 จุด
12,500 × 384 มิติ × 4 ไบต์ = 19,200,000 ไบต์ = 18,750 KB
threshold = 20,000 KB   →  ต่ำกว่าอยู่ 6%  →  ไม่สร้าง index สักตัว
```

ยืนยันกลไกด้วยการลด `indexing_threshold` แล้ว indexing เริ่มทำงานทันที

**ทำไมข้อนี้มีค่ามากกว่าที่คิด — มันคือ Q01 ในอีกรูปแบบหนึ่ง**

| | ผู้ใช้เห็น | ของจริง |
|---|---|---|
| สถานะ collection | `green` = ปกติดี | ไม่มี index สักตัว |
| ผลการค้นหา | ถูกต้อง 100% | ถูกเพราะ brute force |
| ความเร็ว | "ก็เร็วดีนะ" | ที่ 100k ยังไม่ทันช้า |

ทีมที่ย้ายมาใช้ vector database เพราะ "อยากได้ ANN index" อาจ**ไม่ได้ index เลย**
แล้วไม่มีอะไรบอก — จนกว่าข้อมูลจะโตพอให้ brute force ช้าจนทนไม่ไหว
**หรือจนกว่า segment จะโตข้าม threshold แล้ว recall ตกลงเองโดยไม่มีใครแตะอะไร**

**ต่างจาก Q01 ของ pgvector ตรงทิศทาง:**
- pgvector — สั่ง `CREATE INDEX` แล้วได้ index จริง recall ตกทันที
- Qdrant — คิดว่ามี index แต่ยังไม่มี recall ยังเต็ม **แล้วจะตกทีหลังเมื่อข้อมูลโต**

**บทเรียนสำหรับ `quietfail-check`:** ต้องตรวจว่า index **มีอยู่จริงและครอบคลุมทุกแถว**
ไม่ใช่แค่ตรวจว่ามีคำสั่งสร้าง index อยู่ในโค้ด
ฝั่ง pgvector เทียบเท่ากับการตรวจ `indexed_vectors_count` คือดู `pg_index` + จำนวนแถวที่ index ครอบคลุม

**สิ่งที่ทำผิดแล้วต้องบันทึก:** สคริปต์รอบแรกรอแค่ `status == "green"`
ทั้งที่ docstring ของตัวเองเขียนเตือนไว้แล้วว่า "ถ้าไม่รอ index เสร็จจะได้ recall ดีเกินจริง"
→ เขียนเงื่อนไขรอผิด แก้เป็นรอ `indexed_vectors_count >= points_count`

---

## E23 — E16 ซ้ำรอย: ฆ่า client แล้วงานฝั่ง server ยังวิ่งต่อ 20 นาที (2026-07-27)

**บั๊กเดียวกับ E16 เป๊ะ แต่คนละบริบท และรอบนี้ผมเป็นคนก่อเอง**

สั่งรัน pipeline 500k เป็นงานเบื้องหลัง โดยเผลอใส่ `&` ซ้อนใน `docker compose exec`
พอ tool call จบ process ฝั่ง client ตาย → ผมคิดว่างานตายตามไปแล้ว

**ของจริง:** `DO $$ ... $$` ที่ส่งไปแล้วยัง**วิ่งต่อฝั่ง server**
เจอตอนไล่หาสาเหตุ:

```
pid 3783 | active | DO $$ DECLARE v_rows ... | รันมาแล้ว 1,236 วินาที
pid 8282 | active | Lock:relation | blocked_by {3783} | TRUNCATE qf_truth, qf_corpus
```

**ผลที่ตามมาเป็นลูกโซ่ และทุกอันดูเหมือนบั๊กคนละตัว:**

| อาการที่เห็น | ที่คิดตอนแรก | ของจริง |
|---|---|---|
| corpus 500k ได้ 0 แถว | qf12 พัง | orphan แย่ง lock อยู่ |
| qf13 ล้ม `null value in column ids` | เฉลยพัง | corpus ว่างเพราะข้อข้างบน |
| q01 ล้ม "เฉลย k=10 มี 0 แถว" | Q01 พัง | ผลต่อเนื่องจากสองข้อข้างบน |
| ทดสอบ 5,000 แถวค้างเกิน 5 นาที | qf12 ช้าผิดปกติ | TRUNCATE ถูกบล็อกโดย orphan |

**สิ่งที่ทำให้ไม่หลงทาง: assertion ของ qf12**
`ต้องได้ 500000 แถว แต่ได้ 0` — ถ้าไม่มี assertion ข้อนี้ pipeline จะเดินต่อ
แล้วผมจะได้ recall จาก corpus ว่างเปล่า ซึ่ง**ดูเหมือนตัวเลขที่ใช้ได้**

**แก้:** `pg_terminate_backend()` ฆ่า orphan ทิ้ง แล้วรันใหม่บนเครื่องที่ว่างจริง
และเพิ่มขั้นตอนฆ่า session กำพร้าไว้ต้น pipeline

**บทเรียนที่ต้องจำให้ได้สักที:**
> ทุกครั้งที่สั่งงานยาวผ่าน `docker exec` แล้วอยากให้มันหยุด
> **ต้องหยุดที่ฝั่ง server ด้วย `pg_terminate_backend()` เสมอ**
> การฆ่า process ฝั่ง client ไม่ได้แปลว่างานหยุด

E16 บันทึกบทเรียนนี้ไว้แล้วเมื่อวานตอนทำ F05 — **แต่บันทึกไว้ในบริบทของ `ALTER TABLE`
ผมจึงไม่ได้เชื่อมโยงมาที่ `DO` block** ทั้งที่เป็นกลไกเดียวกันทุกประการ

→ ตรงกับบทเรียนของ E17 พอดี: **เจอบั๊กรูปแบบหนึ่งแล้ว ต้องไล่หาที่อื่นที่ใช้รูปแบบเดียวกันทันที**
คราวนี้คือ "ที่อื่น" ที่ไม่ได้ไล่หา

---

## E22 — F10 ล้มสองครั้งเพราะเข้าใจ "สถิติเก่า" ผิด (2026-07-27)

เขียน F10 แล้ว assertion ตกสองรอบติด ทั้งสองรอบเป็นความเข้าใจผิดของผู้ช่วย ไม่ใช่ของ PostgreSQL

### รอบที่ 1 — วัดผิด node

อ่าน `Plan Rows` / `Actual Rows` จาก node บนสุดของ plan
ซึ่งเป็น `Aggregate` ของ `count(*)` → **ประมาณ 1 ได้จริง 1 เสมอ**
ไม่ว่าสถิติจะเพี้ยนแค่ไหน → วัดได้ "คลาด 1.0 เท่า" ตลอด

**แก้:** อ่านจาก node ของ join (`Plan -> Plans -> 0`)
ส่วน buffers ยังอ่านจาก node บนสุดได้ เพราะ EXPLAIN รวมของลูกมาให้แล้ว

### รอบที่ 2 — เข้าใจผิดว่า "สถิติเก่า" คืออะไร ⭐

ทำให้ `reltuples` ล้าสมัยแล้วคิดว่าพอ:
```
reltuples ที่ planner เห็น : 100
แถวจริง                    : 300,100
planner ประมาณ             : 191,200   ← คลาดแค่ 1.6 เท่า
```

**PostgreSQL ไม่ได้เชื่อ `reltuples` ตรงๆ** — มันปรับสเกลตาม
**จำนวน page จริงของไฟล์** เทียบกับ `relpages` ที่บันทึกไว้
ตารางโตขึ้น 3,000 เท่า มันก็ประมาณตามได้เกือบถูก

→ **ความล้าสมัยของจำนวนแถวรวมอย่างเดียว หลอก planner ไม่ได้**

**สิ่งที่หลอกได้จริงคือสถิติระดับคอลัมน์** — MCV และ histogram
ค่าที่ไม่เคยปรากฏตอน `ANALYZE` ครั้งล่าสุด จะถูกเดาว่าเจอน้อยมาก
และไม่มีกลไกไหนมาปรับสเกลให้

`FAULTS.md` เขียนถูกตั้งแต่แรกว่า "เอียงไปที่ merchant_id **ค่าใหม่**"
ผู้ช่วยอ่านข้ามคำว่า "ค่าใหม่" ซึ่งเป็นหัวใจทั้งหมดของข้อนี้

**ผลหลังแก้:**
```
ก่อน ANALYZE : ประมาณ 200 จริง 300,000 (คลาด 1,500x) · buffers 1,202,548 · Nested Loop
หลัง ANALYZE : ประมาณ 298,733 จริง 300,000 (คลาด 1.0x) · buffers 3,726 · Hash Join
```

**บทเรียน:** "สถิติเก่า" ไม่ใช่เรื่องเดียว มีสองชั้นและมีแค่ชั้นเดียวที่อันตราย
ถ้าไม่ได้ลองรัน จะเขียนรายงานผิดว่า "bulk insert แล้วไม่ ANALYZE ทำให้ planner พัง"
ทั้งที่ของจริงคือ "bulk insert **ค่าใหม่** แล้วไม่ ANALYZE ทำให้ planner พัง"

### ข้อสังเกตที่มีค่ากว่าตัว F10 — นาฬิกาโกหก buffers ไม่โกหก

```
เวลา    702 ms → 306 ms   =   2.3 เท่า
buffers 1,202,548 → 3,726 = 322.7 เท่า
```

ต่างกัน **140 เท่า** เพราะข้อมูลชุดนี้อยู่ใน cache หมด
buffer ส่วนเกิน 1.2 ล้านครั้งจึงเป็น `hit` ทั้งหมด ไม่แตะดิสก์เลย

บนเครื่องที่ข้อมูลไม่พอใส่ RAM ส่วนเกินนั้นจะกลายเป็นการอ่านดิสก์จริง
→ **ถ้าวัดด้วยนาฬิกาบนเครื่อง dev จะสรุปว่า "ช้าลงนิดหน่อย" แล้วปล่อยขึ้น production**

นี่คือตัวอย่างที่ชัดที่สุดของกฎเหล็กข้อ 7 ที่โปรเจคมีมาตั้งแต่ต้น
และเป็นเหตุผลว่าทำไม `quietfail-check` ต้องดู buffers ไม่ใช่เวลา

### หมายเหตุการแยกตาราง

F10 ใช้ตาราง `qf_f10_*` ของตัวเอง **ไม่แตะ `orders`**
เพราะ `orders` เป็นฐานของ EXP01/01b/01c ที่ตัวเลขถูกอ้างทั่วทั้ง repo
ถ้าฉีด F10 ใส่ `orders` จะเท่ากับทำลาย baseline ทุกครั้งที่รัน
(บทเรียนเดียวกับ E16 ที่ ALTER TABLE เกือบแก้ schema จริง)

---

## E21 — วัดผลแบบแข่งกับเวลา ทั้งที่หลักฐานเป็นตาราง (2026-07-26)

ตอนแรกให้ตัวนับคะแนนของ F03 วิ่งเข้าไปวัดระหว่างตัวฉีดยังทำงาน
เลียนแบบ F01/F05 ที่ใช้ `HOLD_OPEN` แล้ววัดผิดจังหวะซ้ำๆ
ได้ `CANNOT_CHECK (F03b=0)` เพราะไปวัดตอน F03 เสร็จแต่ F03b ยังไม่เริ่ม
เพิ่มไฟล์สัญญาณแล้วก็ยังพลาด

**ต้นเหตุ: ลอกรูปแบบมาผิดที่**

| fault | หลักฐานคืออะไร | ต้องวัดเมื่อไหร่ |
|---|---|---|
| F01 | สถานะสดของ `pg_stat_activity` | ระหว่าง fault ค้าง |
| F05 | สถานะสดของ `pg_locks` | ระหว่าง fault ค้าง |
| **F03** | **ตาราง `qf_f03_obs` ที่บันทึกไว้แล้ว** | **เมื่อไหร่ก็ได้** |

**แก้:** เพิ่ม `KEEP_OBS=1` ให้ตัวฉีดไม่ลบตารางตอนจบ
แล้ววัดหลังตัวฉีด exit — ตัดการแข่งเวลาทิ้งทั้งหมด

**บทเรียน:** ก่อนลอกโครงสร้างจาก fault ก่อนหน้า ต้องถามก่อนว่า
"หลักฐานของข้อนี้เป็นสถานะสด หรือเป็นบันทึก"

---

## E15 — F05 ไม่เกิด เพราะ role ที่ยิง DDL ไม่ใช่เจ้าของตาราง (2026-07-26)

รัน F05 ครั้งแรก assertion ตกทั้ง 3 ข้อ ไม่มีคิว lock เกิดขึ้นเลย

**สาเหตุ:** `FAULTS.md` เขียนวิธีฉีดว่า session B สั่ง `ALTER TABLE`
แต่**ไม่ได้ระบุว่าใช้ role ไหน** — อ่านตรงๆ แล้วเทียบกับ F01
(ซึ่งเขียนชัดว่าต้องใช้ `app`) จะเข้าใจว่าใช้ `app` เหมือนกัน

ของจริง: ตารางเป็นของ `lab` · `app` มีแค่ SELECT/INSERT/UPDATE/DELETE
```
ERROR:  must be owner of table orders
```
B ตายทันทีก่อนเข้าคิว → ไม่มีอะไรให้บล็อก → fault ไม่เกิด

**แก้:** B ใช้ role เจ้าของ (`lab`) ส่วน A และ C ยังเป็น `app`

**และมันสมจริงกว่าเดิม** — ของจริง migration รันด้วย role ที่มีสิทธิ์
ส่วนแอปรันด้วย role จำกัดสิทธิ์ fault จึงเกิดจาก **สอง role คนละตัวชนกัน**
ซึ่งอธิบายได้ว่าทำไมทีมแอปกับทีม DB ถึงมักโทษกันไปมาแทนที่จะไปดูคิว lock

**บทเรียน:** นิยาม fault ต้องระบุ **role ที่ใช้ยิงแต่ละ session** ให้ครบ
ไม่ใช่แค่คำสั่ง SQL — สิทธิ์เป็นตัวแปรที่ทำให้ fault ไม่เกิดได้เงียบๆ
เพิ่มฟิลด์ `role_requirement_th` ลง `groundtruth/f05.json` แล้ว

---

## E16 — ฆ่า psql ฝั่ง client ไม่ได้ยกเลิก DDL ที่ค้างในคิว (2026-07-26)

ตัวเก็บกวาดของ F05 สั่ง `kill` process ของ psql ตามลำดับ B → C → A
คิดว่าฆ่า B ก่อนแล้ว ALTER จะไม่ทำงาน

**ของจริง:** `ALTER TABLE` ถูกส่งไปที่ server แล้วและกำลังนั่งรอในคิว
การฆ่า client ไม่ได้ยกเลิก query ฝั่ง server พอ A ตาย B ได้ lock ทันที
แล้ว **รัน ALTER สำเร็จจริง** = แก้ schema ของตารางทดลองโดยไม่ตั้งใจ

รอบแรกโดนเข้าจริง ตาข่าย `DROP COLUMN IF EXISTS` ที่เขียนกันไว้รับทัน

**ถ้าไม่มีตาข่ายนั้น:** ตาราง `orders` จะมีคอลัมน์งอกทีละรอบที่รัน
แล้วผลการทดลองรอบถัดไปจะเทียบกับรอบก่อนไม่ได้ **โดยไม่มีอะไรเตือน**

**แก้:** ยกเลิกที่ฝั่ง server ก่อนเสมอ
```sql
SELECT pg_terminate_backend(pid) FROM pg_stat_activity
WHERE query ILIKE 'ALTER TABLE orders ADD COLUMN qf_f05_tmp%'
```
แล้วค่อยฆ่า client · ยืนยันแล้วว่า 3 รอบติดตาข่ายไม่ต้องทำงานเลย
และ `orders` คงที่ 8 คอลัมน์ทุกรอบ

**บทเรียนทั่วไป:** ตัวฉีด fault ที่แก้ schema ต้องมีทั้ง
**การยกเลิกที่ถูกชั้น** และ **ตาข่ายกันเหนียว** — อย่างใดอย่างหนึ่งไม่พอ
ตาข่ายคือสิ่งที่ทำให้รู้ว่าการยกเลิกยังไม่ถูกชั้น

---

## E13 — ข้อความ error ของ F01 ที่เขียนไว้ ไม่มีวันเกิดจริง (2026-07-26)

รัน F01 ครั้งแรกในชีวิตโปรเจค **assertion ไม่ผ่าน** — และนั่นคือสิ่งที่ถูกต้อง

```
คาดไว้ : too many clients already
ได้จริง : FATAL:  remaining connection slots are reserved
                  for roles with the SUPERUSER attribute
```

**สาเหตุ:** โปรไฟล์ fragile ตั้ง `max_connections=20`
คู่กับ `superuser_reserved_connections=3` → role ธรรมดาใช้ได้ 17 slot
พอ 17 slot เต็ม PostgreSQL ตอบว่า "slot ที่เหลือสงวนให้ superuser"
**ไม่ใช่** "too many clients already" ซึ่งจะเกิดเมื่อเต็มหมดรวม slot สำรอง

แอปจริงต่อด้วย role ธรรมดาเสมอ → **ข้อความที่เขียนไว้เดิมไม่มีวันเกิดกับแอปจริง
บน config นี้** ตัวตรวจที่ match ข้อความนั้นจะไม่มีวันจับ fault ได้เลย

**สิ่งที่ทำงานถูกต้อง:** กฎเหล็กข้อ 3 (ทุก fault ต้องมี assertion)
ถ้าไม่มี assertion สคริปต์จะพิมพ์หลักฐานสวยๆ ออกมาแล้วเราจะนับว่าผ่าน
ทั้งที่ยืนยันคนละอย่างกับที่คิด

**แก้:** assert ด้วย regex ที่ครอบทั้งสองแบบ + พิมพ์ว่าเจอแบบไหน
เพราะสองแบบมีความหมายต่างกัน แต่ชี้ผิดทางเหมือนกัน (โทษโควตา connection)

**อ่าน SQLSTATE ไม่ได้:** เป็น connection error ระดับ libpq ไม่ใช่ query error
`psql -v VERBOSITY=verbose` ก็ไม่แสดง → ต้องพึ่งข้อความ
ซึ่งเสถียรเพราะ config ล็อก `lc_messages='C'` และ image tag ล็อกไว้
**ถ้าปลดล็อกอย่างใดอย่างหนึ่ง ต้องรันยืนยันใหม่**

→ ตรงกับตาราง "สิ่งที่ผู้ช่วย AI เชื่อถือไม่ได้" ข้อ "ข้อความ error ที่แน่นอน" พอดี

---

## E14 — ตัวฉีดเก็บกวาดตัวเองเร็วเกินกว่าที่ตัวนับคะแนนจะทัน (2026-07-26)

`f01_idle_in_transaction.sh` มี `trap cleanup EXIT` ซึ่งฆ่า session ที่ฉีดไว้
ตอนสคริปต์จบ — ประมาณ 5 วินาทีหลังเริ่ม

พอเฟส 1 แยกเป็น 3 ขั้น (ฉีด → วินิจฉัย → นับคะแนน) ตัวนับคะแนนที่รันแยกกัน
มาถึงตอน fault ถูกเก็บกวาดไปแล้ว → รายงาน **NOT_DETECTED ทั้งที่ fault เกิดจริง**

**นี่คือ false negative แบบเดียวกับกรณี `LOAD 'vector'`** (ข้อ 2 ใน README)
ต่างกันแค่ต้นเหตุ: อันนั้นตรวจผิดที่ อันนี้ตรวจผิดเวลา

**แก้:** เพิ่ม `HOLD_OPEN` — ค้าง fault ไว้กี่วินาทีหลัง assert ผ่าน
พร้อม assert ซ้อนว่า `HOLD_OPEN + 5 <= HOLD` ไม่งั้น session ตายก่อนอยู่ดี

**วิธีพิสูจน์ว่าตัวนับไม่ได้ตอบผ่านตลอด:** วัดสองจังหวะทุกรอบ
ระหว่าง fault ค้าง → `DETECTED` · หลังเก็บกวาด → `NOT_DETECTED`
ถ้าไม่พลิก แปลว่าตัวนับไม่ได้ดูอะไรเลย

---

## EXP01c — ตอบคำถามที่ E08 ค้างไว้: lossy bitmap เป็นปัญหาที่ config กลบ (2026-07-26)

**คำถามที่ค้าง:** บนโปรไฟล์ realistic (`work_mem=4MB`) อาการของ E08 หายไหม
**คำตอบ: หาย** — และหายตั้งแต่ `work_mem` ต่ำกว่านั้นมาก

แยกตัวแปรให้เหลือ `work_mem` ตัวเดียว: container เดียว · `shared_buffers` เดียว ·
ข้อมูลชุดเดียว · ปิด parallel · หลักฐาน `results/exp01c_work_mem_threshold.txt`

| work_mem | Heap Blocks | rows removed by recheck | **buffers** |
|---|---|---|---|
| 64kB (fragile) | exact=931 **lossy=1548** | 110,062 | **2501** |
| 96kB | exact=932 **lossy=1547** | 109,966 | **2501** |
| 128kB | exact=1446 **lossy=1033** | 73,470 | **2501** |
| 160kB | exact=2479 | 0 | **2501** |
| 256kB | exact=2479 | 0 | **2501** |
| 4MB (realistic) | exact=2479 | 0 | **2501** |

**เส้นแบ่งอยู่ระหว่าง 128kB กับ 160kB** สำหรับชุดข้อมูลนี้ — **เท่าเดิมหลังรันใหม่**
(200k แถว · 2,479 หน้า · แถวที่ตรงเงื่อนไข 23,983)
เลขนี้ผูกกับขนาดข้อมูล ห้ามเอาไปใช้กับชุดอื่นโดยไม่วัดใหม่

**สรุปต่อ E08:** เข้ากลุ่ม **"ปัญหาที่ config กลบไว้"** ตามที่
`config/postgresql.realistic.conf` เขียนนิยามไว้เอง — ซึ่งอันตรายกว่าปัญหาถาวร
เพราะระบบจริงตั้ง `work_mem` สูงพอในภาวะปกติ แต่ `work_mem` เป็นโควตา
**ต่อ operation ไม่ใช่ต่อเครื่อง** ตอนโหลดหนักที่มีหลาย query พร้อมกัน
อาการนี้จะกลับมาโดยไม่มีอะไรเปลี่ยนในโค้ดเลย

**ข้อสังเกตเดิมที่ทำซ้ำไม่ได้ — ต้องถอน:** รอบแรกบันทึกไว้ว่า lossy ไม่ลดแบบ monotonic
(96kB ได้ 775 แต่ 128kB ได้ 1033) **รันใหม่บน seed ที่แก้แล้ว ไม่เกิดอีก**
ตอนนี้ลดลงตามลำดับ 1548 → 1547 → 1033 → 0
→ ข้อสังเกตเดิมเป็นผลข้างเคียงของ seed ที่พัง (E17) ไม่ใช่พฤติกรรมของ PostgreSQL
**บทเรียน:** ข้อสังเกตที่ "อธิบายไม่ได้" ควรถูกสงสัยว่ามาจากข้อมูลก่อน

---

### 🔴 ผลข้างเคียงที่กระทบกฎเหล็กข้อ 7 — ต้องตัดสินใจ

**`buffers = 2501 เท่ากันเป๊ะทั้ง 6 กรณี** (รันใหม่แล้วยังเป็นแบบเดิมทุกประการ)

กฎเหล็กข้อ 7 กับ D08 บอกว่าใช้ buffers เป็นตัวชี้วัดหลักเพราะนิ่งกว่าเวลา
ซึ่งยังถูกอยู่ **แต่ fault ชนิดนี้ buffers มองไม่เห็นเลย**
งานที่เพิ่มขึ้นคือการ recheck 110,062 แถวในหน่วยความจำ ไม่ใช่การอ่าน page เพิ่ม

ส่วนเวลาก็เชื่อไม่ได้: รอบที่โครงสร้าง plan เหมือนกันเป๊ะยังแกว่ง 11.06–13.62 ms

**ตัวชี้วัดที่จับ fault นี้ได้จริงคือ `Rows Removed by Index Recheck`
และการมี/ไม่มี `lossy=` ใน `Heap Blocks`** — ทั้งคู่เป็นค่าเชิงโครงสร้างจาก plan
ไม่ใช่เวลา จึงนิ่งพอๆ กับ buffers

**ยังไม่แก้กฎข้อ 7** เพราะการแก้กฎเหล็กเป็นการตัดสินใจของเจ้าของโปรเจค
ข้อเสนอ: เพิ่มเป็นส่วนขยาย ไม่ใช่แทนที่ —
"ใช้ buffers เป็นตัวชี้วัดหลักด้าน I/O · ถ้า plan มี Bitmap Heap Scan
ต้องดู `Heap Blocks` และ `Rows Removed by Index Recheck` ควบเสมอ
เพราะ buffers มองไม่เห็นต้นทุนฝั่ง CPU"

---

## E11 — 100,000 แถว แต่มี vector ไม่ซ้ำแค่ 50 ตัว (2026-07-26)

เขียนตัวสร้าง corpus โดยให้ noise มาจาก subquery:

```sql
(SELECT array_agg(...random()...) FROM generate_series(1, 384)) AS noise
```

subquery นี้**ไม่ได้อ้างถึง id ของแถวเลย** PostgreSQL จึงยกไปคำนวณเป็น
**InitPlan ครั้งเดียว** แล้วใช้ค่าเดิมซ้ำทุกแถว
→ ทุกจุดในกลุ่มเดียวกันกลายเป็นจุดเดียวกันเป๊ะ ระยะห่างภายในกลุ่ม = 0.0000

**ทำไมข้อนี้น่ากลัว — assertion 4 ข้อที่เขียนไว้ผ่านหมด**

| ตรวจอะไร | ผล |
|---|---|
| จำนวนแถว = 1000 | ✅ ผ่าน |
| มิติ = 384 | ✅ ผ่าน |
| ไม่มี zero vector | ✅ ผ่าน |
| ครบ 50 กลุ่ม | ✅ ผ่าน |

ข้อมูลผิดจนใช้วัดอะไรไม่ได้เลย แต่**ไม่มี error และ assertion ไม่จับ**
จับได้เพราะบังเอิญสั่ง `count(DISTINCT embedding)` ดูเอง

**แก้:** ย้ายการสุ่มเข้าไปใน aggregate ให้คำนวณต่อคู่ (แถว, มิติ)
**และเพิ่ม assertion ที่ขาดไป** — ตัวอย่าง 2,000 แถวแรกต้องไม่ซ้ำกันเลย

**บทเรียนที่ตรงกับกฎเหล็กข้อ 3 พอดี:** assertion ที่ตรวจแต่ "รูปร่าง"
(จำนวน มิติ ชนิด) ผ่านได้ทั้งที่**เนื้อใน**พังหมด
ต้อง assert คุณสมบัติที่ทำให้ข้อมูลมีประโยชน์ ไม่ใช่แค่ที่นับง่าย

---

## E12 — สร้างกลุ่มมาแล้ว แต่กลุ่มไม่มีอยู่จริงในเชิงเรขาคณิต (2026-07-26)

หลังแก้ E11 ข้อมูลไม่ซ้ำกันแล้ว แต่พอวัดระยะจริงได้:

```
ระยะภายในกลุ่มเดียวกัน : 0.9800
ระยะข้ามกลุ่ม         : 1.0000      → ต่างกัน 2%
```

**กลุ่มมีอยู่แค่ในคอลัมน์ `cluster_id` ไม่ได้มีอยู่ในตัว vector**

**สาเหตุ:** ตั้ง noise scale = 0.35 โดยไม่ได้คิดถึงมิติ
ที่ 384 มิติ centroid ที่ normalize แล้วมีขนาดต่อมิติ ≈ 1/√384 ≈ 0.051
noise 0.35 ต่อมิติจึงใหญ่กว่าสัญญาณ ~7 เท่า → กลบจนหมด

**วิธีหาค่าที่ถูก — ใช้สูตรแล้วยืนยันด้วยการวัด (กฎเหล็กข้อ 1 และ 2)**

ระยะ cosine ภายในกลุ่ม ≈ `1 − 1/(1 + s²·D)` เมื่อ D = 384

| s | ทฤษฎี | วัดได้ |
|---|---|---|
| 0.02 | 0.133 | 0.131 |
| **0.04** | **0.381** | **0.377** |
| 0.08 | 0.711 | 0.702 |
| 0.35 | 0.979 | 0.973 |

เลือก **s = 0.04** → ในกลุ่ม 0.382 · ข้ามกลุ่ม 0.9997 · ห่างกัน 2.6 เท่า

**นี่คือความเสี่ยงที่ `PROJECT.md` ข้อ 10 เขียนเตือนตัวเองไว้ล่วงหน้า**
("vector สังเคราะห์ทำให้ recall ไม่มีความหมาย" · รับมือด้วย
"ตรวจสอบว่า exact search ให้ผลสมเหตุสมผล**ก่อน**วัดอะไร")
ถ้าไม่ได้ตรวจขั้นนี้ เฟส 2 จะได้ recall ที่ดูสมเหตุสมผลแต่ไม่มีความหมายเลย
และ **ไม่มีทางรู้ตัวจากตัวเลข recall เพียงอย่างเดียว**

**ผลข้างเคียง:** fingerprint ของชุด query เปลี่ยนจาก `73337343…` เป็น `607babfb…`
ทำได้เพราะยัง**ไม่มีตัวเลข recall จริงสักตัว** — D09 ห้ามแก้หลังเห็นผล ไม่ใช่ห้ามแก้เลย
ตั้งแต่จุดนี้ไปคือล็อกจริง

---

## D15 — ล็อกชุด fault 12 ข้อ ปิดหนี้กฎเหล็กข้อ 4 (2026-07-26)

**ปัญหาที่ต้องปิด:** `PROJECT.md` กับ `EVIDENCE.md` นับได้ 12 ข้อเท่ากัน
แต่เป็นคนละชุด · และ D13 รับ Q06 กับ V07 เข้ามาโดยยังไม่ได้ตัดของเดิมออก
= ค้างชำระกฎเหล็กข้อ 4 มาตั้งแต่วันที่รับ

**ต้นเหตุที่ลึกกว่านั้น: รายการ fault อยู่สามที่**
(`PROJECT.md` ข้อ 6 · `EVIDENCE.md` · `CLAUDE.md`) แล้วมันเคลื่อนออกจากกัน
โดยไม่มีอะไรเตือน — รูปแบบเดียวกับ E09

**ทางแก้เชิงโครงสร้าง (2026-07-26): ย้ายนิยามทั้งหมดไปที่ `FAULTS.md` ไฟล์เดียว**

- `FAULTS.md` = แหล่งอ้างอิงเดียว มีครบ 5 อย่างต่อข้อ
  (อาการ · ต้นเหตุ · วิธีฉีด · ground truth · assertion)
- `PROJECT.md` ข้อ 6 = ชี้มาที่ `FAULTS.md` ไม่มีรายการของตัวเองอีกแล้ว
- `EVIDENCE.md` = ผูก ID เข้ากับหลักฐานเอกสาร ไม่เก็บนิยาม
- ขัดกันเมื่อไหร่ → **เชื่อ `FAULTS.md`**

**ชุดที่ล็อกแล้ว: 16 ข้อ**

```
core   : F01  F03 (+F03b)  F05  F10                          = 4
vector : I01 I02 I03 I04 I05   Q01 Q02 Q03 Q04 Q06   V07 L02 = 12
```

**F03 มาพร้อมคู่แฝด F03b** — ข้อความ error เหมือนกันทุกตัวอักษร
แต่คนละสาเหตุ (รอ lock vs ช้าจริง) แยกได้ด้วย `pg_blocking_pids()` เท่านั้น
เป็นข้อที่พิสูจน์วิทยานิพนธ์ตรงที่สุด และเป็น baseline เทียบกับ
"โยน error ให้ LLM อธิบายลอยๆ" ได้ตรงตัว

**รับเข้า**

| ID | เหตุผล |
|---|---|
| **Q06** `LIMIT` > `ef_search` | FAQ ทางการยืนยันตรงๆ · ตรวจแบบ static ได้ → เข้า `quietfail-check` ได้ทันที |
| **V07** NULL / zero vector | เอกสารยืนยันสองที่ · เป็นปัญหา**คุณภาพข้อมูล** ทีมที่จูน index เก่งก็ไม่เจอ · ตรวจง่ายมาก (นับแถว) · zero vector เกิดจริงเมื่อ embedding ล้มเหลวแล้วโค้ดใส่ศูนย์แทน |

**ตัดออก**

| ID | เหตุผล | เปลี่ยนใจเมื่อ |
|---|---|---|
| **Q05** สลับ metric | เอกสารยืนยันได้แค่บางส่วน และอาการซ้อนทับกับ I01 ซึ่งยืนยันเต็มและตรวจจาก catalog ได้ | เจอกรณีที่ Q05 ให้อาการที่ I01 อธิบายไม่ได้ |
| **L01** พารามิเตอร์ไม่ scale | ต้นทุนเวลาสูงที่สุดในบรรดา fault ทั้งหมด — ต้องมีข้อมูล 4 ขนาดและสร้าง index ใหม่ทุกขนาด | เฟส 3 จบเร็วกว่าแผน หรือมีเครื่องที่รัน 5M ได้สบาย |
| **V08** multi-tenant | ต้องสร้างสถาปัตยกรรมใหม่ก่อนถึงจะฉีดได้ = สร้างระบบเพื่อทดสอบข้อเดียว | โปรเจคมีระบบ multi-tenant ด้วยเหตุผลอื่นอยู่แล้ว |

**ชั้น core ตัดออกด้วย 8 ข้อ** (ตารางเหตุผลเต็มอยู่ใน `FAULTS.md`)

| ID | เหตุผลย่อ |
|---|---|
| F02 deadlock | ซ้อนทับกับ F03 · เก็บไว้ถ้าเหลือเวลา |
| F04 serialization failure | ต้องอธิบาย isolation level ยาว ต้นทุนสูง |
| F06 temp file ระเบิด | ซ้อนทับกับ F10 |
| F07 check-then-act | ไม่เกี่ยวกับ vector · สร้าง load test นาน |
| F08 เงินเป็น float | เป็น static check ล้วน ไม่ต้องฉีด |
| F09 timestamp ไม่มี timezone | เหมือน F08 |
| F11 มี index แต่ใช้ไม่ได้ | ซ้อนทับกับ I01 |
| F12 ตารางบวมเพราะ transaction ค้าง | ซ้อนทับกับ L02 |

คอมเมนต์ใน `init/02_schema.sql` ที่อ้าง F06–F09 และ F11 **เป็นเศษของแผนเดิม
ไม่ใช่ข้อผิดพลาด** — schema เตรียมของไว้แล้ว ถ้าอนาคตรับเข้าชุดก็ทำได้ทันที

**ID ที่ตัดแล้วห้ามนำกลับมาใช้ซ้ำ** และห้ามเรียงเลขใหม่
เพราะ `results/` และบันทึกใน `DECISIONS.md` อ้างถึง ID เดิม
การเรียงใหม่จะทำให้หลักฐานเก่าชี้ไปคนละข้อโดยไม่มีอะไรเตือน
(ซึ่งเป็นความล้มเหลวเงียบอีกแบบหนึ่ง — เทียบ E09)

**ข้อสังเกตที่บันทึกไว้ตอนตัดสินใจ (ผู้ใช้รับทราบแล้วและยืนยันตามเดิม):**
ผู้ช่วยตั้งข้อสังเกตว่า L01 ถูกกว่าที่ประเมิน เพราะ `PROJECT.md` ข้อ 7
สั่งวัด 4 ขนาดอยู่แล้ว โครงสร้างจึงรองรับ L01 อยู่ก่อน — ต่างจาก V08 ที่ต้องสร้างของใหม่จริง
ผู้ใช้พิจารณาแล้วยืนยันตัด **เพื่อคุมขอบเขตให้จบทัน**
บันทึกไว้เพื่อให้รู้ว่านี่คือการเลือกที่รู้ตัว ไม่ใช่หลุดไปเพราะนับให้ครบ 12

**ผลข้างเคียงที่ยังไม่ตัดสิน:** `PROJECT.md` ข้อ 7 ยังสั่งวัด 4 ขนาด (100k/500k/2M/5M)
ซึ่งเดิมมีไว้รองรับ L01 เป็นหลัก ส่วนเฟส 2 ต้องการแค่ 2 ระดับ
→ ยังไม่แก้ ต้องตัดสินแยกว่าจะลดเหลือ 2 ขนาดหรือคงไว้เผื่อ L01 กลับมา

> ✅ **ปิดแล้ว 2026-07-27 — ดู D18** เจ้าของโปรเจคสั่งลบข้อ 7 ทิ้ง
> เนื้อหาย้ายไป `CLAUDE.md` · ขนาดที่วัดจริงคือ 100k และ 500k

---

## หมายเหตุ — ภาษาไทยดูเพี้ยนใน PowerShell

`Get-Content` ของ PowerShell 5.1 อ่านไฟล์ด้วย codepage ระบบแทน UTF-8
ไฟล์ไม่ได้เสีย ใช้ `Get-Content ไฟล์ -Encoding UTF8` หรือดูบน GitHub

(อีกหนึ่งความล้มเหลวเงียบ — ข้อมูลดูพัง ทั้งที่ไม่พัง)
