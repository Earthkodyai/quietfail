==============================================================================
audit.py ตรวจตัวเองด้วยสูตรข้อ 6 — หัวข้อไหนพิสูจน์แล้วว่าตอบลบได้จริง
==============================================================================
วันที่วัด : 2026-08-02
เหตุ      : สูตรข้อ 6 ของโครงงานบังคับว่า **ตัวนับที่ไม่มีวันตอบลบ = ไม่ได้วัดอะไรเลย**
            และ E30 เคยเจอว่าตัวตรวจ I05 ผ่านทุกอย่างยกเว้นข้อนี้ โดยไม่มีใครสังเกต
            แต่กฎนี้ไม่เคยถูกใช้กับ audit.py เอง

------------------------------------------------------------------------------
วิธี 1 — อ่านโค้ด: หัวข้อไหนไม่มีทางออกเป็น FAIL เลย
------------------------------------------------------------------------------
  พบ 2 หัวข้อ และทั้งคู่ **ตั้งใจให้เป็นแบบนั้น**

  "verdict ที่ได้ตอนนี้ (ข้อมูล ไม่ใช่เกณฑ์)"  -> PASS เสมอ
      ชื่อหัวข้อบอกไว้เองว่าเป็นข้อมูล ไม่ใช่เกณฑ์ · Q02 ขยับตามประวัติ
      pg_stat_statements จึงตั้งเป็นเกณฑ์ไม่ได้ (บันทึกไว้ใน CLAUDE.md แล้ว)

  "เชื่อมต่อ docker/psql"  -> PASS หรือ CANNOT
      docker ไม่ขึ้น = ตรวจไม่ได้ ไม่ใช่ผิด · และ CANNOT ทำให้ exit 1 อยู่แล้ว
      ตรงตามกฎเหล็กข้อ 10

------------------------------------------------------------------------------
วิธี 2 — ขุดประวัติ: หัวข้อไหน "เคยตอบลบจริง" ในรายงานที่ commit ไว้
------------------------------------------------------------------------------
  ไล่ `results/audit_latest.txt` ทุกฉบับในประวัติ git · **171 ฉบับ**

    76 ครั้ง   working tree
     3 ครั้ง   จำนวนไฟล์ใน results/ ที่เอกสารอ้าง
     2 ครั้ง   ไม่มี vector index ค้าง
     1 ครั้ง   ตารางของส่งมอบใน CLAUDE.md
     1 ครั้ง   results ถูก track

  -> **มีหลักฐานเชิงประจักษ์ว่าตอบลบได้จริงเพียง 5 จาก 36 หัวข้อ**

------------------------------------------------------------------------------
รวมหลักฐานทั้งหมดที่มี
------------------------------------------------------------------------------
  | ที่มาของหลักฐาน | ครอบคลุมกี่หัวข้อ |
  |---|---|
  | `scripts/audit_numbers_states.py` แก้ตัวเลขให้ผิดทีละข้อ | **8** (หมวด 6 ทั้งหมด) |
  | `results/fingerprint_fn_states.txt` แก้สูตร fingerprint | 1 |
  | เคยตอบลบจริงในประวัติ 171 ฉบับ | 5 (ซ้ำกับข้างบน 1) |
  | **รวมไม่ซ้ำ** | **~13 จาก 36** |

  -> **เหลืออีกราว 23 หัวข้อที่ไม่มีหลักฐานว่าตอบลบได้**

==============================================================================
ต้องอ่านผลนี้อย่างไร
==============================================================================

🟢 **ไม่ได้แปลว่า 23 หัวข้อนั้นพัง**
   ส่วนใหญ่เป็นการเทียบค่ากับ baseline ตรงๆ (จำนวนแถว · มิติ · fingerprint ·
   เวอร์ชัน PG/pgvector) ซึ่งถ้าค่าจริงเปลี่ยนก็ต้องจับได้โดยโครงสร้าง
   และหลายข้อ **เคยจับของจริงมาแล้วก่อนที่จะเริ่ม commit รายงาน**
   เช่น E30 · E31 · หลักฐานที่ไม่ได้ track (บันทึกไว้ใน CLAUDE.md)

🔴 **แต่แปลว่า "ผ่าน 36/36" มีน้ำหนักไม่เท่ากันทุกข้อ**
   ข้อที่มีหลักฐานการพลิก = เชื่อได้ว่ามันวัดจริง
   ข้อที่ไม่มี = เชื่อได้แค่ว่ามันไม่ได้ error

   นี่คือมาตรฐานเดียวกับที่โครงงานบังคับกับ `score.sql` และ
   `quietfail_check.py` (ทั้งคู่มีไฟล์พิสูจน์การพลิกครบ)
   **audit.py เป็นตัวเดียวที่บังคับกฎนี้กับคนอื่น แต่ยังไม่ผ่านกฎนี้เอง**

📌 **ไม่สร้างตัวพิสูจน์อีก 23 ตัวในรอบนี้** เพราะต้นทุนสูงและหลายข้อต้องทำลาย
   สภาพฐานข้อมูลจริงเพื่อทดสอบ (เช่น ลบแถวใน qf_corpus เพื่อดูว่าจับได้ไหม)
   ซึ่งเสี่ยงกว่าประโยชน์ · **สิ่งที่ทำคือบันทึกขอบเขตของความเชื่อมั่นให้ตรงความจริง**
==============================================================================
