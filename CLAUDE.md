# บริบทโปรเจค — อ่านไฟล์นี้ก่อนเสมอ

> ไฟล์นี้มีไว้ให้ผู้ช่วย AI อ่านตอนเริ่มทำงาน
> ถ้าเนื้อหาในนี้ขัดกับสิ่งที่ผู้ช่วยจำได้ **ให้เชื่อไฟล์นี้**

---

## 🟢 ไฟล์นี้เชื่อถือได้แค่ไหน — อ่านก่อนตัดสินใจตรวจอะไร

**ทุกอย่างที่เขียนว่า ✅ ในไฟล์นี้ ผ่านการตรวจด้วยเครื่องแล้ว ไม่ใช่คำกล่าวอ้าง**

```
ตรวจครั้งล่าสุด : 2026-08-01 (หลัง E46 · ทวนเอกสารครบทุกไฟล์) · ผ่าน **36/36** · ไม่ผ่าน 0 · ตรวจไม่ได้ 0
รัน fault ซ้ำ  : **ครบ 9 จาก 9 · ผ่านหมด · assertion 46/46 · 33.8 นาที** (2026-07-31 · หลังแก้ H32)
รายงานเต็ม     : results/audit_latest.txt   ← อ่านไฟล์เดียว ไม่ต้องรันอะไร
ตัวตรวจ        : scripts/audit.py · ค่าที่ใช้เทียบ scripts/audit_baseline.json
```

**ห้ามไล่ตรวจเองทีละข้อ** — context window มีจำกัด และการตรวจด้วยมือกินพื้นที่
จนเหลือที่ทำงานจริงน้อยลงมาก งาน audit เต็มรอบหนึ่งเคยกินไปเกือบครึ่ง session

### ทำแบบนี้แทน (ราคาถูกกว่ามาก)

**ถ้าแค่อยากเชื่อ** → อ่าน `results/audit_latest.txt` แล้วเทียบบรรทัด `commit` กับ

```bash
git rev-parse --short HEAD
```

ตรงกัน + รายงานเขียนว่า *ผ่านครบทุกข้อ* → **เชื่อทั้งไฟล์นี้ได้เลย จบ**

**ถ้าต้องการยืนยันสดๆ** หรือ commit ไม่ตรง → รันคำสั่งเดียวนี้ (~30 วินาที)

```bash
python scripts/audit.py
```

`exit 0` = ผ่านหมด · `exit 1` = มีข้อไม่ผ่าน **หรือตรวจไม่ได้** แล้วมันจะบอกว่าข้อไหน

> **ต่างกัน 1 commit เป็นเรื่องปกติ** — รายงานบันทึก commit ที่มันตรวจ
> ส่วนตัวรายงานเองถูก commit ทีหลัง จึงห่างกันหนึ่งขั้นเสมอ
> ถ้าห่างมากกว่านั้นแปลว่ามีงานเข้ามาหลัง audit → รันใหม่

### ตัวตรวจนี้ตรวจอะไรบ้าง (36 หัวข้อ 6 หมวด)

| หมวด | ตรวจอะไร |
|---|---|
| git | working tree สะอาด · **หลักฐานใน `results/` ถูก track ครบ** (โฟลเดอร์นี้อยู่ใน `.gitignore`) |
| groundtruth | parse ได้ · มี `evidence_expectation` + `verified_on.date` · ไฟล์ที่ `reproduce` ชี้ไปมีจริง · **มีหลักฐานว่าตัวตรวจพลิกได้** |
| เอกสาร | E/D ที่ถูกอ้างมีนิยามจริง · เลข E/D ไม่ซ้ำกัน · สถานะใน CLAUDE.md ตรงกับ baseline · **ตารางของส่งมอบไม่เก่า** · fault ที่ตัดแล้วไม่โผล่กลับ · **นิยาม fingerprint ตรงกันทุกสำเนา** (E40) |
| ฐานข้อมูล | จำนวนแถว · มิติ · **fingerprint ของ corpus** · **ฟังก์ชันกลาง `qf_fingerprint()` ให้ค่าตรง baseline** · ไม่มี index ค้าง · เวอร์ชัน PG/pgvector |
| score.sql | รันจบไม่มี error · ทุก fault มีสาขาที่ตอบได้ |
| **ตัวเลขในเอกสาร** | **นับของจริงใหม่ทุกครั้ง** แล้วเทียบกับที่เขียนไว้ในเอกสารหลักทุกไฟล์ — จำนวนหัวข้อ audit · หลักฐานดิบใน `results/` · แถวทะเบียนบั๊ก · assertion และเวลาของ `--reproduce` (E39) |

**มันตอบ 3 อย่างเหมือน `score.sql`** — `PASS` / `FAIL` / `CANNOT_CHECK`
และ **`CANNOT_CHECK` ไม่นับว่าผ่าน** (กฎเหล็กข้อ 10) docker ไม่ขึ้น = ตรวจไม่ได้ ≠ ผ่าน

### ถ้าไม่มั่นใจว่าผลยังเกิดซ้ำได้ — สั่งรันจริงได้เลย

audit ปกติยืนยันว่า **หลักฐานมีอยู่และครบรูปแบบ** ไม่ได้ยืนยันว่าผลยัง reproduce ได้
ถ้าอยากได้ความมั่นใจระดับนั้น มีโหมดแยกให้:

```bash
python scripts/audit.py --reproduce
```

รันตัวฉีดจริงซ้ำทั้ง 9 ข้อที่รันอัตโนมัติได้ (~32 นาที) แล้ว**ตรวจ `qf_corpus`
fingerprint ก่อน/หลัง กับ index ที่ค้าง** เพราะการรันจริงแตะฐานข้อมูลจริง

รันเฉพาะบางข้อก็ได้ — `python scripts/audit.py --reproduce I04,Q06`

| | ข้อ | ผลรันจริง 2026-07-28 |
|---|---|---|
| **รันอัตโนมัติได้** | I01 · I02 · I04 · L02 · Q02 · Q03 · Q04 · Q06 · V07 | **ครบ 9 ข้อ ผ่านหมด · assertion 46/46** |
| **ต้องรันด้วยมือ** | F01 · F03 · F05 · F10 · I03 · I05 | ✅ **รันครบแล้ว 2026-08-01 ผ่านหมด** |

**ยืนยันแล้วว่าเกิดซ้ำได้จริงทั้ง 9 ข้อ** (`results/audit_reproduce.txt`) — รวม **33.8 นาที**

| ข้อ | assertion | เวลา | | ข้อ | assertion | เวลา |
|---|---|---|---|---|---|---|
| I01 | 4/4 | 385 วิ | | Q02 | 5/5 | 38 วิ |
| **I04** | 5/5 | **536 วิ** | | Q03 | 6/6 | 116 วิ |
| I02 | 6/6 | 105 วิ | | Q04 | 5/5 | 221 วิ |
| **L02** | 6/6 | **491 วิ** | | Q06 | 5/5 | 51 วิ |
| | | | | V07 | 4/4 | 1 วิ |

`qf_corpus` fingerprint เดิมก่อน/หลัง · ไม่มี index ค้าง
**ค่าเวลาใน `audit_baseline.json` เป็นค่าที่วัดจริงแล้ว ไม่ใช่ประมาณ**

### ✅ 6 ข้อที่ต้องรันด้วยมือ — ยืนยันครบแล้ว 2026-08-01

**ทั้ง 15 ข้อในชุดผ่านการรันจริงซ้ำแล้วทั้งหมด** ผลเต็ม `results/manual_faults_run.txt`

รันข้างใน container ตาม `WINDOWS.md` — เลี่ยงปัญหา `.sh` บน Windows ทั้งหมด

```bash
docker compose exec -T db bash -c \
  "tr -d '\r' < /faults/<ไฟล์>.sh > /tmp/x.sh && PGPORT=5432 <ENV> bash /tmp/x.sh"
```

| fault | ENV ที่ต้องใส่ | ผล |
|---|---|---|
| **F01** | `LEAKS=20 HOLD=120 HOLD_OPEN=90` | DETECTED · 17 session · ยืนยัน 3 จังหวะ |
| **F03** | `KEEP_OBS=1` (**ห้าม `HOLD_OPEN`** ตาม E21) | DETECTED · แยก F03/F03b ได้ |
| **F05** | `HOLD=120 HOLD_OPEN=90` | DETECTED · `granted=false` 2 แถว |
| **F10** | `KEEP_OBS=1` | DETECTED · เดาคลาด 1500× · buffers ลด 322.7× |
| **I03** | — | assertion **4/4** |
| **I05** | — (~4 นาที) | assertion **3/3** |

#### 🔴 `HOLD` ต้อง ≥ `HOLD_OPEN + 5` ไม่งั้นวัดไม่ทัน

รอบแรกตั้ง `HOLD_OPEN=75` เฉยๆ (ค่าปริยาย `HOLD=45`) → `score.sql` ตอบ
**`NOT_DETECTED` ทั้งที่ตัวฉีดเห็น 17 session ชัดๆ** เพราะ session ตายก่อนตัวนับคะแนนมาถึง

**ตัวฉีดเตือนเรื่องนี้เอง** — `!! HOLD_OPEN=75 ยาวเกิน HOLD=45`
ตั้งใหม่แล้วจับได้ทันที และยืนยัน 3 จังหวะติดกัน (19.6 / 40.6 / 61.6 วินาที)

> ตรงกับ **E21** — หลักฐานคนละชนิดต้องวัดคนละจังหวะ

#### ⚠️ หลังรัน F03/F10 ด้วย `KEEP_OBS=1` สภาพฐานจะไม่ตรงกับที่บันทึกไว้

ตารางสังเกตการณ์ค้างอยู่ทำให้ทั้งคู่ตอบ `DETECTED` ต่อไป · **คืนสภาพด้วย**

```sql
DROP TABLE IF EXISTS qf_f03_obs;  DROP TABLE IF EXISTS qf_f10_obs;
SELECT pg_stat_statements_reset();   -- ไม่งั้น Q02 ค้าง DETECTED ด้วย
```

ยืนยันแล้วว่าคืนสภาพครบ — `DETECTED 3 · NOT_DETECTED 3 · CANNOT_CHECK 9`
และ `qf_corpus` fingerprint เดิม · ไม่มี index ค้าง · ไม่มี idle in transaction

**ตัวชี้ขาดคือ exit code ไม่ใช่ข้อความใน output** — ทุกสคริปต์ใช้ `ON_ERROR_STOP`
และ `RAISE EXCEPTION` เมื่อ assertion ตก จึงอ่านโครงสร้างได้ตรงๆ (กฎเหล็กข้อ 6)

> ⚠️ **โหมดนี้แตะฐานข้อมูลจริง** ถ้าถูกตัดกลางคัน (ปิดเครื่อง · session จบ)
> จะทิ้ง vector index ค้างไว้ ตามกับดักข้อ 4
> **เกิดขึ้นจริงมาแล้ว** — รอบที่ถูกตัดตอน Q04 ทิ้ง `qf_q04_ivf` 157 MB ไว้
> `python scripts/audit.py` ปกติจับได้เองที่หัวข้อ *"ไม่มี vector index ค้าง"*
> แก้ด้วย `DROP INDEX IF EXISTS <ชื่อ>` แล้วรันใหม่

### สิ่งที่ตัวตรวจนี้ **ไม่ได้** ตรวจ — ต้องรู้ก่อนเชื่อเกินจริง

- **audit ปกติไม่รัน fault ซ้ำ** — ใช้ `--reproduce` ข้างบนถ้าต้องการระดับนั้น
- **ไม่ได้ตรวจว่าตัวเลขในตารางถูก** — ตัวเลขมาจาก `results/` ซึ่งห้ามแก้ด้วยมืออยู่แล้ว

  > ✅ **แคบลงแล้ว 2026-07-30** — ข้อยกเว้นนี้เคยกว้างเกินไป จนตัวเลขเคลื่อนไป
  > **7 ค่าใน 6 ไฟล์** โดยไม่มีอะไรเตือน (E39) · ตอนนี้มี **หมวด 6** ตรวจให้แล้ว
  >
  > **ยังไม่ตรวจ:** ตัวเลข**ผลการทดลอง** (recall · buffers · เวลา) ซึ่งมาจาก `results/`
  > ที่ห้ามแก้ด้วยมืออยู่แล้ว · **ตรวจแล้ว:** ตัวเลขที่**นับตัว repo เอง** ซึ่งโตทุกครั้งที่ทำงาน
  และ **I04 พิสูจน์แล้วว่าตัวเลข recall บางตัวไม่มีวันซ้ำเป๊ะ** เพราะ build ไม่ reproducible
  → `--reproduce` ตรวจว่า **assertion ยังผ่าน** ไม่ใช่ว่าตัวเลขเท่าเดิม
- **ไม่ได้ตรวจ verdict ของ `score.sql` ว่าต้องเป็นค่าใด** — เพราะ Q02 อ่าน
  `pg_stat_statements` ซึ่งเปลี่ยนตามประวัติการรัน · รายงานไว้เป็นข้อมูลเฉยๆ
  ให้เทียบกับตาราง **"ผลของ `score.sql` บนสภาพสะอาด"** ที่อยู่ล่างในไฟล์นี้

---

## สถานะปัจจุบัน ← อัปเดตทุกครั้งที่ทำอะไรเสร็จ

```
อัปเดตล่าสุด : 2026-08-01 (E42–E46 ไล่เทียบเอกสาร 16 fault + ทวนทุกไฟล์รวม sql/ scripts/)
เฟสที่ผ่านแล้ว : 0 · 1 · 2 · 2.5 · 3 — ผ่านครบทุกเกณฑ์
งานวัดผล     : **จบหมดแล้ว** — ชุด fault ครบ 12 ข้อ · มีตัวฉีด+ตัวตรวจ 15 ข้อ
กำลังทำ      : I01 ✅ I02 ✅ I03 ✅ I04 ✅ I05 ✅ L02 ✅ Q02 ✅ Q03 ✅ Q04 ✅ Q06 ✅ V07 ✅ เหลือ **—**
              (Q01 คือการวัดผล ไม่ใช่ fault ที่ต้องมีตัวตรวจแยก)
ติดอยู่ที่     : —
```

### ⭐⭐ ทดสอบบน embedding จริงแล้ว — ทั้งยืนยันและหักล้าง (2026-07-30)

**คุมทุกอย่างเท่าเดิม เปลี่ยนแค่ที่มาของเวกเตอร์** — 100,000 แถว · 384 มิติ · 200 query ·
cosine · โปรไฟล์ fragile · **5 build ต่อการตั้งค่า** ตามกฎที่ I04 บังคับ

```
ข้อมูล : BEIR/Quora (Thakur et al. 2021) · โมเดล all-MiniLM-L6-v2
ตาราง  : qf_real · qf_real_q · qf_real_truth   (ไม่แตะ qf_corpus เด็ดขาด)
ผลเต็ม : results/real_q01.txt
```

| corpus | index | recall@10 | recall@100 | แย่สุด@10 | ครบ 10/10 |
|---|---|---|---|---|---|
| **จริง** | HNSW ef=40 | **0.9935** | **0.4005** | 0.4000 | 193–195/200 |
| สังเคราะห์ | HNSW ef=40 | 0.8565 | 0.3903 | 0.0000 | 52/200 |
| **จริง** | IVFFlat probes=1 | **0.6653** | 0.5433 | **0.0000** | 54–67/200 |
| สังเคราะห์ | IVFFlat probes=1 | 0.7870 | 0.7793 | **0.0000** | 100/200 |

#### ⭐⭐ ยืนยันแล้ว — เพดานของ Q06 ไม่ขึ้นกับข้อมูลเลย

`recall@100` ของ HNSW = **0.4005 เท่ากันเป๊ะทั้ง 5 build** (ช่วง 0.0000)
ขณะที่ `recall@10` ของ build เดียวกันแกว่ง · corpus สังเคราะห์ได้ 0.3903 ต่างกัน 0.01

**เป็นค่าเชิงโครงสร้างล้วน** (`ef_search=40` → 40/100) และนิ่งสนิทข้าม build
ขณะที่ตัวเลขอื่นในตารางเดียวกันแกว่งหมด → **หลักฐานที่แข็งที่สุดของการทดลองนี้**

#### 🔴 ต้องแก้คำพูด — ขนาดของ recall loss ขึ้นกับข้อมูลมาก

```
HNSW ค่าเริ่มต้น เสีย top-10 ไป   สังเคราะห์ 14.4%   จริง 0.65%   <- ต่างกัน 22 เท่า
```

**ห้ามพูดว่า "ค่าเริ่มต้นทำให้ผลหาย 14%" แบบลอยๆ อีก** ต้องระบุว่าวัดบนข้อมูลแบบไหน

#### ⚠️ index สองแบบเคลื่อนคนละทิศ — แต่ **อันดับไม่ได้สลับ**

HNSW ดีขึ้น (0.8565→0.9935) · IVFFlat แย่ลง (0.7870→0.6653)
**HNSW ชนะ IVFFlat ทั้งสองชุดข้อมูล** สิ่งที่เปลี่ยนคือ**ช่องว่าง** 0.07 → 0.33

พูดได้: *"index สองแบบตอบสนองต่อโครงสร้างข้อมูลไม่เหมือนกัน"*
พูดไม่ได้: *"แบบไหนดีกว่า"* · *"อันดับสลับกัน"*

#### I04 เกิดซ้ำ และไปกองที่ IVFFlat

ช่วงของ recall@10 จาก 5 build: HNSW **0.0020** · IVFFlat **0.0960** (แกว่งกว่า 48 เท่า)
บนสังเคราะห์ทั้งคู่ใกล้กันกว่านี้มาก (9.4% vs 4.6% = 2 เท่า)
→ **I04 ไม่ใช่สิ่งประดิษฐ์ของข้อมูลสังเคราะห์ แต่ขนาดของมันขึ้นกับข้อมูล**

#### ยืนยัน I05 โดยไม่ได้ตั้งใจ

```
NOTICE: hnsw graph no longer fits into maintenance_work_mem after 28364 tuples
สังเคราะห์ 28,352  ·  จริง 28,364   <- ต่างกัน 12 tuple
```

**ความจุขึ้นกับมิติและหน่วยความจำ ไม่ขึ้นกับเนื้อข้อมูล** — หลักฐานอิสระคนละชุด

#### สิ่งที่เหมือนเดิมทุกประการ

IVFFlat ยังมี query ที่ได้ของถูก **0 อัน** · exact ได้ 1.0000 ตามนิยาม ·
**ไม่มี error ใดๆ ตลอดการวัด**

> ⚠️ **เวลา build ของ HNSW ไต่ 69→123 วินาที ข้าม build** ทั้งที่ index เท่ากันเป๊ะ 195 MB
> **เป็นสิ่งประดิษฐ์ของการรันต่อเนื่อง ไม่ใช่ผล** — เทียบเวลา build ข้าม build ไม่ได้
> (รูปแบบเดียวกับ buffers ที่ไต่ใน E36)

#### ⭐ รอบสอง — อีก 6 fault บน 768 มิติ (2026-08-01) · ครบ 10 ข้อแล้ว

| fault | ผล |
|---|---|
| **V07** | ✅ หาย **10 แถวพอดี** เท่ากันทั้ง 3 ชุดข้อมูล |
| **I02** | ✅ ยืนยันซ้ำทุกข้อ — B กระจาย 0.607 ชนะ D กระจุก 0.441 · A นิ่งสนิท |
| **L02** | ✅ ขอเยอะไม่ช่วย (10/40/100/1000 ได้ **4** เท่ากันหมด) · VACUUM แก้ได้ · index ไม่เล็กลง |
| **Q04** | 🔴 **ข้ออ้างนี้ถอนแล้ว (E45)** — ทั้งสองจุดคือจุดที่ planner ทิ้ง index |
| **I04** | 🔴 สัดส่วนเปลี่ยน (HNSW 4/200 · IVFFlat 180/200) แต่**รูปแบบเดิมทุกข้อ** |
| **Q03** | ⚠️ **เจอตัวแปรใหม่** ดูข้างล่าง |

#### 🔴 Q03 บน 768 มิติ — planner เลิกใช้ index เอง

```
filter 20% · 10% · 5%   ได้ 5 · 4 · 2 จาก 10   ใช้ index = t   ← อาการเดิม
filter 2% ลงไป          ได้ 10 ครบ            ใช้ index = f   ← เปลี่ยนเส้นทาง
```

**ไม่ใช่เพราะ fault หายไป แต่เพราะ planner ไปทำ Seq Scan บนแถวที่กรองแล้ว**
index 768 มิติใหญ่เท่าตัว (390 MB vs 195 MB) จึงถูกประเมินว่าแพงกว่า

> ⚠️ **ตารางทางแก้ (ขั้น 1–5) ของรอบนี้ใช้เทียบไม่ได้** เพราะวัดที่ filter 1%
> ซึ่งเป็นจุดที่ planner ไม่ใช้ index อยู่แล้ว ได้ 40/40 ทั้ง 5 ขั้น
> **ไม่ได้แปลว่าทางแก้ใช้ได้** · ถ้าจะทดสอบต้อง `enable_seqscan = off` ก่อน

**ข้อค้นพบใหม่: ขนาดของ index มีผลว่า planner จะใช้มันไหม
ซึ่งเปลี่ยนว่า fault จะเกิดหรือไม่ โดยผู้ใช้ไม่ได้เปลี่ยนอะไรเลย**

#### ไฟล์

| ไฟล์ | ทำอะไร |
|---|---|
| `scripts/real_embed_build.py` | โหลด BEIR/Quora + ฝัง + เขียน TSV · รับ `QF_MODEL`/`QF_DIM`/`QF_TAG` |
| `sql/real2_*.sql` | ชุด 768 มิติ ครบ 10 fault |
| `sql/real_load.sql` | สร้างตาราง + เฉลย exact + guard ว่า `qf_corpus` ไม่ถูกแตะ |
| `sql/real_q01.sql` | วัด recall — ส่ง `-v builds=N` |
| `results/real_q01.txt` | ผลเต็มพร้อมบทวิเคราะห์ |

ต้องลง `sentence-transformers` + `torch` ก่อน (ลงแล้วในเครื่องนี้)

### ⭐⭐ ทดสอบ embedding โมเดลที่สอง — 768 มิติ (2026-08-01)

**เปลี่ยนทั้งโมเดลและมิติ ข้อความชุดเดิมเป๊ะ** — `all-mpnet-base-v2` · 768 มิติ
ผลเต็ม `results/real2_model2.txt` · ตาราง `qf_real2*` · สคริปต์ `sql/real2_*.sql`

#### ⭐ ข้อมูลใหม่ที่โมเดลเดียวให้ไม่ได้

| ที่ค่าเริ่มต้น | HNSW ef=40 | IVFFlat probes=1 |
|---|---|---|
| **768 มิติ** | **0.9974** | 0.6896 |
| 384 มิติ | 0.9935 | 0.6653 |
| สังเคราะห์ | 0.8565 | 0.7870 |

| | โมเดลจริงต่างกัน | ต่างจากสังเคราะห์ | อัตราส่วน |
|---|---|---|---|
| HNSW | 0.0039 | 0.1370 | **35×** |
| IVFFlat | 0.0243 | 0.1217 | **5×** |

> 🔴 **ห้ามอ้าง "35 เท่า" ลอยๆ** — เป็นของ HNSW เท่านั้น · IVFFlat ได้ 5 เท่า
> ที่ยกไปใช้ได้คือ **"อย่างน้อย 5 เท่า"** · ทิศทางเดียวกันทั้งสอง index

→ **ความต่างที่เจอมาจาก "สังเคราะห์ vs จริง" ไม่ใช่จาก "โมเดลไหน"**

#### ⭐⭐ Q06 — เพดาน 41 เท่ากันเป๊ะทั้งสองมิติ

ขอ 50 · 100 · 1000 ได้ **41 เท่ากันหมด** ทั้ง 384 และ 768 มิติ
→ **เป็นค่าเชิงโครงสร้างล้วน ไม่ขึ้นกับทั้งข้อมูลและมิติ**

#### 🔴 I05 — ทำนายผิด และความผิดให้ข้อมูลใหม่

**ทำนายไว้ก่อนรัน:** 768 มิติควรจุได้ครึ่งเดียว = 14,000–14,500 tuples

```
384 มิติ  28,364 tuples        อัตราส่วน 1.69 เท่า
768 มิติ  16,759 tuples        ไม่ใช่ 2 เท่า
```

**ทิศทางถูก ขนาดผิด** — ถ้าความจุแปรผกผันกับขนาดเวกเตอร์ล้วนต้องได้ 2 เท่า
การที่ได้ 1.69 แปลว่ามี **ต้นทุนต่อ element ที่ไม่ขึ้นกับมิติ** ปนอยู่

> ### ✅ ตรวจกับซอร์สแล้ว 2026-08-01 — **674 ไบต์พอดี** ไม่ใช่ "ราว 700–1,000"
>
> เดิมกำกับไว้ว่า *"อนุมานจากการวัด ยังไม่ได้ยืนยันกับซอร์ส — อย่าอ้างในเล่ม"*
> **ตอนนี้อ้างได้แล้ว** แยกเป็นรายการจากซอร์สได้ครบทุกไบต์
>
> | ที่มา (pgvector v0.8.5) | ไบต์ |
> |---|---|
> | `sizeof(HnswElementData)` | 128 |
> | `HnswNeighborArrayPtr[1]` | 8 |
> | `HNSW_NEIGHBOR_ARRAY_SIZE(m*2=32)` ชั้น 0 | 520 |
> | ชั้นสูงกว่า 0 เฉลี่ย (`E[level]`=1/15) | 18 |
> | **รวมที่ไม่ขึ้นกับมิติ** | **674** |
> | vector = `MAXALIGN(8 + 4*dim)` | 384→1,544 · 768→3,080 |
>
> **อัตราส่วนอธิบายได้เป๊ะ** (674+3,080)/(674+1,544) = **1.692**
> 🔴 ค่าที่เดาไว้ *"700–1,000"* **ไม่ครอบคลุมค่าจริง** — ทิศทางถูก ช่วงผิด
>
> **งบก็ไม่ใช่ `mwm` เต็มจำนวน** — เส้นทาง parallel หัก `estother` 3MiB
> (`hnswbuild.c:960`) แล้วหัก `memoryMargin` 1MiB อีก → **`mwm - 4MiB`**
>
> ### ⭐⭐ ข้อค้นพบใหม่ — `mwm` เท่ากัน แต่ความจุต่างกัน 5%
>
> ```
> mwm 64MB · parallel  ->  28,364 tuples
> mwm 64MB · serial    ->  29,798 tuples     <- ต่างกัน 5% ที่ค่าเดียวกัน
> ```
>
> เพราะ serial ใช้ `MemoryContextMemAllocated` และ**ไม่หัก 3MiB ไม่มี margin**
> ส่วนที่เลือกเส้นทางคือ **PostgreSQL ไม่ใช่ pgvector** —
> `plan_create_index_workers()` บังคับให้ผู้ร่วม build แต่ละคนได้ ≥ 32MB
> `mwm 32MB → 32/2 = 16MB < 32MB → worker = 0 → serial`
>
> **ผู้ใช้ไม่ได้เลือกเอง และไม่มีอะไรบอก** — เข้าธีมของโครงงานพอดี
>
> **ทำนายก่อนรันแล้วตรง 5 จาก 5** คลาดไม่เกิน 16 tuple
> (และอธิบาย 12 tuple ที่เคยไม่ทราบสาเหตุได้ด้วย — `E[level]` เป็นค่าสุ่มรายbuild)
>
> `results/i05_capacity_model.txt` · `sql/i05_capacity_model.sql`

→ **ข้ออ้างหลักของ I05 ยังยืนและแข็งขึ้น** (ความจุขึ้นกับมิติจริง ลด 41%)
แต่**ตัวเลขความจุยกข้ามมิติไม่ได้แบบง่ายๆ**

> 🔴 **ข้อผิดพลาดของผู้วัดในรอบนี้** — รอบแรกรายงานว่า *"ไม่มี NOTICE เลย"* ซึ่งผิด
> เพราะคำสั่งเก็บผลมี `| tail -30` ต่อท้าย แล้ว NOTICE ออกตอน build ซึ่งอยู่ต้น output
> จึงถูกตัดทิ้ง · **"ไม่เจอ" ไม่เท่ากับ "ไม่มี"** ต้องตรวจว่าเครื่องมือเก็บครบก่อนสรุป

### 🔬 ทดสอบข้ามชุดข้อมูลครบแล้ว **10 fault** (2026-07-30)

**คุมทุกอย่างเท่าเดิม เปลี่ยนแค่ที่มาของเวกเตอร์** — BEIR/Quora · all-MiniLM-L6-v2 ·
100,000 แถว · 384 มิติ · 200 query · โปรไฟล์ fragile

| fault | กลไก | ปริมาณ | รายละเอียด |
|---|---|---|---|
| **I01** opclass ไม่ตรง | ✅ | ✅ | **buffers เท่ากับไม่มี index ทุกบล็อก** (23,388 = 23,388) |
| **Q02** รูปแบบ query | ✅ | ✅ | **ใช้ index ได้ 2 จาก 6 แบบเท่ากันเป๊ะ** · ที่เหลือ buffers เท่ากันหมด |
| **Q06** เพดาน `ef_search` | ✅ | ⚠️ | เพดานตายตัวทั้งคู่ · ค่าขยับ 40 → **41** |
| **Q03** filter หลัง index | ✅ | ✅ | อาการเหมือนกัน · **ข้ออ้างเรื่อง `max_scan_tuples` ถอนแล้ว (E43)** |
| **V07** NULL/zero | ✅ | ✅ | **หาย 10 แถวพอดีทั้งคู่** |
| **I05** ความจุ mwm | ✅ | ✅ | 28,352 vs **28,364** tuples |
| **L02** แถวที่ถูกลบ | ✅ | ❌ | ขอเยอะไม่ช่วย · VACUUM แก้ได้ · **แต่ไม่มีหน้าผา** · (ทางแก้เพิ่ม E43) |
| **I04** build ไม่นิ่ง | ✅ | ❌ | ราย query เปลี่ยน **183/200** · แต่กลุ่มควบคุมพลิก 2 ข้อ |
| **I02** ตัวอย่างตอน build | ⚠️ | ❌ | ความเป็นตัวแทนยังสำคัญ · แต่คำเตือนออกถูกที่บนข้อมูลจริง |
| **Q04** ค่า probes | ✅ | ❌ | ค่าเริ่มต้นไม่พอทั้งคู่ · **แต่สูตรเอกสารเหมาะสม** |

#### ⭐⭐ เส้นแบ่งที่ทำนายล่วงหน้าแล้วถูก

**ข้อสรุปเชิงกลไกยกข้ามชุดข้อมูลได้ทุกข้อ · ข้อสรุปเชิงปริมาณยกไม่ได้เลยสักข้อ**

ทำนายเส้นแบ่งนี้ไว้**ก่อน**รัน L02 · V07 · Q06 · I04 · I01 · Q02 แล้วผลออกมาตามนั้น
**ครบทั้ง 6 ข้อ** → **เป็นข้อค้นพบเชิงระเบียบวิธี ไม่ใช่การอธิบายผลย้อนหลัง**

#### ⭐ I01 ยืนยันว่าเป็นภาพสะท้อนกลับด้านของ Q01

```
Q01  index ถูกใช้     -> เร็วขึ้น 49 เท่า   แต่ recall ตกเหลือ 0.9930
I01  index ไม่ถูกใช้   -> recall สมบูรณ์ 1.0000  แต่ไม่เร็วขึ้นเลย
```

**ทั้งคู่ไม่มี error** · ทีมที่เฝ้าดู recall ไม่เห็น I01 · ทีมที่เฝ้าดู latency ไม่เห็น Q01

#### 🔴 สามข้อที่ต้องเลิกพูด (เพิ่มจาก Q04 · I02 ที่บันทึกไว้แล้ว)

| เคยพูด | ความจริงบนข้อมูลจริง |
|---|---|
| L02 "หน้าผาที่ 50% · 10 เต็มกลายเป็น 0" | ได้ครบ 10 ถึงตาย **80%** · ที่ 90% ได้ 5 · **ไม่เคยถึง 0** |
| I04 "`probes=5` กลบความไม่แน่นอนหมดจด" | ยัง **121/200 (60.5%)** เปลี่ยนคำตอบ · ช่วยแต่ไม่กลบ |
| Q06 "เพดาน = `ef_search` พอดีเป๊ะ" | บนข้อมูลจริงเพดาน = **41** ไม่ใช่ 40 |

#### ⚠️ กระทบการตีความ E32

E32 หักล้างชื่อเดิมของ I04 ("k-means สุ่ม") ด้วยเหตุผลว่า **HNSW ไม่มี k-means แต่แกว่ง 132/200**

บนข้อมูลจริง **HNSW แกว่งแค่ 6/200 (3%)** ขณะที่ IVFFlat แกว่ง 91.5%
→ **สอดคล้องกับสมมติฐาน k-means มากกว่า** · E32 ยังถูกสำหรับ corpus สังเคราะห์
แต่ **ยกไปใช้ทั่วไปไม่ได้** · กลไกของ HNSW ยังไม่ทราบเหมือนเดิม

#### 🔴 บทเรียนจากการวัด V07 — พลาดเองสองกับดักพร้อมกัน

รอบแรกรายงานว่า "ไม่มีแถวหาย" ซึ่งผิด เพราะ **ตาราง 510 แถวเล็กมาก planner
เลือก `Seq Scan` เอง** ไม่เคยใช้ vector index เลย · และตอนตรวจ plan เห็น
`"Node Type": "Index Scan"` แล้วเกือบเชื่อ — **แต่นั่นคือ pkey ของ subquery** (E25)

→ ตอนนี้มี assertion ที่ **หยุดทันทีถ้าไม่ได้ใช้ vector index จริง**
ตรวจจาก**ชื่อ index** ไม่ใช่ค้นคำ

ไฟล์: `sql/real_*.sql` · ผล: `results/real_*.txt`
**เขียนลงเล่มแล้วที่หัวข้อ 5.8** (5.8.1 วิธีการ · 5.8.2 ตารางที่ 9 · 5.8.3 ข้อค้นพบ
เชิงระเบียบวิธี · 5.8.5 ข้อสรุปของตัวเองที่ถูกหักล้าง **7 ข้อ** · 5.9 ไล่เทียบเอกสาร
อีก **7 ข้อ** รวม **14**) และแก้ตารางที่ 7 ให้มีสองชุดข้อมูล

### ⚠️ อ่านตารางนี้ก่อนเริ่มงานใดๆ — กันทำซ้ำของที่เสร็จแล้ว

ของส่งมอบตาม `PROJECT.md` ข้อ 8 · **เช็คไฟล์เองได้ใน 10 วินาที**

> **ตารางนี้ถูกตรวจด้วยเครื่องแล้ว** — `audit.py` เทียบกับ `scripts/audit_baseline.json`
> และเช็คไฟล์จริงสามทาง: ติ๊กตรงกันไหม · ข้อที่ติ๊กเสร็จมีไฟล์ครบไหม ·
> **ข้อที่ยังไม่ติ๊กมีไฟล์โผล่มาแล้วหรือยัง** (= ทำเสร็จแล้วลืมอัปเดต ซึ่งเคยเกิดจริง)
> ถ้าแก้ตารางนี้ **ต้องแก้ `audit_baseline.json` ด้วย** ไม่งั้น audit จะ FAIL ทันที

| # | ของ | สถานะ | ไฟล์ |
|---|---|---|---|
| 1 | harness (docker + ตัวฉีด + เฉลย) | ✅ **เสร็จ** | `docker-compose.yml` · `sql/` · `faults/` · `groundtruth/` |
| 2 | ตัวตรวจที่รันได้จริง **+ CI** | ✅ **เสร็จ** | `sql/score.sql` · `scripts/audit.py` · `scripts/quietfail_check.py` · `.github/workflows/` |
| 3 | รายงานผล | ✅ **เสร็จ** | `REPORT.md` |
| 4 | README | ✅ **เสร็จ** | `README.md` |
| 5 | ชุดโจทย์ AI ตอบ RQ3 | ✅ **เสร็จ · เก็บ 3 ครั้ง** (Opus 5 = **5–7/10** · ChatGPT = 10/10) | `results/rq3_ai_traps.txt` · `rq3/` |
| 6 | **ร่างเล่มโครงงาน** | ✅ **จัดตามโครง CPE แล้ว** (รอยืนยันกับอาจารย์) | `thesis/THESIS_TH.md` |

### 🔴 `thesis/THESIS_TH.md` — รูปแบบเล่มยังไม่ยืนยัน · **แหล่งอ้างอิงเดิมผิดที่** (2026-07-30)

> **เจ้าของโปรเจคยืนยันแล้วว่าเป็น ป.ตรี** — บันทึกเดิมในไฟล์นี้อ้างคู่มือของ
> **บัณฑิตวิทยาลัย** ซึ่ง **ดูแลเฉพาะ ป.โท และ ป.เอก** จึงเป็นแหล่งที่ผิดตั้งแต่ต้น

| | |
|---|---|
| **แหล่งที่ถูกต้อง** | **ภาควิชาวิศวกรรมคอมพิวเตอร์ / คณะวิศวกรรมศาสตร์ มก.** |
| ชื่อที่ถูกต้อง | *ปริญญานิพนธ์* หรือ *โครงงานวิศวกรรมคอมพิวเตอร์* — **ไม่ใช่ "วิทยานิพนธ์"** |
| หาในเว็บได้ไหม | ❌ `cpe.ku.ac.th` ไม่ได้เผยแพร่รูปแบบเล่มไว้สาธารณะ (ค้นแล้ว 2026-07-30) |
| ได้จากไหน | **อาจารย์ที่ปรึกษาโครงงาน · วิชาโครงงาน · หรือ LMS ของภาค** |

### ⭐ deep search 2026-07-30 — ได้โครงเล่มจริงของภาค CPE จากคลังโครงงาน

**ระบบของภาค:** `ecourse.cpe.ku.ac.th/projar/` = คลังโครงงาน (Project Archive)
นิสิตทุกคนต้องอัปโหลดเมื่อจบ · มีรายการถึงหลักสูตร 2565 (= หลักสูตรของรหัส 67)

**ของที่ต้องส่ง (จากคู่มือ upload ของระบบ):** เล่มเต็ม `.pdf` (+`.docx` ได้) ·
ซอร์สโค้ด `.zip` · โปสเตอร์ ≤20MB · **บทสรุปย่อ ~4 หน้า** · รูปนิสิต ≤2MB
· (เลือกได้: technical doc · สไลด์ · installer)

**โครงเล่มจริง** — อ่านจากเล่มตัวอย่างในคลัง (`projar_media/758/RedBook.pdf` · ปี 2558
· 101 หน้า · ที่ปรึกษา รศ.ดร.อนันต์ ผลเพิ่ม):

```
ปกนอก   โครงงานวิศวกรรมคอมพิวเตอร์ / ภาควิชา / คณะ / มหาวิทยาลัย
         เรื่อง <ชื่อไทย + อังกฤษ> / โดย <ชื่อ + รหัสนิสิต> / พ.ศ.
ปกใน    + "ตามหลักสูตรวิศวกรรมศาสตรบัณฑิต สาขาวิศวกรรมคอมพิวเตอร์"
         + ช่องลายเซ็นอาจารย์ที่ปรึกษา (ได้หลายคน) + หัวหน้าภาควิชา
ส่วนหน้า  บทคัดย่อไทย (I) · Abstract (II) · กิตติกรรมประกาศ (III) ·
         สารบัญ/สารบัญภาพ/สารบัญตาราง (IV…) — เลขหน้าโรมันใหญ่
เนื้อหา   1. บทนำ (วัตถุประสงค์ · ขอบเขต · ประโยชน์)
         2. ทฤษฎีและงานที่เกี่ยวข้อง
         3. เครื่องมือที่ใช้ในการทำโครงงาน
         4. วิธีการดำเนินโครงงาน
         5. ผลการดำเนินโครงงานและวิจารณ์
         6. สรุปผลการดำเนินงานและข้อเสนอแนะ (ข้อสรุป · ปัญหา · แนวทางพัฒนาต่อ)
         7. บรรณานุกรม — **ตัวเลข [1] [2] แบบ IEEE + วันที่เข้าถึง ไม่ใช่นาม–ปี**
         8. ภาคผนวก (คู่มือติดตั้ง · คู่มือใช้งาน)
         9. ประวัตินิสิต
```

### ✅ จัดเล่มใหม่ตามโครง CPE แล้ว (2026-07-30)

`thesis/THESIS_TH.md` เดิมใช้โครง 5 บทสายวิทย์ + อ้างอิงนาม–ปี **ผิดทั้งสองอย่าง**
ตอนนี้จัดใหม่ครบแล้ว 44,581 → 49,573 ตัวอักษร

| เดิม | ใหม่ |
|---|---|
| — | **ปกนอก + ปกใน** พร้อมช่องลายเซ็นอาจารย์ที่ปรึกษา/หัวหน้าภาค |
| — | **กิตติกรรมประกาศ** (โครงให้ ต้องเขียนเอง) |
| บทที่ 1 คำนำ | บทที่ 1 **บทนำ** |
| บทที่ 2 การตรวจเอกสาร | บทที่ 2 **ทฤษฎีและงานที่เกี่ยวข้อง** |
| บทที่ 3 อุปกรณ์และวิธีการ | บทที่ 3 **เครื่องมือที่ใช้ในการทำโครงงาน** (+ตารางเครื่องมือใหม่) |
| ↑ รวมอยู่ในบท 3 | บทที่ 4 **วิธีการดำเนินโครงงาน** (แยกออกมา 4.1–4.8) |
| บทที่ 4 ผลและวิจารณ์ | บทที่ 5 **ผลการดำเนินโครงงานและวิจารณ์** (5.1–5.7) |
| บทที่ 5 สรุปฯ | บทที่ 6 **สรุปผลการดำเนินงานและข้อเสนอแนะ** (6.1 ข้อสรุป · 6.2 ปัญหาและอุปสรรค · 6.5 แนวทางพัฒนาต่อ) |
| เอกสารและสิ่งอ้างอิง (นาม–ปี) | **บรรณานุกรม แบบตัวเลข [1]–[13] + วันที่เข้าถึง** |
| ภาคผนวก ก–จ | + **ฉ คู่มือการติดตั้ง** · **ช คู่มือการใช้งาน** |
| — | **ประวัตินิสิต** |

**ทะเบียนภาษาเปลี่ยนทั้งเล่ม** — "งานวิจัยนี้"→"โครงงานนี้" · "ผู้วิจัย"→"ผู้พัฒนา" ·
"คำถามการวิจัย"→"คำถามของโครงงาน" · คำว่า *วิทยานิพนธ์* เหลือ **0 ครั้ง**

**ตรวจด้วยเครื่องแล้ว** `scratchpad/checkrefs.py` — อ้างอิงข้ามหัวข้อทุกจุด
ทั้งหมด (ครบ 6 บท · 7 ภาคผนวก) ชี้ไปยังสิ่งที่มีอยู่จริง ไม่มีเลขค้างจากโครงเก่า

### ข้อมูลเจ้าของโครงงาน — เติมลงเล่มแล้ว (2026-07-30)

```
นิสิต            นายพงศกร  หมู่บ้านม่วง
อาจารย์ที่ปรึกษา  รองศาสตราจารย์ ดร.กฤษณะ  ไวยมัย
หัวหน้าภาควิชา   ผู้ช่วยศาสตราจารย์ ดร.จิตร์ทัศน์  ฝักเจริญผล
ปีการศึกษา       2569 (ใส่ไว้ก่อน — ยืนยันปีที่จะปรากฏบนเล่มจริงอีกครั้ง)
```

เติมครบทุกจุดแล้ว: ปกนอก · ปกใน (ช่องลายเซ็น) · หัวหน้าหน้าบทคัดย่อไทยและอังกฤษ
· กิตติกรรมประกาศ (ร่างเต็มให้ ปรับถ้อยคำได้) · ประวัตินิสิต (โครงตามเล่มตัวอย่าง)

**🔴 ยังต้องเติมเอง 3 อย่าง**

| อะไร | อยู่ตรงไหน |
|---|---|
| **รหัสนิสิต** | ปกนอก + ปกใน (`[รหัสนิสิต]` 2 จุด) |
| **ประวัตินิสิต** | ที่อยู่ · เบอร์โทร · อีเมล · โรงเรียนและปีที่จบ (ช่อง `.....`) |
| **ชื่ออังกฤษ** | หน้า ABSTRACT ใส่ `Pongsakorn Mubanmuang` ไว้ — **ตรวจกับหนังสือเดินทาง/เอกสารทางการ** |

> รูปแบบประวัตินิสิตคัดมาจากเล่มตัวอย่างจริงในคลัง projar (ไม่ได้เดา):
> ชื่อ · เลขประจำตัว · ภาควิชา/คณะ/มหาวิทยาลัย · ที่อยู่ปัจจุบัน · มือถือ · อีเมล ·
> ระดับการศึกษา · ตารางคุณวุฒิ (ม.ปลาย · ม.ต้น) พร้อมสถาบันและปีที่จบ

> ⚠️ หลักฐานคือเล่มตัวอย่างปี 2558 **หนึ่งเล่ม** — โครงอาจปรับได้ตามยุค
> ยังต้องยืนยันกับอาจารย์ที่ปรึกษา แต่ตอนนี้มีของจริงให้ชี้ถามแล้ว

**ยังต้องถามภาควิชา (แคบลงจาก 5 เหลือ 3):**

1. โครงเล่มตามตัวอย่าง 2558 ยังใช้ปัจจุบันไหม · มี template `.docx` ของภาคไหม
2. ฟอนต์/ขอบกระดาษ (เล่มตัวอย่างอ่านค่าไม่ได้จาก PDF — ตาเปล่าดูใกล้ TH SarabunPSK 16pt)
3. ชื่ออาจารย์ที่ปรึกษา + จำนวนกรรมการที่ต้องมีช่องลายเซ็น

### 📌 คู่มือบัณฑิตวิทยาลัย 2553 — อ่านได้แล้ว เก็บไว้เป็นทางเลือกสำรอง

**ใช้กับ ป.ตรี ไม่ได้โดยตรง** แต่หลายภาคอิงรูปแบบของมหาวิทยาลัย ถ้าภาคไม่มีของตัวเอง
อาจารย์อาจให้ใช้ตามนี้ — **ต้องถามก่อน ห้ามเดา**

> ✅ **แก้บันทึกเดิม** — ไฟล์นี้เคยเขียนว่า *"คู่มือเป็น PDF ที่ดึงเนื้อในอ่านอัตโนมัติไม่ได้"*
> **อ่านได้แล้ว** ด้วย `pymupdf` (มีในเครื่องนี้) จาก URL ที่ปุ่มดาวน์โหลดชี้ไป
> `https://www.grad.ku.ac.th/download/stu-sci53-chapter04/?wpdmdl=15455`
> (หน้าเว็บโชว์ขนาด `0.00 KB` ซึ่งเป็นบั๊กของเว็บ ไฟล์จริง 186 KB)

**บทที่ 4 การพิมพ์ — ค่าที่อ่านได้จริง**

```
กระดาษ   A4 ขาว ≥ 80 แกรม พิมพ์หน้าเดียว
ขอบ      บน 1.5"  ล่าง 1"  ซ้าย 1.5"  ขวา 1"
ฟอนต์ไทย 16 pt — ใช้ได้ 6 แบบ: AngsanaUPC · BrowalliaUPC · CordiaUPC
                              DilleniaUPC · EucrosiaUPC · FreesiaUPC
หัวข้อ    ใหญ่ 18 pt หนา · รอง 16 หนา · ข้าง 16 หนา · ย่อหน้า 16 ไม่หนา
         **ห้ามขีดเส้นใต้ทุกระดับ**
ย่อหน้า   เว้นจากขอบซ้ายเข้าไปอีก 0.5" (1 Tab)
ระยะ      เว้น 1 ระยะบรรทัดระหว่างย่อหน้า/หัวข้อ/ภาพ/ตาราง
เลขหน้า   มุมบนขวา · ห่างขอบบน 1" · ห่างขอบขวา 1"
         สารบัญ (1)(2) มีวงเล็บ · เนื้อหา 1 2 3 ไม่มีวงเล็บ
         **ห้ามมีหน้าแทรกแบบ 1ก 2ก**
```

**ยังไม่ได้ดึง:** บทที่ 2 องค์ประกอบ (ชื่อบท) · บทที่ 3 การอ้างอิง (58 หน้า)
ดึงได้ด้วยวิธีเดียวกัน — เปลี่ยน slug เป็น `stu-sci53-chapter02` / `chapter03` แล้วอ่าน `onclick`

**ยังมี iThesis** (`ithesis.grad.ku.ac.th`) ซึ่งจัดรูปแบบให้อัตโนมัติ — **แต่เป็นระบบของบัณฑิตวิทยาลัย ป.ตรีน่าจะไม่ได้ใช้**

**ของส่งมอบครบทั้ง 5 ข้อแล้ว รวม CI · งานที่เหลือเป็นทางเลือกล้วน**

| งาน | ทำไมถึงคุ้ม | เริ่มยังไง |
|---|---|---|
| 🔴 **ยืนยันรูปแบบเล่มกับภาควิชา** | **บล็อกการส่ง** — ไม่รู้ชื่อบท เลขหน้า รูปแบบอ้างอิง ก็ส่งไม่ได้ | **เจ้าของโปรเจคต้องถามเอง** · ดูตารางค้างข้างบน |
| ✅ ~~ทดสอบบน embedding จริง~~ | **เสร็จครบ 10 fault แล้ว 2026-07-30** — Q01·I02·Q04·Q03·Q06·V07·L02·I04·I01·Q02 | `results/real_*.txt` |
| **RQ3 โมเดลที่สาม** | **คุ้มน้อยที่สุด — ประเมินแล้ว 2026-07-30** ดูกล่องข้างล่าง | **เจ้าของโปรเจคต้องเก็บเอง** · `rq3/assignments.md` แล้ว `python scripts/rq3_score.py rq3/answers/<ชื่อโมเดล>` |

> ### RQ3 โมเดลที่สาม — ทำไมถึงคุ้มน้อย (ประเมิน 2026-07-30)
>
> **จุดอ่อนของ RQ3 คือ "โจทย์ไม่ได้สุ่ม" ไม่ใช่ "โมเดลน้อยไป"** — เพิ่มเป็น 5 เจ้า
> ก็ยังไม่ใช่ตัวอย่างสุ่มอยู่ดี ข้อจำกัดในเล่ม 5.2 ข้อ 4 จะยังอยู่เหมือนเดิม (เหตุผลเดียวกับ D20)
>
> **ข้อค้นพบสำคัญได้ครบแล้วด้วย n=2** — โดยเฉพาะข้อที่แข็งที่สุดคือ *"เขียนจนปิด index
> เกิดข้ามค่าย"* ซึ่งต้องการ 2 โมเดลพอดี · โมเดลที่สามจะ**ยืนยันซ้ำ ไม่ได้เปลี่ยนข้อสรุป**
>
> **ต้นทุน:** 10 โจทย์ × 2 รอบ = **20 แชทใหม่** ที่เจ้าของต้องเปิดเอง (~1.5–3 ชม.)
> และรอบเดียวต่อโมเดลไม่นับ จะขัดกับกฎที่ตั้งไว้เอง
>
> **ทำเมื่อ:** อาจารย์หรือกรรมการระบุมาตรงๆ ว่าอยากเห็นเกิน 2 เจ้า
> **ถ้าทำ เลือก Gemini** — คนละค่ายกับทั้ง Anthropic และ OpenAI จึงเสริมข้ออ้าง
> "รูปแบบร่วมข้ามค่าย" ได้จริง (เลือก Claude หรือ GPT รุ่นอื่นได้ค่าน้อยกว่ามาก)
>
> `PROJECT.md` เขียนไว้เองตั้งแต่ต้น: **"RQ3 เป็นส่วนเสริม ถ้าเวลาไม่พอ ตัดได้"**

### `quietfail-check` — ของส่งมอบที่ทีมอื่นเอาไปใช้ได้ (2026-07-28)

`sql/score.sql` เป็นตัวนับคะแนน**ของงานวิจัย** — อ่านไฟล์เฉลยด้วย `pg_read_file()`
จึงรันได้เฉพาะบน container ของ repo นี้ **ยกไปใช้กับฐานข้อมูลคนอื่นไม่ได้**

`scripts/quietfail_check.py` คือของส่งมอบตัวจริง — ยืนได้ด้วยตัวเอง ค้นหาคอลัมน์ vector
และ index เอง ไม่ต้องมีไฟล์เฉลย ไม่ต้องรู้ schema ล่วงหน้า

```bash
python scripts/quietfail_check.py --dsn "postgresql://..." --max-limit 100
python scripts/quietfail_check.py --docker          # ใช้ container ของ repo นี้
```

| exit | ความหมาย |
|---|---|
| 0 | ตรวจครบ ไม่พบปัญหา |
| 1 | **พบปัญหา** — ควร fail build |
| 2 | ไม่พบปัญหา แต่มีข้อที่ **ตรวจไม่ได้** · `--allow-unknown` เพื่อให้ผ่าน |

ตรวจ 6 ข้อที่ยกไปใช้ที่อื่นได้จริง: **I01 · Q02 · Q04 · Q06 · I05 · V07**

> ### ⭐ I05 ตรวจได้ทุกมิติแล้ว 2026-08-01 — และปิดผลบวกลวงที่ไม่มีใครรู้
>
> เดิมใช้ค่าคงที่ `443 tuples/MB` ซึ่ง **calibrate ที่ `mwm = 64MB` จุดเดียว**
> จึงคลาดที่ค่าอื่น และคลาดไปทาง**ประเมินความจุต่ำเกินจริง** = ตอบ `DETECTED`
> ทั้งที่กราฟยังพอ · **ผลบวกลวงแย่กว่า `CANNOT_CHECK` สำหรับเครื่องมือที่ส่งมอบ**
>
> | `mwm` | ค่าคงที่เดิม | วัดจริง |
> |---|---|---|
> | 32MB | 14,176 | **14,902** |
> | 64MB | 28,352 | 28,364 ← จุดที่ calibrate |
> | 128MB | 56,704 | **58,625** |
>
> ตอนนี้ใช้สูตรจากซอร์ส `674 + MAXALIGN(8 + 4×dim)` จึงเลิกข้ามมิติที่ไม่ใช่ 384
> **และคิดแบบ serial (งบเต็ม `mwm`) โดยตั้งใจ** เพราะไม่ทราบว่าปลายทาง build
> เส้นทางไหน — ยอมพลาดดีกว่าเตือนผิด
(Q03 กับ L02 ไม่รวม เพราะต้องยิง query ทดสอบด้วย vector จริง ซึ่งเลือกเองไม่ได้โดยไม่เดาเจตนาแอป)

> 🔴 **เงื่อนไขของฐานข้อมูลปลายทาง — ขาดแล้วได้ `exit 2` ไม่ใช่ `exit 0`** (H31)
>
> | ต้องมี | ไม่งั้นข้อไหนตรวจไม่ได้ |
> |---|---|
> | `shared_preload_libraries = pg_stat_statements` | **I01 · Q02** — `CREATE EXTENSION` อย่างเดียวไม่พอ สร้างได้แต่ query แล้ว error |
> | มี **ivfflat** index และระบุ `lists` ตรงๆ ตอน `CREATE INDEX` | **Q04** — `reloptions` เก็บเฉพาะค่าที่เขียนไว้ ค่าปริยายไม่ถูกบันทึก |
>
> ทั้งคู่พิสูจน์แล้วด้วยการรันจริงบน image เปล่า ไม่ใช่ container ของ repo นี้
> (ซึ่งมี preload อยู่แล้วจึงมองไม่เห็นปัญหา — เป็นเหตุที่ CI แดงอยู่หลายวันโดยไม่มีใครรู้)

**พิสูจน์ว่าพลิกได้แล้ว** — `sql/qfcheck_states.sql` → `_fix.sql` → `_teardown.sql`
ผล: `results/qfcheck_states.txt` · จับได้ 6/6 → 0/6 → exit 1 → 0 → 2

> ⚠️ **`SET` ระดับ session ไม่นับว่าแก้** — ตัวตรวจเปิด connection ของตัวเอง จึงไม่เห็น
> และนั่น**ถูกต้องแล้ว** เพราะ `SET` ในเซสชันเดียวก็ไม่ได้ปกป้อง production เหมือนกัน
> ทางแก้ที่ตัวตรวจนับคือ `ALTER DATABASE` ขึ้นไป

### ✅ fingerprint มีนิยามกลางแล้ว — ปิด E40 (2026-07-31)

```sql
SELECT qf_fingerprint('qf_corpus');          -- 5,000 แถวแรก (ค่าปริยาย)
SELECT qf_fingerprint('qf_real', 5000);
```

**นิยามอยู่ที่ `init/03_fingerprint.sql` ไฟล์เดียว** — โค้ดใหม่ทุกตัว**ต้องเรียกฟังก์ชันนี้
ห้ามเขียนสูตรเอง** เพราะเคยเขียนซ้ำแล้วเคลื่อนออกจากกันจริง (E40)

**โหลดเข้า DB ที่มีอยู่แล้ว** (ไฟล์ใน `init/` รันอัตโนมัติเฉพาะตอนสร้าง DB ใหม่):

```bash
docker compose exec -T db psql -U lab -d faultlab < init/03_fingerprint.sql
```

**audit มีหัวข้อบังคับ 2 ชั้น**

| หัวข้อ | จับอะไร |
|---|---|
| `ฟังก์ชันกลาง qf_fingerprint()` | ฟังก์ชันให้ค่า**ตรงกับ baseline** — ถ้าใครแก้สูตรข้างในจะจับได้ทันที · **หาไม่เจอ = `CANNOT_CHECK` ไม่ใช่ผ่าน** |
| `นิยาม fingerprint ตรงกันทุกที่` | ไล่ทุกไฟล์ `.sql`/`.py` ว่ายังใช้ตัวคั่นเดียวกัน |

> 📌 **ไฟล์เก่ากว่า 30 จุดยังเขียนสูตรเองอยู่ — ตั้งใจไม่แก้**
> เป็นสคริปต์การทดลองที่ผ่านการตรวจแล้ว การไปแก้มีความเสี่ยงมากกว่าประโยชน์
> และหัวข้อ *"นิยาม fingerprint ตรงกันทุกที่"* กันการเคลื่อนได้อยู่แล้ว

### GitHub · CI · `gh` — สภาพปัจจุบัน (2026-07-30)

```
repo   : https://github.com/Earthkodyai/quietfail   (public)
branch : main เท่านั้น · ไม่มีกิ่งค้าง
CI     : .github/workflows/quietfail-check.yml — ✅ ผ่าน (เพิ่งแก้ ดู H31)
gh CLI : ติดตั้งแล้ว C:\Program Files\GitHub CLI\gh.exe · login แล้ว (Earthkodyai)
         scopes: repo, workflow — อ่าน log ของ CI ที่ตกได้ ไม่ต้องเดา
```

`gh` **ไม่อยู่ใน PATH ของ shell ที่ผู้ช่วยเรียก** ต้องเติมเองก่อนใช้:

```bash
export PATH="/c/Program Files/GitHub CLI:$PATH"
```

**PR ไม่จำเป็นสำหรับงานประจำวัน** — โปรเจคคนเดียว ไม่มีคนรีวิว · push `main` ตรงๆ ได้เลย
ทำ PR เมื่อ (ก) งานใหญ่/เสี่ยงที่อยากให้ CI กันก่อน หรือ (ข) อยากให้ประวัติดูมีระเบียบตอนสมัครงาน

> ⚠️ **ทุก PR สร้างงานวน** — merge แล้ว `results/audit_latest.txt` จะชี้ commit เก่าอีก
> **ไม่ต้องไล่แก้ทุกครั้ง** ห่างหนึ่งขั้นเป็นเรื่องปกติตามที่เขียนไว้ข้างบน

### 🔴 เงื่อนไขที่ฐานข้อมูลปลายทางต้องมี ไม่งั้น `quietfail_check` ได้ `exit 2` (H31)

**CI แดงถาวรตั้งแต่วันที่สร้าง ไม่มีใครรู้จนเปิด PR แรก** — ไม่ใช่เพราะพบปัญหา
แต่เพราะ **ตรวจไม่ได้** ซึ่งนโยบายตั้งใจให้ fail (กฎเหล็กข้อ 10) แต่สภาพแวดล้อมตัวอย่าง
ทำให้ไม่มีวันตรวจครบ

| ต้องมี | ขาดแล้วข้อไหนตรวจไม่ได้ |
|---|---|
| `shared_preload_libraries = pg_stat_statements` | **I01 · Q02** — `CREATE EXTENSION` อย่างเดียว**ไม่พอ** สร้างผ่านแต่ query แล้ว error |
| ivfflat index ที่ **ระบุ `lists` ตรงๆ** ตอน `CREATE INDEX` | **Q04** — `reloptions` เก็บเฉพาะค่าที่เขียนไว้ ค่าปริยายไม่ถูกบันทึก |
| `ivfflat.probes` ตั้งด้วย `ALTER DATABASE` | Q04 ตอบ DETECTED ถ้า `probes < sqrt(lists)` |

**`services:` ของ GitHub Actions ใช้ไม่ได้** เพราะส่ง command ให้ container ไม่ได้
และ `shared_preload_libraries` ตั้งผ่าน env ของ postgres image ก็ไม่ได้ → ต้อง `docker run` เอง

> 🔴 **บทเรียนที่ใหญ่กว่าตัวบั๊ก** — แก้รอบแรกไม่หาย เพราะ reproduce บน container ของ repo นี้
> ซึ่ง `docker-compose.yml` ตั้ง preload ไว้อยู่แล้ว จึงเห็นแค่สาเหตุเดียว
> **การจำลองบนสภาพแวดล้อมที่ผ่อนปรนกว่าเป้าหมาย ให้ผลบวกลวง**
> ถ้าจะ reproduce ปัญหาของ CI ให้ใช้ `docker run pgvector/pgvector:0.8.5-pg17` เปล่าๆ
> **ห้ามใช้ `docker compose` ของ repo**

### ⭐⭐ RQ3 เก็บ 3 ครั้งแล้ว — และล้มไปคนละทิศ (2026-07-30)

|  | Opus 5 รอบ 1 | **Opus 5 รอบ 2** | ChatGPT |
|---|---|---|---|
| ติดกับดัก | 5 / 10 | **7 / 10** | **10 / 10** |
| ข้อที่**ไม่ได้ใช้** vector index | 0 | **3** | **8** |
| ผลลัพธ์ | ขาดหายบ่อย | ขาดหายบ่อย | **ครบถูกต้องทุกข้อ** |
| ต้นทุนต่อ query | 974 block | ปนกัน | **20,049 block** (แพงกว่า 21 เท่า) |
| ชนิดความล้มเหลว | **เร็วแต่ผิด** | ปนกัน | **ถูกแต่ช้า** |

### 🔴 อ่านสามบรรทัดนี้ก่อนอ้างตัวเลข RQ3 ที่ไหนก็ตาม

**0. ตัวเลขของ ChatGPT (10/10) มาจากรอบเดียว — ต้องกำกับเสมอ**
โครงงานตั้งกฎเองว่า *"ต้องเก็บ 2 รอบต่อโมเดล"* แล้วรายงานเลขที่ไม่ผ่านกฎนั้น
**ไม่ทราบว่านิ่งไหม** · ที่ยกไปใช้ได้คือ**รูปแบบ** (`CROSS JOIN` ปิด index เกิด 8/10
ในรอบเดียวกัน = ซ้ำภายในรอบ) ไม่ใช่ตัวเลข · `REPORT.md` 9.1.4 และเล่ม 4.5 กำกับไว้แล้ว

**1. ห้ามรายงานเป็นเลขเดี่ยว — Opus 5 = 5–7 จาก 10**
โจทย์เดียวกันเป๊ะ โมเดลเดียวกัน แชทใหม่ ได้ต่างกัน 2 ข้อ
**การถามครั้งเดียวไม่นิ่ง** — วัดแล้ว 2026-07-30 ไม่ใช่ข้อกังวลลอยๆ
(P09 รอบแรกใช้ `vector_norm()` ถูก · รอบสองใช้ `l2_norm()` แล้วรันไม่ผ่าน)

**2. กับดัก "เขียนจนปิด index" เป็นรูปแบบร่วม ไม่ใช่นิสัยของ ChatGPT**
รอบ 2 ของ Opus 5 ไม่ใช้ index 3 ข้อ (P02 · P08 · P10) ด้วยรูปแบบเดียวกัน
คือดึง query vector มาทาง `JOIN`/CTE แทน subquery ตรงใน `ORDER BY`
→ **เดิมเคยสรุปว่าเป็นลักษณะเฉพาะเจ้า ซึ่งตอนนี้ถูกหักล้างแล้ว**

**3. โมเดลตกหลุม E10 ของเราเอง** — P09 รอบสองเรียก `l2_norm(vector)` ซึ่งไม่มีอยู่
และ `HINT` ชวนให้เติม cast → พาไป `::halfvec` แล้วเสียความละเอียดเงียบๆ
เป็นหลักฐานอิสระว่า E10 ไม่ใช่ความซวยครั้งเดียว

> **ลำดับบล็อกคำตอบเป็นของโมเดลจริง ตัดข้อสงสัยออกได้แล้ว** — P07 รอบแรกวาง index
> ไว้หลัง query (5/50) รอบสองวางไว้หน้า (40/50) โดยไม่มีใครแตะคำตอบเลย

### 🔴 RQ3 ตอบเป็น "รูปแบบ" ไม่ใช่ "อัตรา" — แก้ถ้อยคำแล้วทั้ง repo (D20)

**ห้ามเขียนว่า "ติดกับดักบ่อยแค่ไหน" · "อัตราการพลาด" · "สัดส่วนที่ผิด" อีก**

โจทย์ 10 ข้อ**ออกแบบมาเล็งกับดักที่รู้อยู่แล้ว** ตัวหารจึงเป็นจำนวนโจทย์ที่เราเลือกเอง
ไม่ใช่โอกาสที่เกิดในงานจริง · **เพิ่มโจทย์ไม่ช่วย** เพราะปัญหาอยู่ที่วิธีเลือก ไม่ใช่ขนาด n

| พูดไม่ได้ | พูดได้ (และแข็งกว่า) |
|---|---|
| "AI พลาด 50%" | "เจอความล้มเหลว 2 รูปแบบ: **เร็วแต่ผิด** กับ **ถูกแต่ช้า**" |
| "โมเดล ก ดีกว่า ข 2 เท่า" | "**'เขียนจนปิด index' เกิดทั้งสองค่าย**" |
| "Opus 5 = 5/10" | "Opus 5 = **5–7/10** · การถามครั้งเดียวไม่นิ่ง" |

**คำถามเรื่องความถี่ยังเปิดอยู่ทั้งงาน** — เขียนไว้เป็นงานต่อที่ `REPORT.md` หัวข้อ 9.2
ทะเบียนบั๊ก H01–H32 ก็ตอบไม่ได้เหมือนกัน (ไม่มีตัวหาร)

> **เล่มวิทยานิพนธ์ 4.5 เขียนใหม่แล้ว** เป็น 3 การเก็บ · ตั้งชื่อ **แบบจำลอง ก** = Opus 5
> **แบบจำลอง ข** = ChatGPT ใช้อ้างได้ทั้งเล่ม (เดิมเล่มเป็น n=1 ล้วน)

**ห้ามอ่านว่า "ChatGPT แย่กว่า 2 เท่า"** — 8 จาก 10 ข้อของมันมาจากความผิดพลาด
**เดียวกันซ้ำๆ** คือเขียน `CROSS JOIN` แล้ว `ORDER BY` คอลัมน์จากตารางที่ join มา
ซึ่งทำให้ pgvector ใช้ index ไม่ได้ → `Seq Scan` ทั้งตารางทุก query

**และการไม่ใช้ index บังกับดักอื่นทั้งหมด** — Q03 กับ Q06 ต้องมี index อยู่ในเส้นทาง
ถึงจะเกิด ChatGPT จึงไม่เคยไปถึงเลย · ตัวเลข "Q03 5/5" ในไฟล์ผลอ่านผิดได้ง่าย

> **ทั้งคู่คือความล้มเหลวใน production แต่มีแค่แบบเดียวที่ดูจากผลลัพธ์แล้วเห็น**
> ทีมที่ตรวจด้วย "ผลถูกไหม" จะให้ ChatGPT ผ่านเต็ม · ทีมที่ตรวจด้วย "เร็วไหม" จะกลับกัน

**🔴 ตัวให้คะแนนของเราเองก็เกือบพลาด** — รุ่นแรกวัดแค่จำนวนแถวกับ recall
ให้ ChatGPT ผ่าน **8/10** · จับได้เพราะ P03 ได้ 100/100 ที่ `ef_search=40`
ซึ่งขัดกับผลของ Q06 → ไปดู plan ถึงพบว่าไม่ได้ใช้ index (ดู H29)

### 🔴 RQ3 โมเดลที่สอง — ผู้ช่วย AI ทำเองไม่ได้ **ทดสอบแล้ว อย่าลองซ้ำ**

**ทดสอบจริงเมื่อ 2026-07-28:** เรียก subagent (Haiku) แล้วถามตรงๆ ว่าเห็นอะไรบ้าง

ผลคือ subagent เห็น **`CLAUDE.md` ทั้งไฟล์ใน system prompt** และตอบเองว่า

> *"ความรู้ว่า `ef_search=40` สร้างเพดานตายตัว · `probes=1` ทำ recall หายมหาศาล ·
> `iterative_scan=off` บล็อกการ filter — มาจาก context ที่ได้รับ **ไม่ใช่จาก training**"*

→ **โมเดลถูกยื่นเฉลยให้ก่อนตอบโจทย์** วัดต่อไปจะได้ตัวเลขที่ไม่มีความหมาย
และจะทำให้ตัวเลข RQ3 ที่มีอยู่เสียความน่าเชื่อถือไปด้วย

**ไม่มีทางเลี่ยงจากภายในโปรเจค** — `isolation: worktree` ก็คัดลอก `CLAUDE.md` ไปด้วย
การสั่งให้ "ไม่ต้องสนใจ context" ไม่ลบสิ่งที่มันอ่านไปแล้ว

**วิธีเดียวที่ใช้ได้:** เจ้าของโปรเจคเปิดแชทใหม่กับโมเดลที่**ไม่เห็น repo นี้**
(Gemini · GPT · หรือ Claude ในแอปอื่น) แล้ววางโจทย์จาก `rq3/assignments.md` ทีละข้อ

> **ข้อสังเกตที่ควรอยู่ในเล่ม:** ไฟล์ที่ทำให้การส่งงานข้าม session ราบรื่น
> คือไฟล์เดียวกับที่ทำให้วัด RQ3 จากภายในไม่ได้ — **บริบทที่ดีสำหรับการทำงาน
> คือการปนเปื้อนสำหรับการวัด**

> **ถ้าจะทำอย่างอื่นนอกจากสองข้อนี้ ต้องมีเหตุผลว่าตอบ RQ ข้อไหน** (กฎเหล็กข้อ 4)
> และถ้าจะเพิ่ม fault ใหม่ **ต้องตัดของเดิมออกหนึ่งข้อ** — ชุด 12 ข้อล็อกแล้วที่ D15

**audit ครั้งแรก 2026-07-27** เจอ 3 เรื่อง แก้ครบแล้ว — E30 ตัวตรวจ I05 ไม่เคยถูกรัน ·
E31 `n_distinct` จดเป็นค่าตายตัว · `q01_recall_collapse_100k.txt` ไม่ได้ถูก git track
**ทั้งสามข้อกลายเป็นหัวข้อถาวรใน `scripts/audit.py` แล้ว** จะไม่หลุดซ้ำอีก

> **เฟส 2 ผ่านตามเกณฑ์ `PROJECT.md` ข้อ 9** — "Q01 มีตัวเลข recall จริงที่ขนาดข้อมูล 2 ระดับ"
> วัดที่ 100,000 และ 500,000 แถว · หลักฐานใน `results/q01_recall_collapse_*.txt`

## Q01 — ตัวเลขหลักของโปรเจค (2026-07-27)

384 มิติ · 200 query · cosine · **ไม่ได้จูนอะไรเลย** (`ef_search=40` · `probes=1`)

| ขนาด | | recall@10 | recall@100 | แย่สุด@10 | ครบ 10/10 | เร็วขึ้น |
|---|---|---|---|---|---|---|
| 100k | exact | 1.0000 | 1.0000 | 1.0000 | 200/200 | 1.0× |
| 100k | HNSW | 0.8565 | 0.3903 | **0.0000** | 52/200 | 32.6× |
| 100k | IVFFlat | 0.7870 | 0.7793 | **0.0000** | 100/200 | 44.6× |
| **500k** | exact | 1.0000 | 1.0000 | 1.0000 | 200/200 | 1.0× |
| **500k** | **HNSW** | **0.5885** | 0.3871 | **0.0000** | **2/200** | **117.1×** |
| **500k** | IVFFlat | 0.7820 | 0.7857 | **0.0000** | 108/200 | 51.6× |

**ผลที่สำคัญที่สุด: index สองแบบเสื่อมตามขนาดข้อมูลคนละแบบ**

- HNSW recall@10 ตกจาก 0.8565 → **0.5885** (ตกลง 31%) · ครบ 10/10 เหลือ **2 จาก 200 query**
- IVFFlat แทบไม่ขยับ (0.7870 → 0.7820)
- สาเหตุ: `ef_search=40` เป็น**ค่าสัมบูรณ์** ส่วน `probes=1` จาก `lists=100` เป็น**ค่าสัมพัทธ์**
- **HNSW เร็วขึ้นพร้อมกับที่มันพังลง** (32.6× → 117.1×) ทีมที่ดูแต่ latency จะเห็นว่า "ดีขึ้น"

**ต้นทุน build:** HNSW 89 วิ (100k) → **603 วิ (500k)** · IVFFlat 2.2 → 7.1 วิ = ต่างกัน 85 เท่า

## 🔴 Q04 บน embedding จริง — ถอนข้ออ้างหลัก (2026-07-30)

**เดิมอ้างว่า** *"`probes=5` ได้ recall สมบูรณ์ · เอกสารแนะนำ 10 ซึ่งช้ากว่า 2.2 เท่า
โดยไม่ได้อะไรเพิ่ม"* — **ไม่จริงบนข้อมูลจริง**

| `probes` | สังเคราะห์ | **จริง** | ครบ 10/10 (จริง) |
|---|---|---|---|
| **1** ค่าเริ่มต้น | 0.7370 | **0.6607** | **58/200** |
| **5** | **1.0000** | **0.9068** | 125/200 |
| **10** เอกสารแนะนำ | 1.0000 | **0.9527** | 150/200 |
| 100 (ทุกกลุ่ม) | 1.0000 | 1.0000 | 200/200 |

**เอกสารแนะนำถูกแล้วบนข้อมูลจริง** — `probes=10` ดีกว่า 5 จริง 4.6 จุด
corpus สังเคราะห์ต่างหากที่ทำให้สูตรดูสิ้นเปลือง

**ข้อที่รอด และแรงขึ้น:** ค่าเริ่มต้น `probes=1` ไม่พอ — บนข้อมูลจริง**แย่กว่า**
สังเคราะห์ด้วยซ้ำ · แย่สุดต่อ query = 0.0000 ทั้งสองชุด

**HNSW:** ef 40 → 320 บนข้อมูลจริงขยับแค่ 0.9940 → 0.9987
และ**ยังมี query ที่ได้ไม่ครบเสมอ** (แย่สุด 0.8 ที่ ef=320)
บนสังเคราะห์แย่สุด = 0.0000 ทุกค่า → **อาการมีจริง แต่ความรุนแรงเกินจริงบนสังเคราะห์**

ผลเต็ม: `results/real_q04.txt` · ตัวรัน: `sql/real_q04.sql`

> ### 🔴 ข้ออ้างเรื่องเอกสาร — **ถอนครบทั้ง 3 จาก 3**
>
> **และไล่ต่อหลังจากนั้นเจออีก 3 จุดที่เป็นรูปแบบเดียวกัน** (E43 · E44)
> คือ *ไม่ได้หมุนปุ่มที่เอกสารมีให้ แล้วสรุปจากสิ่งที่หมุน* — ดูกับดักข้อ 12
>
> | | ข้ออ้าง | ผล |
> |---|---|---|
> | Q04 | "สูตรเอกสารเกินจำเป็น" | ❌ **ถอน** |
> | I02 | "คำเตือนออกผิดที่" | ❌ **ถอน** — เอกสารแนะนำถูก |
> | Q03 | "`work_mem` ไม่อยู่ในรายการ" | ❌ **ถอน 2026-08-01** (E42) |
>
> 🔴 **ห้ามพูดว่าเอกสารทางการของ pgvector บกพร่อง — พิสูจน์ไม่ได้สักข้อ**
>
> `hnsw.scan_mem_multiplier` เป็น **ตัวคูณของ `work_mem`** ตามที่เอกสารนิยามไว้เอง
> และที่ **×32 ได้ครบ 40 แถว** โดยไม่แตะ `work_mem` เลย — **การวัดเดิมหยุดที่ ×8**
>
> ```
> multiplier  1  ->   5/40      multiplier 32  ->  40/40
> multiplier  8  ->  17/40      work_mem 4MB   ->  40/40
> ```
>
> **กฎใหม่จาก E42** — ก่อนสรุปว่าพารามิเตอร์ใด *ไม่มีผล* ต้องไล่จนถึงค่าที่มันมีผล
> หรือจนชนขีดจำกัดของระบบ · หยุดกลางทางแล้วสรุป = **ไม่ได้วัด**
>
> **สิ่งที่ยังยืน: ค่าเริ่มต้นของ pgvector ไม่พอ** (ทั้ง `probes=1` และ `ef_search=40`)
> ซึ่งเป็นข้อสรุปหลักของ RQ1 อยู่แล้ว
>
> ### 🔴 เส้นแบ่งที่ควรใช้อ่านผลทุกตัวในงานนี้
>
> | | ยกข้ามชุดข้อมูลได้ |
> |---|---|
> | **ข้อสรุปเชิงกลไก** | ✅ Q06 (เพดาน `ef_search`) · I05 (ความจุ mwm) · V07 · I01 · Q02 |
> | **ข้อสรุปเชิงปริมาณ** | ❌ Q04 (ค่า probes ที่พอ) · I02 (ผลของตัวอย่างตอน build) · Q01 (ขนาด recall loss) |
>
> **ข้อที่รอดคือข้อที่อธิบายกลไกของ executor · ข้อที่ล้มคือข้อที่ผูกกับการกระจายตัวของข้อมูล**

---

## Q04 — ค่า default แลกอะไรไปบ้าง (2026-07-27 · corpus สังเคราะห์)

IVFFlat `lists = 100` (ตามสูตร `rows/1000`) · เอกสารแนะนำ `probes = sqrt(lists) = 10`

| `probes` | | recall@10 | ครบ 10/10 | 200 query |
|---|---|---|---|---|
| **1** | **ค่าเริ่มต้น** | **0.7370** | 78/200 | 277 ms |
| 2 | | 0.9525 | 163/200 | 308 ms |
| **5** | | **1.0000** | **200/200** | 495 ms |
| 10 | เอกสารแนะนำ | 1.0000 | 200/200 | 1,101 ms |
| 100 | ทุกกลุ่ม | 1.0000 | 200/200 | 11,146 ms |

> 🔴 **แถว `probes=100` ไม่ได้ใช้ index (E45)** — planner ทิ้ง index เอง
> ตามที่เอกสารเตือน (*"the planner won't use the index"*) · เวลาที่บันทึกจึงเป็น
> ของ exact search · **384 มิติทิ้งที่ 100 · 768 มิติทิ้งตั้งแต่ 50**
> ข้อสรุปหลักอยู่ที่ `probes=1` กับ `10` ซึ่งใช้ index จริงทั้งคู่ — ไม่กระทบ

**`probes=5` ได้ recall สมบูรณ์บน corpus สังเคราะห์** เอกสารแนะนำ 10 ซึ่งช้ากว่า 2.2 เท่า

> 🔴 **ข้ออ้างนี้ถูกถอนแล้ว** — บน embedding จริง `probes=5` ได้แค่ **0.9068**
> และ `probes=10` ได้ **0.9527** ซึ่งดีขึ้นจริง · **สูตรของเอกสารเหมาะสมบนข้อมูลจริง**
> ดูหัวข้อ *"Q04 บน embedding จริง — ถอนข้ออ้างหลัก"* ข้างบน

→ **ทั้งค่าเริ่มต้นและคำแนะนำในเอกสาร ต่างก็ไม่ใช่คำตอบที่ถูกสำหรับข้อมูลชุดนี้**
คำตอบมาจากการวัดเท่านั้น — วิทยานิพนธ์ของโปรเจคตรงตัว

**HNSW** `ef_search` 40 → 320: recall@10 ขึ้นจาก 0.8510 → 0.9860
**แต่แย่สุดต่อ query = 0.0000 ทุกค่า** แม้ที่ 320 → เพิ่ม ef ช่วยค่าเฉลี่ย
แต่ยังมี query ที่ได้ผลถูกศูนย์อันอยู่ดี

**`probes=1` เป็นปัญหาร่วมของวงการ** — FAISS (`size_t nprobe = 1;`) และ OpenSearch ก็ตั้ง 1
ต่างจาก Q06 ที่เป็นของ pgvector เฉพาะตัว

## L02 — แถวที่ถูกลบยังค้างใน index → ขอ 10 ได้ 0 (2026-07-28)

100,000 แถว · `ef_search=40` · `LIMIT 10` · ปิด autovacuum เฉพาะตารางทดสอบ

| ตายไป % | แถวที่ยังอยู่ | ขอ | **ได้** | เฉลย |
|---|---|---|---|---|
| 30 | 70,000 | 10 | 10 | 10 |
| **40** | 60,000 | 10 | **10** | 10 |
| **50** | 50,000 | 10 | **0** | 10 |
| 90 | 10,000 | 10 | **0** | 10 |

**⭐ หน้าผาระหว่าง 40% กับ 50% และไม่มีทางลาดเลย** — 10 เต็ม กลายเป็น 0 ในขั้นเดียว
ทีมที่เฝ้าดูตัวเลขจะเห็นปกติดีจนถึงวินาทีที่มันเป็นศูนย์ · **ตัววัดแบบ threshold จับไม่ทัน**

**ขอเยอะขึ้นไม่ช่วย** — ที่ตาย 90% ขอ 10 · 40 · 100 · 1000 ได้ **0 ทั้งหมด**

> ⭐ **แต่ `iterative_scan` + `scan_mem_multiplier=32` ได้ครบ 10 โดยไม่ต้อง `VACUUM`**
> (E43 · 2026-08-01) — ไม่เคยทดสอบมาก่อน · **หลักฐานเชิงกลไกที่แข็งที่สุดของ L02**
> เพราะทางแก้เดียวกันแก้ได้ทั้ง L02 และ Q03 ตามที่ทฤษฎี "กลไกเดียวกัน" บังคับ
> ⚠️ แพงกว่า `VACUUM` 11 เท่า (10,501 vs 947 buffers) — เป็นหลักฐาน ไม่ใช่คำแนะนำ
> `results/doc_reread_probe.txt`

**⭐ กลไกเดียวกับ Q03 เป๊ะ แต่ตัวกรองมองไม่เห็น**

| | ตัวกรองคืออะไร | เห็นในโค้ดไหม |
|---|---|---|
| Q03 | `WHERE` ที่เขียนเอง | ✅ |
| **L02** | **MVCC** ตัดแถวที่ตาย | ❌ **ไม่มีอะไรในคำสั่งบอกเลย** |

### `VACUUM` แก้ได้หมด แต่ไม่ลดขนาด index — และ `REINDEX` เร็วกว่า

27 วินาทีที่ 100k แถว · 118 วินาทีหลัง `UPDATE` ทั้งตาราง · index **195 MB → 195 MB**

> 🔴 **แก้แล้ว (E45)** — เคยจัด `REINDEX` เป็น "คนมักแก้ผิด" ว่า *แพงกว่า VACUUM*
> **ทั้งที่เอกสารแนะนำ และไม่เคยวัดเลย** · วัดคู่กันบนตารางสภาพเดียวกัน:
> **`REINDEX` 3.4 วิ · index เหลือ 19.5 MB** เทียบ **`VACUUM` 12.8 วิ · 195.3 MB**
> → เร็วกว่า 3.8 เท่า ลดขนาด 10 เท่า · ⚠️ แต่ล็อกตาราง (เอกสารบอกใช้ `CONCURRENTLY`)
> ตัวเลข 27 วินาทีข้างบนวัดที่ `mwm` 64MB คนละเงื่อนไขกับ 12.8 วินาที — เทียบข้ามกันไม่ได้

### 🔴 กลุ่มควบคุม `UPDATE` หักล้างชื่อในทะเบียน

`UPDATE` ทั้งตาราง: index บวม 195 → **202 MB** แต่ **ได้ครบ 10 ทุกขั้น recall ไม่ตก**

→ **บวมกับ recall ตกเป็นคนละเรื่อง** · ต้นเหตุคือ **แถวที่ถูกลบแล้วไม่มีตัวแทน**
ไม่ใช่ dead tuple ทุกชนิด — ดู **E34** (ครั้งที่ 4 ที่ชื่อ fault ถูกการวัดหักล้าง)

**ห้ามพูด:** "dead tuple ทำให้ recall ตก" (UPDATE สร้าง dead tuple แต่ไม่ตก) ·
"index บวมแล้วผลหาย" (บวม 202 MB แต่ผลครบ)

> **ตัวตรวจรุ่นแรกไม่พลิก** เพราะใช้แถวในตารางเองเป็น query vector ซึ่งยังไม่ตาย
> **query vector ต้องมาจากนอกตารางเสมอ** — ดู E35 · H27

## ✅ Q03 บน embedding จริง — รอด (2026-07-30)

**ข้อเดียวใน 3 ข้ออ้างเรื่องเอกสารที่ยืนได้** · ที่ filter 1% ขอ 40 แถว

| เปลี่ยนอะไร | สังเคราะห์ | **จริง** | buffers (จริง) |
|---|---|---|---|
| ค่าเริ่มต้น | 0 | 1 | 530 |
| เปิด `iterative_scan` | 0 | 5 | 873 |
| `+ max_scan_tuples` ×10 | **0** | **5** | **873** |
| `+ scan_mem_multiplier` ×8 | 0 | 13 | 3,005 |
| **`+ work_mem` → 4MB** | **40** | **40** | 27,917 |

> 🔴 **ข้ออ้างนี้ถอนแล้ว (E42)** — ตารางนี้หยุดที่ `scan_mem_multiplier = 8`
> ซึ่งไกลไม่พอ · ที่ **×32 ได้ครบ 40** โดยไม่แตะ `work_mem` เลย
> เพราะ **`scan_mem_multiplier` คือตัวคูณของ `work_mem`** ตามที่เอกสารนิยามไว้
> `results/q03_multiplier_probe.txt`

**ที่ยังยืน:** ค่าเริ่มต้นได้ **5 จาก 40** และไม่มีอะไรบอกว่ายังหมุนไม่พอ

> 🔴 **ถอนแล้ว (E43)** — เดิมเขียนว่า *"`max_scan_tuples` ไม่ได้อะไรเลยทั้งสองชุด
> buffers ไม่ขยับแม้แต่บล็อกเดียว"* · **ปุ่มไม่ได้ตาย แค่ไม่ใช่ตัวที่ผูกอยู่**
>
> ทำให้หน่วยความจำเหลือเฟือก่อน (`mult=32`) แล้วไล่ `max_scan_tuples`:
> **100 → 4 แถว · 1000 → 7 · 5000 → 40** ตอบสนองเป็นลำดับชัดเจน
>
> **เอกสารเขียนไว้เองว่า** *"Try increasing this [scan_mem_multiplier] if increasing
> hnsw.max_scan_tuples does not improve recall"* — เราเจออาการที่เอกสารทำนายไว้
> แล้วอ่านเป็นข้อบกพร่องของเอกสาร · `results/doc_reread_probe.txt`
>
> 🔴 **การวัดซ้ำสองชุดข้อมูลไม่ได้ทำให้แข็งขึ้น** เพราะวัดใต้เงื่อนไขที่ผิดเหมือนกัน
> **การทำซ้ำยืนยันความสม่ำเสมอ ไม่ได้ยืนยันความถูกต้อง**

🔴 **ผ่อนถ้อยคำ** — เดิมว่า "ไม่ทำงานเลย" · บนข้อมูลจริงช่วยบางส่วน (1→5→5→13)
แต่ไม่เคยครบ → ที่ถูกคือ **"ไม่เพียงพอ แม้ทำครบทุกข้อ"**

**อาการหลักเกิดซ้ำ เส้นโค้งต่างกัน** — จริงเริ่มขาดที่ 20% (สังเคราะห์ 10%)
ถึงศูนย์ที่ 0.5% (สังเคราะห์ 2%) · **buffers นิ่งที่ 530 ตั้งแต่ 20% ลงไป
ทั้งที่ผลลดจาก 6 เหลือ 0** — ทีมที่เฝ้าดู I/O ไม่เห็นอะไรผิดปกติเลย

ผลเต็ม: `results/real_q03.txt` · ตัวรัน: `sql/real_q03.sql`

---

## Q03 — filter ทำงานหลังสแกน index → ขอ 10 ได้ 0 (2026-07-27 · corpus สังเคราะห์)

`ef_search=40` · `LIMIT 10` (ต่ำกว่า ef ชัดเจน เพื่อ**แยกจาก Q06**) · ทุกระดับมีแถวเข้าเงื่อนไขเกิน 10

| filter ตรง | แถวที่เข้าเงื่อนไข | ขอ | **ได้** | เฉลย |
|---|---|---|---|---|
| 20% | 20,000 | 10 | 10 | 10 |
| **10%** | 10,000 | 10 | **2** | 10 |
| 5% | 5,000 | 10 | **1** | 10 |
| **2% ลงไป** | 2,000–100 | 10 | **0** | 10 |

**ตั้งแต่ 2% ลงไปได้ศูนย์แถว** ทั้งที่มีแถวเข้าเงื่อนไข 2,000 แถว
· ใช้ index ทุกกรณี · **ไม่มี error สักครั้ง** · buffers นิ่งที่ 977 ตลอด

### ⭐ ทางแก้ไม่ทำงานเลย จนกว่าจะแตะค่าที่เอกสารไม่ได้พูดถึง

ที่ filter 1% ขอ 40 แถว

| เปลี่ยนอะไร | ได้ | buffers |
|---|---|---|
| ค่าเริ่มต้น | 0 | 977 |
| เปิด `hnsw.iterative_scan` | **0** | 1,743 |
| `+ max_scan_tuples` ×10 | **0** | 1,743 |
| `+ scan_mem_multiplier` ×8 | **0** | 3,035 |
| **`+ work_mem` 64kB → 4MB** | **40** | 27,145 |

> 🔴 **ถอนแล้ว (E42)** — เดิมเขียนว่า *"ตัวปลดล็อกคือ `work_mem` ซึ่งเอกสารไม่พูดถึง"*
> ตารางนี้หยุดที่ `scan_mem_multiplier = 8` ซึ่งไกลไม่พอ · **×32 ได้ครบ 40**
> และเอกสารนิยาม `scan_mem_multiplier` ว่าเป็นตัวคูณของ `work_mem` อยู่แล้ว

**สามขั้นแรกจ่าย I/O เพิ่มโดยไม่ได้แถวเพิ่มเลย** ยังจริง · แต่ข้อสรุปที่ถูกคือ
**ค่าเริ่มต้นต่ำเกินไปมาก และไม่มีสัญญาณบอกว่ายังหมุนไม่พอ**

**สามขั้นแรกจ่าย I/O เพิ่มโดยไม่ได้แถวเพิ่มเลย** · ทางแก้ที่ได้ผลแพงขึ้น **28 เท่า**

**`+ 0` ยืนยันจากโครงสร้าง plan แล้ว** — ไม่มี `+ 0` → ไม่มี Sort node บน PG17
เป็นทริกเดียวกับรูปแบบ E ของ Q02 ที่ทำให้ index ใช้ไม่ได้

**ตัวตรวจไม่ต้องรู้เฉลย** — เทียบ "แถวที่เข้าเงื่อนไข" กับ "แถวที่ได้คืนมา"
และ **ปฏิเสธที่จะตอบถ้า `limit_k >= ef_search`** เพราะจะแยกจาก Q06 ไม่ออก

## I02 — คำเตือนของ pgvector ออกผิดที่ (2026-07-27)

4 เงื่อนไข · ข้อมูลปลายทางชุดเดียวกันเป๊ะ 100,000 แถว · เปลี่ยนแค่ **ตอนที่ build index**

| | build ตอนมี | กลุ่มที่เห็นตอน build | **pgvector เตือน** | recall |
|---|---|---|---|---|
| **A** | 50 แถว | 50 จาก 50 | **✅ เตือน** | **1.0000** |
| **B** | 1,000 แถว | 50 จาก 50 | ❌ เงียบ | 0.8798 |
| **C** | 100,000 แถว (วิธีที่เอกสารบอก) | 50 จาก 50 | ❌ เงียบ | 0.7682 |
| **D** | 1,000 แถว | **5 จาก 50** | ❌ **เงียบ** | **0.2278** |

**⭐ คำเตือนออกกับเงื่อนไขเดียวที่ได้ recall สมบูรณ์ และเงียบสนิทกับเงื่อนไขที่ผลหายไป 77%**

NOTICE ออกเมื่อ `rows < lists` เท่านั้น (วัดแล้ว: ออกที่ 50 ไม่ออกที่ 100)
และ `DETAIL` เขียนว่า *This will cause low recall* ซึ่ง **วัดแล้วเป็นเท็จบนชุดสังเคราะห์**

> 🔴 **บน embedding จริงข้อความนี้เป็นจริง** — A ได้ 0.4520 แย่เป็นอันดับสอง
> คำเตือนจึงออกถูกที่ · ดูหัวข้อ *"I02 บน embedding จริง"* ข้างบน

**ตัวแปรจริงไม่ใช่จำนวนแถว แต่คือความเป็นตัวแทน** — 50 แถวครบ 50 กลุ่ม
ดีกว่า 1,000 แถวจาก 5 กลุ่ม **20 เท่า** · เทียบเป็นคู่ในรอบเดียวกันไปทางเดียวกันทั้ง 5 รอบ

**ห้ามพูด:** "build เร็วดีกว่า" (A ได้ 1.0000 เพราะ corpus เรียง id สลับกลุ่ม
50 แถวแรกจึงครบ 50 กลุ่มพอดี — **คุณสมบัติของข้อมูลชุดนี้ ไม่ใช่กฎทั่วไป**) ·
"จำนวนแถวไม่สำคัญ" · "คำเตือนไร้ประโยชน์" (มันถูกในกรณีที่มันจับ แค่ไม่ใช่กรณีอันตราย)

**⚠️ แก้แล้ว 2026-07-28 — เดิมเทียบแต่ recall ไม่ได้วัดต้นทุน (ดู E36) · วัด 3 รอบแล้ว**

| corpus | | รอบ 1 | รอบ 2 | รอบ 3 |
|---|---|---|---|---|
| ชุดหลัก | A recall / buffers | 1.0000 / 433 | 1.0000 / 433 | 1.0000 / 433 |
| ชุดหลัก | C recall / buffers | 0.8285 / 364 | 0.8165 / 341 | 0.7880 / 345 |
| ยากขึ้น | A recall | 0.8615 | **0.8420** | 0.8365 |
| ยากขึ้น | C recall | 0.7655 | **0.8495** | 0.7850 |

**A อ่านมากกว่า C 1.19–1.27 เท่า บนชุดหลัก** (ไม่ใช่ 1.42 ที่เคยเขียนจากรอบเดียว)
**A นิ่งสนิททั้งสามรอบ · C แกว่งตาม I04**

🔴 **บน corpus ที่ยากขึ้น รอบที่ 2 C ชนะ A** — ทิศทางไม่นิ่ง ห้ามอ้างว่า A ชนะ

⚠️ buffers ของ A บน corpus ยากขึ้นไต่ 523→568→644 ทั้งที่ index คงที่ 313 MB
= **ลูปวัดสะสม dead tuple ข้ามรอบ** เป็นสิ่งประดิษฐ์ของวิธีวัด เทียบข้ามรอบไม่ได้

**ห้ามพูดเพิ่ม:** "build ตอนข้อมูลน้อยดีกว่า" แบบลอยๆ — ได้ผลเดียวกันจากการเพิ่ม `probes`
ซึ่งควบคุมได้และไม่ทำให้ index บวม · **สิ่งที่ยังยืนคือเรื่องคำเตือนออกผิดที่**

**ตัวตรวจอ่าน catalog ไม่ได้** — `reltuples` ของ index เก็บจำนวนแถวตอน build ไว้จริง
**แต่ `ANALYZE` เขียนทับทันที** จึงวัด recall สดๆ เทียบกับ `enable_indexscan = off` แทน
เกณฑ์ 0.50 มาจากช่องว่างที่วัดได้ (C ต่ำสุด 0.7160 · D สูงสุด 0.2580)

## 🔴 I02 บน embedding จริง — ข้อสรุปหนึ่งข้อถูกหักล้าง (2026-07-30)

**ข้อมูลปลายทางเหมือนกันทุกเงื่อนไข 100,000 แถว · IVFFlat lists=100 · 5 รอบ**

| | build ตอนมี · ตัวอย่าง | เตือน | สังเคราะห์ | **จริง** | ช่วง (จริง) |
|---|---|---|---|---|---|
| A | 50 · กระจาย | ✅ | **1.0000** | **0.4520** | **0.0000** |
| B | 1,000 · กระจาย | ❌ | 0.8798 | 0.5974 | 0.0495 |
| C | 100,000 (ตามเอกสาร) | ❌ | 0.7682 | **0.6653** | 0.0490 |
| D | 1,000 · **กระจุก** | ❌ | **0.2278** | **0.4151** | 0.0225 |

### ห้ามพูดอีก 3 ข้อ — จริงเฉพาะบน corpus สังเคราะห์

| เคยพูด | ความจริงบนข้อมูลจริง |
|---|---|
| "คำเตือนออกกับกรณีที่ดีที่สุด" | A **แย่เป็นอันดับสอง** → เตือน**ถูกที่** |
| "`This will cause low recall` เป็นเท็จ" | **เป็นจริง** |
| "build ตอนข้อมูลมาก ยิ่งแย่" | **กลับกัน** 0.45 → 0.60 → 0.67 |

**สาเหตุระบุได้ และ CLAUDE.md เตือนไว้เองตั้งแต่ต้น** — A ได้ 1.0000 เพราะ corpus
สังเคราะห์เรียง id สลับกลุ่ม 50 แถวแรกจึงครบ 50 กลุ่มพอดี · ตอนนี้พิสูจน์แล้ว

### ⭐ ข้อที่รอด — และเป็นข้อที่ควรใช้ต่อไป

**ความเป็นตัวแทนสำคัญกว่าจำนวนแถว** — คุมจำนวนแถวเท่ากันเป๊ะ 1,000

```
B กระจาย  0.5974  ·  query ที่ได้ 0 อัน  44/1000
D กระจุก  0.4151  ·  query ที่ได้ 0 อัน 104/1000
```

และ **A (50 กระจาย) ยังชนะ D (1,000 กระจุก)** ทั้งสองชุดข้อมูล
→ ช่องว่างของเอกสารที่ยังยืน: **ไม่มีคำเตือนสำหรับตัวอย่างที่ไม่เป็นตัวแทน**

### ⭐ ผลใหม่ที่ corpus สังเคราะห์มองไม่เห็น

**A นิ่งสนิท 100% (ช่วง 0.0000)** เพราะ 50 แถว < lists 100 → k-means ไม่มีอะไรให้สุ่ม
แต่ละแถวเป็นจุดศูนย์กลางของตัวเอง
→ **เงื่อนไขเดียวกับที่ pgvector เตือน คือเงื่อนไขเดียวที่ reproduce ได้ 100%**

ผลเต็ม: `results/real_i02.txt` · ตัวรัน: `sql/real_i02.sql`

---

## I04 — สร้าง index ใหม่แล้วได้คำตอบไม่เหมือนเดิม (2026-07-27)

**ข้อเดียวใน 12 ข้อที่เอกสารทางการยืนยันให้ไม่ได้** — เอกสาร pgvector ไม่รับประกันว่า
สร้าง index ใหม่แล้วได้ผลเหมือนเดิม **และไม่เตือนว่าจะไม่เหมือน** ต้องพิสูจน์ด้วยการรันล้วนๆ

5 build ต่อการตั้งค่า · ข้อมูลเดิมเป๊ะ · พารามิเตอร์เดิมเป๊ะ · 200 query · k=10

> 🔴 **ตารางนี้เป็นของ corpus สังเคราะห์** — บน embedding จริงตัวเลขต่างกันมาก
> `probes=5` เปลี่ยนคำตอบ **121/200** (ไม่ใช่ 0) · HNSW `ef=40` เปลี่ยนแค่ **6/200**
> (ไม่ใช่ 132) · ดูหัวข้อ *ทดสอบข้ามชุดข้อมูล* ข้างบน

| การตั้งค่า | recall เฉลี่ย ต่ำ–สูง | ช่วง | **query ที่เปลี่ยนคำตอบ** |
|---|---|---|---|
| **IVFFlat `probes=1`** | 0.7415 – 0.8145 | **9.4%** | **196 / 200** |
| **IVFFlat `probes=5`** | 1.0000 – 1.0000 | **0%** | **0 / 200** |
| HNSW `ef_search=40` | 0.8125 – 0.8515 | 4.6% | 132 / 200 |
| HNSW `ef_search=200` | 0.9545 – 0.9790 | 2.5% | 86 / 200 |

**⭐ ค่าเฉลี่ยซ่อนความจริงไว้เกือบทั้งหมด** — เฉลี่ยแกว่ง 9.4% ดูเหมือน noise ที่รับได้
แต่ราย query **196 จาก 200 ข้อได้คำตอบไม่เท่ากัน** · กลไกเดียวกับที่ Q01 พบว่า
"แย่สุดต่อ query = 0.0000" ซ่อนอยู่ใต้ค่าเฉลี่ย 0.8565

**รัน 3 รอบเต็ม ได้ 185 · 199 · 192 จาก 200** (ช่วงแกว่ง 0.0515 · 0.0885 · 0.0720)
ตัวเลขต่างกันทุกรอบ — **ซึ่งคือ fault ที่กำลังวัดอยู่พอดี** แต่ข้อสรุปเหมือนกันทุกรอบ

> 🔴 **"กลบได้หมดจด" จริงเฉพาะบน corpus สังเคราะห์** — บน embedding จริง
> `probes=5` ยังมี **121/200 (60.5%)** ที่เปลี่ยนคำตอบ · ช่วยลดจาก 91.5% แต่ไม่กำจัด

**⭐ `probes=5` กลบความไม่แน่นอนได้หมดจดบนชุดสังเคราะห์** — ไม่ใช่แค่เฉลี่ยนิ่ง แต่ไม่มี query ไหน
เปลี่ยนเลยสักข้อทั้ง 5 build · ค่าเดียวกับที่ Q04 พบว่าได้ recall เต็ม
→ **ค่าที่ต่ำเกินไม่ได้แค่ทำ recall ตก มันทำให้ผลไม่นิ่งด้วย**

### กลุ่มควบคุม 3 ชั้น — และมันหักล้างชื่อ fault เดิม

| | ผล |
|---|---|
| **ก** วัดซ้ำบน index ตัวเดิม | ตรงกันทุก query ทั้ง 4 การตั้งค่า → ฝั่ง query นิ่ง |
| **ข** HNSW (ไม่มี k-means) | **ก็แกว่ง 132/200** → ชื่อเดิมที่โทษ k-means แคบเกินไป |
| **ค** ปิด parallel build | **ยังแกว่งทั้งคู่** (IVFFlat 200/200 · HNSW 127/200) → ไม่ใช่ parallel |
| **ง** `setseed` ค่าเดิมทุก build | **ยังแกว่ง 101/200** → ไม่ใช่ PRNG ของ session (2026-07-31) |

**เปลี่ยนชื่อจาก "k-means ของ IVFFlat สุ่ม" แล้ว — ดู E32**

### 🔬 ทดสอบสมมติฐาน seed แล้ว — ตัดออกได้สาเหตุที่ 3 (2026-07-31)

**คำถาม:** อัลกอริทึม HNSW สุ่ม "ระดับชั้น" ให้แต่ละจุดตอน build
ถ้าตัวสุ่มนั้นผูกกับ PRNG ของ session การ `setseed` ค่าเดิมควรทำให้ build ซ้ำได้

```
A  setseed(0.42) ทุก build   ->  เปลี่ยนคำตอบ 101/200  (50.5%)
B  ไม่ setseed เลย (ค่าฐาน)   ->  เปลี่ยนคำตอบ 117/200  (58.5%)
```

**ถ้า PRNG ของ session เป็นกลไก A ต้องได้ 0 — ได้ 101 จึงตัดออกได้**

> 🔴 **ห้ามอ่านว่า "setseed ช่วยลดจาก 117 เหลือ 101"** — ค่าฐานเองแกว่งข้ามรอบ
> (I04 เดิมวัดได้ **132/200** · รอบนี้ได้ **117/200** เงื่อนไขเดียวกันเป๊ะ)
> ส่วนต่าง 16 จึงอยู่ในระดับเดียวกับที่ค่าฐานแกว่งเอง และมีเงื่อนไขละรอบเดียว
> **ที่พูดได้อย่างเดียวคือ `setseed` ไม่ได้ทำให้ผลนิ่ง**

### 🔴 อ่านซอร์สแล้ว — การทดลองนี้เล็งผิดตัว และกลไก**ทราบแล้ว** (E41)

```c
// pgvector src/hnsw.h
#define RandomDouble() pg_prng_double(&pg_global_prng_state)

// pgvector src/hnswutils.c — HnswInitElement
int level = (int) (-log(RandomDouble()) * ml);

// PostgreSQL src/backend/utils/adt/pseudorandomfuncs.c — random() กับ setseed()
static pg_prng_state prng_state;     // <- file-local คนละตัวกับ global
```

**`setseed()` คุม PRNG คนละตัวกับที่ HNSW ใช้ จึงไม่มีทางมีผลตั้งแต่แรก**

| | |
|---|---|
| **กลไกของ I04** | **ทราบแล้ว** — HNSW สุ่มระดับชั้นให้ทุกจุดตอน build (อัลกอริทึมมาตรฐาน) |
| **ที่การทดลองนี้ยืนยันได้จริง** | **ไม่มีวิธีทำให้ build ซ้ำได้จากฝั่ง SQL** เพราะ `pg_global_prng_state` ไม่มีอินเทอร์เฟซให้ตั้งค่า |

**กลุ่มควบคุม ก/ข/ค ไม่เสียเปล่า** — โดยเฉพาะกลุ่ม ข (HNSW ไม่มี k-means แต่ก็แกว่ง)
ซึ่ง**ซอร์สยืนยันให้ว่าถูกต้อง** HNSW มีการสุ่มของตัวเองแยกจาก k-means ของ IVFFlat

> 🔴 **บทเรียน** — กฎเหล็กข้อ 1 บอกให้ซอร์สเป็นคนตัดสิน รอบนี้ทำสลับลำดับ
> ออกแบบการทดลองก่อน แล้วค่อยอ่านซอร์ส ทั้งที่ซอร์สเปิดสาธารณะและตอบได้ใน 3 บรรทัด
> **ก่อนออกแบบการทดลองเพื่อหา "กลไก" ให้ค้นซอร์สก่อนเสมอ**

ไฟล์: `sql/i04_seed_probe.sql` · ผล: `results/i04_seed_probe.txt`

> ⚠️ **บั๊กที่เจอตอนรัน** — plpgsql ไม่ยอมให้ตัวแปรชนิด `record` อยู่ใน `INTO`
> ร่วมกับตัวแปรอื่น (`record variable cannot be part of multiple-item INTO list`)
> ทำให้ assertion ท้ายไฟล์ไม่รัน · ตรวจด้วยมือแล้วว่า `qf_corpus` fingerprint เดิม
> และไม่มี index ค้าง · แก้สคริปต์แล้ว

**ห้ามพูด:** "k-means คือต้นเหตุ" (HNSW ไม่มีแต่ก็แกว่ง) · "HNSW แกว่งเพราะ X"
(**กลไกทราบแล้วจากซอร์ส — HNSW สุ่มระดับชั้นตอน build** ดู E41) ·
"ปิด parallel ทำให้แย่ลง" (5 ตัวอย่างไม่พอ)

**ตัวตรวจออกแบบยากที่สุดในชุด** — ถามว่า "ใช้ IVFFlat อยู่ไหม" ไม่ได้ เพราะจะตอบ
`DETECTED` ตลอดกาล จึงเทียบ `relfilenode` ที่จดตอนวัด recall กับ index ตัวปัจจุบันแทน
(วัดแล้วว่าเปลี่ยนทุก build — 5 build = 5 ค่าไม่ซ้ำ) · **"ไม่เคยจด" = `CANNOT_CHECK`**

## Q02 — รูปแบบ query ที่ใช้ index ได้ / ไม่ได้ (2026-07-27)

index เดียวกัน · ไม่มี error สักรูปแบบ · buffers ต่างกัน 21 เท่า

| เขียนแบบ | index | ได้กี่แถว | buffers |
|---|---|---|---|
| `ORDER BY <=> ASC + LIMIT` | ✅ | 10 | **947** |
| `WHERE <=> < 0.5` อย่างเดียว | ❌ | 2,000 | 20,005 |
| **`WHERE` + `ORDER BY` + `LIMIT`** | **✅** | 10 | 947 |
| `ORDER BY <=> DESC` | ❌ | 10 | 20,005 |
| `ORDER BY (<=>) + 0` | ❌ | 10 | 20,005 |
| `ORDER BY` ไม่มี `LIMIT` | ❌ | 100,000 | 20,005 |

**`WHERE` ไม่ใช่ตัวปัญหา** — การขาด `ORDER BY` + `LIMIT` ต่างหาก
ชื่อ fault เดิม "เขียน WHERE แทน ORDER BY" ชี้เป้าคลาดไปนิด

**`+ 0` คือทริกเดียวกับที่ Q03 ต้องใช้บน PG17+** เพื่อให้ iterative scan ทำงาน
→ **ทางแก้ของ fault หนึ่ง คือวิธีฉีดของอีก fault หนึ่ง**

## V07 — แถวหายถาวรเพราะคุณภาพข้อมูล (2026-07-27)

500 แถวปกติ + 5 NULL + 5 zero = 510 แถว · `hnsw.ef_search=1000` (สูงเพื่อแยกจาก Q06)

| เส้นทาง | ขอ | ได้ | ปกติ | NULL | zero |
|---|---|---|---|---|---|
| ไม่มี index (exact) | 510 | **510** | 500 | 5 | 5 |
| **มี index แต่ปิด index scan** | 510 | **510** | 500 | 5 | 5 |
| ใช้ index | 510 | **500** | 500 | **0** | **0** |

**หายไป 10 แถวพอดี** · แถวกลางคือกลุ่มควบคุมสำคัญ — index มีอยู่แต่ปิด index scan
แล้วแถวกลับมาครบ → **เส้นทางที่ใช้ index ต่างหากที่ทำหาย ไม่ใช่ข้อมูลหาย**

```
'[0,0,0]'::vector <=> '[1,0,0]'::vector  ->  NaN    (ไม่ใช่ error)
NULL::vector      <=> '[1,0,0]'::vector  ->  NULL
```

**เป็นปัญหาคุณภาพข้อมูล ไม่ใช่การตั้งค่า** — ไม่มีพารามิเตอร์ตัวไหนแก้ได้
และตรวจง่ายที่สุดในชุด: `WHERE embedding IS NULL OR vector_norm(embedding) = 0`

## Q06 — หน้าผาที่ k = ef_search + 1 (2026-07-27)

`hnsw.ef_search = 40` (ค่าเริ่มต้น) · 100,000 แถว · **ไม่มี error สักกรณี**

| ขอ | ได้ | | ขอ | ได้ |
|---|---|---|---|---|
| 39 | 39 ครบ | | **41** | **40 ขาด 1** |
| 40 | 40 ครบ | | 100 | **40 ขาด 60** |

**เพดานเท่ากับ `ef_search` พอดีเป๊ะ** — `recall@100 = 0.3864` จึงไม่ใช่ "คุณภาพแย่"
แต่เป็น **40/100 = เพดานที่เป็นไปได้** ตั้งแต่แรก

> 🔴 **ตัวเลขข้างบนเป็นของ corpus สังเคราะห์** — บน embedding จริง**เพดานอยู่ที่ 41**
> (ขอ 50 · 100 · 1000 ได้ 41 เท่ากันหมด) หน้าผาจึงอยู่ที่ `k = ef_search + 2`
> ไม่ใช่ `+ 1` · ผลเต็ม `results/real_q06.txt`
>
> **กลไกยืนยันข้ามชุดข้อมูลแล้ว** — มีเพดานตายตัวที่ขอเยอะขึ้นไม่ช่วย
> **ตำแหน่งเพดานยกข้ามชุดข้อมูลไม่ได้** ตามเส้นแบ่งกลไก/ปริมาณ

**⚠️ แก้ Q06 ไม่ได้แก้ Q01 — ต้องแก้แยกกัน**

> ⭐ **หลักฐานที่ตรงกว่า วัดแล้ว (E44 · 2026-08-01)** — `iterative_scan`
> ที่ไม่เคยลอง ยกเพดานได้**สมบูรณ์** 40 → 100 ครบทั้ง 200 query จ่ายเพิ่มแค่ 6%
> **แต่ recall@100 ได้แค่ 0.7572** · `+ mult=32` ได้ 0.9397
> → จำนวนแถวกลับมาครบสนิท ขณะที่ 1 ใน 4 ของแถวเป็นของผิด
> **ตัวชี้วัดที่มองเห็นง่ายที่สุดคือตัวที่หลอกที่สุด** · `results/q06_iterative_probe.txt`

| `ef_search` | ขอ 100 ได้ | recall@100 | ครบ 100/100 |
|---|---|---|---|
| 40 | **40 แถว** | 0.3864 | 0/200 |
| 100 | 100 แถว | **0.9057** | 0/200 |
| 200 | 100 แถว | 0.9658 | 27/200 |

ตั้ง `ef_search = LIMIT` ได้แถวครบ แต่ **90.6% เท่านั้นที่ถูก** และแม้ที่ 200
ยังมี query ที่ได้ถูก **1 จาก 100**

## เฟส 2.5 — Q01 บน Qdrant (2026-07-27)

100,000 แถวชุดเดียวกัน · เฉลยจาก exact search ของ PostgreSQL · สูตรเดียวกัน

| engine (สองแถวแรก ef เท่ากัน) | recall@10 | recall@100 |
|---|---|---|
| pgvector HNSW `ef_search=40` | 0.8565 | **0.3903** |
| Qdrant HNSW `hnsw_ef=40` | 0.9945 | **0.9895** |
| Qdrant ค่าเริ่มต้นของตัวเอง | 1.0000 | 0.9996 |

**ตัวสอบทาน:** Qdrant `exact` = 1.0000 เทียบกับเฉลยของ PostgreSQL → เทียบกันได้จริง

**พูดได้:** recall loss มีจริงทั้งสอง engine (Qdrant แย่สุดต่อ query = 0.90 · ที่ k=100 ครบแค่ 69/200)
→ **เป็นธรรมชาติของ ANN ไม่ใช่บั๊กของ pgvector**

**ห้ามพูด:** "ANN ทุกตัวพังเท่ากัน" — ขนาดต่างกันมหาศาล
pgvector recall@100 = 0.39 เพราะ `ef_search < LIMIT` (กลไกของ Q06)
ส่วน Qdrant ที่ ef เท่ากันยังได้ 0.99 = **ไม่ปล่อยให้ ef ต่ำกว่า limit มาตัดคุณภาพ**

**Qdrant ค่าเริ่มต้นล้วนๆ ไม่สร้าง HNSW เลยที่ขนาดนี้** (`indexed=0` · `status: green`) — ดู E24

**ชั้น 3 (จับคู่เอกสาร) เสร็จแล้ว** — ตารางเต็มใน `EVIDENCE.md`
ผลสำคัญ: `ef_search=40` คงที่ของ pgvector **แปลกแยกในกลุ่ม** —
Milvus ตั้ง `ef = limit` · Weaviate ปรับอัตโนมัติ 100–500 · OpenSearch 100 และ lucene บังคับ `= k`
→ **Q06 เป็นของ pgvector เฉพาะตัว** เพราะเจ้าอื่นป้องกันไว้ให้แล้ว
(⚠️ **ผ่อนแล้ว E44** — pgvector มี `iterative_scan` ตั้งแต่ 0.8.0 ที่ยกเพดานได้จริง
สิ่งที่ต่างคือ **ปริยายเป็นปิด** ไม่ใช่ "ไม่มีกลไก")
แต่ `probes=1` เป็นปัญหาร่วม — FAISS (`size_t nprobe = 1;`) และ OpenSearch ก็ตั้ง 1

---

## ฐานวัด recall ที่ล็อกแล้ว (2026-07-26)

```
ชุด query   : 200 ข้อ · salt quietfail-v1 · fingerprint 607babfb6344eab74d3e76496b04fa9f
corpus      : 100,000 แถว · 384 มิติ · 50 กลุ่ม · noise 0.04
โครงสร้าง    : ในกลุ่ม 0.3820 · ข้ามกลุ่ม 0.9997  (ห่าง 2.6 เท่า)
ค่าฐาน       : ไม่มี index → recall@10 = recall@100 = 1.0000 ทั้ง 200 query
```

**ก่อนเชื่อ recall ใดๆ ต้องเช็ค 2 อย่าง** — fingerprint ตรง และระยะในกลุ่ม/ข้ามกลุ่มยังห่างกัน
เคยพลาดทั้งสองแบบมาแล้ว ดู **E11** (ข้อมูลซ้ำกันหมด) และ **E12** (กลุ่มไม่มีอยู่จริง)

รันทั้งชุด: `qf10` → `qf11` → `qf12 -v rows=100000` → `qf13`

## กฎการรันซ้ำ (ย้ายมาจาก `PROJECT.md` ข้อ 7 · ดู D18)

- สร้าง index **5 รอบ** ต่อการตั้งค่าหนึ่งชุด — **จำเป็นเพราะ I04** และ I04 พิสูจน์แล้วว่า
  ทำไม: 5 build ได้คำตอบไม่เหมือนกัน 185–199 จาก 200 query
- query **3 รอบ** ต่อ index หนึ่งตัว
- รายงานเป็น **ค่าเฉลี่ย ± ช่วง** ไม่ใช่เลขเดี่ยว
- **และรายงานราย query ด้วยเมื่อเกี่ยวกับ recall** — I04 แสดงว่าค่าเฉลี่ยนิ่งได้
  ทั้งที่ query เกือบทุกข้อเปลี่ยนคำตอบ (ข้อนี้เพิ่มใหม่จากผลของ I04)
- รันทั้งสอง config profile (fragile / realistic)

**ขนาดข้อมูล: 100k และ 500k** — เกณฑ์ผ่านเฟส 2 คือ **2 ระดับ** (`PROJECT.md` ข้อ 9)
เดิมข้อ 7 สั่งไว้ 4 ขนาด (100k/500k/2M/5M) ซึ่งมีไว้รองรับ **L01 ที่ถูกตัดไปแล้ว** — ยกเลิกแล้ว

---

## ชุด fault ที่ล็อกแล้ว — ห้ามเพิ่มโดยไม่ตัด (D15)

```
I01 I02 I03 I04 I05   Q01 Q02 Q03 Q04 Q06   V07 L02      = 12
```

ตัดออกแล้ว: **Q05, L01, V08** · **ช่องว่างของเลข ID ตั้งใจ ห้ามเรียงใหม่ ห้ามใช้เลขซ้ำ**
เพราะ `results/` และ `DECISIONS.md` อ้างถึง ID เดิม

core อีก 4 ข้อ: **F01 · F03 (+F03b) · F05 · F10** = รวม 16

**`FAULTS.md` คือแหล่งอ้างอิงเดียว** ถ้า `PROJECT.md` หรือ `EVIDENCE.md` ขัดกับมัน = `FAULTS.md` ถูก
(เดิมรายการอยู่สามที่แล้วเคลื่อนออกจากกัน — ดู D15)

---

## สภาพ DB ที่โหลดไว้ตอนนี้ (ตรวจล่าสุด 2026-07-31)

```
ขนาดรวม   411 MB · ไม่มีตารางไหน dead tuple เกิน 1,000 · VACUUM ANALYZE แล้ว

qf_corpus  100,000 แถว · 384 มิติ · fingerprint corpus 5a32cba54ea2be4ed022fe8bfedae9b0
qf_truth   400 แถว (200 query x k=10,100) — เฉลยพร้อมใช้
qf_real    100,000 แถว · embedding จริงจาก BEIR/Quora
orders     200,000 แถว (seed แก้แล้ว ดู E17)
vector index: ไม่มีค้าง
maintenance_work_mem: 64MB (ค่าโปรไฟล์ fragile)
```

**สองตารางกินพื้นที่ 344 MB จาก 411 MB และห้ามลบทั้งคู่**

| ตาราง | ขนาด | ทำไมห้ามลบ |
|---|---|---|
| `qf_real` | 185 MB | สร้างใหม่ต้องฝัง embedding ใหม่ (~10 นาที + ต้องมีโมเดล) |
| `qf_corpus` | 159 MB | **ฐานที่ล็อกไว้** — fingerprint ผูกกับผลทุกตัวในเล่ม |

ที่เหลือเป็นตารางบันทึกผลขนาด kB (`*_obs` `*_meta` `*_sweep` `*_ans`)
ซึ่ง `score.sql` อ่านบางตัว — **ไม่ต้องเก็บกวาด**

ถ้าสภาพไม่ตรงนี้ ให้รัน `qf10` → `qf11` → `qf12 -v rows=100000` → `qf13` ใหม่

### ✅ ตรวจสภาพ DB แล้ว 2026-07-31 — แทบไม่มีอะไรต้องเก็บกวาด

ตารางใหญ่ที่เคยค้าง (`qf_i02b` 477MB · `qf_l02` 396MB · `qf_i02` · `qf_q03` ·
`qf_i02r` · `qf_q03r`) **ถูกลบไปหมดแล้ว**ตั้งแต่รอบเก็บกวาดก่อนหน้า

ลบเพิ่มรอบนี้: **`qf_seed_guard` ตัวเดียว** — ตารางกันชนที่ค้างเพราะ
`i04_seed_probe.sql` error กลางคัน (บั๊ก plpgsql `INTO` ดูหัวข้อ I04 ข้างบน)

**ยืนยันหลังเก็บกวาดแล้วว่า `score.sql` ให้ verdict ตรงกับสภาพสะอาดที่บันทึกไว้เป๊ะ**

```
DETECTED 3 (I05 · Q06 · V07) · NOT_DETECTED 3 · CANNOT_CHECK 9
```

### ✅ เก็บกวาด DB แล้ว 2026-07-30 — เหลือ 350 MB จาก 2,019 MB

```bash
MSYS_NO_PATHCONV=1 docker compose exec -T db \
  psql -U lab -d faultlab -v ON_ERROR_STOP=1 -f //sql/cleanup_scratch.sql
```

**ลบตารางทำงานขนาดใหญ่ที่สร้างใหม่ได้ทั้งหมด** — คืนพื้นที่ **1.67 GB**

| ลบไปแล้ว | ขนาดเดิม | สร้างใหม่ด้วย |
|---|---|---|
| `qf_i02b` · `qf_i02b_truth` | 477 MB | `sql/i02b_harder_corpus.sql` (~15 นาที) |
| `qf_l02` | 396 MB | `sql/l02_dead_tuples.sql` |
| `qf_i02` | 159 MB | `sql/i02_index_built_too_early.sql` |
| `qf_q03` | 159 MB | `sql/q03_filter_after_scan.sql` |
| `qf_i02r` · `qf_l02r` · `qf_q03r` | 478 MB | `sql/real_{i02,l02,q03}.sql` (เร็ว ใช้ `qf_real`) |

**ที่เก็บไว้และเหตุผล**

| เก็บไว้ | ทำไม |
|---|---|
| `qf_corpus` · `qf_queries` · `qf_truth` · `qf_manifest` · `qf_centroids` | **ฐานที่ล็อกไว้ ห้ามแตะ** |
| `qf_real` · `qf_real_q` · `qf_real_truth` | สร้างใหม่ต้องฝัง embedding ใหม่ (~10 นาที + ต้องมีโมเดล) |
| `qf_v07` · `qf_v07r` | **ถ้าลบ verdict ของ V07 จะเปลี่ยน** ไม่ตรงกับสภาพสะอาดที่บันทึกไว้ |
| ตารางบันทึกผลทั้งหมด (`*_obs` `*_meta` `*_results` ฯลฯ) | ขนาดหลัก kB · `score.sql` อ่านบางตัว |

**ตัวสคริปต์มี guard** — ตรวจ fingerprint ของ `qf_corpus` ทั้งก่อนและหลัง
ถ้าเปลี่ยนจะ `RAISE EXCEPTION` ทันที · **ยืนยันแล้วว่า verdict ของ `score.sql`
ไม่เปลี่ยนสักข้อ** หลังเก็บกวาด (เปลี่ยนแค่ข้อความอธิบายของ I02 · L02 · Q03
จาก "ไม่มี vector index" เป็น "หาตารางไม่เจอ" ซึ่งยังเป็น `CANNOT_CHECK` เหมือนเดิม)

> 📌 **ข้อสังเกตจาก E36 ที่ยังใช้ได้** — `qf_i02b` เคยบวมเป็น 477 MB สำหรับข้อมูล
> ที่ควรใช้ราว 160 MB เพราะลูปวัดทำ `DELETE` แล้ว `INSERT` กลับหลายรอบโดยไม่ `VACUUM`
> **เป็นสิ่งประดิษฐ์ของวิธีวัด ไม่ใช่ผลการทดลอง** ถ้าสร้างใหม่แล้วจะวัดขนาด
> ต้อง `VACUUM FULL` ก่อนเสมอ

### ผลของ `score.sql` บนสภาพสะอาด — จำค่านี้ไว้ (audit 2026-07-27)

**สองข้อนี้ตอบ `DETECTED` เสมอ ทั้งที่ยังไม่ได้ฉีดอะไรเลย — ไม่ใช่ fault ค้าง**

```
I05  DETECTED  — mwm 64MB รับได้ 28,352 tuples แต่ corpus มี 100,000
Q06  DETECTED  — ef_search 40 แต่โค้ดมี LIMIT ถึง 100
```

ทั้งคู่เป็น**ตัวทำนายจากค่าตั้งค่า** จึงเตือนถูกต้องแล้วตั้งแต่ยังไม่ฉีด

**Q02 ขยับได้ ห้ามใช้เป็นค่าอ้างอิง** — มันอ่าน `pg_stat_statements` ซึ่งเปลี่ยนตามประวัติ
การรัน (เคยเห็นทั้ง `DETECTED` และ `NOT_DETECTED` บนสภาพสะอาดเดียวกัน)
ล้างด้วย `SELECT pg_stat_statements_reset();` ถ้าต้องการฐานสะอาดจริง

**ยอดรวมที่พบบ่อย** (15 ข้อ) — ตัวเลขนี้เป็นข้อมูล ไม่ใช่เกณฑ์:

```
DETECTED 2-3 · NOT_DETECTED 3-4 · CANNOT_CHECK 9
```

`CANNOT_CHECK` 9 ข้อคือข้อที่ตัวฉีดยังไม่ได้รัน (ไม่มีตารางสังเกตการณ์ / ไม่มี index)
— **ถูกต้องแล้วบนสภาพสะอาด ไม่ใช่ความผิดพลาด**

**ก่อนสรุปว่ามี fault ค้าง ให้เทียบกับสองข้อข้างบนก่อนเสมอ**

---

## วิธีทำ fault หนึ่งข้อ — สูตรที่ใช้ได้ผลแล้ว 7 ข้อ

> **ตอนนี้เป็นเอกสารอ้างอิง ไม่ใช่งานที่ค้าง** — ชุด 12 ข้อครบแล้ว
> จะเพิ่ม fault ใหม่ได้ **ต้องตัดของเดิมออกหนึ่งข้อ** (กฎเหล็กข้อ 4 · D15)
> อ่านหัวข้อนี้เมื่อ **เจ้าของโปรเจคสั่งเปลี่ยนชุด** เท่านั้น

1. **เขียนนิยามลง `FAULTS.md` ให้ครบ 5 อย่างก่อน** (อาการ · ต้นเหตุ · วิธีฉีด · ground truth · assertion)
   ตารางดัชนีชั้น I/Q/V/L **ไม่ใช่นิยาม** — ทุกข้อที่ทำมาต้องเขียนนิยามเพิ่มก่อนเสมอ
2. เขียนตัวฉีด — `.sh` ถ้าต้องหลาย session · `.sql` ถ้าอยู่ใน DB ล้วน
3. `groundtruth/<id>.json` — ต้องมี `evidence_expectation` และ `verified_on.date`
   ไม่งั้น `score.sql` จะตอบ `CANNOT_CHECK`
4. เพิ่มตัวตรวจใน `sql/score.sql`
5. **รัน 3 รอบติด** + เขียนผลลง `results/<id>_phase3_cycle.txt` (ต้อง `git add -f`)
6. **พิสูจน์ว่าตัวนับคะแนนพลิกได้** — ต้องเห็นทั้งสถานะที่เจอและไม่เจอ
   ตัวนับที่ไม่มีวันตอบลบ = ไม่ได้วัดอะไรเลย

   **ต้องมีไฟล์ `sql/<id>_checker_states.sql` + ผลใน `results/` เป็นหลักฐาน**
   assertion ผ่าน 3 รอบ **ไม่นับ** ว่าทำข้อ 6 แล้ว — คนละเรื่องกัน
   assertion ถามว่า "fault เกิดจริงไหม" · ข้อ 6 ถามว่า "**ตัวตรวจ**ใช้ได้ไหม"

   > I05 เคยผ่านครบทุกอย่างยกเว้นข้อนี้ แล้วไม่มีใครสังเกตจน audit — ดู **E30**
   > ผลรัน 102 บรรทัดของมันไม่มีคำว่า `DETECTED` เลยสักครั้ง

7. **เพิ่มชื่อ fault ลง `scripts/audit_baseline.json` แล้วรัน `python scripts/audit.py`**
   ต้องได้ `exit 0` ก่อน commit — ตัวตรวจจะจับให้เองว่าลืมอะไร
   (ไฟล์ผลไม่ได้ `git add -f` · groundtruth ขาดช่อง · ไม่มีหลักฐานว่าตัวตรวจพลิกได้)

   ย้าย `<ID>` จาก `faults_remaining` ไป `faults_done` และเติม `checker_states_file_required`
   ถ้าลืมย้าย ตัวตรวจจะบอกว่า CLAUDE.md ไม่ตรงกับ baseline ทันที

**หลักฐานของแต่ละข้อเป็นคนละชนิด — ต้องถามก่อนออกแบบการวัด**

| ชนิดหลักฐาน | ตัวอย่าง | วัดเมื่อไหร่ |
|---|---|---|
| สถานะสดของ DB | F01 `pg_stat_activity` · F05 `pg_locks` | ระหว่าง fault ค้าง → ต้องมี `HOLD_OPEN` |
| ตารางที่บันทึกไว้ | F03 `qf_f03_obs` | หลังตัวฉีดจบ → ใช้ `KEEP_OBS=1` **ห้ามใช้ HOLD_OPEN** |
| catalog ล้วน | I01 opclass · I05 ความจุ · Q06 GUC | เมื่อไหร่ก็ได้ ไม่ต้องมีข้อมูลด้วยซ้ำ |
| นับแถวในตาราง | V07 NULL/zero vector | เมื่อไหร่ก็ได้ · ตรวจง่ายที่สุดในชุด |
| **query ที่เคยรัน** | Q02 `pg_stat_statements` | เมื่อไหร่ก็ได้หลังแอปเคยยิง query · ไม่ต้องมีซอร์สโค้ด |

ลอกโครงสร้างข้ามชนิดกัน = วัดผิดจังหวะแน่นอน (ดู E21)

**lock ที่แต่ละคำสั่งถือ — วัดเองแล้ว ใช้ออกแบบ fault ชั้น I ที่เหลือได้**

| คำสั่ง | lock | บล็อกอะไร | fault |
|---|---|---|---|
| `ALTER TABLE` | `AccessExclusiveLock` | **ทุกอย่าง** รวมการอ่าน | F05 |
| `CREATE INDEX` | `ShareLock` | **เฉพาะการเขียน** อ่านได้ปกติ | I03 |
| `CREATE INDEX CONCURRENTLY` | `ShareUpdateExclusiveLock` | ไม่บล็อกทั้งอ่านและเขียน | ทางแก้ของ I03 |

**เขียนทดสอบต้อง `ROLLBACK` เสมอ** ไม่งั้น `qf_corpus` เปลี่ยนจำนวนแถว
แล้ว fingerprint กับเฉลยใช้ไม่ได้ — สคริปต์ต้องตรวจจำนวนแถวก่อน/หลัง

---

## กับดักที่เสียเวลาไปแล้ว — ห้ามเหยียบซ้ำ

**1. ห้ามใส่ `2>/dev/null` รอบคำสั่งที่เปลี่ยนสภาพระบบ**
`CREATE INDEX` / `INSERT` / `ALTER` — กลบไปแล้ว **3 ครั้งในวันเดียว** (E26)
ครั้งหนึ่งทำให้ผลของตัวตรวจผิดทั้ง 12 ช่องโดยไม่มีอะไรบอก
และ I05 มี ground truth อยู่ใน stderr ล้วน — กลบ = ทำลายการทดลอง

**2. 🔴 ห้ามฝัง SQL ที่มี quote ใดๆ ใน `bash -c`** → ย้ายไปไฟล์ `.sql` แล้วเรียกด้วย `psql -f`
**ละเมิดไปแล้ว 5 ครั้ง** (E19 ตัวเฝ้าดู F03 · E26 harness I01 · I03 probe · H20 Q06 · H22 V07)

`'` `'` `E'...'` ใน heredoc **ไม่ถูกตีความ** — คำสั่งพังแต่ script เดินต่อ
ผลคือ `SET` / `DELETE` ไม่ทำงาน แล้วตัวตรวจรายงานผลของสภาพที่ไม่ได้เปลี่ยน

**เกณฑ์ตัดสินใจง่ายๆ:** ถ้า SQL มี `'` แม้แต่ตัวเดียว → เขียนเป็นไฟล์
อย่าพยายาม escape ให้ฉลาด มันไม่คุ้มและพลาดทุกครั้ง

**3. ฆ่า process ฝั่ง client ไม่ได้หยุดงานฝั่ง server**
ต้องหยุดที่ฝั่ง server เสมอ — `DO` block เคยวิ่งต่อ 20 นาทีหลังฆ่า client
แล้วบล็อก `TRUNCATE` จนงานถัดไปได้ corpus ว่าง (E16, E23)

**3ก. 🔴 แต่ห้ามฆ่า parallel worker เด็ดขาด**

> 🔴 **กฎนี้เคยถูกละเมิดอยู่เงียบๆ ในไฟล์เดียว — เจอตอนทวน `faults/` 2026-08-01**
> `f05_lock_queue.sh` ใช้ `pg_terminate_backend` และ**ไม่มีตัวกรอง `backend_type`**
> ขณะที่ `i03_*.sh` แก้ไปตั้งแต่ E29 แล้ว · **ไม่มีใครย้อนกลับมาดูไฟล์อื่น**
> ตรงกับกับดักข้อ 9 พอดี
>
> ความเสี่ยงจริงต่ำเพราะ `ALTER TABLE ADD COLUMN` ไม่ใช้ parallel worker
> **แต่ `pg_stat_activity.query` ของ worker เหมือน leader ทุกตัวอักษร**
> การกรองด้วย `query` อย่างเดียวจึงไม่ปลอดภัยโดยตัวมันเอง
> แก้แล้ว (cancel ก่อน · terminate ทีหลัง · กรอง `backend_type` ทั้งสองขั้น)
> และรัน F05 ซ้ำยืนยันว่ายังจับได้และเก็บกวาดสะอาด

ต้องเล็งเฉพาะ leader (`backend_type = 'client backend'`)
และใช้ **`pg_cancel_backend()`** ไม่ใช่ `pg_terminate_backend()`
ฆ่า worker → leader ตาย exit 2 → **postmaster รีเซ็ตทั้งคลัสเตอร์** (E29)
คำสั่งถัดไปจะล้มด้วย `system is in recovery` ซึ่งไม่ชี้กลับมาที่ต้นเหตุเลย

**4. รอบที่ตายกลางคันทิ้ง state ค้าง** → ใส่ `DROP INDEX IF EXISTS` / `DROP TABLE IF EXISTS`
ที่**ต้น**สคริปต์เสมอ ไม่งั้นรอบถัดไปตายด้วย error ที่ชี้ไปคนละเรื่อง

**5. `pg_stat_activity` ถูก snapshot ครั้งเดียวต่อ transaction**
ลูปเฝ้าระวังใน `DO` block ต้องเรียก `pg_stat_clear_snapshot()` ทุกรอบ
ไม่งั้นเห็นภาพเดิมซ้ำแล้วรายงานว่า "ปกติดี" ตลอดกาล (E19)

**6. ตัวตรวจห้าม match ข้อความใน output** — ให้อ่านโครงสร้าง (catalog / plan node ที่ระบุชื่อตาราง)
ค้นคำ `Index Scan` ทั้ง plan เคยไปโดน pkey ของตารางอื่นใน subquery (E25)

**6ก. อ่าน buffers ต้องรวม `hit` + `read` เสมอ**
Seq Scan บนตารางที่ vector ถูก TOAST จะได้ `hit` น้อยมากแต่ `read` เยอะมาก
นับแค่ `hit` เคยได้ 12 ทั้งที่ความจริงอ่าน 20,007 block
แล้วสรุปกลับหัวว่า "ไม่ใช้ index อ่านน้อยกว่า" — ใช้ JSON `Shared Hit Blocks + Shared Read Blocks`

**7. `current_setting()` คืนหน่วยตามที่ SET** (`'64MB'`) ไม่ normalize เป็น kB แบบ `pg_settings`
ใช้ `pg_size_bytes()` แปลงก่อนเทียบ

**8. IVFFlat build ชน `temp_file_limit` 64MB ของ fragile** → `SET temp_file_limit='2GB'` เฉพาะตอน build
และ **`maintenance_work_mem` สูงเกิน `/dev/shm` 256MB ทำให้ build ล้มเหลว** พร้อม error ที่ชี้ไปที่ดิสก์ (E28)

**9. เจอบั๊กรูปแบบหนึ่งแล้ว ต้องไล่หาที่อื่นที่ใช้รูปแบบเดียวกันทันที**
E11 เจอ InitPlan hoisting ใน corpus แต่ไม่ได้ย้อนไปดู `seed.sql` → E17 ซ่อนอยู่อีก 1 วัน

**12. 🔴 ห้ามสรุปว่าพารามิเตอร์ "ไม่มีผล" ถ้ายังไม่ได้พิสูจน์ว่ามันคือตัวที่ผูกอยู่**

**เกิดซ้ำ 4 ครั้ง** (E42 · E43 ×2 · E44) — ทุกครั้งคือวัดใต้เงื่อนไขที่มีตัวอื่น
ผูกอยู่ก่อน แล้วอ่านผลว่าตัวที่ปรับไม่มีผล · ตัวที่ผูกอยู่คือ**หน่วยความจำ**ทุกครั้ง

| | ปุ่มที่ไม่ได้หมุน / หมุนไม่พอ | สรุปผิดว่า |
|---|---|---|
| E42 | `scan_mem_multiplier` เกิน 8 | "เอกสารไม่ครบ" |
| E43 | `max_scan_tuples` (mem ผูกอยู่) | "ปุ่มไม่ทำงาน" |
| E43 | `iterative_scan` กับ **L02** | "`VACUUM` เท่านั้น" |
| E44 | `iterative_scan` กับ **Q06** | "เพิ่ม `ef_search` เท่านั้น" |

**วิธีที่ถูก** — ทำให้ตัวอื่นเหลือเฟือก่อน แล้วค่อยไล่ตัวที่สนใจ
ถ้าไล่แล้วยังไม่ขยับ **จึงจะ**พูดได้ว่าไม่มีผล

**และก่อนเขียนว่า "ทางแก้มีเท่านี้" ต้องไล่รายการปุ่มในเอกสารให้ครบก่อน**
L02 กับ Q06 ไม่ได้วัดผิด — **ไม่ได้วัดเลย**

🔴 **การวัดซ้ำหลายชุดข้อมูลไม่ช่วยเรื่องนี้** — Q03 วัด 2 ชุดได้ผลตรงกัน
แล้วเขียนว่า "ข้อค้นพบนี้แข็งขึ้น" ทั้งที่ทั้งสองชุดวัดใต้เงื่อนไขที่ผิดเหมือนกัน
**การทำซ้ำยืนยันความสม่ำเสมอ ไม่ได้ยืนยันความถูกต้อง**

**12ก. ตัวอย่างเดิม — ห้ามสรุปว่า "ไม่มีผล" ถ้ายังไม่ได้ไล่จนถึงค่าที่มันมีผล**
Q03 ไล่ `scan_mem_multiplier` ถึงแค่ 8 (ได้ 17/40) แล้วสรุปว่าปุ่มไม่ทำงาน
และกล่าวหาว่าเอกสารไม่ครบ · **ที่ 32 ได้ครบ 40/40** (E42)
ต้องไล่จนมันมีผล หรือจนชนขีดจำกัดของระบบ · หยุดกลางทาง = **ไม่ได้วัด**
และ **ห้ามอ่านพารามิเตอร์แยกจากกันโดยไม่ดูนิยาม** — `scan_mem_multiplier`
เป็นตัวคูณของ `work_mem` ทั้งคู่จึงเป็นปุ่มเดียวกัน ไม่ใช่คนละเรื่อง

**13. 🔴 `git checkout -- <ไฟล์>` ลบงานที่ยังไม่ commit ทิ้ง**
`scripts/audit_numbers_states.py` เคยใช้คำสั่งนี้คืนไฟล์หลังแก้ชั่วคราว
**รันตอนกำลังแก้เล่มอยู่ แล้วงานหายทั้งหมด** (เกิดจริง 2026-08-01)
ตอนนี้สคริปต์เก็บเนื้อไฟล์ในหน่วยความจำแล้วเขียนคืน + มีด่านกัน dirty tree
**ก่อนรันสคริปต์ที่แก้ไฟล์เอกสาร ต้อง commit งานค้างก่อนเสมอ**

**14ก. ค่าที่ calibrate จากจุดเดียว จะคลาดทันทีที่ออกนอกจุดนั้น**
`quietfail_check.py` ใช้ `443 tuples/MB` ที่วัดจาก `mwm=64MB` จุดเดียว
แล้วเอาไปคูณกับ `mwm` ค่าใดก็ได้ · **ผิดทาง false positive** เพราะงบจริงคือ
`mwm − 4MiB` ส่วนที่หักคงที่ ค่าต่อ MB จึงไม่คงที่
**ถ้าจะ extrapolate ต้องมีสูตร ไม่ใช่ค่าคงที่จากจุดเดียว** (แก้แล้ว 2026-08-01)

**14. สคริปต์ตรวจสอบก็ผุได้ ถ้า hardcode ตัวเลขไว้**
สคริปต์เดียวกันนั้นเขียนค่าที่คาดหวังไว้ตรงๆ ในโค้ด (จำนวนหัวข้อ · จำนวนไฟล์ ·
เลข H ตัวสุดท้าย) แล้วผุเงียบเมื่อค่าจริงขยับ จนรันไม่ผ่านเพราะหาข้อความไม่เจอ
— **บั๊กชนิดเดียวกับที่มันมีไว้ตรวจ** · ตอนนี้ค้นเลขปัจจุบันจากเอกสารเองแล้วบวกหนึ่ง

> ⚠️ **และห้ามยกตัวเลขเก่ามาเป็นตัวอย่างในเอกสารด้วย** — หมวด 6 ของ audit
> อ่านเป็นข้ออ้างปัจจุบันแล้วฟ้องทันที (เกิดกับย่อหน้านี้เองตอนเขียนครั้งแรก)

**11. reproduce ปัญหาบนสภาพแวดล้อมที่ผ่อนปรนกว่าของจริง = ได้ผลบวกลวง**
CI ตกเพราะไม่มี `pg_stat_statements` preload แต่ container ของ repo มี → แก้รอบแรกไม่หาย (H31)
**ถ้าจะจำลองปัญหาของ CI ต้องใช้ image เปล่า** `docker run pgvector/pgvector:0.8.5-pg17`
ห้ามใช้ `docker compose` ของ repo เพราะมัน config ไว้ใจดีกว่าเป้าหมาย

**10. Git Bash แปลง path ที่ขึ้นต้นด้วย `/` ให้เป็น path ของ Windows โดยอัตโนมัติ**
`psql -f /sql/score.sql` ผ่าน Bash จะกลายเป็น `C:/Program Files/Git/sql/score.sql`
แล้วตอบ `No such file or directory` — เหมือนไฟล์หาย ทั้งที่ไฟล์อยู่ครบ

```bash
MSYS_NO_PATHCONV=1 docker compose exec -T db psql -U lab -d faultlab -f //sql/score.sql
```

ต้องใส่ **ทั้งสองอย่าง** — `MSYS_NO_PATHCONV=1` และ `//` นำหน้า
ถ้าใช้ PowerShell ตามที่ `WINDOWS.md` เขียนไว้ จะไม่เจอปัญหานี้เลย

---

## โปรเจคนี้คืออะไร (30 วินาที)

**ชื่อ: QuietFail**

ใส่ index ให้ vector search แล้วเร็วขึ้นหลายสิบเท่า แต่ผลลัพธ์ที่ควรเจอหายไปส่วนหนึ่ง
**โดยไม่มี error ใดๆ แจ้ง** — QuietFail วัดว่าหายไปเท่าไหร่ และเตือนก่อนขึ้น production

วิธีการ: **fault injection** — จงใจสร้างความล้มเหลวขึ้นมาเอง จึงรู้เฉลย 100%

บริบท: โปรเจคจบของนิสิตปริญญาตรี ใช้สมัครงานสาย data/backend ในไทย

---

## ผู้ใช้และสภาพแวดล้อม

- นิสิตวิศวะไทย ใช้ภาษาไทย ประสบการณ์ระบบยังไม่มาก
- ต้องการคำอธิบายเป็นขั้นตอน **แต่ไม่ต้องการให้ลดความยากของเนื้อหา**
- **Windows 11 + PowerShell 5.1 — ไม่มี `make`** ใช้คำสั่ง docker ตรงๆ (ดู `WINDOWS.md`)
- โฟลเดอร์: `C:\dev\pg-fault-lab` (ห้ามย้ายเข้า OneDrive หรือ path ภาษาไทย)
- เขียนไฟล์จาก PowerShell ต้องใส่ `-Encoding utf8` เสมอ ไม่งั้นได้ UTF-16

---

## แผนที่ไฟล์

| ไฟล์ | เนื้อหา | อ่านเมื่อ |
|---|---|---|
| `CLAUDE.md` | ไฟล์นี้ — สถานะ + กฎ | ทุกครั้ง |
| **`FAULTS.md`** | **ทะเบียน fault — นิยาม อาการ วิธีฉีด ground truth assertion ครบทุกข้อ** · **แหล่งอ้างอิงเดียว** | **ก่อนแตะ fault ข้อไหนก็ตาม** |
| `DECISIONS.md` | ตัดสินใจอะไรไปแล้ว **ปฏิเสธอะไรเพราะอะไร** + ความผิดพลาดที่เกิดจริง | ก่อนเสนอทางเลือกใหม่ |
| `EVIDENCE.md` | หลักฐานอ้างอิงเอกสารทางการต่อ fault ทีละข้อ | ก่อนเขียนตัวฉีด fault |
| `PROJECT.md` | ขอบเขตงานฉบับเต็ม fault ทั้งหมด วิธีวัดผล เฟส | เมื่อคุยเรื่องเนื้องาน |
| `README.md` | คู่มือใช้งาน harness | เมื่อคุยเรื่องการรัน |
| `WINDOWS.md` | คำสั่ง PowerShell แทน make | เมื่อติดปัญหาคำสั่ง |
| **`scripts/audit.py`** | **ตัวตรวจความพร้อมทั้ง repo — คำสั่งเดียวแทนการไล่ตรวจเอง** · `--reproduce` รันตัวฉีดจริงซ้ำ | **ก่อน commit ทุกครั้ง · ตอนเริ่ม session ถ้าไม่แน่ใจ** |
| `scripts/rq3_score.py` | ให้คะแนน SQL ที่โมเดลเขียน — **รันจริง + อ่าน plan ว่าใช้ index ไหม** | เมื่อเก็บคำตอบ RQ3 จากโมเดลใหม่ |
| `sql/qfcheck_states.sql` · `_fix` · `_teardown` | พิสูจน์ว่า `quietfail_check.py` พลิกสถานะได้ | ก่อนเชื่อผลของตัวตรวจ |
| `scripts/review_claims.py` | **ทวนว่าข้ออ้างที่ถอนไปแล้วไม่โผล่โดยไม่มีคำกำกับ** — audit ตรวจตัวเลขได้ แต่ตรวจข้อความไม่ได้ · **ไม่คืน exit 1** เพราะค้นคำมีผลบวกลวงเสมอ ต้องอ่านเอง | หลังถอนหรือแก้ข้อสรุปใดๆ |
| `scripts/audit_numbers_states.py` | **พิสูจน์ว่าหมวด 6 ของ audit พลิกได้** — แก้ตัวเลขให้ผิดทีละข้อแล้วดูว่าจับไหม · ผลที่ `results/audit_numbers_states.txt` | ก่อนเชื่อว่าตัวเลขในเอกสารถูกตรวจจริง |
| **`scripts/quietfail_check.py`** | **ของส่งมอบ — ตรวจฐานข้อมูลของใครก็ได้** · ไม่ต้องมีไฟล์เฉลย | เมื่อจะเอาไปใช้จริง หรือทำ CI |
| **`sql/score.sql`** | **ตัวนับคะแนน** — ตอบ `DETECTED` / `NOT_DETECTED` / `CANNOT_CHECK` ต่อ fault ทุกข้อ | เมื่อต้องรู้ว่าตอนนี้มี fault อะไรอยู่ |
| `init/03_fingerprint.sql` | **นิยามกลางของ fingerprint** — โค้ดใหม่ต้องเรียก `qf_fingerprint()` ห้ามเขียนสูตรเอง (E40) | ก่อนเขียนโค้ดที่ต้องตรวจว่า corpus ไม่ถูกแตะ |
| `sql/i04_seed_probe.sql` | ทดสอบว่า `setseed` ทำให้ HNSW build ซ้ำได้ไหม — **ไม่ได้** | เมื่อจะไล่กลไกของ I04 ต่อ |
| `sql/i04_kmeans_nondeterminism.sql` | **ชื่อไฟล์ล้าสมัย** (E32 หักล้าง k-means แล้ว) · ไม่เปลี่ยนชื่อเพราะ groundtruth/baseline/results อ้างอยู่ | เมื่อสงสัยว่าทำไมชื่อไม่ตรงเนื้อหา |
| **`sql/doc_reread_probe.sql`** | **ตรวจว่าปุ่มที่สรุปว่า "ไม่ทำงาน" ตายจริงไหม** — `max_scan_tuples` + `iterative_scan` กับ L02 (E43) | ก่อนเขียนว่าพารามิเตอร์ใดไม่มีผล |
| `sql/q06_iterative_probe.sql` · `_recall.sql` | Q06 กับ `iterative_scan` — ยกเพดานได้ แต่ recall ยังขาด (E44) | เมื่อจะอ้างทางแก้ของ Q06 |
| **`sql/i05_capacity_model.sql`** | **สูตรความจุ HNSW ที่ตรวจกับซอร์สแล้ว** — ทำนายล่วงหน้าตรง 5/5 | เมื่อจะอ้างตัวเลขความจุของ I05 |
| `sql/doc_crosscheck_probe.sql` | ตรวจ Q04 ว่า plan ใช้ index จริงไหม + REINDEX เทียบ VACUUM (E45) | ก่อนอ้างตัวเลขปลายตารางของ Q04 หรือทางแก้ของ L02 |
| **`results/doc_crosscheck.txt`** · **`_f.txt`** | **ผลไล่เทียบทุก fault กับเอกสาร 16 ข้อ** | **ก่อนเขียนอะไรก็ตามที่พาดพิงเอกสารทางการ** |
| `results/q03_multiplier_probe.txt` | หลักฐานว่า `scan_mem_multiplier=32` ได้ครบ 40 (E42) | เมื่อสงสัยข้ออ้างเรื่องเอกสารของ Q03 |
| `results/audit_reproduce.txt` | ผลการรันตัวฉีดจริงซ้ำครั้งล่าสุด | เมื่อสงสัยว่าผลยังเกิดซ้ำได้ไหม |
| `results/audit_latest.txt` | ผล audit ครั้งล่าสุด พร้อม commit ที่ตรวจ | **ตอนเริ่ม session — อ่านแทนการตรวจเอง** |
| **`REPORT.md`** | **รายงานสรุปผล — ผล / อภิปราย / ภัยคุกคามต่อความสมเหตุสมผล / งานต่อ** ตามรูปแบบวิทยานิพนธ์ (`PROJECT.md` ข้อ 8 ของส่งมอบที่ 3) | เมื่อต้องสรุปผลให้คนนอกอ่าน |
| **`thesis/THESIS_TH.md`** | **ร่างเล่มวิทยานิพนธ์** 5 บท — รูปแบบยังรอยืนยันกับภาควิชา | เมื่อจะส่งเล่ม |
| **`slides/proposal.html`** | **สไลด์สอบเสนอหัวข้อ** 20 หน้า ไฟล์เดียว เปิด offline ได้ | เมื่อจะนำเสนอ |
| `rq3/assignments.md` | โจทย์ RQ3 แยกแชทละข้อ พร้อม copy ไปวาง | เมื่อจะทดสอบโมเดลที่สอง |
| `.github/workflows/quietfail-check.yml` | GitHub Action ของ `quietfail_check.py` · **ผ่านแล้ว** — เคยแดงถาวรตั้งแต่วันแรก ดู H31 | เมื่อจะต่อ CI หรือ CI ตก |
| `sql/i02b_harder_corpus.sql` | corpus ที่ยากขึ้น — ทดสอบว่า I02 รอดบนข้อมูลที่ไม่สะอาดไหม (E36) | เมื่อจะตรวจข้อจำกัดของ I02 |
| `sql/i02_cost_control.sql` · `i02b_buffers_control.sql` | วัด **ต้นทุน** ของเงื่อนไข I02 — ตัวที่เปิดโปง E36 | เมื่อจะเทียบ recall ต้องดูคู่กับ buffers |
| `results/` | ผลการทดลองดิบ ห้ามแก้ด้วยมือ | เมื่อวิเคราะห์ผล |

---

## กฎเหล็ก — ห้ามละเมิด

**1. ผู้ช่วย AI ห้ามเป็นคนตัดสินว่าอะไรถูก**
ให้เอกสารทางการ / `EXPLAIN ANALYZE` / exact search / ผลรันจริง เป็นคนตัดสิน

**2. ห้ามใส่ตัวเลขที่ยังไม่ได้วัด**
ค่า `m` / `ef_search` / `lists` / `probes` ต้องมาจากการวัดเท่านั้น

**3. ทุก fault ต้องมี assertion**
ฉีดแล้วไม่เกิดตามที่ตั้งใจ = ต้อง exit ด้วย error ห้ามปล่อยผ่านแล้วนับคะแนน

**4. ห้ามขยายขอบเขตโดยไม่ตัดของเดิม**
ของใหม่ต้องตอบได้ว่าตอบ RQ ข้อไหน

**5. เดินตามลำดับเฟส ห้ามข้าม**

**6. ทุกผลลัพธ์ต้องประทับเวอร์ชัน + วันที่**

**7. ใช้ buffers เป็นตัวชี้วัดหลักด้าน I/O ไม่ใช่เวลา**
เวลาแกว่งตามเครื่อง buffers นิ่ง

**7ก. ถ้า plan มี Bitmap Heap Scan ต้องดู `Heap Blocks` และ
`Rows Removed by Index Recheck` ควบกับ buffers เสมอ**
เพราะ buffers มองไม่เห็นต้นทุนฝั่ง CPU
พิสูจน์แล้วใน EXP01c: `work_mem` 6 ค่า ต่างกันจนเกิด lossy bitmap
และ recheck 110,062 แถว แต่ **buffers = 2501 เท่ากันเป๊ะทุกค่า**
ทั้งสองตัวนี้เป็นค่าเชิงโครงสร้างจาก plan จึงนิ่งพอๆ กับ buffers

**8. อุ่น cache ก่อนวัดเสมอ**
รอบแรกหลัง `CREATE INDEX` จะมี `read=` เพราะ page ยังไม่เข้า cache

**9. ทุก check ที่อ่าน GUC ต้อง `LOAD 'vector'` ก่อน**
ไม่งั้นได้ 0 rows แล้วรายงานว่าผ่านทั้งที่ไม่ได้ตรวจ (false negative)

**10. ถ้า checker หาสิ่งที่ควรมีไม่เจอ ต้องรายงานว่า "ตรวจไม่ได้" ไม่ใช่ "ผ่าน"**

---

## สิ่งที่ผู้ช่วย AI เชื่อถือไม่ได้ในโปรเจคนี้

| เรื่อง | ทำไม |
|---|---|
| query plan ที่ planner จะเลือก | **พลาดมาแล้วจริง — ดู E07** |
| ข้อความ error ที่แน่นอน | ต่างตามเวอร์ชัน ต้องรันแล้วคัดลอกของจริง |
| ค่าพารามิเตอร์ pgvector | ต้องวัดกับข้อมูลจริง |
| ตัวเลข latency ใดๆ | ต่างกันได้ 10 เท่าตามฮาร์ดแวร์ |
| ส่วนแบ่งตลาด / ความนิยมของเครื่องมือ | ค้นแล้วไม่พบข้อมูลไทยที่ยืนยันได้ |

---

### ⭐⭐ ไล่เทียบผลทุก fault กับเอกสารแล้ว — 16 ข้อ (E45 · E46 · 2026-08-01)

```
pgvector 12 ข้อ -> 10 ตรง · 2 ผิด (Q04 · L02)      results/doc_crosscheck.txt
PostgreSQL 4 ข้อ ->  3 ตรง · 1 ผ่อน (F03)          results/doc_crosscheck_f.txt
```

🔴 **ไม่มีข้อใดที่พิสูจน์ได้ว่าเอกสารเขียนผิด** — ที่เจอทั้งหมดคือเราอ่านไม่ครบ
หรือไม่ได้ลองสิ่งที่เอกสารมีให้ · **ห้ามเขียนว่าเอกสารบกพร่องอีก ทุกไฟล์**

**เอกสารเงียบจริง 2 ข้อ** — I04 (build ซ้ำไม่ได้) · **F05 (พฤติกรรมคิว lock)**
F05 ต้องเขียนว่า *"เอกสารให้ตารางความขัดแย้ง แต่ไม่ได้อธิบายผลของการเข้าคิว"*
ห้ามเขียนว่า *"เอกสารไม่พูดถึง lock"*

**⭐ FAQ ของ pgvector รวม Q03 · L02 · Q06 ไว้ประโยคเดียว และให้ทางแก้ตัวเดียว**
(`iterative_scan`) → สนับสนุนข้ออ้าง "กลไกเดียวกัน" ด้วยหลักฐานจากเอกสาร

**ข้อสรุปที่ถูกถอน/แก้ รวม 14 ข้อ** — 7 จากการทดสอบข้ามชุดข้อมูล (เล่ม 5.8.5)
+ 7 จากการไล่เทียบเอกสาร (เล่ม 5.9.4) · **7 ข้อหลังไม่มีเครื่องมือใดจับได้
ต้องมีคนตั้งคำถามให้ตรวจซ้ำ**

### ⭐ รูปแบบร่วม — เครื่องมือมีอยู่แล้ว แต่ปริยายปิด (E46 · 2026-08-01)

| fault | กลไกที่ช่วยได้ | ปริยาย |
|---|---|---|
| Q03 · L02 · Q06 | `hnsw.iterative_scan` | **off** |
| **F03** | `lock_timeout` | **0** |
| **F01** | `idle_in_transaction_session_timeout` | **0** |

**"ค่าเริ่มต้นคือปัญหา" ครอบคลุมทั้ง pgvector และ PostgreSQL** ไม่ใช่แค่ฝั่งเดียว
เจอจากการไล่เทียบเอกสารเท่านั้น ไม่มีเครื่องมือใดในโครงงานจับให้ได้
`results/doc_crosscheck_f.txt`

---

## คำศัพท์ที่ต้องใช้ให้ถูก

| ห้ามพูด | ให้พูด |
|---|---|
| "AI clustering" แบบเหมารวม | **ANN search** |
| "index ทำให้เร็วขึ้น" | "index ทำให้เร็วขึ้น**และเปลี่ยนคำตอบ**" |
| "AI หาไม่เจอ" | "recall@10 ลดลงเหลือ X%" |

IVFFlat ใช้ k-means = clustering จริง / HNSW เป็นกราฟ = ไม่ใช่ clustering

---

# ข้อเท็จจริงที่ยืนยันด้วยการรันจริงแล้ว

## สภาพแวดล้อม (2026-07-26)

```
PostgreSQL : 17.10 (Debian 17.10-1.pgdg12+1)
pgvector   : 0.8.5
image      : pgvector/pgvector:0.8.5-pg17   ← ล็อกแล้ว ห้ามใช้ tag :pg17 เฉยๆ
extensions : vector, pg_trgm, btree_gin, pg_stat_statements, plpgsql
roles      : lab (superuser), app, observer
config     : fragile — max_connections=20, work_mem=64kB,
             deadlock_timeout=200ms, lc_messages=C, shared_buffers=128MB
dataset    : orders 200,000 แถว (23 MB) — seed แก้แล้ว 2026-07-26 ดู E17
```

## ค่า default ของ pgvector 0.8.5 (ทุกค่า setting == boot_val)

```
hnsw.ef_search           40
hnsw.iterative_scan      off      ← มีมาตั้งแต่ 0.8.0 แต่ยังปิดอยู่
hnsw.max_scan_tuples     20000
hnsw.scan_mem_multiplier 1
ivfflat.iterative_scan   off
ivfflat.max_probes       32768
ivfflat.probes           1        ← เอกสารแนะนำ sqrt(lists)
```

**ต้อง `LOAD 'vector'` ก่อน ไม่งั้น pg_settings คืน 0 rows**

## EXP01 — index selectivity (200k แถว, ปิด parallel) · รันใหม่หลังแก้ E17

| query | แถวจริง | buffers ก่อน | buffers หลัง | ดีขึ้น |
|---|---|---|---|---|
| `merchant_id = 1` | 23,983 | 2,479 | 25 | 99× |
| `merchant_id = 4999` | 6 | 2,479 | 3 | 826× |

ช่องว่างระหว่างสองกรณี **8.3×** · ขนาด index 1,472 kB

ข้อสังเกตเพิ่ม:
- ทั้งคู่กลายเป็น **Index Only Scan** — `Heap Fetches` = 16 และ 0
  (เดิม 0 ทั้งคู่ · ที่มี 16 เพราะ F03 ไป UPDATE แถว id=1 ทิ้ง dead tuple ไว้)
- นี่คือเหตุผลที่ผลไม่ตรงกับที่ทำนายไว้ — ดู E07

## EXP01b — บังคับอ่าน heap ด้วย `sum()` (200k แถว, work_mem=64kB) · รันใหม่หลังแก้ E17

| merchant_id = 1 | buffers | เวลา | plan |
|---|---|---|---|
| ไม่มี index | 2,479 | 20.53 ms | Seq Scan |
| **มี index** | **2,501** | **44.77 ms** | Bitmap Heap Scan |

**index ทำให้ช้าลง 118%** เพราะ lossy bitmap (`exact=931 lossy=1548`, recheck 110,062 แถว)
และ **planner เลือกเอง** ทั้งที่ผลแย่กว่า — cost บอกถูกกว่า 3.5% แต่จริงแพงกว่า 2.2 เท่า

**ผลแรงกว่าตอนใช้ seed เก่ามาก** (เดิมช้าลง 27%) ข้อสรุปเดิมยังยืนและหนักแน่นขึ้น

`merchant_id = 4999` ยังดีขึ้น 310× (2,479 → 8) ตามปกติ · รายละเอียดเต็มใน E08

## สถิติ: MCV ครอบคลุมแค่ 2.1%

```
n_mcv_tracked = 100 (คงที่ตาม default_statistics_target)
n_distinct    = ~4,68x  ← ค่าประมาณจากการสุ่ม เปลี่ยนทุกครั้งที่ ANALYZE
ค่าใน MCV  (merchant 1)    → ประมาณ 23,987 จริง 23,983  = คลาด 0.02%
ค่านอก MCV (merchant 4999) → ประมาณ 27     จริง 6       = คลาด 4.5 เท่า
```

**`n_distinct` เป็นค่าประมาณ ห้ามอ้างเป็นตัวเลขตายตัว** — วัดได้ 4,656 / 4,679 / 4,695
ในสามครั้งบนข้อมูลชุดเดียวกันที่ไม่ได้เปลี่ยนแม้แต่แถวเดียว
(พบตอน audit 2026-07-27 ตอนที่เอกสารสามที่ให้ตัวเลขไม่ตรงกัน)
ส่วนจำนวนแถวจริง 23,983 และ 6 เป็นการนับ จึงนิ่ง — ใช้อ้างได้

**ข้อสรุปไม่กระทบ**: 100/4,68x = **2.1%** เท่าเดิมทุกค่า
ความคลาดแย่ที่สุดตรงค่าที่พบน้อย ซึ่งเป็นจุดที่การเลือก plan สำคัญที่สุด
→ ฐานรากของ F10

---

## วิธีหา ground truth ของ recall (จากเอกสารทางการ)

```sql
BEGIN;
SET LOCAL enable_indexscan = off;   -- บังคับ exact search
SELECT ...
COMMIT;
```

**ห้ามใช้ `DROP INDEX` เพื่อหาเฉลย** — วิธีข้างบนเร็วกว่าและไม่เปลี่ยนสภาพฐานข้อมูล
