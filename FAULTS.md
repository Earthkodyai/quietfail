# ทะเบียน fault

> **ไฟล์นี้คือแหล่งอ้างอิงเดียวของนิยาม fault ทั้งหมด**
> ถ้า `PROJECT.md` หรือ `EVIDENCE.md` ขัดกับไฟล์นี้ ให้เชื่อไฟล์นี้
>
> ทุก fault ต้องมีครบ 5 อย่าง: อาการ · ต้นเหตุ · วิธีฉีด · ground truth · assertion
> ถ้าข้อไหนขาด แปลว่ายังนิยามไม่เสร็จ ห้ามนับว่าพร้อมทำ

**รวม 16 fault** = core 4 ข้อ (F) + vector 12 ข้อ (I/Q/V/L)

---

# ชุด core — 4 ข้อ (เกณฑ์ผ่านเฟส 1)

มีไว้แสดงว่ากลไกวัดผลชุดเดียวกันใช้ได้ทั้งกับปัญหา DB ทั่วไปและปัญหาเฉพาะของ vector
ทั้ง 4 ข้อเป็นแบบ **"error ชี้ไปผิดที่"** ซึ่งเป็นแกนของ QuietFail

---

## F01 — connection หมด เพราะ transaction ค้าง

| | |
|---|---|
| **อาการที่เห็น** | `FATAL: remaining connection slots are reserved for roles with the SUPERUSER attribute` ← **ยืนยันด้วยการรันจริง** · เดิมเขียนไว้ว่า `sorry, too many clients already` ซึ่งผิด (ดู E13) |
| **คนมักแก้ผิด** | เพิ่ม `max_connections` → กิน RAM มากขึ้น แล้วเต็มอีกอยู่ดี |
| **ต้นเหตุจริง** | โค้ดเปิด transaction แล้วไม่ commit/rollback |
| **ทำไม dev ไม่เจอ** | dev มี connection เดียว ไม่มีวันเต็ม |

**วิธีฉีด** — เปิด N connection แต่ละอันสั่ง `BEGIN; SELECT 1;` แล้วปล่อยค้าง
โดยที่ท่อ stdin ยังเปิดอยู่

> ⚠️ ห้ามใช้ `pg_sleep()` เพราะทำให้ state เป็น `active` ซึ่งคนละเรื่อง
> ⚠️ ต้องยิงด้วย role `app` ไม่ใช่ `lab` เพราะ `superuser_reserved_connections`
> ทำให้ superuser ต่อได้เสมอ → fault จะไม่มีวันเกิด

**ground truth**
```sql
SELECT pid, usename, state, now() - state_change AS stuck_for, query
FROM pg_stat_activity
WHERE state = 'idle in transaction'
ORDER BY state_change ASC;
```

**assertion** — ต้องผ่านทั้ง 3 ข้อ ไม่งั้น exit ด้วย error
1. connection ใหม่ได้ error ที่ตรงกับ
   `remaining connection slots are reserved|too many clients already`
   **และต้องบันทึกว่าเจอแบบไหน** เพราะสองแบบคนละความหมาย
2. `pg_stat_activity` มี `state = 'idle in transaction'` ≥ 10 session
3. session ที่ค้างนานสุด ค้างเกิน 2 วินาที

> ⚠️ **ข้อ 1 เคยเขียนผิดจนตัวตรวจใช้ไม่ได้** — ดู E13
>
> `max_connections=20` คู่กับ `superuser_reserved_connections=3`
> → role ธรรมดาเต็มที่ **17 slot** แล้วได้ข้อความ "สงวนให้ superuser"
> ส่วน `too many clients already` เกิดเมื่อเต็มหมดรวม slot สำรอง
>
> แอปจริงต่อด้วย role ธรรมดาเสมอ → **ข้อความเดิมไม่มีวันเกิดกับแอปจริงบน config นี้**
> ตัวตรวจที่ match ข้อความเดิมจะไม่มีวันจับ F01 ได้เลย
>
> อ่าน SQLSTATE ไม่ได้เพราะเป็น connection error ระดับ libpq ไม่ใช่ query error
> (`VERBOSITY=verbose` ก็ไม่แสดง) → พึ่งข้อความ ซึ่งเสถียรเพราะ config ล็อก
> `lc_messages='C'` และ image tag ล็อกที่ `0.8.5-pg17`
> **ปลดล็อกอย่างใดอย่างหนึ่งเมื่อไหร่ ต้องรันยืนยันใหม่**

**ต้องมี `HOLD_OPEN`** — ค้าง fault ไว้หลัง assert ผ่าน ให้ตัวนับคะแนนที่รันแยกขั้นมาทัน
ไม่งั้น `trap cleanup EXIT` เก็บกวาดที่ ~5 วินาที แล้วตัวนับรายงาน "ไม่พบ"
ทั้งที่ fault เกิดจริง (ดู E14)

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-26)**

| | |
|---|---|
| สคริปต์ฉีด | `faults/f01_idle_in_transaction.sh` |
| ground truth | `groundtruth/f01.json` · `verified_on` เติมครบแล้ว |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 ทุกรอบ · 17 idle-in-transaction session ทุกรอบ** |
| หลักฐาน | `results/f01_phase1_cycle.txt` |
| ตัวนับแยกแยะได้ | `DETECTED` ระหว่าง fault ค้าง → `NOT_DETECTED` หลังเก็บกวาด ทุกรอบ |

---

## F03 — statement timeout ที่ไม่ได้ช้า แต่รอ lock ⭐

**นี่คือข้อที่พิสูจน์วิทยานิพนธ์ของโปรเจคทั้งหมด** เพราะต้องทำคู่กับ F03b
ซึ่งให้ **ข้อความ error เหมือนกันเป๊ะ แต่คนละสาเหตุ**

| | F03 | F03b |
|---|---|---|
| **อาการที่เห็น** | `ERROR: canceling statement due to statement timeout` | **เหมือนกันทุกตัวอักษร** |
| **ต้นเหตุจริง** | รอ row lock จาก transaction อื่น | query ช้าจริง (ไม่มี index) |
| **คนมักแก้ผิด** | เพิ่ม timeout · ไล่ optimize query ที่ไม่ได้ช้า | เพิ่ม timeout |
| **ทางแก้ที่ถูก** | ไล่หา transaction ที่ถือ lock | สร้าง index |

**วิธีฉีด F03**
```
session A:  BEGIN; UPDATE orders SET status='x' WHERE id=1;   (ค้างไว้)
session B:  SET statement_timeout = '2s';
            UPDATE orders SET status='y' WHERE id=1;          → timeout
```

**วิธีฉีด F03b**
```
session เดียว [role app]: SET statement_timeout = '100ms';
    SELECT count(*) FROM (
      SELECT * FROM orders
      ORDER BY created_at_naive, total_float, status, total_satang) t;
    (ไม่มี index บนคอลัมน์ที่ใช้เรียงเลยสักคอลัมน์)
```

> ⚠️ **สเปกเดิมเขียน `2s` กับ `WHERE created_at_naive < '2020-01-01'` ซึ่งใช้ไม่ได้**
> query นั้นวัดได้ 41ms และหลังแก้ E17 ข้อมูลเริ่มปี 2025 เงื่อนไขจึงตรง 0 แถว
> ไล่วัดทุกตัวเลือกบนชุด 200k แถวแล้ว **ไม่มีอันไหนช้าถึง 2 วินาที** (ดู E18)
>
> ค่า 100ms มาจากการวัด: query ที่เลือกมีค่าต่ำสุดจาก 5 รอบ = 331ms → margin 3.3 เท่า
> **เลือกจากค่าต่ำสุด ไม่ใช่ค่าเฉลี่ย** เพราะค่าต่ำสุดคือกรณีแย่สุดของ margin
>
> **สคริปต์ต้องวัด margin เองก่อนเริ่มทุกครั้ง** และ exit ถ้าไม่ถึง 2 เท่า —
> พิสูจน์แล้วว่าจำเป็น รอบหนึ่ง cache อุ่นทำให้เหลือ 151ms และ pre-flight ปฏิเสธที่จะรัน

**ground truth — ตัวแยกคือบรรทัดเดียวนี้**
```sql
SELECT pid, pg_blocking_pids(pid) AS blocked_by, state, query
FROM pg_stat_activity WHERE pid = <victim>;
```

| | `pg_blocking_pids` |
|---|---|
| F03 | **ไม่ว่าง** — มี pid ที่บล็อกอยู่ |
| F03b | **ว่าง** — ไม่มีใครบล็อก แค่ช้าเอง |

**assertion**
1. ทั้ง F03 และ F03b ต้องได้ **บรรทัด `ERROR:`** เหมือนกัน (ถ้าต่างกัน = ออกแบบผิด ต้อง exit)
2. ตัวเฝ้าดูต้องเก็บตัวอย่างได้ทั้งสองเคส — **ถ้าเก็บไม่ได้ = "ตรวจไม่ได้" ห้ามสรุป** (กฎข้อ 10)
3. F03: `pg_blocking_pids()` ต้องไม่ว่าง
4. F03b: `pg_blocking_pids()` ต้องว่าง **และต้องมีตัวอย่าง > 0** ไม่งั้นเป็นจริงแบบว่างเปล่า
5. ถ้าข้อไหนไม่เป็นจริง = fault ไม่ได้เกิดตามที่ตั้งใจ ต้อง exit

> ⚠️ **CONTEXT ต่างกัน แต่ไม่ได้ช่วยแยกสาเหตุ** (ดู E20)
> ```
> F03  : ERROR:  canceling statement due to statement timeout
>        CONTEXT:  while updating tuple (0,1) in relation "orders"
> F03b : ERROR:  canceling statement due to statement timeout
> ```
> CONTEXT บอกว่าคำสั่งกำลัง update tuple = คุณสมบัติของ **ชนิดคำสั่ง** ไม่ใช่ของ **สาเหตุ**
> `UPDATE` ที่ช้าเองก็ได้ CONTEXT แบบเดียวกัน จึงยังบอกไม่ได้ว่าต้องแก้ตรงไหน
>
> **ข้อสรุปแข็งกว่าสเปกเดิม** — พูดได้ว่า "ต่อให้อ่าน CONTEXT ด้วยก็ยังแยกไม่ออก"

> ⚠️ **ต้องเรียก `pg_stat_clear_snapshot()` ทุกรอบของลูปเฝ้าดู**
> `pg_stat_activity` ถูก snapshot ครั้งเดียวต่อ transaction — ลูปใน `DO` block เดียว
> จะเห็นภาพเดิมซ้ำแล้วรายงานว่า "ไม่มีใครถูกบล็อก" โดยไม่มี error ใดๆ (ดู E19)

> ⚠️ **หลักฐานของข้อนี้เป็น *ตาราง* ไม่ใช่สถานะสด** ต่างจาก F01/F05
> ตัวนับคะแนนจึงต้องรัน **หลัง** ตัวฉีดจบ โดยใช้ `KEEP_OBS=1`
> อย่าลอก `HOLD_OPEN` มาใช้ — วัดผิดจังหวะแน่นอน (ดู E21)

**ทำไมข้อนี้สำคัญที่สุด:** LLM ที่เห็นแค่ข้อความ error แยกสองกรณีนี้ไม่ออกเลย
ต้องดูสถานะจริงของ DB เท่านั้น — นี่คือช่องว่างที่ `quietfail-check` จะเข้าไปเติม
และเป็นข้อที่ใช้เทียบกับ baseline "โยน error ให้ LLM อธิบายลอยๆ" ได้ตรงที่สุด

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-26)**

| | |
|---|---|
| สคริปต์ฉีด | `faults/f03_timeout_two_causes.sh` |
| ตัวเฝ้าดู | `sql/f03_watcher.sql` (แยกไฟล์ ไม่ฝังใน shell — ดู E19) |
| ground truth | `groundtruth/f03.json` · ครอบทั้ง F03 และ F03b |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 · assertion ผ่าน 4/4 ทุกรอบ** |
| หลักฐาน | `results/f03_phase1_cycle.txt` |
| ผลที่วัดได้ | F03 18 ตัวอย่าง blocked=**true** · F03b 18-19 ตัวอย่าง blocked=**false** |
| ตัวนับแยกแยะได้ | `DETECTED` เมื่อมีหลักฐาน → `CANNOT_CHECK` เมื่อลบตารางทิ้ง |

> ตัวนับพลิกเป็น `CANNOT_CHECK` ไม่ใช่ `NOT_DETECTED` — ถูกต้องตามกฎเหล็กข้อ 10
> ไม่มีหลักฐาน แปลว่าตรวจไม่ได้ ไม่ใช่แปลว่าไม่มี fault

---

## F05 — อ่านข้อมูลก็ค้าง เพราะ migration ที่ยังไม่ได้เริ่ม

| | |
|---|---|
| **อาการที่เห็น** | `SELECT` ธรรมดา timeout ทั้งที่ query เบามาก |
| **คนมักแก้ผิด** | โทษ network · โทษ DB ช้า · restart แอป |
| **ต้นเหตุจริง** | `ALTER TABLE` รออยู่ในคิว lock แล้ว**คิวบล็อกทุกคนที่มาทีหลัง** |
| **ทำไม dev ไม่เจอ** | ต้องมี 3 session เรียงกันพอดี |

**จุดที่หลอกที่สุด:** `ALTER TABLE` ยัง**ไม่ได้เริ่มทำงานเลย** มันแค่รอ
แต่การรอของมันทำให้ SELECT ที่ควรเสร็จใน 1ms ค้างไปด้วย

**วิธีฉีด**
```
session A [role app]:  BEGIN; SELECT * FROM orders LIMIT 1;   (ค้างไว้ ไม่ commit)
session B [role lab]:  ALTER TABLE orders ADD COLUMN tmp int; (จะรอ A)
session C [role app]:  SELECT * FROM orders LIMIT 1;          (ค้าง! ทั้งที่ควรได้ทันที)
```

> ⚠️ **role ของแต่ละ session เป็นส่วนหนึ่งของนิยาม ไม่ใช่รายละเอียดปลีกย่อย**
>
> B ต้องเป็น **เจ้าของตาราง (`lab`)** ถ้ายิงด้วย `app` จะได้
> `ERROR: must be owner of table orders` ทันที B ตายก่อนเข้าคิว → fault ไม่เกิด
> เคยพลาดมาแล้วจริง ดู **E15**
>
> และมันสมจริงกว่า — migration รันด้วย role ที่มีสิทธิ์ ส่วนแอปรันด้วย role จำกัดสิทธิ์
> fault จึงเกิดจาก **สอง role คนละตัวชนกัน** ซึ่งอธิบายว่าทำไมทีมแอปกับทีม DB
> ถึงมักโทษกันไปมาแทนที่จะไปดูคิว lock

**ground truth**
```sql
SELECT l.pid, l.granted, l.mode, a.state, a.query
FROM pg_locks l JOIN pg_stat_activity a USING (pid)
WHERE l.relation = 'orders'::regclass
ORDER BY l.granted DESC, a.query_start;
```

**assertion**
1. session C ต้องค้างเกิน 2 วินาที (query ที่ปกติเสร็จใน < 10ms)
2. `pg_locks` ต้องมีแถวที่ `granted = false` อย่างน้อย 2 แถว
3. ต้องไล่ห่วงโซ่ได้ว่า C ถูกบล็อกโดย B และ B ถูกบล็อกโดย A

> ⚠️ **ตัวเก็บกวาดต้องยกเลิก DDL ที่ฝั่ง server ไม่ใช่แค่ฆ่า psql ฝั่ง client**
> `ALTER TABLE` ที่ส่งไปแล้วนั่งรออยู่ในคิว การฆ่า client ไม่ยกเลิกมัน
> พอ session A ตาย มันจะได้ lock แล้ว**แก้ schema จริง** — ดู **E16**
> ต้องมีทั้ง `pg_terminate_backend()` และตาข่าย `DROP COLUMN IF EXISTS`

**ต่อยอด:** I03 (`CREATE INDEX` บน vector column ไม่ใช้ `CONCURRENTLY`)
ใช้กลไกเดียวกันทุกประการ ทำ F05 เสร็จแล้ว I03 แทบไม่ต้องเขียนใหม่

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-26)**

| | |
|---|---|
| สคริปต์ฉีด | `faults/f05_lock_queue.sh` |
| ground truth | `groundtruth/f05.json` · `verified_on` เติมครบแล้ว |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 · assertion ผ่าน 3/3 ทุกรอบ** |
| หลักฐาน | `results/f05_phase1_cycle.txt` |
| ห่วงโซ่ที่วัดได้ | `SELECT` ← `ALTER TABLE` ← `idle in transaction` |
| schema ไม่ถูกแตะ | `orders` คงที่ 8 คอลัมน์ทุกรอบ · ตาข่ายไม่ต้องทำงานเลย |

---

## F10 — statistics เก่า ทำให้ planner เลือกผิด

| | |
|---|---|
| **อาการที่เห็น** | query เดิม โค้ดเดิม แต่ช้าลงหลายสิบเท่า **ไม่มี error** |
| **คนมักแก้ผิด** | rewrite query · เพิ่ม index ที่ไม่จำเป็น |
| **ต้นเหตุจริง** | สถิติไม่ทันข้อมูล planner จึงประมาณจำนวนแถวผิด |
| **ทำไม dev ไม่เจอ** | dev ไม่ได้ bulk insert แล้ว query ทันที |

**วิธีฉีด**
```sql
ALTER TABLE qf_f10_big SET (autovacuum_enabled = off);   -- กัน autovacuum มาช่วย
-- bulk insert 300,000 แถว โดยให้คอลัมน์ที่ใช้กรองเป็น **ค่าใหม่ที่ histogram ไม่เคยเห็น**
-- query ทันทีโดยไม่ ANALYZE
```

> ⚠️ **"ค่าใหม่" คือหัวใจทั้งหมด ห้ามอ่านข้าม** (ดู E22)
>
> ความล้าสมัยของ **จำนวนแถวรวม** อย่างเดียวหลอก planner **ไม่ได้**
> PostgreSQL ปรับสเกล `reltuples` ตามขนาดไฟล์จริงเสมอ
> ลองมาแล้ว: `reltuples` บอก 100 · แถวจริง 300,100 · planner เดา 191,200
> = คลาดแค่ **1.6 เท่า** ไม่ผ่าน assertion
>
> ตัวที่หลอกได้จริงคือ **สถิติระดับคอลัมน์** (MCV / histogram)
> ค่าที่ไม่เคยปรากฏตอน `ANALYZE` ครั้งล่าสุด จะถูกเดาว่าเจอน้อยมาก
> และไม่มีกลไกไหนมาปรับสเกลให้

> ⚠️ **ใช้ตาราง `qf_f10_*` ของตัวเอง ห้ามฉีดใส่ `orders`**
> `orders` เป็นฐานของ EXP01/01b/01c ที่ตัวเลขถูกอ้างทั่วทั้ง repo
> ฉีดใส่มันคือทำลาย baseline ทุกครั้งที่รัน (บทเรียนเดียวกับ E16)

**ground truth — ปิดวงจรได้ในตัว**
```sql
EXPLAIN (ANALYZE, BUFFERS) SELECT ...;   -- เทียบ rows= กับ actual rows=
ANALYZE orders;
EXPLAIN (ANALYZE, BUFFERS) SELECT ...;   -- ต้องกลับมาเร็ว
```

ถ้าสั่ง `ANALYZE` แล้วหาย = ยืนยันสาเหตุแบบเถียงไม่ได้

**assertion**
1. ก่อน ANALYZE: `rows` ต่างจาก `actual rows` **≥ 10 เท่า**
2. หลัง ANALYZE: อัตราส่วนลดลงเหลือ ≤ 2 เท่า
3. buffers หลัง ANALYZE ต้องน้อยกว่าก่อน อย่างน้อย 5 เท่า
4. ถ้าข้อ 1 ไม่เป็นจริง = autovacuum แอบมาทำงาน ต้อง exit ไม่ใช่ปล่อยผ่าน

> ⚠️ **ต้องอ่าน `Plan Rows` / `Actual Rows` จาก node ของ join ไม่ใช่ node บนสุด**
> node บนสุดคือ `Aggregate` ของ `count(*)` → ประมาณ 1 ได้จริง 1 **เสมอ**
> ไม่ว่าสถิติจะเพี้ยนแค่ไหน วัดตรงนั้นจะได้ "คลาด 1.0 เท่า" ตลอดแล้วสรุปผิด (ดู E22)
> ส่วน buffers อ่านจาก node บนสุดได้ เพราะ EXPLAIN รวมของลูกมาให้แล้ว

**ข้อมูลที่วัดไว้แล้ว (EXP01b Phase D) — ใช้เป็นฐานเปรียบเทียบ**
```
MCV ติดตาม 100 จาก n_distinct 4,695 = 2.1% ของค่าทั้งหมด
ค่าใน MCV  (merchant 1)    → ประมาณ 23,987 จริง 23,983 = คลาด 0.02%
ค่านอก MCV (merchant 4999) → ประมาณ 27     จริง 6      = คลาด 4.5 เท่า
```
(รันใหม่ 2026-07-26 หลังแก้ E17 · ค่าเดิมบน seed ที่พัง: n_distinct 4,677 คลาด 0.6% / 50%)
ความคลาด **แย่ที่สุดตรงค่าที่พบน้อย** ซึ่งเป็นจุดที่การเลือก plan สำคัญที่สุด
สถิติเก่าจะทำให้ตัวเลขนี้แย่ลงไปอีก

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตรรกะ | `sql/f10_stale_stats.sql` (SQL ล้วน — ไม่ต้องใช้หลาย session) |
| ตัวห่อ | `faults/f10_stale_statistics.sh` (รัน · ตรวจ exit code · เก็บกวาด) |
| ground truth | `groundtruth/f10.json` · `verified_on` เติมครบแล้ว |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 · assertion ผ่าน 3/3 ทุกรอบ** |
| หลักฐาน | `results/f10_phase1_cycle.txt` |
| ตัวนับแยกแยะได้ | `DETECTED` เมื่อมีหลักฐาน → `CANNOT_CHECK` เมื่อลบตารางทิ้ง |

**ผลที่วัดได้ (300,000 แถว):**

```
ก่อน ANALYZE : ประมาณ 200 จริง 300,000 (คลาด 1,500x) · buffers 1,202,548 · Nested Loop · 702 ms
หลัง ANALYZE : ประมาณ 298,733 จริง 300,000 (คลาด 1.0x) · buffers 3,726     · Hash Join   · 306 ms
```

**นิ่งมาก:** 3 รอบได้ 1500.0x / 1507.5x / 1507.5x · buffers ลด 322.7x ทุกรอบ

### ⭐ ข้อสังเกตที่มีค่ากว่าตัว fault — นาฬิกาโกหก buffers ไม่โกหก

```
เวลา    702 ms → 306 ms   =   2.3 เท่า
buffers 1,202,548 → 3,726 = 322.7 เท่า
```

ต่างกัน **140 เท่า** เพราะข้อมูลชุดนี้อยู่ใน cache หมด
buffer ส่วนเกิน 1.2 ล้านครั้งจึงเป็น `hit` ล้วน ไม่แตะดิสก์เลย

บนเครื่องที่ข้อมูลไม่พอใส่ RAM ส่วนเกินนั้นจะกลายเป็นการอ่านดิสก์จริง
→ **วัดด้วยนาฬิกาบนเครื่อง dev จะสรุปว่า "ช้าลงนิดหน่อย" แล้วปล่อยขึ้น production**

นี่คือหลักฐานที่ชัดที่สุดของ **กฎเหล็กข้อ 7** และเป็นเหตุผลว่าทำไม
`quietfail-check` ต้องดู buffers ไม่ใช่เวลา

---

# ชุด vector — 12 ข้อ (ล็อกแล้ว)

## ชั้น I — ติดตั้งและสร้าง index

| ID | fault | หลักฐาน |
|---|---|---|
| **I01** | opclass ตอนสร้างไม่ตรงกับ operator ตอน query → index ไม่เคยถูกใช้ | ✅ เอกสาร |
| **I02** | สร้าง index ก่อนข้อมูล**เป็นตัวแทน** → ผลหายไปเงียบๆ (ชื่อเดิมโทษ 'ตารางเล็ก' · **แก้แล้ว** ดู E33) | ✅ เอกสาร + **คำเตือนออกผิดที่** |
| **I03** | `CREATE INDEX` ไม่ใช้ `CONCURRENTLY` → ล็อกตาราง (กลไกเดียวกับ F05) | ✅ เอกสาร |
| **I04** | สร้าง index ใหม่ → ได้คำตอบไม่เหมือนเดิม (ชื่อเดิมโทษ k-means · **แก้แล้ว** ดู E32) | ⚠️ **พิสูจน์เองแล้ว** เอกสารไม่พูดถึง |
| **I05** | `maintenance_work_mem` ต่ำ → build ช้าผิดปกติ | ✅ มีข้อความ NOTICE |

> ⚠️ ตารางข้างบนเป็น**ดัชนี** ไม่ใช่นิยาม — ตามกฎหัวไฟล์นี้ fault ต้องมีครบ 5 อย่าง
> ข้อที่ลงมือทำแล้วจะมีนิยามเต็มต่อจากนี้ ข้อที่ยังไม่มี = **ยังไม่พร้อมทำ**

---

### I01 — opclass ตอนสร้าง ไม่ตรงกับ operator ตอน query

| | |
|---|---|
| **อาการที่เห็น** | ช้าเหมือนไม่มี index ทั้งที่ `\di` เห็น index อยู่ชัดๆ และ build ไปแล้วเป็นนาที |
| **คนมักแก้ผิด** | เพิ่ม `m` / `ef_construction` · rebuild index · เพิ่ม RAM · โทษว่า HNSW ไม่เวิร์ค |
| **ต้นเหตุจริง** | index สร้างด้วย `vector_l2_ops` แต่ query ใช้ `<=>` (cosine) → **planner ใช้ index นั้นไม่ได้เลย** |
| **ทำไม dev ไม่เจอ** | ข้อมูลชุดเล็ก seq scan ก็เร็วอยู่แล้ว ไม่มีใครสังเกต |

**หลักฐานจากเอกสาร:** ต้องสร้าง index แยกต่อหนึ่ง distance function —
`vector_l2_ops` · `vector_ip_ops` · `vector_cosine_ops` · `vector_l1_ops` (ดู `EVIDENCE.md`)

**วิธีฉีด**
```sql
-- ผิด: สร้างด้วย l2 แต่ค้นด้วย cosine
CREATE INDEX ON qf_corpus USING hnsw (embedding vector_l2_ops);
SELECT id FROM qf_corpus ORDER BY embedding <=> $1 LIMIT 10;   -- <=> คือ cosine
```

**ground truth — มีสองชั้น และชั้นที่สองคือของจริง**

1. **ชั้นแผน (ต้องรัน):** `EXPLAIN` ไม่มี `Index Scan` → เป็น Seq Scan
2. **ชั้น catalog (ไม่ต้องรัน query เลย):** อ่าน opclass ของ index จาก `pg_index`
   แล้วเทียบกับ operator ที่โค้ดใช้ — `quietfail-check` ใช้ชั้นนี้
```sql
SELECT c.relname, opc.opcname
FROM pg_index i
JOIN pg_class c   ON c.oid = i.indexrelid
JOIN pg_opclass opc ON opc.oid = i.indclass[0];
```

**assertion** — ต้องผ่านครบ ไม่งั้น exit
1. opclass ผิด → แผน **ต้องไม่มี** `Index Scan`
2. opclass ถูก (กลุ่มควบคุม) → แผน **ต้องมี** `Index Scan`
   *ถ้าข้อนี้ไม่ผ่าน แปลว่าสภาพแวดล้อมใช้ index ไม่ได้อยู่แล้ว ข้อ 1 จึงไม่พิสูจน์อะไร*
3. catalog ต้องอ่าน opclass ออกมาได้ตรงกับที่สร้าง — อ่านไม่ได้ = **"ตรวจไม่ได้"** ไม่ใช่ "ผ่าน"
4. เวลา/buffers ของกรณี opclass ผิด ต้องใกล้เคียงกรณีไม่มี index

**⭐ I01 คือภาพสะท้อนกลับด้านของ Q01 — ต้องเขียนคู่กันในรายงาน**

| | index ถูกใช้ไหม | recall | ความเร็ว | ผู้ใช้คิดว่า |
|---|---|---|---|---|
| **Q01** | ใช้ | **ตก** | เร็วขึ้นมาก | "ระบบเร็วดี" |
| **I01** | **ไม่ถูกใช้** | **1.0 สมบูรณ์** | ไม่เร็วขึ้นเลย | "มี index แล้วนี่" |

I01 จ่ายค่า build เต็มราคา (HNSW 89 วิ · 195 MB ที่ 100k) แล้ว**ไม่ได้อะไรกลับมาเลย**
แต่คำตอบถูกต้อง 100% เพราะมันตกกลับไปเป็น exact search
→ **ไม่มีทางรู้จากคุณภาพผลลัพธ์ ต้องดู plan หรือ catalog เท่านั้น**

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/i01_opclass_mismatch.sql` |
| ตัวพิสูจน์ตัวตรวจ | `sql/i01_checker_states.sql` |
| ground truth | `groundtruth/i01.json` |
| ตัวนับคะแนน | `sql/score.sql` — **static ล้วน อ่าน catalog อย่างเดียว** |
| รันจริง | **3 รอบติด (ivfflat) + 1 รอบ (hnsw) · assertion ผ่าน 4/4 ทุกรอบ** |
| หลักฐาน | `results/i01_phase3_cycle.txt` |

**ผลที่วัดได้ — HNSW ที่ 100,000 แถว**

| | plan | build | ขนาด | 200 query | buffers | recall@10 |
|---|---|---|---|---|---|---|
| A ไม่มี index | Seq Scan | — | — | 10,988 ms | 20,000 | 1.0000 |
| **B opclass ผิด** | **Seq Scan** | **55,456 ms** | **195 MB** | 10,502 ms | 20,000 | **1.0000** |
| C opclass ถูก | Index Scan | 65,091 ms | 195 MB | **235 ms** | 949 | 0.8435 |

**จ่าย 55 วินาที + 195 MB แล้วได้กลับมาศูนย์** — plan เดิม buffers เดิม เวลาเท่าเดิมกับไม่มี index
ส่วน opclass ที่ถูกเร็วกว่า **44.7 เท่า**

**และ recall ของ B สมบูรณ์ 1.0000** เพราะตกกลับไปเป็น exact search
→ **ตรวจจากคุณภาพผลลัพธ์ไม่ได้เลย ต้องดู plan หรือ catalog เท่านั้น**

**ตัวตรวจ static แยกได้ครบ 3 สถานะ 3 รอบติด**
```
ไม่มี vector index  -> CANNOT_CHECK   (ไม่ใช่ "ผ่าน" — กฎเหล็กข้อ 10)
opclass ผิด         -> DETECTED
opclass ถูก         -> NOT_DETECTED
```
**ไม่ต้องมีข้อมูล ไม่ต้องรัน query เลย** → เข้ากลุ่ม "ยืนยันได้ → fail build ทันที" (`PROJECT.md` ข้อ 8)

**ข้อสังเกตที่ยังไม่สรุป — สัญญาณของ I04**
IVFFlat opclass ถูก 3 build ในรอบเดียวกัน ข้อมูลเดียวกัน พารามิเตอร์เดียวกัน
ได้ recall@10 = **0.7780 · 0.8020 · 0.7415** (ช่วง 0.06 = 8%)
เข้ากันได้กับ I04 (k-means สุ่ม) แต่ **ยังไม่ใช่ข้อสรุป** — I04 ต้องออกแบบการทดลองของตัวเอง

### I02 — สร้าง index ก่อนข้อมูลเป็นตัวแทน → ผลหายไปเงียบๆ

> **ชื่อเดิมในทะเบียนคือ "สร้าง IVFFlat ตอนตารางยังเล็ก" — เปลี่ยนแล้ว 2026-07-27**
> วัดแล้วพบว่า build ที่ **50 แถว** ได้ recall **1.0000**
> ส่วน build ที่ **1,000 แถว** ได้ **0.2278** — ตัวแปรไม่ใช่ "เล็กแค่ไหน" (ดู **E33**)

| | |
|---|---|
| **อาการที่เห็น** | ผลที่ควรเจอหายไปเป็นส่วนใหญ่ · ไม่มี error · ขนาด index ปกติ · เร็วปกติ · `\di` เห็น index ครบ |
| **คนมักแก้ผิด** | เพิ่ม `lists` · เพิ่ม `probes` · เปลี่ยนไป HNSW · โทษ embedding model · **รอให้ข้อมูลมากขึ้นแล้วมันจะดีเอง** (มันไม่ดีขึ้นเอง) |
| **ต้นเหตุจริง** | IVFFlat วางจุดศูนย์กลางจากข้อมูลที่มี**ตอนสร้าง index** แถวที่ใส่ทีหลังถูกจับเข้ากลุ่มเดิม ไม่มีการคำนวณใหม่ |
| **ทำไม dev ไม่เจอ** | ตอน dev ข้อมูลชุดแรกก็คือข้อมูลทั้งหมด จุดศูนย์กลางจึงตรงเสมอ |

**การออกแบบ — 4 เงื่อนไข จบที่ข้อมูลชุดเดียวกันเป๊ะ เปลี่ยนแค่ "ตอนที่ build"**

**I04 บังคับให้ต้องทำ 5 รอบ** — I04 พิสูจน์ว่าสร้าง IVFFlat ใหม่บนข้อมูลเดิมได้ recall
แกว่งถึง 0.073 การเทียบครั้งเดียวจึงแยกไม่ออกว่าต่างเพราะเงื่อนไขหรือเพราะ build ไม่นิ่ง
→ เทียบ**เป็นคู่ในรอบเดียวกัน** ตัดปัญหานี้ออก

| เงื่อนไข | build ตอนมี | กลุ่มที่เห็นตอน build | **pgvector เตือน** | recall เฉลี่ย | ช่วงแกว่ง |
|---|---|---|---|---|---|
| **A** | 50 แถว | 50 จาก 50 | **✅ เตือน** | **1.0000** | 0.0000 |
| **B** | 1,000 แถว | 50 จาก 50 | ❌ เงียบ | 0.8798 | 0.0580 |
| **C** | 100,000 แถว | 50 จาก 50 | ❌ เงียบ | 0.7682 | 0.0980 |
| **D** | 1,000 แถว | **5 จาก 50** | ❌ **เงียบ** | **0.2278** | 0.0735 |

เทียบเป็นคู่ในรอบเดียวกัน (บวก = ดีกว่า C) — **ไปทางเดียวกันทั้ง 5 รอบทุกเงื่อนไข**

```
A - C : +0.2145  +0.1860  +0.2315  +0.2430  +0.2840
B - C : +0.1000  +0.0895  +0.0915  +0.1470  +0.1300
D - C : -0.5450  -0.6295  -0.5170  -0.5525  -0.4580
```

#### ⭐ คำเตือนของ pgvector ออกผิดที่ — และสวนทางกับอันตรายจริง

NOTICE ออก**เฉพาะเมื่อ `rows < lists`** (วัดเส้นแบ่งแล้ว: ออกที่ 50 แถว ไม่ออกที่ 100)

```
NOTICE:  ivfflat index created with little data
DETAIL:  This will cause low recall.
HINT:    Drop the index until the table has more data.
```

- มันเตือน **A** ซึ่งเป็นเงื่อนไขเดียวที่ได้ recall **สมบูรณ์ 1.0000**
- มันเงียบสนิทกับ **D** ซึ่ง**ผลหายไป 77%**
- `DETAIL` เขียนว่า *This will cause low recall* — **วัดแล้วเป็นเท็จ**บนชุดข้อมูลนี้
- HNSW ที่ 50 แถวไม่เตือนอะไรเลย → คำเตือนนี้เป็นของ IVFFlat เฉพาะตัว

**เกณฑ์ที่มันใช้คือจำนวนแถว ซึ่งไม่ใช่ตัวแปรที่ทำให้พัง** ตัวแปรจริงคือ
*ตัวอย่างตอน build เป็นตัวแทนของข้อมูลสุดท้ายหรือไม่* ซึ่งจำนวนแถววัดไม่ได้เลย

#### ทำไม A ถึงได้ recall เต็ม — และทำไมห้ามสรุปว่า "build เร็วดีกว่า"

corpus ชุดนี้มี 50 กลุ่ม และเรียง id แบบสลับกลุ่ม **50 แถวแรกจึงมีครบ 50 กลุ่มพอดี
กลุ่มละ 1 แถว** k-means จึงได้จุดศูนย์กลางตรงกับกลุ่มจริงเป๊ะ

→ เป็น**คุณสมบัติของข้อมูลสังเคราะห์ชุดนี้** ไม่ใช่กฎทั่วไป
**D พิสูจน์ตรงข้ามบนข้อมูลชุดเดียวกัน** — 1,000 แถวที่เอียงแย่กว่า 50 แถวที่เป็นตัวแทน 20 เท่า

**ห้ามพูด:** "build ตอนข้อมูลน้อยดีกว่า" · "จำนวนแถวตอน build ไม่สำคัญ"
(ไม่สำคัญ**เท่ากับ**ความเป็นตัวแทน แต่ยังเป็นตัวแปรอยู่) · "คำเตือนของ pgvector ไร้ประโยชน์"
(มันถูกต้องในกรณีที่มันออกแบบมาจับ — แค่กรณีนั้นไม่ใช่กรณีที่อันตรายที่สุด)

**buffers ใกล้เคียงกันทุกเงื่อนไข** (375–526) และ **ขนาด index เท่ากันหมด 157 MB**
→ ตัวชี้วัดด้าน I/O และขนาดมองไม่เห็น fault นี้เลย เหมือน F13 ที่ buffers มองไม่เห็น

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/i02_index_built_too_early.sql` |
| ground truth | `groundtruth/i02.json` |
| ตัวนับคะแนน | `sql/score.sql` — **วัด recall สดๆ** ไม่ได้อ่าน catalog |
| รันจริง | **3 รอบติด · assertion ผ่าน 6/6 ทุกรอบ** |
| หลักฐาน | `results/i02_phase3_cycle.txt` · probe ใน `results/i02_run1.txt` · `i02_run2.txt` |
| พิสูจน์ตัวตรวจ | `sql/i02_checker_states.sql` |

**ตัวตรวจข้อนี้อ่าน catalog ไม่ได้เลย — พิสูจน์แล้วด้วย probe**

`pg_class.reltuples` ของ *ตัว index* เก็บจำนวนแถวตอน build ไว้จริง
**แต่ `ANALYZE` เขียนทับเป็นจำนวนแถวปัจจุบันทันที** และ autovacuum ทำเองเสมอ
→ ร่องรอยหายไปเองภายในไม่กี่นาที · และจำนวนแถวก็ไม่ใช่ตัวแปรที่สำคัญอยู่ดี

จึง**วัดผลลัพธ์ตรงๆ**: เทียบสิ่งที่ index คืน กับ exact search จาก `enable_indexscan = off`
(วิธีที่เอกสาร pgvector แนะนำเอง — ห้ามใช้ `DROP INDEX` หาเฉลย)

```
ไม่มี ivfflat index / ตารางเล็กเกินวัด  -> CANNOT_CHECK
recall@10 วัดสด >= 0.50                -> NOT_DETECTED   (วัดได้ 0.8200)
recall@10 วัดสด <  0.50                -> DETECTED       (วัดได้ 0.2850)
```

**เกณฑ์ 0.50 ไม่ได้ตั้งลอยๆ** — C ต่ำสุด 0.7160 · D สูงสุด 0.2580 ช่องว่างกว้างมาก
**ข้อจำกัดที่ต้องพูดตรงๆ:** บอกได้แค่ "index คืนผลแย่กว่าที่ควร" บอกไม่ได้ว่าเพราะ I02
หรือเพราะ `probes` ต่ำตาม Q04 · และเกณฑ์ผูกกับ corpus ชุดนี้ ชุดอื่นต้องวัดใหม่

---

### I03 — `CREATE INDEX` ไม่ใช้ `CONCURRENTLY` → บล็อกการเขียนทั้งตาราง

| | |
|---|---|
| **อาการที่เห็น** | ระบบเขียนไม่ได้ทั้งระบบตอน deploy · **แต่การอ่านยังปกติทุกอย่าง** |
| **คนมักแก้ผิด** | restart แอป · โทษ connection pool · rollback deploy ทั้งชุด |
| **ต้นเหตุจริง** | `CREATE INDEX` ธรรมดาถือ `ShareLock` ตลอดเวลาที่ build · ชนกับ `RowExclusiveLock` ของการเขียน |
| **ทำไม dev ไม่เจอ** | ข้อมูลเล็ก build เสร็จในวินาทีเดียว · และ staging มักทดสอบด้วย traffic อ่านอย่างเดียว |

**⭐ จุดที่ต่างจาก F05 และเป็นหัวใจของข้อนี้**

| | lock ที่ถือ | บล็อกอะไร |
|---|---|---|
| **F05** `ALTER TABLE` | `AccessExclusiveLock` | **ทุกอย่าง** รวมทั้งการอ่าน |
| **I03** `CREATE INDEX` | `ShareLock` | **เฉพาะการเขียน** — การอ่านยังทำงานปกติ |

→ ทีมที่ทดสอบ migration ด้วย read traffic **ไม่เห็นอะไรผิดเลย**
แล้วไปเจอตอน production ที่มีการเขียนจริง

**⭐ ระยะเวลาที่เขียนไม่ได้ = เวลา build ซึ่ง I05 วัดไว้แล้ว**
```
100k แถว @ maintenance_work_mem=64MB  ->  ~60 วินาที
500k แถว                              ->  603 วินาที = 10 นาที
```
**I03 กับ I05 ประกอบกัน** — `maintenance_work_mem` ต่ำไม่ได้แค่ทำให้ build ช้า
แต่ทำให้**หน้าต่างที่ระบบเขียนไม่ได้ยาวขึ้นตามไปด้วย**

**วิธีฉีด**
```
session A [role lab]:  CREATE INDEX ... USING hnsw (...)   (ไม่ใส่ CONCURRENTLY)
session B [role app]:  SELECT ...   -> ผ่านปกติ
session C [role app]:  INSERT ...   -> ค้างจนกว่า build จะเสร็จ
```

**ground truth**
```sql
SELECT a.pid, l.mode, l.granted, left(a.query,44)
FROM pg_locks l JOIN pg_stat_activity a USING (pid)
WHERE l.relation = 'qf_corpus'::regclass
ORDER BY l.granted DESC;
```
ต้องเห็น `ShareLock granted=t` ของ `CREATE INDEX`
คู่กับ `RowExclusiveLock granted=f` ของการเขียนที่รออยู่

**assertion** — ต้องผ่านครบ 4 ข้อ
1. ระหว่าง build ธรรมดา **การเขียนถูกบล็อก**
2. ระหว่าง build ธรรมดา **การอ่านต้องยังทำงาน** — ข้อนี้คือจุดที่อธิบายว่าทำไมไม่มีใครเจอ
3. `pg_locks` ต้องเห็น `ShareLock` จริง — เห็นไม่ได้ = **"ตรวจไม่ได้"** ห้ามสรุปจากเวลาอย่างเดียว
4. `CONCURRENTLY` **ต้องไม่บล็อกการเขียน** (กลุ่มควบคุม — พิสูจน์ว่าทางแก้ใช้ได้จริง)

> ⚠️ **การเขียนที่ใช้ทดสอบต้อง `ROLLBACK` เสมอ**
> ไม่งั้น `qf_corpus` ที่ล็อกไว้จะเปลี่ยนจำนวนแถว แล้ว fingerprint กับเฉลยใช้ไม่ได้
> สคริปต์ตรวจจำนวนแถวก่อน/หลังทุกครั้ง

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `faults/i03_create_index_lock.sh` + `sql/i03_build_index.sql` |
| ตัวพิสูจน์ตัวนับ | `faults/i03_scorer_states.sh` |
| ground truth | `groundtruth/i03.json` |
| ตัวนับคะแนน | `sql/score.sql` — อ่าน **สถานะสดของ `pg_locks`** |
| รันจริง | **3 รอบติด assertion ผ่าน 4/4 · ตัวนับพิสูจน์ 3 สถานะ อีก 3 รอบติด** |
| หลักฐาน | `results/i03_phase3_cycle.txt` |

**ผลที่วัดได้ 3 รอบ**

| | ระหว่าง `CREATE INDEX` ธรรมดา | ระหว่าง `CONCURRENTLY` |
|---|---|---|
| SELECT | 48 · 40 · 122 ms — **ปกติ** | 35 · 64 · 46 ms |
| INSERT | **BLOCKED ทุกรอบ** | 37 · 67 · 50 ms |
| `pg_locks` | `ShareLock` granted 3 แถว (leader + 2 worker) | — |
| corpus | คงที่ 100,000 ทุกรอบ | คงที่ |

**ตัวนับคะแนนพลิกครบ 3 สถานะ 3 รอบติด**
```
ไม่มี build ค้าง        -> NOT_DETECTED
build ค้าง + เขียนรออยู่ -> DETECTED (ShareLock 3 · เขียนรอ 1)
หลังเก็บกวาด            -> NOT_DETECTED
```

> ⚠️ **เก็บกวาดต้องเล็งเฉพาะ leader** (`backend_type = 'client backend'`)
> และใช้ `pg_cancel_backend()` ไม่ใช่ `pg_terminate_backend()`
> ฆ่า parallel worker ตรงๆ ทำให้ **postmaster รีเซ็ตทั้งคลัสเตอร์** — ดู **E29**

---

### I04 — สร้าง index ใหม่แล้วได้คำตอบไม่เหมือนเดิม

> **ชื่อเดิมในทะเบียนคือ "k-means ของ IVFFlat สุ่ม" — เปลี่ยนแล้ว 2026-07-27**
> เพราะกลุ่มควบคุมพบว่า **HNSW ที่ไม่มี k-means เลยก็แกว่งเหมือนกัน**
> ชื่อเดิมชี้ต้นเหตุแคบเกินจริง · รูปแบบเดียวกับ Q02 ที่พบว่า `WHERE` ไม่ใช่ตัวปัญหา

| | |
|---|---|
| **อาการที่เห็น** | recall ที่ validate ไว้ ไม่ตรงกับที่เจอบน production หลัง reindex · หรือรันการทดลองเดิมซ้ำแล้วได้เลขไม่เท่าเดิม |
| **คนมักแก้ผิด** | โทษว่าเครื่องมือวัดเพี้ยน · โทษว่าข้อมูลเปลี่ยน (ทั้งที่ fingerprint เดิม) · **รันซ้ำจนได้เลขที่ถูกใจแล้วใช้เลขนั้น** |
| **ต้นเหตุจริง** | การสร้าง index ไม่ reproducible — ข้อมูลเดิมเป๊ะ พารามิเตอร์เดิมเป๊ะ ได้ index คนละตัว |
| **ทำไม dev ไม่เจอ** | สร้าง index ครั้งเดียวแล้ววัดครั้งเดียว · ไม่มีใครสร้างซ้ำ 5 รอบเพื่อเทียบ |

**⭐ ข้อนี้พิเศษกว่าอีก 11 ข้อในชุด**

`EVIDENCE.md` สรุปว่า **11 จาก 12 ข้อยืนยันได้จากเอกสารทางการ** — I04 เป็นข้อเดียวที่ไม่ได้
เอกสาร pgvector ไม่รับประกันว่าสร้าง index ใหม่แล้วได้ผลเหมือนเดิม **และไม่เตือนว่าจะไม่เหมือน**
ความเงียบของเอกสารตรงนี้ คือส่วนหนึ่งของ fault

→ ต้องพิสูจน์ด้วยการรันล้วนๆ ตามกฎเหล็กข้อ 1

**การออกแบบ — ต้องมีกลุ่มควบคุม ไม่งั้นสรุปสาเหตุไม่ได้เลย**

| | คำถามที่มันตอบ |
|---|---|
| **ควบคุม ก** วัดซ้ำบน index ตัวเดิม 2 ครั้ง | ฝั่ง query นิ่งไหม ถ้าไม่นิ่ง จะโทษ build ไม่ได้ |
| **ควบคุม ข** ทำแบบเดียวกันกับ HNSW | เป็นเรื่องเฉพาะ IVFFlat หรือเปล่า |
| **ควบคุม ค** ปิด parallel build | ลำดับการใส่ข้อมูลที่ไม่แน่นอนคือต้นเหตุไหม |

**ผลที่วัดได้ — 100,000 แถว · 384 มิติ · สร้างใหม่ 5 รอบต่อการตั้งค่า · 200 query · k=10**

**ควบคุม ก ผ่าน:** วัดซ้ำบน index ตัวเดิม ได้ผลเหมือนกัน **ทุก query ทั้ง 4 การตั้งค่า**
→ ความแกว่งทั้งหมดมาจากฝั่ง build ไม่ใช่ฝั่ง query

| การตั้งค่า | recall เฉลี่ยต่ำสุด–สูงสุด | ช่วง | **query ที่เปลี่ยนคำตอบ** |
|---|---|---|---|
| **IVFFlat `probes=1`** (ค่าเริ่มต้น) | 0.7415 – 0.8145 | **9.4%** | **196 / 200** |

> ตารางนี้มาจากรอบสำรวจแรก · **รัน 3 รอบเต็มแล้วได้ 185 · 199 · 192 จาก 200**
> ช่วงแกว่ง 0.0515 · 0.0885 · 0.0720 — ตัวเลขต่างกันทุกรอบ (มันคือ fault ที่วัดอยู่พอดี)
> แต่**ข้อสรุปเชิงคุณภาพเหมือนกันทุกรอบ**: query ส่วนใหญ่เปลี่ยนคำตอบ

| **IVFFlat `probes=5`** | 1.0000 – 1.0000 | **0%** | **0 / 200** |
| HNSW `ef_search=40` (ค่าเริ่มต้น) | 0.8125 – 0.8515 | 4.6% | 132 / 200 |
| HNSW `ef_search=200` | 0.9545 – 0.9790 | 2.5% | 86 / 200 |

**⭐ ผลที่สำคัญที่สุด: ค่าเฉลี่ยซ่อนความจริงไว้เกือบทั้งหมด**

ค่าเฉลี่ยแกว่ง **9.4%** ซึ่งทีมส่วนใหญ่จะมองว่าเป็น noise ที่ยอมรับได้
แต่ราย query มี **196 จาก 200 ข้อ** ที่ได้คำตอบไม่เท่ากันข้าม build

→ **ทีมที่ดูแต่ค่าเฉลี่ยจะสรุปว่า "เสถียรพอ" ทั้งที่ผู้ใช้เกือบทุกคนได้ผลไม่เหมือนเดิม**
เป็นกลไกเดียวกับที่ Q01 พบว่า "แย่สุดต่อ query = 0.0000" ซ่อนอยู่ใต้ค่าเฉลี่ย 0.8565

**⭐ และการจูนที่พอเพียง กลบความไม่แน่นอนได้ทั้งหมด**

`probes=5` ไม่ใช่แค่ค่าเฉลี่ยนิ่ง — **ไม่มี query ไหนเปลี่ยนเลยสักข้อ** ทั้ง 5 build
เชื่อมกับ Q04 โดยตรง: `probes=5` คือค่าเดียวกับที่ Q04 พบว่าได้ recall เต็ม
→ **ค่าที่ตั้งไว้ต่ำเกิน ไม่ได้แค่ทำ recall ตก มันทำให้ผลไม่นิ่งด้วย**

#### ควบคุม ข — HNSW ก็แกว่ง ทั้งที่ไม่มี k-means เลย

ทะเบียนเดิมโทษ k-means ของ IVFFlat แต่ HNSW เป็นกราฟ **ไม่มี k-means อยู่ในนั้น**
และแกว่ง 132 จาก 200 query → **สมมติฐานเดิมอธิบายไม่ครบ**

#### ควบคุม ค — ปิด parallel build แล้วยังแกว่งทั้งคู่

สมมติฐานถัดไปที่สมเหตุสมผลที่สุด: `max_parallel_maintenance_workers = 2`
ทำให้ลำดับการใส่ข้อมูลไม่แน่นอน ซึ่งกระทบ HNSW โดยตรงเพราะกราฟขึ้นกับลำดับ

ตั้งเป็น **0** แล้ววัดใหม่ 5 build ทั้งสองแบบ · เปลี่ยนตัวแปรเดียวเท่านั้น

| | parallel เปิด | **parallel ปิด** |
|---|---|---|
| IVFFlat — query ที่เปลี่ยนคำตอบ | 196 / 200 | **200 / 200** |
| IVFFlat — ช่วงแกว่ง | 0.0730 | **0.1065** |
| HNSW — query ที่เปลี่ยนคำตอบ | 132 / 200 | **127 / 200** |
| HNSW — ช่วงแกว่ง | 0.0390 | 0.0180 |
| HNSW — เวลา build | 58 วิ | **108 วิ** (ช้าลง 1.9 เท่า) |

**ไม่ใช่เรื่อง parallel** — ปิดแล้วยังแกว่งทั้งคู่ IVFFlat ถึงขั้นเปลี่ยนครบ 200 จาก 200

**สรุปเท่าที่ข้อมูลรองรับ (ห้ามพูดเกินนี้)**

| พูดได้ | หลักฐาน |
|---|---|
| สร้าง index ใหม่แล้วคำตอบเปลี่ยน ทั้ง IVFFlat และ HNSW | 5 build × 2 ชนิด × 2 การตั้งค่า |
| ไม่ได้เกิดจากฝั่ง query | ควบคุม ก — วัดซ้ำ index เดิม ตรงกันทุก query |
| ไม่ได้เกิดจาก parallel build | ควบคุม ค — ปิดแล้วยังแกว่ง |
| จูนให้พอ กลบได้ทั้งหมด (IVFFlat) | `probes=5` เปลี่ยน 0 จาก 200 ทั้ง 5 build |

| **ห้ามพูด** | ทำไม |
|---|---|
| "k-means คือต้นเหตุ" | HNSW ไม่มี k-means แต่แกว่งเหมือนกัน |
| "HNSW แกว่งเพราะ X" | **ยังไม่ได้แยกกลไกของ HNSW** — ตัดออกได้แค่ parallel เท่านั้น |
| "ปิด parallel ทำให้แย่ลง" | 5 ตัวอย่างไม่พอสรุปทิศทาง บอกได้แค่ว่า "ไม่หาย" |

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/i04_kmeans_nondeterminism.sql` + ควบคุม ค `sql/i04_parallel_control.sql` |
| ground truth | `groundtruth/i04.json` |
| ตัวนับคะแนน | `sql/score.sql` — เทียบ `relfilenode` ที่จดไว้ กับ index ตัวปัจจุบัน |
| รันจริง | **3 รอบติด · assertion ผ่าน 5/5 ทุกรอบ** |
| หลักฐาน | `results/i04_phase3_cycle.txt` · `results/i04_parallel_control.txt` |
| พิสูจน์ตัวตรวจ | `sql/i04_checker_states.sql` |

**ตัวตรวจออกแบบยากที่สุดในชุด** — ถามว่า "ใช้ IVFFlat อยู่ไหม" ไม่ได้
เพราะทุก ivfflat index ก็สุ่มเหมือนกันหมด ตัวตรวจจะตอบ `DETECTED` ตลอดกาล
ซึ่งขัดสูตรข้อ 6 ตรงๆ (*ตัวนับที่ไม่มีวันตอบลบ = ไม่ได้วัดอะไรเลย*)

จึงตรวจ **ผลกระทบจริง** แทน: *ตัวเลข recall ที่ทีมถืออยู่ ยังอธิบาย index ตัวปัจจุบันไหม*
ใช้ `relfilenode` เป็นตัวชี้ตัวตนของ build — วัดแล้วว่าเปลี่ยนทุกครั้งที่สร้างใหม่ (5 build = 5 ค่าไม่ซ้ำ)

```
ไม่มี index / ไม่เคยจดว่าวัดบน build ไหน  -> CANNOT_CHECK
index ยังเป็น build เดิมที่วัดไว้          -> NOT_DETECTED
index ถูกสร้างใหม่หลังวัด                  -> DETECTED
```

> **"ไม่เคยจด" = `CANNOT_CHECK` ไม่ใช่ "ผ่าน"** (กฎเหล็กข้อ 10)
> ทีมที่ไม่เคยบันทึกว่า recall วัดบน build ไหน คือทีมที่เสี่ยงที่สุด
> ไม่ใช่ทีมที่ปลอดภัยที่สุด

---

### I05 — `maintenance_work_mem` ต่ำ ทำให้ build ช้าผิดปกติ

| | |
|---|---|
| **อาการที่เห็น** | `CREATE INDEX` ใช้เวลานานผิดปกติ · migration ที่ควรเสร็จในนาทีกลายเป็นสิบนาที |
| **คนมักแก้ผิด** | ลด `m` / `ef_construction` (ซึ่งทำให้ recall แย่ลงด้วย) · แบ่ง build เป็นก้อน · สรุปว่า HNSW ช้าเกินใช้จริง |
| **ต้นเหตุจริง** | กราฟ HNSW ใหญ่เกิน `maintenance_work_mem` → pgvector เปลี่ยนไป build แบบใช้ดิสก์ |
| **ทำไม dev ไม่เจอ** | ข้อมูลชุดเล็กกราฟพอดีหน่วยความจำ ไม่มีอะไรผิดปกติเลย |

**⚠️ ข้อนี้ต่างจากข้ออื่นในชุด — มันไม่เงียบสนิท**

pgvector **พิมพ์ NOTICE บอกตรงๆ** พร้อมตัวเลขว่าเกินตอนไหน
แต่ NOTICE ออกทาง **stderr** และแทบไม่มีใครอ่าน log ของ migration ที่ "ผ่าน"

→ ข้อนี้จึงเป็น **"เตือนแล้วไม่มีใครอ่าน"** ไม่ใช่ **"ไม่เตือน"**
เป็นคนละชนิดกับ Q01 / I01 ที่เงียบสนิท และควรเขียนแยกให้ชัดในรายงาน
(เทียบกับ D11 ซึ่งพบรูปแบบเดียวกันใน I02)

**วิธีฉีด** — build HNSW ตัวเดิมซ้ำที่ `maintenance_work_mem` หลายค่า
```sql
SET maintenance_work_mem = '64MB';   -- ค่าของโปรไฟล์ fragile
CREATE INDEX ON qf_corpus USING hnsw (embedding vector_cosine_ops);
```

**ground truth**
1. ข้อความ NOTICE จริงจาก stderr พร้อมจำนวน tuple ที่กราฟเริ่มไม่พอดี
2. เวลา build ที่ค่า `maintenance_work_mem` ต่างกัน บนข้อมูลชุดเดียวกัน

```
NOTICE:  hnsw graph no longer fits into maintenance_work_mem after <N> tuples
DETAIL:  Building will take significantly more time.
HINT:  Increase maintenance_work_mem to speed up builds.
```

**assertion** — ต้องผ่านครบ ไม่งั้น exit
1. ที่ค่าต่ำสุด **ต้องได้ NOTICE** — ไม่ได้ = fault ไม่เกิด หรือข้อมูลเล็กเกินไป
2. ที่ค่าสูงสุด **ต้องไม่ได้ NOTICE** (กลุ่มควบคุม)
   *ถ้าข้อนี้ไม่ผ่าน แปลว่าเพิ่มหน่วยความจำแล้วยังไม่พอ ข้อ 1 จึงไม่พิสูจน์ว่าตัวแปรคือ `maintenance_work_mem`*
3. เวลา build ที่ค่าต่ำ ต้องช้ากว่าที่ค่าสูงเกิน 1.2 เท่า

> ⚠️ **ห้ามใส่ `2>/dev/null` รอบ `CREATE INDEX` ของข้อนี้เด็ดขาด**
> NOTICE ที่เป็น ground truth ทั้งข้ออยู่ใน stderr — กลบเมื่อไหร่คือทำลายการทดลอง
> (และเคยกลบจนพลาดมาแล้วสามครั้ง ดู E26)

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `faults/i05_maintenance_work_mem.sh` + `sql/i05_build_one.sql` |
| ground truth | `groundtruth/i05.json` |
| ตัวนับคะแนน | `sql/score.sql` — **ทำนายล่วงหน้า ไม่ต้อง build เลย** |
| รันจริง | **3 รอบติด · assertion ผ่าน 3/3 ทุกรอบ** |
| หลักฐาน | `results/i05_phase3_cycle.txt` |
| พิสูจน์ตัวตรวจ | `sql/i05_checker_states.sql` → `results/i05_checker_states.txt` |

**⚠️ ข้อนี้เคยถูกนับว่าเสร็จทั้งที่ตัวตรวจไม่เคยถูกรัน — ดู E30**

ครั้งแรกมีครบทุกช่องยกเว้นแถวสุดท้าย · ผลรัน 102 บรรทัดไม่มีคำว่า `DETECTED` เลย
เพราะตัวฉีดไม่เคยเรียก `score.sql` · เจอตอน audit ก่อนส่งงาน แล้วเติมย้อนหลัง

**ตัวตรวจพลิกตรงขอบที่คำนวณล่วงหน้าพอดี** (ความจุ = MB × 443 tuples)

| `maintenance_work_mem` | ความจุที่ทำนาย | คำตอบ |
|---|---|---|
| 64MB (fragile) | 28,352 | **DETECTED** |
| **225MB** | 99,675 | **DETECTED** ← ขอบ |
| **226MB** | 100,118 | **NOT_DETECTED** ← พลิกที่ 1MB |
| 256MB | 113,408 | NOT_DETECTED |

**ทำนายได้โดยไม่ build สักครั้ง** — ทั้งสี่สถานะรวมกันใช้เวลาไม่ถึงนาที
เทียบกับ 72 วินาที**ต่อหนึ่ง build** ตอนวัดจริง นี่คือคุณค่าของตัวตรวจแบบ static

**ยังพิสูจน์ไม่ได้ 2 ทาง** (กฎเหล็กข้อ 10 — ระบุตรงๆ ไม่อ้างว่าครบ):
`CANNOT_CHECK` จาก "ไม่มีตาราง" และ "มิติไม่ใช่ 384" ต้อง DROP/ALTER `qf_corpus` ที่ล็อกไว้

**ผลที่วัดได้ — 100,000 แถว · 384 มิติ · 3 รอบ**

| `maintenance_work_mem` | NOTICE ที่ tuple ที่ | build (3 รอบ) | ช้ากว่า |
|---|---|---|---|
| **64MB** (fragile) | 28,353 · 28,360 · 28,363 | 60.7 · 70.0 · 72.7 s | **2.97–3.50×** |
| 128MB | 58,616 · 58,630 · 58,632 | 50.2 · 56.7 · 56.9 s | 2.46–2.74× |
| 256MB | **ไม่มี** | 20.4 · 20.7 · 20.7 s | 1.00× |

### ⭐ สิ่งที่ค้นพบเพิ่ม — ตัวเลขใน NOTICE เป็นค่าเชิงโครงสร้าง

จำนวน tuple ที่ NOTICE บอก **นิ่งระดับ 0.03%** ข้าม 3 รอบ
(28,353–28,363 และ 58,616–58,632) ขณะที่**เวลา build แกว่ง 20%**

→ ตรงกับกฎเหล็กข้อ 7 พอดี: ใช้ค่าเชิงโครงสร้างเป็นตัวชี้วัด ไม่ใช่นาฬิกา
ข้อนี้ตัวเลขที่ควร assert คือจำนวน tuple ไม่ใช่เวลา

**ความจุเกือบเป็นเส้นตรง** — 443 tuples/MB ที่ 64MB · 458 tuples/MB ที่ 128MB
(หน่วยความจำ 2 เท่า รับได้ 2.07 เท่า)

**index ที่ได้เท่ากันเป๊ะ 195 MB ทั้ง 9 build**
→ fault นี้เสีย **เวลา** ไม่ใช่คุณภาพ · ต่างจาก Q01 (เสีย recall) และ I01 (เสียทรัพยากรเปล่า)

### ตัวตรวจ — ทำนายจุด spill ได้โดยไม่ต้อง build

ใช้ความจุที่วัดเอง 443 tuples/MB คำนวณจากจำนวนแถวเทียบกับ `maintenance_work_mem`

```
64MB  -> DETECTED     ทำนายความจุ 28,352 tuples · ตารางมี 100,000 แถว
256MB -> NOT_DETECTED ทำนายความจุ 113,408 tuples · ตารางมี 100,000 แถว
```

**ทำนาย 28,352 · pgvector รายงานจริง 28,353–28,363 → คลาด 0.04%**

**ด่านกันการเดา:** ถ้าตารางมีมิติอื่นที่ไม่ใช่ 384 ตัวตรวจตอบ **`CANNOT_CHECK`**
เพราะค่าความจุ calibrate ไว้ที่ 384 มิติเท่านั้น — ทดสอบแล้วด้วยตาราง 128 มิติ
(กฎเหล็กข้อ 10: ไม่รู้ ต้องบอกว่าไม่รู้ ห้ามคำนวณต่อ)

---

## ชั้น Q — เรียกใช้

| ID | fault | หลักฐาน |
|---|---|---|
| **Q01** ⭐ | recall collapse — เร็วขึ้นมาก ผลดูดี แต่ของที่ควรเจอหายไป | ✅ เอกสารเขียนเอง · **วัดแล้ว ดูข้างล่าง** |
| **Q02** | เขียน `WHERE distance < x` แทน `ORDER BY ... LIMIT` | ✅ เอกสาร |
| **Q03** | filter ทำงานหลังสแกน index → **ขอ 10 ได้ 0** · ทางแก้ต้องแตะ `work_mem` ที่เอกสารไม่ได้พูดถึง | ✅ มีตัวเลขในเอกสาร |
| **Q04** | ใช้ค่า default โดยไม่เคยวัด (`probes = 1`) | ✅ วัดเองแล้ว |
| **Q06** | `LIMIT` มากกว่า `ef_search` (40) | ✅ FAQ ทางการ |

### Q01 — ผลที่วัดได้จริง ⭐ (2026-07-27)

**ที่ 100,000 แถว · 384 มิติ · 50 กลุ่ม · 200 query · cosine**
หลักฐาน `results/q01_recall_collapse_100k.txt` · สคริปต์ `sql/q01_recall_collapse.sql`

| | recall@10 | recall@100 | แย่สุด@10 | ครบ 10/10 | 200 query | เร็วขึ้น | ขนาด index |
|---|---|---|---|---|---|---|---|
| exact (ไม่มี index) | **1.0000** | **1.0000** | 1.0000 | 200/200 | 14,148 ms | 1.0× | — |
| HNSW ค่าเริ่มต้น | **0.8565** | **0.3903** | **0.0000** | 52/200 | 434 ms | **32.6×** | 195 MB |
| IVFFlat ค่าเริ่มต้น | **0.7870** | **0.7793** | **0.0000** | 100/200 | 317 ms | **44.6×** | 157 MB |

**ไม่ได้จูนอะไรเลย** — สร้าง index โดยไม่ระบุพารามิเตอร์ และ query ด้วยค่าเริ่มต้น
(`ef_search=40` · `probes=1`) เพราะนั่นคือสิ่งที่คนทำจริง

**ตัวเลขที่ควรอยู่ในบรรทัดเรซูเม่:**

> เร็วขึ้น **44.6 เท่า** แลกกับ recall@10 ที่ **0.7870**
> = ผลลัพธ์ที่ควรเจอ **หายไป 21%** โดยไม่มี error ใดๆ

**สามอย่างที่น่ากลัวกว่าค่าเฉลี่ย:**

1. **`แย่สุด@10 = 0.0000` ทั้งสองแบบ** — มี query ที่ผลถูกต้อง **หายเกลี้ยงทั้ง 10 อันดับ**
   ค่าเฉลี่ย 0.79 ซ่อนกรณีที่ผู้ใช้ค้นแล้วไม่เจออะไรที่ถูกเลยไว้ข้างใน
2. **HNSW ครบ 10/10 แค่ 52 จาก 200 query** — 74% ของ query ได้คำตอบไม่ครบ
3. **HNSW recall@100 = 0.3903 แย่กว่า recall@10 มาก** — หายไป 61%
   เพราะ `ef_search=40` เล็กกว่า `LIMIT 100` → **นี่คือกลไกของ Q06 โผล่มาในผลของ Q01**
   ขอ 100 แต่ candidate list มีแค่ 40 จะเอาที่ถูก 100 อันมาจากไหน

**IVFFlat ต่างจาก HNSW ตรงรูปทรงของความเสียหาย** — IVFFlat recall@10 กับ @100
ใกล้เคียงกัน (0.787 / 0.779) เพราะ `probes=1` ตัดทิ้งทั้งกลุ่มตั้งแต่ต้น
ส่วน HNSW ยิ่งขอเยอะยิ่งแย่ลง เพราะเพดานอยู่ที่ขนาด candidate list

**ที่ 500,000 แถว — พารามิเตอร์เดิมทุกตัว**
หลักฐาน `results/q01_recall_collapse_500k.txt`

| | recall@10 | recall@100 | แย่สุด@10 | ครบ 10/10 | 200 query | เร็วขึ้น | ขนาด index |
|---|---|---|---|---|---|---|---|
| exact (ไม่มี index) | **1.0000** | **1.0000** | 1.0000 | 200/200 | 53,281 ms | 1.0× | — |
| HNSW ค่าเริ่มต้น | **0.5885** | **0.3871** | **0.0000** | **2/200** | 455 ms | **117.1×** | 977 MB |
| IVFFlat ค่าเริ่มต้น | **0.7820** | **0.7857** | **0.0000** | 108/200 | 1,032 ms | 51.6× | 782 MB |

### ⭐⭐ ผลที่สำคัญที่สุดของทั้งโปรเจค — index สองแบบเสื่อมตามขนาดข้อมูลไม่เหมือนกัน

| | 100k | 500k | เปลี่ยนไป |
|---|---|---|---|
| **HNSW** recall@10 | 0.8565 | **0.5885** | **ตกลง 31%** |
| **HNSW** ครบ 10/10 | 52/200 | **2/200** | **แทบไม่เหลือ** |
| **HNSW** เร็วขึ้น | 32.6× | **117.1×** | เร็วขึ้นอีก |
| IVFFlat recall@10 | 0.7870 | 0.7820 | **แทบไม่ขยับ** |
| IVFFlat เร็วขึ้น | 44.6× | 51.6× | ใกล้เคียงเดิม |

**HNSW ยิ่งข้อมูลเยอะยิ่งพัง ส่วน IVFFlat นิ่ง** — และสาเหตุอยู่ที่รูปแบบของค่าเริ่มต้น:

- `hnsw.ef_search = 40` เป็น **ค่าสัมบูรณ์** — 40 candidate จาก 100k คือ 1 ใน 2,500
  แต่ 40 จาก 500k คือ 1 ใน 12,500 · ข้อมูลโต 5 เท่า สัดส่วนที่ค้นจริงหดลง 5 เท่า
- `ivfflat.probes = 1` เป็น **ค่าสัมพัทธ์** — 1 จาก `lists=100` คือ 1% เสมอ
  ไม่ว่าข้อมูลจะโตแค่ไหน สัดส่วนที่ค้นจริงเท่าเดิม

**ที่อันตรายที่สุดคือ HNSW เร็วขึ้นพร้อมกับที่มันพังลง** — 32.6× → 117.1×
ทีมที่ดูแต่ latency จะเห็นว่า "ระบบดีขึ้นเรื่อยๆ ตอนข้อมูลโต"
ทั้งที่ recall กำลังพังลงเงียบๆ ในเวลาเดียวกัน

**และมันตอบคำถามที่ L01 ถูกตัดไป** — Q01 สองขนาดให้หลักฐานบางส่วนแล้วว่า
พารามิเตอร์ที่เหมาะกับข้อมูลขนาดหนึ่ง ไม่เหมาะกับอีกขนาดหนึ่ง
โดยไม่ต้องรันครบ 4 ขนาดตามที่ L01 ต้องการ (ดู D15)

**ต้นทุนที่ต้องบันทึกด้วย:** HNSW build ที่ 500k ใช้ **603 วินาที (10 นาที)**
เทียบกับ 89 วินาทีที่ 100k = โตขึ้น 6.7 เท่าจากข้อมูล 5 เท่า
ส่วน IVFFlat ใช้ 7.1 วินาที = **ต่างกัน 85 เท่า**

**หลักฐานว่า corpus ทำซ้ำได้จริง:** `corpus_fingerprint_first5k` ของรอบ 500k
เท่ากับรอบ 100k เป๊ะ (`5a32cba5…`) เพราะ 5,000 แถวแรกถูกสร้างด้วยลำดับเดียวกัน

---

### เฟส 2.5 — Q01 บน Qdrant: "นี่มันบั๊กของ pgvector หรือเปล่า"

หลักฐาน `results/q01_qdrant_cross_engine.txt` · Qdrant 1.12.4 · 100,000 แถวชุดเดียวกัน

**สิ่งที่คุมให้เหมือนกันทุกอย่าง ตัวแปรเดียวที่ต่างคือ engine:**
corpus ส่งออกทีละแถวจาก `qf_corpus` · ชุด query fingerprint เดิม ·
**เฉลยใช้ `qf_truth` ที่มาจาก exact search ของ PostgreSQL** · สูตร recall เดียวกันทุกตัวอักษร

**ตัวสอบทานที่ทำให้เทียบกันได้จริง:** Qdrant ค้นแบบ `exact` ได้ **recall@10 = recall@100 = 1.0000**
เทียบกับเฉลยของ PostgreSQL → ข้อมูลส่งไปครบทุกจุด และทั้งสอง engine ใช้ระยะทางเดียวกัน
ถ้าข้อนี้ไม่ผ่าน ตัวเลขที่เหลือไม่มีความหมายเลย

| engine (ef เท่ากันสองแถวแรก) | recall@10 | recall@100 |
|---|---|---|
| **pgvector** HNSW `ef_search=40` | **0.8565** | **0.3903** |
| **Qdrant** HNSW `hnsw_ef=40` | **0.9945** | **0.9895** |
| Qdrant HNSW ค่าเริ่มต้นของ Qdrant เอง | 1.0000 | 0.9996 |

### ข้อสรุปที่ตอบได้จริง — และที่ตอบไม่ได้

**✅ ตอบได้: recall loss ไม่ใช่บั๊กของ pgvector**
Qdrant ก็เสีย recall เหมือนกันเมื่อใช้ความพยายามค้นหาเท่ากัน —
`0.9945` ไม่ใช่ `1.0` · แย่สุดต่อ query = **0.90** · ที่ k=100 ครบทุกอันแค่ **69 จาก 200 query**
การค้นแบบประมาณทำผลลัพธ์หายจริงในทั้งสอง engine

**⛔ ตอบไม่ได้ และห้ามเขียนว่าตอบได้: "ANN ทุกตัวพังเท่ากัน"**
ขนาดของความเสียหายต่างกันมหาศาล — pgvector recall@100 ตกเหลือ 0.39
ส่วน Qdrant ที่ ef เท่ากันยังได้ 0.99

**สาเหตุที่ต่างกันคือ `ef_search < LIMIT` ซึ่งเป็นพฤติกรรมเฉพาะของ pgvector**
- pgvector: `ef_search=40` กับ `LIMIT 100` → candidate list เล็กกว่าจำนวนที่ขอ
  จะเอาคำตอบที่ถูก 100 อันมาจากไหน → recall@100 พังยับ (**นี่คือ Q06**)
- Qdrant: ตั้ง `hnsw_ef=40` กับ `limit=100` แล้ว recall@100 ยังได้ 0.9895
  แปลว่า **Qdrant ไม่ปล่อยให้ ef ต่ำกว่า limit มาตัดคุณภาพทิ้ง**

**ประโยคที่เขียนลงรายงานได้แบบเถียงไม่ได้:**
> การค้นแบบประมาณทำให้ผลลัพธ์หายจริงในทั้งสอง engine — **มันคือธรรมชาติของ ANN**
> แต่ **ขนาด**ของความเสียหายถูกกำหนดโดยค่าเริ่มต้นที่ผู้พัฒนาเลือกให้
> และโดยพฤติกรรมเมื่อ `ef < k` ซึ่งเป็นเรื่องของแต่ละ engine ไม่ใช่ของ ANN

**ผลข้างเคียงที่มีค่าไม่แพ้กัน:** Qdrant ที่ค่าเริ่มต้นล้วนๆ **ไม่สร้าง HNSW เลย**
ที่ขนาดนี้ (`indexed_vectors_count: 0` · `status: green`) → ดู **E24** และ **U04**
ต้องลด `indexing_threshold` เพื่อบังคับให้สร้าง index ก่อนถึงจะวัดได้
โดย `m=16` และ `ef_construct=100` ยังเป็นค่าเริ่มต้นของ Qdrant ไม่ได้แตะ

---

**ของแถมที่ยืนยัน I05 ไปในตัว** — ตอน build HNSW ได้ NOTICE จริงบน 0.8.5:
```
NOTICE:  hnsw graph no longer fits into maintenance_work_mem after 28365 tuples
DETAIL:  Building will take significantly more time.
HINT:  Increase maintenance_work_mem to speed up builds.
```
ตรงกับที่ `EVIDENCE.md` เขียนไว้ทุกตัวอักษร และได้ตัวเลขจริงมาด้วย (28,365 tuples
จาก 100,000 = `maintenance_work_mem` 64MB รับได้แค่ 28%)
build ใช้เวลา **89.4 วินาที** ส่วน IVFFlat แค่ **2.2 วินาที** = ต่างกัน 40 เท่า

---

### Q02 — เขียน query ผิดรูป → index ไม่ถูกใช้ ทั้งที่มี index อยู่

| | |
|---|---|
| **อาการที่เห็น** | ช้าเหมือนไม่มี index ทั้งที่ `\di` เห็น index อยู่ และ opclass ถูกทุกอย่าง |
| **คนมักแก้ผิด** | rebuild index · เปลี่ยน opclass · จูน `m` / `ef_construction` · เพิ่ม RAM |
| **ต้นเหตุจริง** | รูปแบบ query ไม่ตรงกับที่เอกสารกำหนด |
| **ทำไม dev ไม่เจอ** | ทุกรูปแบบ **คืนผลลัพธ์ได้หมด ไม่มี error** ต่างกันแค่เร็วช้า |

**⭐ ต่างจาก I01 ที่อาการเหมือนกันเป๊ะ**
I01 ผิดที่ **ตอนสร้าง index** (opclass) · Q02 ผิดที่ **ตอนเขียน query** โดย index ถูกทุกอย่าง
→ เห็น Seq Scan ทั้งที่มี index เหมือนกัน **แต่แก้คนละที่** ต้องแยกให้ออก

**เอกสารกำหนดไว้ 3 อย่าง** ผิดข้อใดข้อหนึ่งก็ใช้ index ไม่ได้
1. ต้องมี `ORDER BY` **และ** `LIMIT`
2. `ORDER BY` ต้องเป็นผลของ distance operator **โดยตรง**
3. ต้องเรียง **จากน้อยไปมาก**

**ground truth** — อ่าน plan node ที่สแกนตารางเป้าหมาย (ระบุชื่อตาราง ไม่ใช่ค้นคำ — E25)

**assertion** — ต้องผ่านครบ 5 ข้อ
1. รูปแบบที่ถูกต้อง **ต้องใช้ index** (กลุ่มควบคุม — ถ้าตกแปลว่าสภาพแวดล้อมมีปัญหา)
2. `WHERE` threshold อย่างเดียว **ต้องไม่ใช้ index**
3. ต้องมีอย่างน้อย 2 รูปแบบที่เขียนผิดแล้ว index ใช้ไม่ได้
4. buffers ของรูปแบบผิดต้องมากกว่ารูปแบบถูกอย่างน้อย 5 เท่า
5. **ต้องไม่มี error เลยสักรูปแบบ** — ถ้ามี error แปลว่าไม่เงียบ

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/q02_wrong_query_shape.sql` |
| ตัวพิสูจน์ตัวตรวจ | `sql/q02_checker_states.sql` |
| ground truth | `groundtruth/q02.json` |
| ตัวนับคะแนน | `sql/score.sql` — อ่าน **`pg_stat_statements`** |
| หลักฐาน | `results/q02_phase3_cycle.txt` |

### ผลที่วัดได้ — 6 รูปแบบ บน index เดียวกัน

| | เขียนแบบ | index | plan node | ได้กี่แถว | ms | buffers |
|---|---|---|---|---|---|---|
| **A** | `ORDER BY <=> ASC + LIMIT` | ✅ ใช้ | Index Scan | 10 | 1.3 | **947** |
| **B** | `WHERE <=> < 0.5` อย่างเดียว | ❌ | Seq Scan | **2,000** | 34.1 | **20,005** |
| **C** | `WHERE <=> < 0.5` + `ORDER BY` + `LIMIT` | ✅ ใช้ | Index Scan | 10 | 0.7 | 947 |
| **D** | `ORDER BY <=> DESC + LIMIT` | ❌ | Seq Scan | 10 | 46.4 | 20,005 |
| **E** | `ORDER BY (<=>) + 0 + LIMIT` | ❌ | Seq Scan | 10 | 46.8 | 20,005 |
| **F** | `ORDER BY <=> ASC` ไม่มี `LIMIT` | ❌ | Seq Scan | **100,000** | 79.8 | 20,005 |

**buffers ต่างกัน 21 เท่า · ไม่มี error สักรูปแบบ**

### 🔍 สิ่งที่ค้นพบแล้วขัดกับชื่อของ fault เอง

**C ใช้ index ได้** → **`WHERE` ไม่ใช่ตัวปัญหา** การขาด `ORDER BY` + `LIMIT` ต่างหาก

ชื่อเดิม "เขียน `WHERE` แทน `ORDER BY`" จึงชี้เป้าคลาดไปนิด
ใส่ `WHERE` threshold เพิ่มได้โดย index ยังทำงาน ตราบใดที่ยังมี `ORDER BY` + `LIMIT`

### 🔗 รูปแบบ E เชื่อมกับ Q03 โดยตรง

`ORDER BY (embedding <=> q) + 0` คือ **สิ่งเดียวกัน** กับที่ `EVIDENCE.md` ระบุว่า
ต้องใส่ใน `ORDER BY` ชั้นนอกบน PostgreSQL 17+ เพื่อให้ iterative scan ของ Q03 ทำงาน

→ **ทริกที่แก้ Q03 คือทริกที่ทำให้ index ใช้ไม่ได้ใน Q02**
ทางแก้ของ fault หนึ่งคือวิธีฉีดของอีก fault หนึ่ง — ต้องเขียนคู่กันในรายงาน

### ตัวตรวจ — หลักฐานชนิดที่สามของโปรเจค

อ่าน **query ที่ระบบเคยรันจริง** จาก `pg_stat_statements`
ไม่ใช่ catalog และไม่ใช่สถานะสด · ตรวจได้โดยไม่ต้องเข้าถึงซอร์สโค้ดของแอป

```
ยังไม่มี query ที่ใช้ vector operator  -> CANNOT_CHECK
มีแต่รูปแบบที่ถูกต้อง                  -> NOT_DETECTED
มีรูปแบบที่ขาด ORDER BY / LIMIT       -> DETECTED
```

> ⚠️ **ข้อจำกัดที่ต้องระบุตรงๆ** — ตัวตรวจอ่านข้อความ query จึงจับได้แค่ชั้น
> "ขาด `ORDER BY` หรือ `LIMIT`" กับ "เรียง `DESC`"
> **รูปแบบ E ที่ห่อ operator ด้วยนิพจน์ ตรวจจากข้อความไม่ได้ ต้องดู plan**
> ห้ามอ้างว่าครอบคลุมทุกแบบ (กฎเหล็กข้อ 10)

---

### Q03 — filter ทำงานหลังสแกน index → ขอ 10 ได้ 0

| | |
|---|---|
| **อาการที่เห็น** | query คืนแถวน้อยกว่า `LIMIT` หรือ **ไม่คืนเลย** ทั้งที่มีแถวเข้าเงื่อนไขเป็นพัน · ไม่มี error · plan บอกว่าใช้ index ถูกต้อง |
| **คนมักแก้ผิด** | เพิ่ม `LIMIT` (ยิ่งขอยิ่งไม่ได้) · สร้าง index ใหม่ · เพิ่ม btree บนคอลัมน์ filter (ไม่เกี่ยว) · **สรุปว่าไม่มีข้อมูลตรงเงื่อนไข** |
| **ต้นเหตุจริง** | HNSW คืน candidate ตาม `ef_search` **ก่อน** แล้ว filter ค่อยคัดทีหลัง — ที่รอดจึงน้อยกว่าที่ขอ |
| **ทำไม dev ไม่เจอ** | ตอน dev ข้อมูลน้อย filter ตรงแทบทุกแถว candidate จึงรอดหมด |

**⚠️ ต้องแยกจาก Q06 ให้ขาด** — Q06 คือ *ขอเกิน `ef_search` แล้วได้ไม่ครบ*
ส่วน Q03 เพดานคือจำนวน candidate ที่ **รอดจาก filter** ซึ่งน้อยกว่านั้นมาก

รอบสำรวจแรกใช้ `LIMIT 40` กับ `ef_search=40` ซึ่งเป็น**ขอบของ Q06 พอดี** แยกไม่ออก
→ ส่วนหลักจึงใช้ **`LIMIT 10`** ซึ่งอยู่ในเขตปลอดภัยของ Q06 ชัดเจน
(Q06 วัดแล้วว่าขอ 39–40 ยังได้ครบ) แถวที่หายจึงเป็นผลของ filter ล้วนๆ

**ผลที่วัดได้ — 100,000 แถว · `ef_search=40` · `LIMIT 10` · ทุกระดับมีแถวเข้าเงื่อนไขเกิน 10**

| filter ตรง | แถวที่เข้าเงื่อนไข | ขอ | **ได้จาก index** | เฉลย | buffers |
|---|---|---|---|---|---|
| 100% | 100,000 | 10 | 10 | 10 | 947 |
| 50% | 50,000 | 10 | 10 | 10 | 954 |
| 20% | 20,000 | 10 | 10 | 10 | 966 |
| **10%** | 10,000 | 10 | **2** | 10 | 977 |
| 5% | 5,000 | 10 | **1** | 10 | 977 |
| **2%** | 2,000 | 10 | **0** | 10 | 977 |
| 1% | 1,000 | 10 | **0** | 10 | 977 |
| 0.1% | 100 | 10 | **0** | 10 | 977 |

**หน้าผาอยู่ระหว่าง 20% กับ 10%** · ตั้งแต่ **2% ลงไปได้ศูนย์แถว**
ทั้งที่มีแถวเข้าเงื่อนไข 2,000 แถว และ **ใช้ index ทุกกรณี ไม่มี error สักครั้ง**

เอกสารบอกว่า filter 10% กับ `ef_search=40` เหลือราว 4 แถว — วัดเองที่ `LIMIT 10` ได้ **2**
อันดับเดียวกัน แต่ตัวเลขต้องมาจากการวัดเอง (กฎเหล็กข้อ 2)

#### ⭐ ค่าตั้งต้นของทางแก้ต่ำเกินไปมาก และไม่มีอะไรบอกว่ายังไม่พอ

> 🔴 **แก้ข้อสรุป 2026-08-01 (E42)** — หัวข้อนี้เคยชื่อ *"ทางแก้ที่เอกสารบอก
> ไม่ทำงานเลยจนกว่าจะแตะค่าที่เอกสารไม่ได้พูดถึง"* ซึ่ง**ไม่ถูกต้อง**
>
> `hnsw.scan_mem_multiplier` เป็น **ตัวคูณของ `work_mem`** ตามที่เอกสารนิยามไว้เอง
> ตารางข้างล่างหยุดที่ ×8 ซึ่งไกลไม่พอ · ที่ **×32 ได้ครบ 40 แถว** โดยไม่แตะ `work_mem`
> `results/q03_multiplier_probe.txt`
>
> **ที่ยังยืน:** ค่าเริ่มต้น (×1) ได้ **5 จาก 40** และไม่มีสัญญาณใดบอกว่ายังหมุนไม่พอ

ที่ filter ตรง 1% · ขอ 40 แถว

| ขั้น | เปลี่ยนอะไร | ได้ | buffers |
|---|---|---|---|
| 1 | ค่าเริ่มต้นทั้งหมด | 0 | 977 |
| 2 | เปิด `hnsw.iterative_scan = relaxed_order` | **0** | 1,743 |
| 3 | `+ max_scan_tuples` 20000 → 200000 | **0** | 1,743 |
| 4 | `+ scan_mem_multiplier` 1 → 8 | **0** | 3,035 |
| 5 | **`+ work_mem` 64kB → 4MB** | **40** | 27,145 |
| 6 | `strict_order` + `work_mem` 4MB | 40 | 28,212 |

**ตัวที่ปลดล็อกคือ `work_mem`** ซึ่งเป็นค่าทั่วไปของ PostgreSQL **ไม่ใช่พารามิเตอร์ของ pgvector**
และไม่มีอยู่ในรายการข้อจำกัด 4 ข้อที่จดมาจากเอกสาร

**ขั้น 2–4 จ่าย I/O เพิ่มโดยไม่ได้แถวเพิ่มแม้แต่แถวเดียว** — buffers 977 → 1,743 → 3,035
คนที่จูนตามเอกสารจะเห็นระบบช้าลง ผลยังเป็นศูนย์ แล้วสรุปว่า iterative scan ใช้ไม่ได้

**ราคาของทางแก้ที่ได้ผล: buffers 27,145 เทียบกับ 977 = แพงขึ้น 28 เท่า**

#### ยืนยันข้อ 4 ของ `EVIDENCE.md` ด้วยโครงสร้าง plan

บน PG 17.10 `ORDER BY d` ชั้นนอกของ materialized CTE **ไม่เกิด Sort node เลย**
(planner ถือว่าเรียงมาแล้ว) · ใส่ `ORDER BY d + 0` แล้ว **Sort node โผล่**

จำเป็นเมื่อใช้ `relaxed_order` ซึ่งไม่รับประกันลำดับ — อ่านจากโครงสร้าง ไม่ใช่จากเอกสาร (กฎเหล็กข้อ 6)

> **`+ 0` คือทริกเดียวกับรูปแบบ E ของ Q02 ที่ทำให้ index ใช้ไม่ได้**
> ทางแก้ของ fault หนึ่ง คือวิธีฉีดของอีก fault หนึ่ง

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/q03_filter_after_scan.sql` |
| ground truth | `groundtruth/q03.json` |
| ตัวนับคะแนน | `sql/score.sql` — **ไม่ต้องรู้เฉลย** เทียบจำนวนแถวที่เข้าเงื่อนไขกับที่ได้คืนมา |
| รันจริง | **3 รอบติด · assertion ผ่าน 6/6 ทุกรอบ** |
| หลักฐาน | `results/q03_phase3_cycle.txt` · `results/q03_run1.txt` |
| พิสูจน์ตัวตรวจ | `sql/q03_checker_states.sql` |

**ตัวตรวจข้อนี้ง่ายที่สุดในบรรดาข้อที่ต้องวัดสด** — ไม่ต้องมีเฉลย ไม่ต้อง exact search
แค่ถามว่า *มีแถวเข้าเงื่อนไขเกิน k แต่ได้คืนไม่ถึง k หรือเปล่า*

```
ไม่มีตาราง / ไม่มี hnsw index          -> CANNOT_CHECK
limit_k >= ef_search                   -> CANNOT_CHECK  (แยกจาก Q06 ไม่ออก)
แถวเข้าเงื่อนไขไม่เกิน k                -> CANNOT_CHECK  (ได้ไม่ครบก็ไม่ผิด)
ขอ 10 ได้ 3 ทั้งที่เข้าเงื่อนไข 1,000   -> DETECTED
ขอ 10 ได้ครบ 10                        -> NOT_DETECTED
```

**ตัวตรวจปฏิเสธที่จะตอบเมื่อแยกจาก Q06 ไม่ได้** — เงื่อนไข `limit_k < ef_search`
ถูกบังคับในโค้ด ไม่ใช่แค่เขียนเตือนไว้

---

### Q04 — ใช้ค่า default โดยไม่เคยวัด → recall ต่ำแบบเงียบ

| | |
|---|---|
| **อาการที่เห็น** | recall ต่ำตั้งแต่วันแรก · **ไม่มีใครรู้ เพราะไม่เคยวัด** |
| **คนมักแก้ผิด** | เปลี่ยน embedding model · เพิ่มข้อมูล · โทษว่า ANN ใช้ไม่ได้จริง |
| **ต้นเหตุจริง** | ค่าเริ่มต้นของ pgvector ต่ำกว่าที่เอกสารของตัวเองแนะนำ |
| **ทำไม dev ไม่เจอ** | ไม่มีอะไรผิดปกติให้เห็น · ต้องมีเฉลยถึงจะรู้ว่าหายไปเท่าไหร่ |

**⭐ ต่างจาก Q01 ที่วัดของเดียวกัน แต่ถามคนละคำถาม**
Q01 ถาม *"index ทำผลหายไปเท่าไหร่"* → ปัญหาของ **ANN**
Q04 ถาม *"ที่หายไปนั้น เกิดจากค่าที่ไม่มีใครแตะกี่ %"* → ปัญหาของ **ค่าที่ผู้พัฒนาเลือกให้**

**สูตรจากเอกสาร** ที่ 100,000 แถว: `lists = rows/1000 = 100` → `probes = sqrt(lists) = 10`
**ค่าเริ่มต้นคือ 1** — ต่ำกว่าคำแนะนำของผู้พัฒนาเอง **10 เท่า**

**assertion** — ต้องผ่านครบ 5 ข้อ
1. ค่าเริ่มต้นต้องแย่กว่าค่าที่เอกสารแนะนำอย่างมีนัย
2. สแกนทุกกลุ่ม (`probes = lists`) ต้องได้ recall ≥ 0.99 (กลุ่มควบคุม — พิสูจน์ว่าตัวแปรคือ `probes` จริง)
3. HNSW ค่าเริ่มต้นต้องแย่กว่าค่าที่สูงกว่า
4. **การแลกต้องมีจริง** — ค่าสูงต้องช้ากว่าค่าต่ำ ไม่งั้นค่าเริ่มต้นไม่มีเหตุผลอยู่เลย
5. วัดได้ครบทุกค่า ไม่มี error → ค่าที่แย่ก็ยังทำงานได้ปกติ

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/q04_default_params.sql` |
| ตัวพิสูจน์ตัวตรวจ | `sql/q04_checker_states.sql` |
| ground truth | `groundtruth/q04.json` |
| ตัวนับคะแนน | `sql/score.sql` — **static: อ่าน `lists` จาก `reloptions` เทียบกับ `probes`** |
| หลักฐาน | `results/q04_phase3_cycle.txt` |

### IVFFlat — `lists = 100` ตามสูตร ไล่ค่า `probes`

| `probes` | | recall@10 | แย่สุด | ครบ 10/10 | 200 query |
|---|---|---|---|---|---|
| **1** | **ค่าเริ่มต้น** | **0.7370** | 0.0000 | 78/200 | 277 ms |
| 2 | | 0.9525 | 0.4000 | 163/200 | 308 ms |
| **5** | | **1.0000** | **1.0000** | **200/200** | 495 ms |
| 10 | **เอกสารแนะนำ** | 1.0000 | 1.0000 | 200/200 | 1,101 ms |
| 20 | | 1.0000 | 1.0000 | 200/200 | 1,979 ms |
| 50 | | 1.0000 | 1.0000 | 200/200 | 5,854 ms |
| 100 | ทุกกลุ่ม = exact | 1.0000 | 1.0000 | 200/200 | 11,146 ms |

### 🔍 ผลที่ขัดกับคำแนะนำของเอกสารเอง

**`probes = 5` ได้ recall สมบูรณ์แล้ว** — เอกสารแนะนำ 10 ซึ่ง**ช้ากว่า 2.2 เท่าโดยไม่ได้ recall เพิ่มเลย**

| | recall@10 | เวลา | สรุป |
|---|---|---|---|
| `probes=1` (เริ่มต้น) | 0.7370 | 277 ms | **หาย 26% เพื่อประหยัดเวลา 1.8 เท่า** |
| `probes=5` (วัดเอง) | **1.0000** | 495 ms | จุดที่คุ้มที่สุดบนข้อมูลชุดนี้ |
| `probes=10` (เอกสาร) | 1.0000 | 1,101 ms | ปลอดภัยเกินจำเป็น |

→ **ทั้งค่าเริ่มต้นและคำแนะนำในเอกสาร ต่างก็ไม่ใช่คำตอบที่ถูกสำหรับข้อมูลชุดนี้**
คำตอบที่ถูกมาจากการวัดเท่านั้น — ซึ่งคือวิทยานิพนธ์ของ QuietFail ตรงตัว

> **ตัวเลขในตารางมาจากรอบแรก · รัน 3 รอบแล้วค่าที่ `probes=1` แกว่ง**
> `0.7370 · 0.7820 · 0.7910 · 0.8125` ข้าม 4 การ build = แกว่ง 10%
> เพราะ IVFFlat สร้าง k-means ใหม่ทุกครั้ง — **สัญญาณของ I04 อีกครั้ง** (ดู E27)
> ห้ามอ้างเป็นผลของ I04 จนกว่าจะทำ I04 จริง
>
> **ข้อสรุปเชิงคุณภาพไม่เปลี่ยน:** `probes=5` ได้ `1.0000` ทุกรอบ · ค่าเริ่มต้นแย่กว่ามากทุกรอบ

### HNSW — ไล่ค่า `ef_search` (วัดที่ k=10 เพื่อแยกจาก Q06)

| `ef_search` | | recall@10 | ครบ 10/10 | 200 query |
|---|---|---|---|---|
| **40** | **ค่าเริ่มต้น** | **0.8510** | 51/200 | 202 ms |
| 80 | | 0.9405 | 132/200 | 241 ms |
| 160 | | 0.9795 | 171/200 | 511 ms |
| 320 | | 0.9860 | 183/200 | 389 ms |

**แย่สุดต่อ query = 0.0000 ทุกค่า แม้ที่ `ef_search=320`**
→ เพิ่ม `ef_search` ช่วยค่าเฉลี่ย แต่**ยังมี query ที่ได้ผลถูกศูนย์อันอยู่ดี**

> หมายเหตุ: `ef_search=320` วัดได้ 389 ms เร็วกว่า `ef_search=160` ที่ 511 ms
> เป็นความแกว่งของเวลา ไม่ใช่แนวโน้ม — บันทึกไว้ตรงๆ ตามกฎเหล็กข้อ 7

### เป็นปัญหาร่วมของวงการ ไม่ใช่ของ pgvector

ชั้น 3 ยืนยันแล้วว่า **FAISS ตั้ง `size_t nprobe = 1;` ในซอร์สโค้ด** และ
**OpenSearch ตั้ง `nprobes` เริ่มต้น 1** เหมือนกัน (ดู `EVIDENCE.md` และ D16)
→ ต่างจาก Q06 ซึ่งเป็นของ pgvector เฉพาะตัว

---

### Q06 — `LIMIT` มากกว่า `ef_search` → ขอ 100 ได้ 40

| | |
|---|---|
| **อาการที่เห็น** | ขอ 100 อันดับแรก ได้กลับมา 40 · **ไม่มี error ไม่มี warning** |
| **คนมักแก้ผิด** | เขียนโค้ดวนขอเพิ่ม · โทษว่าข้อมูลมีไม่พอ · เพิ่ม `LIMIT` ให้มากขึ้นอีก (ยิ่งไม่ช่วย) |
| **ต้นเหตุจริง** | HNSW คืนผลได้ไม่เกินขนาด candidate list ซึ่ง `hnsw.ef_search` กำหนด — ค่าเริ่มต้น **40** |
| **ทำไม dev ไม่เจอ** | ตอนทดสอบมักใช้ `LIMIT 10` ซึ่งต่ำกว่า 40 จึงได้ครบเสมอ |

**⭐ ต่างจาก Q01 คนละชนิด — ต้องเขียนแยกในรายงาน**

| | ได้กี่แถว | คุณภาพ |
|---|---|---|
| **Q01** | ครบ k | บางแถวไม่ใช่คำตอบที่ถูก |
| **Q06** | **ไม่ครบ k ตั้งแต่แรก** | สิ่งที่ได้มาถูก แต่มาไม่ครบ |

**วิธีฉีด**
```sql
SET hnsw.ef_search = 40;                      -- ค่าเริ่มต้นอยู่แล้ว ไม่ต้องตั้งด้วยซ้ำ
SELECT id FROM qf_corpus ORDER BY embedding <=> $1 LIMIT 100;   -- ได้ 40 แถว
```

**ground truth — นับจำนวนแถวที่ได้จริง ไม่ต้องใช้เฉลยด้วยซ้ำ**
ส่วนนี้ไม่ได้วัดคุณภาพ วัดแค่ "ขอ k ได้กี่แถว" ซึ่งเป็นข้อเท็จจริงล้วน

**assertion** — ต้องผ่านครบ 5 ข้อ
1. ขอ 100 ที่ `ef_search=40` **ต้องได้ไม่ครบ**
2. ขอ 40 ที่ `ef_search=40` **ต้องได้ครบ** (กลุ่มควบคุม — พิสูจน์ว่าพังเฉพาะตอน k > ef)
3. `ef_search=200` ขอ 100 **ต้องได้ครบ** (กลุ่มควบคุม — ทางแก้ใช้ได้จริง)
4. **ต้องไม่มี error เลยสักกรณี** — ถ้ามี error แปลว่าไม่เงียบ ผิดนิยาม
5. recall@100 ต้องดีขึ้นชัดเจนเมื่อเพิ่ม `ef_search`

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/q06_limit_exceeds_ef.sql` |
| ground truth | `groundtruth/q06.json` |
| ตัวนับคะแนน | `sql/score.sql` — **static ล้วน อ่าน GUC เทียบกับ LIMIT ที่ประกาศไว้** |
| ตัวพิสูจน์ตัวตรวจ | `sql/q06_checker_states.sql` |
| หลักฐาน | `results/q06_phase3_cycle.txt` |
| รันจริง | **3 รอบติด assertion ผ่าน 5/5 · ตัวตรวจพลิกถูก 3 รอบติด** |

### หน้าผาอยู่ตรง k = ef_search + 1 พอดี

`hnsw.ef_search = 40` · 100,000 แถว · 200 query

| ขอ | ได้ | ผล | error |
|---|---|---|---|
| 10 | 10 | ครบ | ไม่มี |
| 20 | 20 | ครบ | ไม่มี |
| 39 | 39 | ครบ | ไม่มี |
| **40** | **40** | **ครบ** | ไม่มี |
| **41** | **40** | **ขาด 1 แถว** | **ไม่มี** |
| 60 | 40 | ขาด 20 แถว | ไม่มี |
| 100 | 40 | **ขาด 60 แถว** | ไม่มี |

**ขอ 41 ได้ 40** — เพดานเท่ากับ `ef_search` พอดีเป๊ะ ไม่ใช่ใกล้เคียง

### เพิ่ม `ef_search` แล้วได้แถวครบ — แต่ recall ยังไม่ครบ

| `ef_search` | ขอ 100 ได้ | recall@100 | แย่สุดต่อ query | ครบ 100/100 |
|---|---|---|---|---|
| **40** | **40 แถว** | **0.3864** | 0.0000 | 0/200 |
| 100 | 100 แถว | 0.9057 | 0.0100 | 0/200 |
| 200 | 100 แถว | 0.9658 | 0.0100 | 27/200 |

**⚠️ จุดที่ต้องเขียนให้ชัด: แก้ Q06 แล้วไม่ได้แก้ Q01**
ที่ `ef_search=100` ได้ครบ 100 แถวแล้ว แต่ **90.6% เท่านั้นที่เป็นคำตอบที่ถูก**
และแม้ที่ `ef_search=200` ยังมี query ที่ได้ถูกแค่ **1 จาก 100**
→ **สองปัญหานี้ต้องแก้แยกกัน** การตั้ง `ef_search >= LIMIT` แก้แค่ "จำนวน" ไม่ได้แก้ "คุณภาพ"



### ⭐ ทางแก้ที่ไม่เคยลอง — และมันเปิดกับดักชั้นที่สอง (E44 · 2026-08-01)

**ไม่เคยทดสอบ `hnsw.iterative_scan` กับ Q06 เลย** ทั้งที่เอกสารเขียนว่ามันจะ
*"scan more of the index until enough results are found"*

ขอ 100 · `ef_search=40` (ค่าเริ่มต้น) · 200 query · เทียบเฉลย exact

| | ได้เฉลี่ย | recall@100 | แย่สุด | buffers |
|---|---|---|---|---|
| ค่าเริ่มต้น (`iterative_scan = off`) | 40.00 | 0.3920 | 0.0000 | 970 |
| **`relaxed_order`** | **100.00** | **0.7572** | 0.0000 | 1,030 |
| `relaxed_order` + `mult=32` | 100.00 | **0.9397** | 0.0000 | 1,715 |
| *(ทางแก้เดิม)* `ef=100` | 100.00 | 0.9244 | 0.0000 | 1,594 |
| *(ทางแก้เดิม)* `ef=200` | 100.00 | 0.9740 | 0.0000 | 2,058 |

**`iterative_scan` ยกเพดานได้สมบูรณ์ 200/200 query จ่ายเพิ่มแค่ 6%**
→ รายการทางแก้เดิมที่มีแต่ `ef_search` **ไม่ครบ**

#### ⭐ แต่ผลนี้ทำให้ข้อสรุปหลักแข็งขึ้น ไม่ใช่อ่อนลง

```
เปิด iterative_scan  ->  ได้ครบ 100 แถวทั้ง 200 query
                         แต่ 1 ใน 4 ของแถวที่ได้ เป็นของผิด
```

ข้ออ้าง *"แก้ Q06 ไม่ได้แก้ Q01"* เดิมแสดงด้วยการเพิ่ม `ef_search`
ตอนนี้มีหลักฐานตรงกว่ามาก — **จำนวนแถวกลับมาครบสมบูรณ์ ขณะที่ recall ยังขาด 24%**

**และเป็นกับดักที่อันตรายกว่าเดิม** — ทีมที่เปิด `iterative_scan` แล้วเห็นจำนวนแถว
กลับมาครบ จะเชื่อว่าแก้ได้แล้ว · **ตัวชี้วัดที่มองเห็นง่ายที่สุดคือตัวที่หลอกที่สุด**

`results/q06_iterative_probe.txt`

### เป็นของ pgvector เฉพาะตัว — ยืนยันจากเฟส 2.5 และชั้น 3

Qdrant ตั้ง `hnsw_ef=40` กับ `limit=100` แล้วยังได้ **100 แถว · recall 0.9895**
Milvus ตั้ง `ef = limit` โดยนิยาม · Weaviate ปรับอัตโนมัติ · OpenSearch engine `lucene` บังคับ `ef = k`
→ **เจ้าอื่นป้องกันไว้ให้แล้ว ไม่ปล่อยให้ candidate list เล็กกว่าจำนวนที่ขอ** (ดู D16 และ `EVIDENCE.md`)

> ⚠️ **ผ่อนถ้อยคำ (E44)** — pgvector **มีกลไกป้องกันตั้งแต่ 0.8.0** คือ `iterative_scan`
> ซึ่งวัดแล้วยกเพดานได้จริง · สิ่งที่ต่างจากเจ้าอื่นคือ **ตั้งค่าปริยายเป็นปิด**
> ไม่ใช่ "ไม่มีกลไก" · ข้อสรุปเรื่องค่าเริ่มต้นไม่กระทบ เพราะปริยายยังเป็น `off` จริง

---

## ชั้น V — คุณภาพข้อมูล

| ID | fault | หลักฐาน |
|---|---|---|
| **V07** | NULL vector และ zero vector ไม่ถูก index → แถวหายถาวรเงียบสนิท | ✅ เอกสาร |

### V07 — NULL / zero vector ไม่ถูก index → แถวหายถาวร

| | |
|---|---|
| **อาการที่เห็น** | บางแถวไม่เคยโผล่ในผลค้นหาเลย **ตลอดกาล** · ไม่มี error |
| **คนมักแก้ผิด** | จูน `ef_search` / `probes` / `m` · rebuild index · เปลี่ยน embedding model |
| **ต้นเหตุจริง** | NULL vector ไม่ถูก index · zero vector ไม่ถูก index เมื่อใช้ cosine เพราะ distance เป็น `NaN` |
| **ทำไม dev ไม่เจอ** | ข้อมูลทดสอบมักสะอาด · และแถวที่หายไม่ส่งเสียงอะไรเลย |

**⭐ ข้อนี้ต่างจากทุกข้อในชุด — เป็นปัญหา *คุณภาพข้อมูล* ไม่ใช่ *การตั้งค่า***
ทีมที่จูน index เก่งแค่ไหนก็ไม่มีทางเจอ เพราะ**ไม่มีพารามิเตอร์ตัวไหนเกี่ยวข้องเลย**

**พฤติกรรมที่วัดได้จริง**
```
'[0,0,0]'::vector <=> '[1,0,0]'::vector   ->  NaN     (ไม่ใช่ error)
NULL::vector      <=> '[1,0,0]'::vector   ->  NULL
vector_norm('[0,0,0]'::vector)            ->  0
```
ทั้ง `NaN` และ `NULL` เรียงไปท้ายสุดใน `ORDER BY ASC`
→ **exact search ยังเห็น** แต่ **index ไม่เคยเก็บไว้ตั้งแต่แรก**

**วิธีฉีด** — ใช้ตารางแยก `qf_v07` ห้ามแตะ `qf_corpus` ที่ล็อกไว้
```sql
INSERT ... 'null_vec', NULL;
INSERT ... 'zero_vec', array_fill(0::real, ARRAY[384])::vector;
CREATE INDEX ... USING hnsw (embedding vector_cosine_ops);
```

**ground truth** — ยิง query เดียว ขอเกินจำนวนแถวทั้งตาราง แล้วนับว่าแต่ละชนิดโผล่กี่แถว
ต้องตั้ง `hnsw.ef_search` สูง เพื่อ**แยก V07 ออกจาก Q06** ให้ชัด
ไม่งั้นแยกไม่ออกว่าแถวหายเพราะไม่ถูก index (V07) หรือเพราะ candidate list เล็ก (Q06)

**assertion** — ต้องผ่านครบ 4 ข้อ
1. exact search ต้องเห็นครบทุกแถว **รวมแถวเสีย**
2. index ต้อง **ไม่คืน** แถว NULL และ zero เลยสักแถว
3. index ต้องคืนแถวปกติ **ครบ** — พิสูจน์ว่าหายเฉพาะแถวเสีย ไม่ใช่หายทั่วไป
4. จำนวนที่หาย **ต้องเท่ากับจำนวนแถวเสียพอดี**

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/v07_null_zero_vectors.sql` |
| ground truth | `groundtruth/v07.json` |
| ตัวนับคะแนน | `sql/score.sql` — **static ล้วน นับแถวที่ NULL หรือ norm = 0** |
| ตัวพิสูจน์ตัวตรวจ | `sql/v07_checker_states.sql` |
| หลักฐาน | `results/v07_phase3_cycle.txt` |
| รันจริง | **3 รอบติด assertion ผ่าน 4/4 · ตัวตรวจพลิกครบ 3 สถานะ 3 รอบติด** |

### ผลที่วัดได้ — 500 แถวปกติ + 5 NULL + 5 zero

| | เส้นทาง | ขอ | ได้ | ปกติ | NULL | zero |
|---|---|---|---|---|---|---|
| ไม่มี index | exact | 510 | **510** | 500 | **5** | **5** |
| มี index แต่ปิด index scan | exact | 510 | **510** | 500 | **5** | **5** |
| **ใช้ index** | index | 510 | **500** | 500 | **0** | **0** |

**หายไป 10 แถวพอดี ไม่มากไม่น้อย**

แถวกลางคือกลุ่มควบคุมที่สำคัญที่สุด — **index มีอยู่ แต่ปิด index scan แล้วแถวกลับมาครบ**
→ พิสูจน์ว่าแถวยังอยู่ในตาราง ยังค้นเจอได้ **เส้นทางที่ใช้ index ต่างหากที่ทำหาย**

### ตรวจง่ายที่สุดในชุด

```sql
SELECT count(*) FROM <table>
WHERE embedding IS NULL OR vector_norm(embedding) = 0;
```

ไม่ต้องรัน query ค้นหา ไม่ต้องมี index ไม่ต้องมีเฉลย
→ `quietfail-check` ทำข้อนี้ได้ถูกที่สุดในบรรดา fault ทั้งหมด

---

## ชั้น L — เสื่อมตามเวลา

| ID | fault | หลักฐาน |
|---|---|---|
| **L02** | แถวที่ถูกลบยังค้างใน index → **ขอ 10 ได้ 0** (ชื่อเดิมโทษ "index บวม" · **แก้แล้ว** ดู E34) | ✅ FAQ ทางการ |

### L02 — แถวที่ถูกลบแต่ยังไม่ vacuum ทำให้ index คืนผลไม่ครบ

| | |
|---|---|
| **อาการที่เห็น** | query ที่ **ไม่มี `WHERE` เลย** คืนศูนย์แถว ทั้งที่ตารางมีข้อมูลเป็นหมื่นแถว · ไม่มี error · แย่ลงตามเวลาโดยไม่มีใครแตะโค้ด |
| **คนมักแก้ผิด** | `REINDEX` (แพงกว่า `VACUUM` มากโดยไม่จำเป็น) · เพิ่ม `LIMIT` (วัดแล้วไม่ช่วยเลย) · เพิ่ม `ef_search` · **สรุปว่าข้อมูลถูกลบไปหมดแล้ว** |
| **ต้นเหตุจริง** | แถวที่ถูก `DELETE` ยังมีรายการค้างใน index จนกว่าจะ `VACUUM` · index คืน candidate ตาม `ef_search` ก่อน แล้ว MVCC ค่อยคัดแถวที่ตายออก |
| **ทำไม dev ไม่เจอ** | ตอน dev ไม่มีใครลบข้อมูลเยอะพอ และ autovacuum เก็บกวาดให้ทัน |

**⭐ กลไกเดียวกับ Q03 เป๊ะ แต่ตัวกรองมองไม่เห็น**

| | ตัวกรองคืออะไร | เห็นในโค้ดไหม |
|---|---|---|
| **Q03** | `WHERE` ที่เขียนเอง | ✅ เห็น |
| **L02** | **MVCC** ตัดแถวที่ตายแล้ว | ❌ **ไม่มีอะไรในคำสั่งบอกเลย** |

→ L02 หายากกว่า Q03 มาก เพราะคำสั่งที่พังหน้าตาเหมือนคำสั่งที่ถูกทุกประการ

**ผลที่วัดได้ — 100,000 แถว · `ef_search=40` · `LIMIT 10` · ปิด autovacuum เฉพาะตารางทดสอบ**

| ตายไป % | แถวที่ยังอยู่ | ขอ | **ได้จาก index** | เฉลย |
|---|---|---|---|---|
| 0 | 100,000 | 10 | 10 | 10 |
| 10 | 90,000 | 10 | 10 | 10 |
| 20 | 80,000 | 10 | 10 | 10 |
| 30 | 70,000 | 10 | 10 | 10 |
| **40** | 60,000 | 10 | **10** | 10 |
| **50** | 50,000 | 10 | **0** | 10 |
| 70 | 30,000 | 10 | **0** | 10 |
| 90 | 10,000 | 10 | **0** | 10 |

**⭐ หน้าผาอยู่ระหว่างตาย 40% กับ 50% และไม่มีทางลาดเลย**
10 แถวเต็ม กลายเป็น **0 แถว** ในขั้นเดียว — ไม่มีสัญญาณเตือนระหว่างทาง
ทีมที่เฝ้าดูตัวเลขจะเห็นทุกอย่างปกติดีจนถึงวินาทีที่มันเป็นศูนย์

**เฉลยได้ครบ 10 ทุกขั้น** → แถวยังอยู่ในตารางจริง เส้นทางที่ใช้ index ต่างหากที่ทำหาย
(กลุ่มควบคุมแบบเดียวกับ V07)

**ขอเยอะขึ้นไม่ช่วยเลย** — ที่ตาย 90% ขอ 10 · 40 · 100 · 1000 ได้ **0 ทั้งหมด**

#### ⭐ ทางแก้ที่ไม่ต้อง `VACUUM` — เจอ 2026-08-01 (E43)

**ไม่เคยทดสอบ `hnsw.iterative_scan` กับ L02 เลย** ทั้งที่เขียนไว้เองว่า
*"L02 กลไกเดียวกับ Q03 เป๊ะ"* และ iterative scan คือทางแก้ที่เอกสารให้ไว้สำหรับ Q03

100,000 แถว · ลบ 90% · **ไม่ `VACUUM`** · ขอ 10

| | ได้ | buffers |
|---|---|---|
| ค่าเริ่มต้น | 0 | 1,436 |
| `iterative_scan = relaxed_order` | **0** | 2,667 |
| `iterative_scan = strict_order` | **0** | 2,667 |
| **`relaxed_order` + `scan_mem_multiplier = 32`** | **10** | 10,501 |
| *(กลุ่มควบคุม)* หลัง `VACUUM` | 10 | 947 |

**⭐ เป็นหลักฐานเชิงกลไกที่แข็งที่สุดที่ L02 เคยมี** — ข้ออ้างว่า L02 กับ Q03
เป็นกลไกเดียวกันเคยเป็นการให้เหตุผลจากคำอธิบาย · ตอนนี้มีหลักฐานตรงว่า
**ทางแก้เดียวกันแก้ได้ทั้งคู่** ซึ่งเป็นสิ่งที่ทฤษฎีนั้นบังคับให้เป็น

⚠️ **ไม่ใช่คำแนะนำให้ใช้แทน `VACUUM`** — แพงกว่า **11 เท่า** (10,501 vs 947 buffers)
และจ่ายทุก query ขณะที่ `VACUUM` จ่ายครั้งเดียว

🔴 **เกือบพลาดด้วยเหตุผลเดียวกับ E42/E43** — ถ้าลอง `iterative_scan` ที่หน่วยความจำ
ค่าปริยายจะได้ **0 แถว** แล้วสรุปว่า *"iterative scan ก็ไม่ช่วยเหมือนกัน"*

`results/doc_reread_probe.txt`

เพราะ `LIMIT` ไม่ได้ทำให้ `ef_search` โตตาม candidate ยังเป็น 40 ตัวที่ตายหมดเหมือนเดิม

#### `VACUUM` แก้ได้หมด แต่มีราคาและมีสิ่งที่มันไม่ทำ

| | ค่า |
|---|---|
| เวลา `VACUUM` ที่ 100k แถว | **27 วินาที** |
| เวลาหลัง `UPDATE` ทั้งตาราง | **118 วินาที** |
| ผลหลัง `VACUUM` | กลับมาครบ 10 แถวทันที |
| ขนาด index ก่อน → หลัง | **195 MB → 195 MB** (ไม่ลดเลย) |

FAQ บอกว่า vacuum ของ HNSW ใช้เวลานาน — ยืนยันแล้วด้วยตัวเลข
และ **`VACUUM` ไม่ลดขนาด index** มันแค่ทำให้รายการที่ตายแล้วถูกนำกลับมาใช้ได้

#### 🔴 กลุ่มควบคุม `UPDATE` หักล้างชื่อในทะเบียน

ทะเบียนเดิมเขียนว่า *"dead tuple สะสม → **index บวม** recall ตก"*
ทดสอบด้วยการ `UPDATE` ค่าเดิมทับตัวเอง — ข้อมูลไม่เปลี่ยนเลย แต่เกิดเวอร์ชันเก่าตายค้าง

| ขั้น | แถวที่ยังอยู่ | ขนาด index | ขอ | ได้ | recall |
|---|---|---|---|---|---|
| ฐาน | 100,000 | 195 MB | 10 | **10** | 0.8415 |
| `UPDATE` 50% | 100,000 | 198 MB | 10 | **10** | 0.8445 |
| `UPDATE` ทั้งตาราง | 100,000 | **202 MB** | 10 | **10** | 0.8400 |

**index บวมจริง (195 → 202 MB) แต่ผลยังครบทุกขั้น และ recall ไม่ตก**
(0.8415 → 0.8400 อยู่ในช่วงแกว่งปกติที่ I04 วัดไว้)

→ **บวมกับ recall ตกเป็นคนละเรื่อง** · ต้นเหตุคือ **แถวที่ถูกลบแล้วไม่มีตัวแทน**
ไม่ใช่ dead tuple ทุกชนิด เพราะแถวที่ถูก `UPDATE` ได้รายการใหม่ใน index ทันที
candidate จึงยังมีตัวเป็นๆ ให้เลือกเสมอ

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-28)**

| | |
|---|---|
| ตัวฉีด | `sql/l02_dead_tuples.sql` |
| ground truth | `groundtruth/l02.json` |
| ตัวนับคะแนน | `sql/score.sql` — ถาม k แถว**โดยไม่มี filter** แล้วเทียบกับแถวที่ยังอยู่ |
| รันจริง | **3 รอบติด · assertion ผ่าน 6/6 ทุกรอบ** |
| หลักฐาน | `results/l02_phase3_cycle.txt` · `results/l02_run1.txt` |
| พิสูจน์ตัวตรวจ | `sql/l02_checker_states.sql` |

```
ไม่มีตาราง / ไม่มี vector index          -> CANNOT_CHECK
limit_k >= ef_search                    -> CANNOT_CHECK  (แยกจาก Q06 ไม่ออก)
ไม่มีตารางชุดโจทย์ qf_queries             -> CANNOT_CHECK  (ดูข้างล่าง)
ขอ 10 ได้ 0 ทั้งที่มีแถวอยู่ 10,000       -> DETECTED
หลัง VACUUM ได้ครบ 10                    -> NOT_DETECTED
```

> **🔴 ตัวตรวจรุ่นแรกไม่พลิก และสาเหตุน่าจดจำ** — มันใช้ **แถวในตารางเองเป็น query vector**
> ซึ่งเป็นแถวที่ยังไม่ตาย การค้นจึงเริ่มจากบริเวณที่ยังมีชีวิตแล้วได้ผลครบ
> ทั้งที่ fault เกิดอยู่จริง · **query vector ต้องมาจากนอกตารางเสมอ** — ดู **H27**

---

# ที่พิจารณาแล้วไม่รับเข้าชุด

**เลขที่หายไปคือข้อมูล ไม่ใช่ความผิดพลาด** — มันบอกว่าเคยพิจารณาแล้วตัดออก
**ห้ามเรียงเลขใหม่** เพราะไฟล์อื่นอ้างเลขเหล่านี้ไปแล้ว

| ID | fault | เหตุผลที่ตัด |
|---|---|---|
| F02 | deadlock จากลำดับ lock ไม่ตรงกัน | ซ้อนทับกับ F03 · เก็บไว้ถ้าเหลือเวลา |
| F04 | serialization failure ถูกแก้ผิดทาง | ต้องอธิบาย isolation level ยาว ต้นทุนสูง |
| F06 | temp file ระเบิดเพราะไม่มี index | ซ้อนทับกับ F10 |
| F07 | check-then-act ทำให้ข้อมูลซ้ำ | ไม่เกี่ยวกับ vector · ใช้เวลาสร้าง load test นาน |
| F08 | เงินเพี้ยนเพราะใช้ float | schema เตรียมไว้แล้ว แต่เป็น static check ล้วน ไม่ต้องฉีด |
| F09 | เวลาเพี้ยนเพราะ timestamp ไม่มี timezone | เหมือน F08 |
| F11 | มี index แต่ใช้ไม่ได้ | ซ้อนทับกับ I01 |
| F12 | ตารางบวมเพราะ transaction ค้าง | ซ้อนทับกับ L02 |
| Q05 | สลับ cosine กับ inner product | ยืนยันได้แค่บางส่วน · ซ้อนทับกับ I01 |
| L01 | พารามิเตอร์ไม่ scale ตามขนาดข้อมูล | ต้องมีข้อมูล 4 ขนาด ต้นทุนสูง |
| V08 | index ร่วมกันหลาย tenant กระทบ recall กัน | ต้องสร้างระบบหลาย tenant ก่อน |

> `init/02_schema.sql` มีคอมเมนต์อ้าง F06–F09, F11 อยู่
> เป็นเศษของแผนเดิม ไม่ใช่ข้อผิดพลาด — schema เตรียมของไว้แล้ว
> ถ้าอนาคตรับเข้าชุดก็ทำได้ทันที

---

# บั๊กนอกแผนที่เจอระหว่างทาง

> **ที่นี่คือดัชนี ไม่ใช่ที่เก็บเนื้อหา** — บันทึกเต็มอยู่ใน `DECISIONS.md`
> คัดเนื้อหามาซ้ำเมื่อไหร่ มันจะเคลื่อนออกจากกันเมื่อนั้น ซึ่งคือ D15 ทั้งดุ้น
>
> **ไม่มีข้อไหนอยู่ในชุด 16** การรับเข้าชุดต้องตัดของเดิมออก (กฎเหล็กข้อ 4)
> และเป็นการตัดสินใจของเจ้าของโปรเจค

**กติกาการบันทึก:** เจอบั๊กที่ไม่อยู่ในแผนเมื่อไหร่ เพิ่มแถวที่นี่ทันที
พร้อมบันทึกเต็มใน `DECISIONS.md` — **ห้ามแก้เงียบๆ แล้วเดินต่อ**
เพราะบั๊กที่ไม่ได้บันทึกคือบั๊กที่จะเกิดซ้ำ และเป็นวัตถุดิบของหัวข้อ
"สิ่งที่ลองแล้วไม่ได้ผล" ใน README ซึ่งหายากมากในพอร์ตนิสิต

## ก) พฤติกรรมของ PostgreSQL / pgvector — อาจกลายเป็น fault ได้

| ID | เจออะไร | บันทึกเต็ม | เงียบไหม | สถานะ |
|---|---|---|---|---|
| **F13** | เพิ่ม index แล้วช้าลง **118%** เพราะ lossy bitmap · **planner เลือกเอง** (cost บอกถูกกว่า 3.5% แต่จริงแพงกว่า 2.2 เท่า) · `work_mem` ที่พอทำให้หายไป เส้นแบ่งอยู่ระหว่าง 128kB–160kB | E08 · EXP01c · **D18** | ✅ เงียบสนิท | **ปิดถาวร** — เจ้าของตัดสินแล้ว ไม่พิจารณาเป็น fault หลักอีก |
| **U01** | uncorrelated subquery ถูกยกเป็น **InitPlan** คำนวณครั้งเดียว → 100,000 แถวได้ค่าซ้ำกันหมด | E11 | ✅ **เงียบสนิท ไม่มี error** | ยังไม่เสนอ |
| **U02** | `l2_norm(vector)` ไม่มีใน 0.8.5 แต่ error บอกว่า `is not unique` แล้ว HINT ชวนให้ใส่ cast → ถ้าทำตามจะไปลง `::halfvec` ซึ่ง**ลดความละเอียดข้อมูลเงียบๆ** | E10 | ⚠️ error ดัง แต่ชี้ผิดทาง | ยังไม่เสนอ |
| **U03** ⭐ | `pg_stat_activity` ถูก snapshot **ครั้งเดียวต่อ transaction** → ลูปเฝ้าระวังใน `DO` block เดียวเห็นภาพเดิมซ้ำตลอด แล้วรายงานว่า "ระบบปกติดี" ตลอดไป | E19 | ✅ **เงียบสนิท** | ยังไม่เสนอ |
| **U05** | เพิ่ม `maintenance_work_mem` (ทางแก้ของ I05) สูงเกิน `/dev/shm` → `CREATE INDEX` ล้มเหลวทั้งหมด · error บอก `No space left on device` ทั้งที่ดิสก์ว่าง | E28 | ⚠️ error ดัง แต่ชี้ผิดที่ | ยังไม่เสนอ |
| **U06** | `pg_stat_statements` ทำ **normalization** แทนค่าคงที่ทุกตัวด้วย `$n` ก่อนเก็บ → `LIMIT 100` กลายเป็น `LIMIT $2` · ตัวเลข LIMIT ที่โค้ดใช้จริง **กู้กลับจากประวัติ query ไม่ได้เลย** | — | ⚠️ ไม่ใช่บั๊ก แต่ทำให้ตัวตรวจที่พึ่งค่านี้ตอบไม่ได้ตลอดกาล | เจอตอนทำ quietfail-check |
| **U04** ⭐ | Qdrant 1.12.4 ที่ค่าเริ่มต้นล้วนๆ **ไม่สร้าง HNSW เลย** ที่ 100k จุด · `status: green` · `indexed_vectors_count: 0` · ค้นแบบ brute force โดยผู้ใช้ไม่รู้ | E24 | ✅ **เงียบสนิท** | ยังไม่เสนอ |

**U03 ควรอ่านคู่กับวิทยานิพนธ์ของโปรเจค** — มันคือความล้มเหลวเงียบ
**ในเครื่องมือที่เขียนขึ้นมาเพื่อตรวจความล้มเหลวเงียบ** เครื่องมือเฝ้าระวัง
ที่เขียนเป็นลูปใน transaction เดียวจะบอกว่าทุกอย่างปกติไปตลอดกาล

**U01 คือข้อที่แรงที่สุดในตารางนี้** — ไม่มี error ไม่มี warning จำนวนแถวถูก
มิติถูก ชนิดถูก ครบทุกกลุ่ม แต่ข้อมูลใช้ไม่ได้เลย
และ assertion 4 ข้อที่เขียนไว้ตอนนั้น **ผ่านหมด**

**U02 อยู่ตระกูลเดียวกับ F03** — ข้อความ error ชี้ไปคนละทางกับต้นเหตุ
ต่างกันตรง F03 ทำให้แก้ผิดที่ ส่วน U02 ทำให้แก้แล้วเสียข้อมูลเพิ่ม

## ข) บั๊กของ harness ตัวเอง — ไม่ใช่ fault แต่ต้องจำ

ทุกข้อคือความล้มเหลวเงียบในโปรเจคที่ศึกษาความล้มเหลวเงียบ

| ID | เจออะไร | บันทึกเต็ม | จับได้เพราะ |
|---|---|---|---|
| **H01** | `make seed` ตั้ง default 2M แต่ผลทุกชุดมาจาก 200k | E09 | ไล่ตรวจอย่างเป็นระบบ (ยังไม่เคยแสดงอาการ) |
| **H02** | กลุ่ม vector มีอยู่แค่ในคอลัมน์ `cluster_id` ไม่ได้มีอยู่จริงเชิงเรขาคณิต (ในกลุ่ม 0.98 vs ข้ามกลุ่ม 1.00) | E12 | ตรวจ sanity ก่อนวัด ตามที่ `PROJECT.md` ข้อ 10 สั่งไว้เอง |
| **H03** | assertion ของ F01 ระบุข้อความ error ที่ไม่มีวันเกิดกับ role ธรรมดา | E13 | รันจริงครั้งแรก assertion ไม่ผ่าน |
| **H04** | ตัวฉีดเก็บกวาดตัวเองที่ ~5 วิ ตัวนับคะแนนที่รันแยกขั้นมาไม่ทัน → false negative | E14 | แยกขั้นตอนแล้วผลไม่ตรงกับที่เห็นตอนรันรวม |
| **H05** | buffers มองไม่เห็น lossy bitmap (2501 เท่ากันทั้ง 6 ค่า) | EXP01c | วัดหลายค่าแล้วเทียบ |
| **H06** | นิยาม F05 ไม่ระบุ role ของแต่ละ session · ยิง DDL ด้วย `app` ได้ `must be owner` → fault ไม่เกิดเลย | E15 | รันจริงครั้งแรก assertion ตกทั้ง 3 ข้อ |
| **H07** | ฆ่า psql ฝั่ง client ไม่ยกเลิก DDL ที่ค้างในคิว → พอ A ตาย ALTER ทำสำเร็จ **แก้ schema จริง** | E16 | ตาข่าย `DROP COLUMN IF EXISTS` ที่กันไว้ล่วงหน้า |
| **H08** 🔴 | `seed.sql` สร้างค่าเดียวซ้ำ 200,000 แถว (`created_at`, `total_satang`) ตั้งแต่ commit แรก — รูปแบบเดียวกับ U01 · **แก้แล้วและรัน EXP01/01b/01c ใหม่ทั้งหมด** | E17 | วัดก่อนเขียน F03b ว่า query ช้าพอไหม |
| **H09** | สเปก F03 ระบุ timeout 2s ทั้งที่ไม่มี query ไหนบนชุดนี้ช้าถึง | E18 | วัดทุกตัวเลือกก่อนเลือก |
| **H10** | ลอกโครงสร้างการวัดจาก F01/F05 มาใช้กับ F03 ทั้งที่หลักฐานคนละชนิด (สถานะสด vs ตาราง) | E21 | วัดผิดจังหวะซ้ำๆ จนต้องกลับไปถามว่าหลักฐานคืออะไร |
| **H11** | วัด `Plan Rows` จาก node `Aggregate` ซึ่งเป็น 1 เสมอ → ได้ "คลาด 1.0x" ตลอดไม่ว่าสถิติจะเพี้ยนแค่ไหน | E22 | assertion ข้อ 1 ตก |
| **H12** ⭐ | เข้าใจว่า "สถิติเก่า" = จำนวนแถวเก่า · ของจริง PostgreSQL ปรับสเกล `reltuples` ตามขนาดไฟล์ ตัวที่หลอกได้คือ MCV/histogram กับ **ค่าใหม่** | E22 | assertion ข้อ 1 ตกซ้ำ ทั้งที่ `reltuples` เก่าจริง |
| **H13** | ฆ่า `docker exec` ฝั่ง client แล้ว `DO` block ยังวิ่งต่อฝั่ง server 20 นาที · บล็อก TRUNCATE จนงานถัดไปได้ corpus ว่าง แล้วดูเหมือนบั๊กคนละสามตัว | E23 | assertion ของ qf12 (`ต้องได้ 500000 แถว แต่ได้ 0`) |
| **H14** ⭐ | ตัวตรวจ "planner ใช้ index ไหม" ค้นคำ `Index Scan` ทั้ง plan → ไปโดน pkey ของตารางอื่นใน subquery · ตอบว่าใช้ index ทุกครั้ง | E25 | assertion ตกทั้งที่ catalog บอกว่าเป็นไปไม่ได้ |
| **H15** | `count(*) = 2` บน `pg_opclass` ได้ 4 เพราะ opclass ชื่อเดียวกันมีทั้ง hnsw และ ivfflat | E25 | assertion ข้อ 3 ตก |
| **H16** 🔴 | `2>/dev/null` กลบ `CREATE INDEX` ที่ล้มเหลว **เป็นครั้งที่สามในวันเดียว** · ผล 12 ช่องของตัวตรวจผิดทั้งหมด | E26 | ตัวตรวจขึ้น CANNOT_CHECK ทั้งที่ทดสอบมือแล้วผ่าน |
| **H17** | `score.sql` สร้าง temp table โดยไม่ DROP ก่อน → เรียกซ้ำในเซสชันเดียวไม่ได้ | E26 | เจอทันทีที่เลิกกลบ stderr |
| **H18** 🔴 | `pg_terminate_backend()` ใส่ **parallel worker** → leader ตาย exit 2 → postmaster **รีเซ็ตทั้งคลัสเตอร์** · คำสั่งถัดไปล้มด้วย "system is in recovery" ที่ไม่ชี้กลับมาที่ต้นเหตุ | E29 | สถานะ 3 ของตัวพิสูจน์ล้มเหลว |
| **H19** | กรองผล psql ด้วย pattern ที่มีช่องว่าง แต่ `-qAt` ให้ผลแบบ `I03|DETECTED|...` ไม่มีช่องว่าง → ตัวนับรันจริงแต่มองไม่เห็นผล | — | ตารางล็อกว่างเปล่าทั้งที่ assertion ผ่าน |
| **H20** | ยัด `LOAD 'vector'` ผ่าน `-c` ใน `bash -c` → quote พัง `SET hnsw.ef_search` ไม่เคยทำงาน → ตัวตรวจขึ้น DETECTED ทั้ง 9 ครั้ง | — | ตัวตรวจไม่พลิกทั้งที่ควรพลิก |
| **H21** | script อ่านไฟล์ครั้งเดียวแล้ว `write()` สองครั้งจากตัวแปรเดิม → การแก้รอบหลังลบรอบแรกทิ้ง ทั้งที่ทั้งสองรอบรายงานว่าสำเร็จ | — | ไล่ตรวจ H-series แล้วพบว่า H18/H19 หายไป |
| **H22** 🔴 | ฝัง SQL ที่มี quote ใน `bash -c` **เป็นครั้งที่สามในวันเดียว** (`'` ไม่ถูกตีความ) → `DELETE` ไม่ทำงาน ตัวตรวจไม่พลิก · และพังแบบเงียบคือ script เดินต่อ | — | ตัวตรวจขึ้น DETECTED ซ้ำในสถานะที่ควรพลิก |
| **H23** | `scripts/audit.py` เขียนรายงานลง `results/` ที่ตัวเองตรวจ → working tree สกปรกทุกครั้ง → **ตัวตรวจไม่มีวันผ่านตัวเอง** | D17 | รันครั้งแรกก็ FAIL ทันที |
| **H24** | ตัดชื่อไฟล์จาก `git status --porcelain` ด้วยตำแหน่งคงที่ `[3:]` → รายงานว่า `LAUDE.md` (porcelain มีหลายรูปแบบ ต้องใช้ `split()`) | D17 | ชื่อไฟล์ในรายงานแหว่ง |
| **H25** 🔴 | `audit.py` ค้นคำว่า "เหลือ" ทั้งไฟล์ → ไปโดนประโยค **"จนเหลือที่ทำงานจริงน้อยลง"** ที่เพิ่งเขียนเองในย่อหน้าอื่น แล้วรายงานว่าสถานะไม่ตรง | D17 | audit FAIL ทั้งที่ CLAUDE.md ถูก |
| **H26** 🔴 | อ่าน verdict จากผลรันด้วย `sed 's/.*\(DETECTED\|NOT_DETECTED\)...'` — regex **ตะกละ** กิน `NOT_` ไปแล้วรายงาน `NOT_DETECTED` เป็น `DETECTED` → เกือบสรุปว่าตัวตรวจ I04 ไม่พลิก ทั้งที่มันพลิกถูกต้อง | — | ผลขัดกับ output ดิบที่เปิดดูเอง |

| **H32** 🔴 | เฉลย exact ทั้ง `qf_truth` และชุด embedding จริง เก็บ id **เรียงตาม id ไม่ใช่ตามระยะ** — `row_number() OVER ()` ทำงาน**ก่อน** `ORDER BY`/`LIMIT` ตามลำดับประเมินผลของ SQL เลขที่ได้จึงเป็นลำดับที่สแกนเจอ แล้ว `array_agg(... ORDER BY rn)` เรียงตามนั้น · **เซ็ตถูกต้อง** เพราะ `LIMIT` ตัดหลัง `ORDER BY` → ตัวเลข recall ที่รายงานไปแล้ว**ไม่กระทบ** (ไล่ดูครบ 11 จุด ใช้ `unnest` + `= ANY` แบบเซ็ตทั้งหมด) แต่ใครเอาไปวัด **MRR · nDCG · top-1** จะได้ค่าผิดสนิท เพราะอาเรย์หน้าตาเหมือนรายการจัดอันดับทุกประการ | — | sanity check ของชุด embedding จริงคืน NULL หมด แล้วไล่ย้อนไปเจอต้นเหตุ |
| **H31** | CI ของ `quietfail-check` **แดงถาวรตั้งแต่วันแรก** — นโยบายตั้งไว้ว่า `CANNOT_CHECK` = fail build (ถูกต้องตามกฎเหล็กข้อ 10) แต่สภาพแวดล้อมตัวอย่างในไฟล์เดียวกันทำให้ตรวจไม่ได้ **2 สาเหตุ**: (ก) สร้างแต่ HNSW จึงไม่มี ivfflat ให้ Q04 เทียบ `lists` (ข) service container ไม่มี `shared_preload_libraries=pg_stat_statements` — `CREATE EXTENSION` ผ่าน แต่ query แล้ว error จึงอ่านประวัติไม่ได้ ทำให้ **I01 กับ Q02** ตรวจไม่ได้ด้วย → `exit 2` ตลอดกาล **ไม่มีอะไรผิดจริงสักครั้ง** · 🔴 **แก้รอบแรกไม่หาย** เพราะ reproduce บน container ของ repo ซึ่งมี preload อยู่แล้ว จึงเห็นแค่สาเหตุ (ก) | — | เปิด PR แรกแล้วเห็นเช็คแดง · reproduce ด้วยการสร้าง DB จำลอง CI แล้วรันตัวตรวจตัวเดียวกัน |
| **H30** | `rq3_score.py` `check_index_created` `return` ตอนคำตอบล้ม **ก่อนเก็บกวาด** → index ที่โมเดลสร้างสำเร็จก่อนพังค้างไว้ รอบถัดไปล้มด้วย `relation already exists` ซึ่งคนละเรื่องกับความผิดของโมเดล = ตัวให้คะแนนสร้างความล้มเหลวปลอมให้ตัวเอง | — | P06 ให้ผลต่างกันสองรอบติด |
| **H29** 🔴 | ตัวให้คะแนน RQ3 วัดแค่ **จำนวนแถว + recall** → ChatGPT ที่ไม่ใช้ index เลย (Seq Scan ทั้งตาราง) ได้ recall 1.00 ทุกข้อ จึง**ผ่าน 8/10** ทั้งที่อ่านมากกว่า 21 เท่า — **จุดบอดแบบเดียวกับกับดัก Q02 ที่โปรเจคนี้ศึกษาอยู่** | — | P03 ได้ 100/100 ที่ ef_search=40 ซึ่งขัดกับผลของ Q06 |
| **H28** 🔴 | `quietfail_check.py` ใช้ `` เป็นตัวคั่นคอลัมน์ แล้ว `out.strip()` กินมันทิ้ง (Python ถือว่า 0x1C–0x1F เป็น whitespace) → แถวที่ field สุดท้ายว่าง **หายไปหนึ่งคอลัมน์เงียบๆ** แล้วไปพังที่ `check_i01` ซึ่งไม่ชี้กลับมาที่ต้นเหตุเลย | — | traceback ชี้ผิดที่ ต้องไล่ดู output ดิบถึงเจอ |
| **H27** 🔴 | ตัวตรวจ L02 รุ่นแรกใช้ **แถวในตารางเองเป็น query vector** — ซึ่งเป็นแถวที่ยังไม่ตาย การค้นจึงเริ่มจากบริเวณที่ยังมีชีวิตแล้วได้ผลครบ **ทั้งที่ fault เกิดอยู่** → ตอบ NOT_DETECTED ในสถานะที่ต้องเป็น DETECTED | E35 | พิสูจน์ตัวตรวจ 3 สถานะแล้วไม่พลิก |

> **H27 บอกอะไรที่ใหญ่กว่าตัวมันเอง** — ตัวตรวจต้องวัด**สิ่งเดียวกัน**กับที่การทดลองวัด
> การทดลองใช้ `qf_queries` เป็นโจทย์ แต่ตัวตรวจไปหยิบแถวในตารางมาใช้แทน
> เลขที่ได้จึงไม่ใช่เลขเดียวกัน และมันเงียบสนิท — ไม่มี error ไม่มีอะไรผิดปกติให้เห็น

> **H25 และ H26 คือกฎเหล็กข้อ 6 ซ้ำอีกสองครั้ง** — *ห้าม match ข้อความ ให้อ่านโครงสร้าง*
> เดิมกฎนี้เขียนไว้สำหรับตัวตรวจ query plan (E25) แต่มันใช้กับ**ทุกอย่างที่อ่าน output**
> รวมถึงเครื่องมือที่ผู้ช่วยเขียนขึ้นเพื่ออ่านผลของตัวเอง

**H05 กลายเป็นกฎเหล็กข้อ 7ก แล้ว** ส่วน H01–H04, H06, H07 แก้แล้วทุกข้อ

**H06 เพิ่มบทเรียนใหม่ให้ทะเบียนนี้:** นิยาม fault ต้องระบุ **role ที่ใช้ยิงแต่ละ session**
ไม่ใช่แค่คำสั่ง SQL — สิทธิ์เป็นตัวแปรที่ทำให้ fault ไม่เกิดได้เงียบๆ

**H07 คือกรณีที่ตาข่ายกันเหนียวพิสูจน์คุณค่าตัวเอง** — มันไม่ได้แค่กันความเสียหาย
แต่เป็นตัวที่**บอกให้รู้ว่าการยกเลิกยังไม่ถูกชั้น** ถ้าไม่มีมัน ตาราง `orders`
จะมีคอลัมน์งอกทีละรอบที่รัน แล้วผลรอบถัดไปเทียบกับรอบก่อนไม่ได้โดยไม่มีอะไรเตือน

> **จุดร่วมที่ควรเขียนลงรายงาน:** H02, H03, H04 ถูกจับได้เพราะมี**ขั้นตอนตรวจสอบ
> ที่ออกแบบไว้ล่วงหน้า** (sanity check ก่อนวัด · assertion บังคับ · แยกขั้นวัดผล)
> ไม่ใช่เพราะสังเกตเห็นเอง
> ส่วน H01 ไม่เคยแสดงอาการเลย จับได้จากการไล่ตรวจความสอดคล้องล้วนๆ
>
> **นี่คือข้อโต้แย้งที่ตรงที่สุดว่าทำไมต้องมี `quietfail-check`**

---

# สรุปสถานะการเขียนสคริปต์

| fault | นิยาม | สคริปต์ฉีด | groundtruth | รันจริงแล้ว |
|---|---|---|---|---|
| **F01** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| **F03 + F03b** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| **F05** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| **F10** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| I01–L02 (12 ข้อ) | ตาราง | ❌ | ❌ | ❌ |

ของกลางที่ใช้ได้กับทุก fault แล้ว: **`sql/score.sql`** (ตัวนับคะแนน 3 สถานะ
`DETECTED` / `NOT_DETECTED` / `CANNOT_CHECK` ตามกฎเหล็กข้อ 10)

**เฟส 1 ผ่านเมื่อ:** F01, F03, F05, F10 มีครบทั้ง 4 คอลัมน์ และฉีดซ้ำได้ 3 ครั้งติด
→ ✅ **ครบทุกข้อแล้ว 2026-07-27 — เฟส 1 ผ่าน**
