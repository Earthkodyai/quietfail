# ทะเบียน fault

> **ไฟล์นี้คือแหล่งอ้างอิงเดียวของนิยาม fault ทั้งหมด**
> ถ้า `PROJECT.md` หรือ `EVIDENCE.md` ขัดกับไฟล์นี้ ให้เชื่อไฟล์นี้
>
> ทุก fault ต้องมีครบ 5 อย่าง: อาการ · ต้นเหตุ · วิธีฉีด · ground truth · assertion
> ถ้าข้อไหนขาด แปลว่ายังนิยามไม่เสร็จ ห้ามนับว่าพร้อมทำ

**รวม 16 fault** = core 4 ข้อ (F) + vector 12 ข้อ (I/Q/V/L)

---

# ชุด core — 4 ข้อ (เกณฑ์ผ่านเฟส 1)

มีไว้แสดงว่ากลไกวัดผลชุดเดียวกันใช้ได้ทั้งกับปัญหา DB ทั่วไปและปัญหาเฉพาะของ vector
ทั้ง 4 ข้อเป็นแบบ **"error ชี้ไปผิดที่"** ซึ่งเป็นแกนของ QuietFail

---

## F01 — connection หมด เพราะ transaction ค้าง

| | |
|---|---|
| **อาการที่เห็น** | `FATAL: remaining connection slots are reserved for roles with the SUPERUSER attribute` ← **ยืนยันด้วยการรันจริง** · เดิมเขียนไว้ว่า `sorry, too many clients already` ซึ่งผิด (ดู E13) |
| **คนมักแก้ผิด** | เพิ่ม `max_connections` → กิน RAM มากขึ้น แล้วเต็มอีกอยู่ดี |
| **ต้นเหตุจริง** | โค้ดเปิด transaction แล้วไม่ commit/rollback |
| **ทำไม dev ไม่เจอ** | dev มี connection เดียว ไม่มีวันเต็ม |

**วิธีฉีด** — เปิด N connection แต่ละอันสั่ง `BEGIN; SELECT 1;` แล้วปล่อยค้าง
โดยที่ท่อ stdin ยังเปิดอยู่

> ⚠️ ห้ามใช้ `pg_sleep()` เพราะทำให้ state เป็น `active` ซึ่งคนละเรื่อง
> ⚠️ ต้องยิงด้วย role `app` ไม่ใช่ `lab` เพราะ `superuser_reserved_connections`
> ทำให้ superuser ต่อได้เสมอ → fault จะไม่มีวันเกิด

**ground truth**
```sql
SELECT pid, usename, state, now() - state_change AS stuck_for, query
FROM pg_stat_activity
WHERE state = 'idle in transaction'
ORDER BY state_change ASC;
```

**assertion** — ต้องผ่านทั้ง 3 ข้อ ไม่งั้น exit ด้วย error
1. connection ใหม่ได้ error ที่ตรงกับ
   `remaining connection slots are reserved|too many clients already`
   **และต้องบันทึกว่าเจอแบบไหน** เพราะสองแบบคนละความหมาย
2. `pg_stat_activity` มี `state = 'idle in transaction'` ≥ 10 session
3. session ที่ค้างนานสุด ค้างเกิน 2 วินาที

> ⚠️ **ข้อ 1 เคยเขียนผิดจนตัวตรวจใช้ไม่ได้** — ดู E13
>
> `max_connections=20` คู่กับ `superuser_reserved_connections=3`
> → role ธรรมดาเต็มที่ **17 slot** แล้วได้ข้อความ "สงวนให้ superuser"
> ส่วน `too many clients already` เกิดเมื่อเต็มหมดรวม slot สำรอง
>
> แอปจริงต่อด้วย role ธรรมดาเสมอ → **ข้อความเดิมไม่มีวันเกิดกับแอปจริงบน config นี้**
> ตัวตรวจที่ match ข้อความเดิมจะไม่มีวันจับ F01 ได้เลย
>
> อ่าน SQLSTATE ไม่ได้เพราะเป็น connection error ระดับ libpq ไม่ใช่ query error
> (`VERBOSITY=verbose` ก็ไม่แสดง) → พึ่งข้อความ ซึ่งเสถียรเพราะ config ล็อก
> `lc_messages='C'` และ image tag ล็อกที่ `0.8.5-pg17`
> **ปลดล็อกอย่างใดอย่างหนึ่งเมื่อไหร่ ต้องรันยืนยันใหม่**

**ต้องมี `HOLD_OPEN`** — ค้าง fault ไว้หลัง assert ผ่าน ให้ตัวนับคะแนนที่รันแยกขั้นมาทัน
ไม่งั้น `trap cleanup EXIT` เก็บกวาดที่ ~5 วินาที แล้วตัวนับรายงาน "ไม่พบ"
ทั้งที่ fault เกิดจริง (ดู E14)

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-26)**

| | |
|---|---|
| สคริปต์ฉีด | `faults/f01_idle_in_transaction.sh` |
| ground truth | `groundtruth/f01.json` · `verified_on` เติมครบแล้ว |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 ทุกรอบ · 17 idle-in-transaction session ทุกรอบ** |
| หลักฐาน | `results/f01_phase1_cycle.txt` |
| ตัวนับแยกแยะได้ | `DETECTED` ระหว่าง fault ค้าง → `NOT_DETECTED` หลังเก็บกวาด ทุกรอบ |

---

## F03 — statement timeout ที่ไม่ได้ช้า แต่รอ lock ⭐

**นี่คือข้อที่พิสูจน์วิทยานิพนธ์ของโปรเจคทั้งหมด** เพราะต้องทำคู่กับ F03b
ซึ่งให้ **ข้อความ error เหมือนกันเป๊ะ แต่คนละสาเหตุ**

| | F03 | F03b |
|---|---|---|
| **อาการที่เห็น** | `ERROR: canceling statement due to statement timeout` | **เหมือนกันทุกตัวอักษร** |
| **ต้นเหตุจริง** | รอ row lock จาก transaction อื่น | query ช้าจริง (ไม่มี index) |
| **คนมักแก้ผิด** | เพิ่ม timeout · ไล่ optimize query ที่ไม่ได้ช้า | เพิ่ม timeout |
| **ทางแก้ที่ถูก** | ไล่หา transaction ที่ถือ lock | สร้าง index |

**วิธีฉีด F03**
```
session A:  BEGIN; UPDATE orders SET status='x' WHERE id=1;   (ค้างไว้)
session B:  SET statement_timeout = '2s';
            UPDATE orders SET status='y' WHERE id=1;          → timeout
```

**วิธีฉีด F03b**
```
session เดียว [role app]: SET statement_timeout = '100ms';
    SELECT count(*) FROM (
      SELECT * FROM orders
      ORDER BY created_at_naive, total_float, status, total_satang) t;
    (ไม่มี index บนคอลัมน์ที่ใช้เรียงเลยสักคอลัมน์)
```

> ⚠️ **สเปกเดิมเขียน `2s` กับ `WHERE created_at_naive < '2020-01-01'` ซึ่งใช้ไม่ได้**
> query นั้นวัดได้ 41ms และหลังแก้ E17 ข้อมูลเริ่มปี 2025 เงื่อนไขจึงตรง 0 แถว
> ไล่วัดทุกตัวเลือกบนชุด 200k แถวแล้ว **ไม่มีอันไหนช้าถึง 2 วินาที** (ดู E18)
>
> ค่า 100ms มาจากการวัด: query ที่เลือกมีค่าต่ำสุดจาก 5 รอบ = 331ms → margin 3.3 เท่า
> **เลือกจากค่าต่ำสุด ไม่ใช่ค่าเฉลี่ย** เพราะค่าต่ำสุดคือกรณีแย่สุดของ margin
>
> **สคริปต์ต้องวัด margin เองก่อนเริ่มทุกครั้ง** และ exit ถ้าไม่ถึง 2 เท่า —
> พิสูจน์แล้วว่าจำเป็น รอบหนึ่ง cache อุ่นทำให้เหลือ 151ms และ pre-flight ปฏิเสธที่จะรัน

**ground truth — ตัวแยกคือบรรทัดเดียวนี้**
```sql
SELECT pid, pg_blocking_pids(pid) AS blocked_by, state, query
FROM pg_stat_activity WHERE pid = <victim>;
```

| | `pg_blocking_pids` |
|---|---|
| F03 | **ไม่ว่าง** — มี pid ที่บล็อกอยู่ |
| F03b | **ว่าง** — ไม่มีใครบล็อก แค่ช้าเอง |

**assertion**
1. ทั้ง F03 และ F03b ต้องได้ **บรรทัด `ERROR:`** เหมือนกัน (ถ้าต่างกัน = ออกแบบผิด ต้อง exit)
2. ตัวเฝ้าดูต้องเก็บตัวอย่างได้ทั้งสองเคส — **ถ้าเก็บไม่ได้ = "ตรวจไม่ได้" ห้ามสรุป** (กฎข้อ 10)
3. F03: `pg_blocking_pids()` ต้องไม่ว่าง
4. F03b: `pg_blocking_pids()` ต้องว่าง **และต้องมีตัวอย่าง > 0** ไม่งั้นเป็นจริงแบบว่างเปล่า
5. ถ้าข้อไหนไม่เป็นจริง = fault ไม่ได้เกิดตามที่ตั้งใจ ต้อง exit

> ⚠️ **CONTEXT ต่างกัน แต่ไม่ได้ช่วยแยกสาเหตุ** (ดู E20)
> ```
> F03  : ERROR:  canceling statement due to statement timeout
>        CONTEXT:  while updating tuple (0,1) in relation "orders"
> F03b : ERROR:  canceling statement due to statement timeout
> ```
> CONTEXT บอกว่าคำสั่งกำลัง update tuple = คุณสมบัติของ **ชนิดคำสั่ง** ไม่ใช่ของ **สาเหตุ**
> `UPDATE` ที่ช้าเองก็ได้ CONTEXT แบบเดียวกัน จึงยังบอกไม่ได้ว่าต้องแก้ตรงไหน
>
> **ข้อสรุปแข็งกว่าสเปกเดิม** — พูดได้ว่า "ต่อให้อ่าน CONTEXT ด้วยก็ยังแยกไม่ออก"

> ⚠️ **ต้องเรียก `pg_stat_clear_snapshot()` ทุกรอบของลูปเฝ้าดู**
> `pg_stat_activity` ถูก snapshot ครั้งเดียวต่อ transaction — ลูปใน `DO` block เดียว
> จะเห็นภาพเดิมซ้ำแล้วรายงานว่า "ไม่มีใครถูกบล็อก" โดยไม่มี error ใดๆ (ดู E19)

> ⚠️ **หลักฐานของข้อนี้เป็น *ตาราง* ไม่ใช่สถานะสด** ต่างจาก F01/F05
> ตัวนับคะแนนจึงต้องรัน **หลัง** ตัวฉีดจบ โดยใช้ `KEEP_OBS=1`
> อย่าลอก `HOLD_OPEN` มาใช้ — วัดผิดจังหวะแน่นอน (ดู E21)

**ทำไมข้อนี้สำคัญที่สุด:** LLM ที่เห็นแค่ข้อความ error แยกสองกรณีนี้ไม่ออกเลย
ต้องดูสถานะจริงของ DB เท่านั้น — นี่คือช่องว่างที่ `quietfail-check` จะเข้าไปเติม
และเป็นข้อที่ใช้เทียบกับ baseline "โยน error ให้ LLM อธิบายลอยๆ" ได้ตรงที่สุด

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-26)**

| | |
|---|---|
| สคริปต์ฉีด | `faults/f03_timeout_two_causes.sh` |
| ตัวเฝ้าดู | `sql/f03_watcher.sql` (แยกไฟล์ ไม่ฝังใน shell — ดู E19) |
| ground truth | `groundtruth/f03.json` · ครอบทั้ง F03 และ F03b |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 · assertion ผ่าน 4/4 ทุกรอบ** |
| หลักฐาน | `results/f03_phase1_cycle.txt` |
| ผลที่วัดได้ | F03 18 ตัวอย่าง blocked=**true** · F03b 18-19 ตัวอย่าง blocked=**false** |
| ตัวนับแยกแยะได้ | `DETECTED` เมื่อมีหลักฐาน → `CANNOT_CHECK` เมื่อลบตารางทิ้ง |

> ตัวนับพลิกเป็น `CANNOT_CHECK` ไม่ใช่ `NOT_DETECTED` — ถูกต้องตามกฎเหล็กข้อ 10
> ไม่มีหลักฐาน แปลว่าตรวจไม่ได้ ไม่ใช่แปลว่าไม่มี fault

---

## F05 — อ่านข้อมูลก็ค้าง เพราะ migration ที่ยังไม่ได้เริ่ม

| | |
|---|---|
| **อาการที่เห็น** | `SELECT` ธรรมดา timeout ทั้งที่ query เบามาก |
| **คนมักแก้ผิด** | โทษ network · โทษ DB ช้า · restart แอป |
| **ต้นเหตุจริง** | `ALTER TABLE` รออยู่ในคิว lock แล้ว**คิวบล็อกทุกคนที่มาทีหลัง** |
| **ทำไม dev ไม่เจอ** | ต้องมี 3 session เรียงกันพอดี |

**จุดที่หลอกที่สุด:** `ALTER TABLE` ยัง**ไม่ได้เริ่มทำงานเลย** มันแค่รอ
แต่การรอของมันทำให้ SELECT ที่ควรเสร็จใน 1ms ค้างไปด้วย

**วิธีฉีด**
```
session A [role app]:  BEGIN; SELECT * FROM orders LIMIT 1;   (ค้างไว้ ไม่ commit)
session B [role lab]:  ALTER TABLE orders ADD COLUMN tmp int; (จะรอ A)
session C [role app]:  SELECT * FROM orders LIMIT 1;          (ค้าง! ทั้งที่ควรได้ทันที)
```

> ⚠️ **role ของแต่ละ session เป็นส่วนหนึ่งของนิยาม ไม่ใช่รายละเอียดปลีกย่อย**
>
> B ต้องเป็น **เจ้าของตาราง (`lab`)** ถ้ายิงด้วย `app` จะได้
> `ERROR: must be owner of table orders` ทันที B ตายก่อนเข้าคิว → fault ไม่เกิด
> เคยพลาดมาแล้วจริง ดู **E15**
>
> และมันสมจริงกว่า — migration รันด้วย role ที่มีสิทธิ์ ส่วนแอปรันด้วย role จำกัดสิทธิ์
> fault จึงเกิดจาก **สอง role คนละตัวชนกัน** ซึ่งอธิบายว่าทำไมทีมแอปกับทีม DB
> ถึงมักโทษกันไปมาแทนที่จะไปดูคิว lock

**ground truth**
```sql
SELECT l.pid, l.granted, l.mode, a.state, a.query
FROM pg_locks l JOIN pg_stat_activity a USING (pid)
WHERE l.relation = 'orders'::regclass
ORDER BY l.granted DESC, a.query_start;
```

**assertion**
1. session C ต้องค้างเกิน 2 วินาที (query ที่ปกติเสร็จใน < 10ms)
2. `pg_locks` ต้องมีแถวที่ `granted = false` อย่างน้อย 2 แถว
3. ต้องไล่ห่วงโซ่ได้ว่า C ถูกบล็อกโดย B และ B ถูกบล็อกโดย A

> ⚠️ **ตัวเก็บกวาดต้องยกเลิก DDL ที่ฝั่ง server ไม่ใช่แค่ฆ่า psql ฝั่ง client**
> `ALTER TABLE` ที่ส่งไปแล้วนั่งรออยู่ในคิว การฆ่า client ไม่ยกเลิกมัน
> พอ session A ตาย มันจะได้ lock แล้ว**แก้ schema จริง** — ดู **E16**
> ต้องมีทั้ง `pg_terminate_backend()` และตาข่าย `DROP COLUMN IF EXISTS`

**ต่อยอด:** I03 (`CREATE INDEX` บน vector column ไม่ใช้ `CONCURRENTLY`)
ใช้กลไกเดียวกันทุกประการ ทำ F05 เสร็จแล้ว I03 แทบไม่ต้องเขียนใหม่

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-26)**

| | |
|---|---|
| สคริปต์ฉีด | `faults/f05_lock_queue.sh` |
| ground truth | `groundtruth/f05.json` · `verified_on` เติมครบแล้ว |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 · assertion ผ่าน 3/3 ทุกรอบ** |
| หลักฐาน | `results/f05_phase1_cycle.txt` |
| ห่วงโซ่ที่วัดได้ | `SELECT` ← `ALTER TABLE` ← `idle in transaction` |
| schema ไม่ถูกแตะ | `orders` คงที่ 8 คอลัมน์ทุกรอบ · ตาข่ายไม่ต้องทำงานเลย |

---

## F10 — statistics เก่า ทำให้ planner เลือกผิด

| | |
|---|---|
| **อาการที่เห็น** | query เดิม โค้ดเดิม แต่ช้าลงหลายสิบเท่า **ไม่มี error** |
| **คนมักแก้ผิด** | rewrite query · เพิ่ม index ที่ไม่จำเป็น |
| **ต้นเหตุจริง** | สถิติไม่ทันข้อมูล planner จึงประมาณจำนวนแถวผิด |
| **ทำไม dev ไม่เจอ** | dev ไม่ได้ bulk insert แล้ว query ทันที |

**วิธีฉีด**
```sql
ALTER TABLE qf_f10_big SET (autovacuum_enabled = off);   -- กัน autovacuum มาช่วย
-- bulk insert 300,000 แถว โดยให้คอลัมน์ที่ใช้กรองเป็น **ค่าใหม่ที่ histogram ไม่เคยเห็น**
-- query ทันทีโดยไม่ ANALYZE
```

> ⚠️ **"ค่าใหม่" คือหัวใจทั้งหมด ห้ามอ่านข้าม** (ดู E22)
>
> ความล้าสมัยของ **จำนวนแถวรวม** อย่างเดียวหลอก planner **ไม่ได้**
> PostgreSQL ปรับสเกล `reltuples` ตามขนาดไฟล์จริงเสมอ
> ลองมาแล้ว: `reltuples` บอก 100 · แถวจริง 300,100 · planner เดา 191,200
> = คลาดแค่ **1.6 เท่า** ไม่ผ่าน assertion
>
> ตัวที่หลอกได้จริงคือ **สถิติระดับคอลัมน์** (MCV / histogram)
> ค่าที่ไม่เคยปรากฏตอน `ANALYZE` ครั้งล่าสุด จะถูกเดาว่าเจอน้อยมาก
> และไม่มีกลไกไหนมาปรับสเกลให้

> ⚠️ **ใช้ตาราง `qf_f10_*` ของตัวเอง ห้ามฉีดใส่ `orders`**
> `orders` เป็นฐานของ EXP01/01b/01c ที่ตัวเลขถูกอ้างทั่วทั้ง repo
> ฉีดใส่มันคือทำลาย baseline ทุกครั้งที่รัน (บทเรียนเดียวกับ E16)

**ground truth — ปิดวงจรได้ในตัว**
```sql
EXPLAIN (ANALYZE, BUFFERS) SELECT ...;   -- เทียบ rows= กับ actual rows=
ANALYZE orders;
EXPLAIN (ANALYZE, BUFFERS) SELECT ...;   -- ต้องกลับมาเร็ว
```

ถ้าสั่ง `ANALYZE` แล้วหาย = ยืนยันสาเหตุแบบเถียงไม่ได้

**assertion**
1. ก่อน ANALYZE: `rows` ต่างจาก `actual rows` **≥ 10 เท่า**
2. หลัง ANALYZE: อัตราส่วนลดลงเหลือ ≤ 2 เท่า
3. buffers หลัง ANALYZE ต้องน้อยกว่าก่อน อย่างน้อย 5 เท่า
4. ถ้าข้อ 1 ไม่เป็นจริง = autovacuum แอบมาทำงาน ต้อง exit ไม่ใช่ปล่อยผ่าน

> ⚠️ **ต้องอ่าน `Plan Rows` / `Actual Rows` จาก node ของ join ไม่ใช่ node บนสุด**
> node บนสุดคือ `Aggregate` ของ `count(*)` → ประมาณ 1 ได้จริง 1 **เสมอ**
> ไม่ว่าสถิติจะเพี้ยนแค่ไหน วัดตรงนั้นจะได้ "คลาด 1.0 เท่า" ตลอดแล้วสรุปผิด (ดู E22)
> ส่วน buffers อ่านจาก node บนสุดได้ เพราะ EXPLAIN รวมของลูกมาให้แล้ว

**ข้อมูลที่วัดไว้แล้ว (EXP01b Phase D) — ใช้เป็นฐานเปรียบเทียบ**
```
MCV ติดตาม 100 จาก n_distinct 4,695 = 2.1% ของค่าทั้งหมด
ค่าใน MCV  (merchant 1)    → ประมาณ 23,987 จริง 23,983 = คลาด 0.02%
ค่านอก MCV (merchant 4999) → ประมาณ 27     จริง 6      = คลาด 4.5 เท่า
```
(รันใหม่ 2026-07-26 หลังแก้ E17 · ค่าเดิมบน seed ที่พัง: n_distinct 4,677 คลาด 0.6% / 50%)
ความคลาด **แย่ที่สุดตรงค่าที่พบน้อย** ซึ่งเป็นจุดที่การเลือก plan สำคัญที่สุด
สถิติเก่าจะทำให้ตัวเลขนี้แย่ลงไปอีก

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตรรกะ | `sql/f10_stale_stats.sql` (SQL ล้วน — ไม่ต้องใช้หลาย session) |
| ตัวห่อ | `faults/f10_stale_statistics.sh` (รัน · ตรวจ exit code · เก็บกวาด) |
| ground truth | `groundtruth/f10.json` · `verified_on` เติมครบแล้ว |
| ตัวนับคะแนน | `sql/score.sql` |
| รันจริง | **3 รอบติด exit=0 · assertion ผ่าน 3/3 ทุกรอบ** |
| หลักฐาน | `results/f10_phase1_cycle.txt` |
| ตัวนับแยกแยะได้ | `DETECTED` เมื่อมีหลักฐาน → `CANNOT_CHECK` เมื่อลบตารางทิ้ง |

**ผลที่วัดได้ (300,000 แถว):**

```
ก่อน ANALYZE : ประมาณ 200 จริง 300,000 (คลาด 1,500x) · buffers 1,202,548 · Nested Loop · 702 ms
หลัง ANALYZE : ประมาณ 298,733 จริง 300,000 (คลาด 1.0x) · buffers 3,726     · Hash Join   · 306 ms
```

**นิ่งมาก:** 3 รอบได้ 1500.0x / 1507.5x / 1507.5x · buffers ลด 322.7x ทุกรอบ

### ⭐ ข้อสังเกตที่มีค่ากว่าตัว fault — นาฬิกาโกหก buffers ไม่โกหก

```
เวลา    702 ms → 306 ms   =   2.3 เท่า
buffers 1,202,548 → 3,726 = 322.7 เท่า
```

ต่างกัน **140 เท่า** เพราะข้อมูลชุดนี้อยู่ใน cache หมด
buffer ส่วนเกิน 1.2 ล้านครั้งจึงเป็น `hit` ล้วน ไม่แตะดิสก์เลย

บนเครื่องที่ข้อมูลไม่พอใส่ RAM ส่วนเกินนั้นจะกลายเป็นการอ่านดิสก์จริง
→ **วัดด้วยนาฬิกาบนเครื่อง dev จะสรุปว่า "ช้าลงนิดหน่อย" แล้วปล่อยขึ้น production**

นี่คือหลักฐานที่ชัดที่สุดของ **กฎเหล็กข้อ 7** และเป็นเหตุผลว่าทำไม
`quietfail-check` ต้องดู buffers ไม่ใช่เวลา

---

# ชุด vector — 12 ข้อ (ล็อกแล้ว)

## ชั้น I — ติดตั้งและสร้าง index

| ID | fault | หลักฐาน |
|---|---|---|
| **I01** | opclass ตอนสร้างไม่ตรงกับ operator ตอน query → index ไม่เคยถูกใช้ | ✅ เอกสาร |
| **I02** | สร้าง IVFFlat ตอนตารางยังเล็ก → กลุ่มไม่สะท้อนข้อมูลจริง | ✅ เอกสาร |
| **I03** | `CREATE INDEX` ไม่ใช้ `CONCURRENTLY` → ล็อกตาราง (กลไกเดียวกับ F05) | ✅ เอกสาร |
| **I04** | k-means ของ IVFFlat สุ่ม → สร้างใหม่ได้คำตอบไม่เหมือนเดิม | ⚠️ ต้องพิสูจน์เอง |
| **I05** | `maintenance_work_mem` ต่ำ → build ช้าผิดปกติ | ✅ มีข้อความ NOTICE |

> ⚠️ ตารางข้างบนเป็น**ดัชนี** ไม่ใช่นิยาม — ตามกฎหัวไฟล์นี้ fault ต้องมีครบ 5 อย่าง
> ข้อที่ลงมือทำแล้วจะมีนิยามเต็มต่อจากนี้ ข้อที่ยังไม่มี = **ยังไม่พร้อมทำ**

---

### I01 — opclass ตอนสร้าง ไม่ตรงกับ operator ตอน query

| | |
|---|---|
| **อาการที่เห็น** | ช้าเหมือนไม่มี index ทั้งที่ `\di` เห็น index อยู่ชัดๆ และ build ไปแล้วเป็นนาที |
| **คนมักแก้ผิด** | เพิ่ม `m` / `ef_construction` · rebuild index · เพิ่ม RAM · โทษว่า HNSW ไม่เวิร์ค |
| **ต้นเหตุจริง** | index สร้างด้วย `vector_l2_ops` แต่ query ใช้ `<=>` (cosine) → **planner ใช้ index นั้นไม่ได้เลย** |
| **ทำไม dev ไม่เจอ** | ข้อมูลชุดเล็ก seq scan ก็เร็วอยู่แล้ว ไม่มีใครสังเกต |

**หลักฐานจากเอกสาร:** ต้องสร้าง index แยกต่อหนึ่ง distance function —
`vector_l2_ops` · `vector_ip_ops` · `vector_cosine_ops` · `vector_l1_ops` (ดู `EVIDENCE.md`)

**วิธีฉีด**
```sql
-- ผิด: สร้างด้วย l2 แต่ค้นด้วย cosine
CREATE INDEX ON qf_corpus USING hnsw (embedding vector_l2_ops);
SELECT id FROM qf_corpus ORDER BY embedding <=> $1 LIMIT 10;   -- <=> คือ cosine
```

**ground truth — มีสองชั้น และชั้นที่สองคือของจริง**

1. **ชั้นแผน (ต้องรัน):** `EXPLAIN` ไม่มี `Index Scan` → เป็น Seq Scan
2. **ชั้น catalog (ไม่ต้องรัน query เลย):** อ่าน opclass ของ index จาก `pg_index`
   แล้วเทียบกับ operator ที่โค้ดใช้ — `quietfail-check` ใช้ชั้นนี้
```sql
SELECT c.relname, opc.opcname
FROM pg_index i
JOIN pg_class c   ON c.oid = i.indexrelid
JOIN pg_opclass opc ON opc.oid = i.indclass[0];
```

**assertion** — ต้องผ่านครบ ไม่งั้น exit
1. opclass ผิด → แผน **ต้องไม่มี** `Index Scan`
2. opclass ถูก (กลุ่มควบคุม) → แผน **ต้องมี** `Index Scan`
   *ถ้าข้อนี้ไม่ผ่าน แปลว่าสภาพแวดล้อมใช้ index ไม่ได้อยู่แล้ว ข้อ 1 จึงไม่พิสูจน์อะไร*
3. catalog ต้องอ่าน opclass ออกมาได้ตรงกับที่สร้าง — อ่านไม่ได้ = **"ตรวจไม่ได้"** ไม่ใช่ "ผ่าน"
4. เวลา/buffers ของกรณี opclass ผิด ต้องใกล้เคียงกรณีไม่มี index

**⭐ I01 คือภาพสะท้อนกลับด้านของ Q01 — ต้องเขียนคู่กันในรายงาน**

| | index ถูกใช้ไหม | recall | ความเร็ว | ผู้ใช้คิดว่า |
|---|---|---|---|---|
| **Q01** | ใช้ | **ตก** | เร็วขึ้นมาก | "ระบบเร็วดี" |
| **I01** | **ไม่ถูกใช้** | **1.0 สมบูรณ์** | ไม่เร็วขึ้นเลย | "มี index แล้วนี่" |

I01 จ่ายค่า build เต็มราคา (HNSW 89 วิ · 195 MB ที่ 100k) แล้ว**ไม่ได้อะไรกลับมาเลย**
แต่คำตอบถูกต้อง 100% เพราะมันตกกลับไปเป็น exact search
→ **ไม่มีทางรู้จากคุณภาพผลลัพธ์ ต้องดู plan หรือ catalog เท่านั้น**

**สถานะ: ✅ ครบวงจรแล้ว (2026-07-27)**

| | |
|---|---|
| ตัวฉีด | `sql/i01_opclass_mismatch.sql` |
| ตัวพิสูจน์ตัวตรวจ | `sql/i01_checker_states.sql` |
| ground truth | `groundtruth/i01.json` |
| ตัวนับคะแนน | `sql/score.sql` — **static ล้วน อ่าน catalog อย่างเดียว** |
| รันจริง | **3 รอบติด (ivfflat) + 1 รอบ (hnsw) · assertion ผ่าน 4/4 ทุกรอบ** |
| หลักฐาน | `results/i01_phase3_cycle.txt` |

**ผลที่วัดได้ — HNSW ที่ 100,000 แถว**

| | plan | build | ขนาด | 200 query | buffers | recall@10 |
|---|---|---|---|---|---|---|
| A ไม่มี index | Seq Scan | — | — | 10,988 ms | 20,000 | 1.0000 |
| **B opclass ผิด** | **Seq Scan** | **55,456 ms** | **195 MB** | 10,502 ms | 20,000 | **1.0000** |
| C opclass ถูก | Index Scan | 65,091 ms | 195 MB | **235 ms** | 949 | 0.8435 |

**จ่าย 55 วินาที + 195 MB แล้วได้กลับมาศูนย์** — plan เดิม buffers เดิม เวลาเท่าเดิมกับไม่มี index
ส่วน opclass ที่ถูกเร็วกว่า **44.7 เท่า**

**และ recall ของ B สมบูรณ์ 1.0000** เพราะตกกลับไปเป็น exact search
→ **ตรวจจากคุณภาพผลลัพธ์ไม่ได้เลย ต้องดู plan หรือ catalog เท่านั้น**

**ตัวตรวจ static แยกได้ครบ 3 สถานะ 3 รอบติด**
```
ไม่มี vector index  -> CANNOT_CHECK   (ไม่ใช่ "ผ่าน" — กฎเหล็กข้อ 10)
opclass ผิด         -> DETECTED
opclass ถูก         -> NOT_DETECTED
```
**ไม่ต้องมีข้อมูล ไม่ต้องรัน query เลย** → เข้ากลุ่ม "ยืนยันได้ → fail build ทันที" (`PROJECT.md` ข้อ 8)

**ข้อสังเกตที่ยังไม่สรุป — สัญญาณของ I04**
IVFFlat opclass ถูก 3 build ในรอบเดียวกัน ข้อมูลเดียวกัน พารามิเตอร์เดียวกัน
ได้ recall@10 = **0.7780 · 0.8020 · 0.7415** (ช่วง 0.06 = 8%)
เข้ากันได้กับ I04 (k-means สุ่ม) แต่ **ยังไม่ใช่ข้อสรุป** — I04 ต้องออกแบบการทดลองของตัวเอง

## ชั้น Q — เรียกใช้

| ID | fault | หลักฐาน |
|---|---|---|
| **Q01** ⭐ | recall collapse — เร็วขึ้นมาก ผลดูดี แต่ของที่ควรเจอหายไป | ✅ เอกสารเขียนเอง · **วัดแล้ว ดูข้างล่าง** |
| **Q02** | เขียน `WHERE distance < x` แทน `ORDER BY ... LIMIT` | ✅ เอกสาร |
| **Q03** | filter ทำงานหลังสแกน index → ขอ 40 ได้ 4 | ✅ มีตัวเลขในเอกสาร |
| **Q04** | ใช้ค่า default โดยไม่เคยวัด (`probes = 1`) | ✅ วัดเองแล้ว |
| **Q06** | `LIMIT` มากกว่า `ef_search` (40) | ✅ FAQ ทางการ |

### Q01 — ผลที่วัดได้จริง ⭐ (2026-07-27)

**ที่ 100,000 แถว · 384 มิติ · 50 กลุ่ม · 200 query · cosine**
หลักฐาน `results/q01_recall_collapse_100k.txt` · สคริปต์ `sql/q01_recall_collapse.sql`

| | recall@10 | recall@100 | แย่สุด@10 | ครบ 10/10 | 200 query | เร็วขึ้น | ขนาด index |
|---|---|---|---|---|---|---|---|
| exact (ไม่มี index) | **1.0000** | **1.0000** | 1.0000 | 200/200 | 14,148 ms | 1.0× | — |
| HNSW ค่าเริ่มต้น | **0.8565** | **0.3903** | **0.0000** | 52/200 | 434 ms | **32.6×** | 195 MB |
| IVFFlat ค่าเริ่มต้น | **0.7870** | **0.7793** | **0.0000** | 100/200 | 317 ms | **44.6×** | 157 MB |

**ไม่ได้จูนอะไรเลย** — สร้าง index โดยไม่ระบุพารามิเตอร์ และ query ด้วยค่าเริ่มต้น
(`ef_search=40` · `probes=1`) เพราะนั่นคือสิ่งที่คนทำจริง

**ตัวเลขที่ควรอยู่ในบรรทัดเรซูเม่:**

> เร็วขึ้น **44.6 เท่า** แลกกับ recall@10 ที่ **0.7870**
> = ผลลัพธ์ที่ควรเจอ **หายไป 21%** โดยไม่มี error ใดๆ

**สามอย่างที่น่ากลัวกว่าค่าเฉลี่ย:**

1. **`แย่สุด@10 = 0.0000` ทั้งสองแบบ** — มี query ที่ผลถูกต้อง **หายเกลี้ยงทั้ง 10 อันดับ**
   ค่าเฉลี่ย 0.79 ซ่อนกรณีที่ผู้ใช้ค้นแล้วไม่เจออะไรที่ถูกเลยไว้ข้างใน
2. **HNSW ครบ 10/10 แค่ 52 จาก 200 query** — 74% ของ query ได้คำตอบไม่ครบ
3. **HNSW recall@100 = 0.3903 แย่กว่า recall@10 มาก** — หายไป 61%
   เพราะ `ef_search=40` เล็กกว่า `LIMIT 100` → **นี่คือกลไกของ Q06 โผล่มาในผลของ Q01**
   ขอ 100 แต่ candidate list มีแค่ 40 จะเอาที่ถูก 100 อันมาจากไหน

**IVFFlat ต่างจาก HNSW ตรงรูปทรงของความเสียหาย** — IVFFlat recall@10 กับ @100
ใกล้เคียงกัน (0.787 / 0.779) เพราะ `probes=1` ตัดทิ้งทั้งกลุ่มตั้งแต่ต้น
ส่วน HNSW ยิ่งขอเยอะยิ่งแย่ลง เพราะเพดานอยู่ที่ขนาด candidate list

**ที่ 500,000 แถว — พารามิเตอร์เดิมทุกตัว**
หลักฐาน `results/q01_recall_collapse_500k.txt`

| | recall@10 | recall@100 | แย่สุด@10 | ครบ 10/10 | 200 query | เร็วขึ้น | ขนาด index |
|---|---|---|---|---|---|---|---|
| exact (ไม่มี index) | **1.0000** | **1.0000** | 1.0000 | 200/200 | 53,281 ms | 1.0× | — |
| HNSW ค่าเริ่มต้น | **0.5885** | **0.3871** | **0.0000** | **2/200** | 455 ms | **117.1×** | 977 MB |
| IVFFlat ค่าเริ่มต้น | **0.7820** | **0.7857** | **0.0000** | 108/200 | 1,032 ms | 51.6× | 782 MB |

### ⭐⭐ ผลที่สำคัญที่สุดของทั้งโปรเจค — index สองแบบเสื่อมตามขนาดข้อมูลไม่เหมือนกัน

| | 100k | 500k | เปลี่ยนไป |
|---|---|---|---|
| **HNSW** recall@10 | 0.8565 | **0.5885** | **ตกลง 31%** |
| **HNSW** ครบ 10/10 | 52/200 | **2/200** | **แทบไม่เหลือ** |
| **HNSW** เร็วขึ้น | 32.6× | **117.1×** | เร็วขึ้นอีก |
| IVFFlat recall@10 | 0.7870 | 0.7820 | **แทบไม่ขยับ** |
| IVFFlat เร็วขึ้น | 44.6× | 51.6× | ใกล้เคียงเดิม |

**HNSW ยิ่งข้อมูลเยอะยิ่งพัง ส่วน IVFFlat นิ่ง** — และสาเหตุอยู่ที่รูปแบบของค่าเริ่มต้น:

- `hnsw.ef_search = 40` เป็น **ค่าสัมบูรณ์** — 40 candidate จาก 100k คือ 1 ใน 2,500
  แต่ 40 จาก 500k คือ 1 ใน 12,500 · ข้อมูลโต 5 เท่า สัดส่วนที่ค้นจริงหดลง 5 เท่า
- `ivfflat.probes = 1` เป็น **ค่าสัมพัทธ์** — 1 จาก `lists=100` คือ 1% เสมอ
  ไม่ว่าข้อมูลจะโตแค่ไหน สัดส่วนที่ค้นจริงเท่าเดิม

**ที่อันตรายที่สุดคือ HNSW เร็วขึ้นพร้อมกับที่มันพังลง** — 32.6× → 117.1×
ทีมที่ดูแต่ latency จะเห็นว่า "ระบบดีขึ้นเรื่อยๆ ตอนข้อมูลโต"
ทั้งที่ recall กำลังพังลงเงียบๆ ในเวลาเดียวกัน

**และมันตอบคำถามที่ L01 ถูกตัดไป** — Q01 สองขนาดให้หลักฐานบางส่วนแล้วว่า
พารามิเตอร์ที่เหมาะกับข้อมูลขนาดหนึ่ง ไม่เหมาะกับอีกขนาดหนึ่ง
โดยไม่ต้องรันครบ 4 ขนาดตามที่ L01 ต้องการ (ดู D15)

**ต้นทุนที่ต้องบันทึกด้วย:** HNSW build ที่ 500k ใช้ **603 วินาที (10 นาที)**
เทียบกับ 89 วินาทีที่ 100k = โตขึ้น 6.7 เท่าจากข้อมูล 5 เท่า
ส่วน IVFFlat ใช้ 7.1 วินาที = **ต่างกัน 85 เท่า**

**หลักฐานว่า corpus ทำซ้ำได้จริง:** `corpus_fingerprint_first5k` ของรอบ 500k
เท่ากับรอบ 100k เป๊ะ (`5a32cba5…`) เพราะ 5,000 แถวแรกถูกสร้างด้วยลำดับเดียวกัน

---

### เฟส 2.5 — Q01 บน Qdrant: "นี่มันบั๊กของ pgvector หรือเปล่า"

หลักฐาน `results/q01_qdrant_cross_engine.txt` · Qdrant 1.12.4 · 100,000 แถวชุดเดียวกัน

**สิ่งที่คุมให้เหมือนกันทุกอย่าง ตัวแปรเดียวที่ต่างคือ engine:**
corpus ส่งออกทีละแถวจาก `qf_corpus` · ชุด query fingerprint เดิม ·
**เฉลยใช้ `qf_truth` ที่มาจาก exact search ของ PostgreSQL** · สูตร recall เดียวกันทุกตัวอักษร

**ตัวสอบทานที่ทำให้เทียบกันได้จริง:** Qdrant ค้นแบบ `exact` ได้ **recall@10 = recall@100 = 1.0000**
เทียบกับเฉลยของ PostgreSQL → ข้อมูลส่งไปครบทุกจุด และทั้งสอง engine ใช้ระยะทางเดียวกัน
ถ้าข้อนี้ไม่ผ่าน ตัวเลขที่เหลือไม่มีความหมายเลย

| engine (ef เท่ากันสองแถวแรก) | recall@10 | recall@100 |
|---|---|---|
| **pgvector** HNSW `ef_search=40` | **0.8565** | **0.3903** |
| **Qdrant** HNSW `hnsw_ef=40` | **0.9945** | **0.9895** |
| Qdrant HNSW ค่าเริ่มต้นของ Qdrant เอง | 1.0000 | 0.9996 |

### ข้อสรุปที่ตอบได้จริง — และที่ตอบไม่ได้

**✅ ตอบได้: recall loss ไม่ใช่บั๊กของ pgvector**
Qdrant ก็เสีย recall เหมือนกันเมื่อใช้ความพยายามค้นหาเท่ากัน —
`0.9945` ไม่ใช่ `1.0` · แย่สุดต่อ query = **0.90** · ที่ k=100 ครบทุกอันแค่ **69 จาก 200 query**
การค้นแบบประมาณทำผลลัพธ์หายจริงในทั้งสอง engine

**⛔ ตอบไม่ได้ และห้ามเขียนว่าตอบได้: "ANN ทุกตัวพังเท่ากัน"**
ขนาดของความเสียหายต่างกันมหาศาล — pgvector recall@100 ตกเหลือ 0.39
ส่วน Qdrant ที่ ef เท่ากันยังได้ 0.99

**สาเหตุที่ต่างกันคือ `ef_search < LIMIT` ซึ่งเป็นพฤติกรรมเฉพาะของ pgvector**
- pgvector: `ef_search=40` กับ `LIMIT 100` → candidate list เล็กกว่าจำนวนที่ขอ
  จะเอาคำตอบที่ถูก 100 อันมาจากไหน → recall@100 พังยับ (**นี่คือ Q06**)
- Qdrant: ตั้ง `hnsw_ef=40` กับ `limit=100` แล้ว recall@100 ยังได้ 0.9895
  แปลว่า **Qdrant ไม่ปล่อยให้ ef ต่ำกว่า limit มาตัดคุณภาพทิ้ง**

**ประโยคที่เขียนลงรายงานได้แบบเถียงไม่ได้:**
> การค้นแบบประมาณทำให้ผลลัพธ์หายจริงในทั้งสอง engine — **มันคือธรรมชาติของ ANN**
> แต่ **ขนาด**ของความเสียหายถูกกำหนดโดยค่าเริ่มต้นที่ผู้พัฒนาเลือกให้
> และโดยพฤติกรรมเมื่อ `ef < k` ซึ่งเป็นเรื่องของแต่ละ engine ไม่ใช่ของ ANN

**ผลข้างเคียงที่มีค่าไม่แพ้กัน:** Qdrant ที่ค่าเริ่มต้นล้วนๆ **ไม่สร้าง HNSW เลย**
ที่ขนาดนี้ (`indexed_vectors_count: 0` · `status: green`) → ดู **E24** และ **U04**
ต้องลด `indexing_threshold` เพื่อบังคับให้สร้าง index ก่อนถึงจะวัดได้
โดย `m=16` และ `ef_construct=100` ยังเป็นค่าเริ่มต้นของ Qdrant ไม่ได้แตะ

---

**ของแถมที่ยืนยัน I05 ไปในตัว** — ตอน build HNSW ได้ NOTICE จริงบน 0.8.5:
```
NOTICE:  hnsw graph no longer fits into maintenance_work_mem after 28365 tuples
DETAIL:  Building will take significantly more time.
HINT:  Increase maintenance_work_mem to speed up builds.
```
ตรงกับที่ `EVIDENCE.md` เขียนไว้ทุกตัวอักษร และได้ตัวเลขจริงมาด้วย (28,365 tuples
จาก 100,000 = `maintenance_work_mem` 64MB รับได้แค่ 28%)
build ใช้เวลา **89.4 วินาที** ส่วน IVFFlat แค่ **2.2 วินาที** = ต่างกัน 40 เท่า

---

## ชั้น V — คุณภาพข้อมูล

| ID | fault | หลักฐาน |
|---|---|---|
| **V07** | NULL vector และ zero vector ไม่ถูก index → แถวหายถาวรเงียบสนิท | ✅ เอกสาร |

## ชั้น L — เสื่อมตามเวลา

| ID | fault | หลักฐาน |
|---|---|---|
| **L02** | dead tuple สะสม → index บวม recall ตก | ✅ FAQ ทางการ |

---

# ที่พิจารณาแล้วไม่รับเข้าชุด

**เลขที่หายไปคือข้อมูล ไม่ใช่ความผิดพลาด** — มันบอกว่าเคยพิจารณาแล้วตัดออก
**ห้ามเรียงเลขใหม่** เพราะไฟล์อื่นอ้างเลขเหล่านี้ไปแล้ว

| ID | fault | เหตุผลที่ตัด |
|---|---|---|
| F02 | deadlock จากลำดับ lock ไม่ตรงกัน | ซ้อนทับกับ F03 · เก็บไว้ถ้าเหลือเวลา |
| F04 | serialization failure ถูกแก้ผิดทาง | ต้องอธิบาย isolation level ยาว ต้นทุนสูง |
| F06 | temp file ระเบิดเพราะไม่มี index | ซ้อนทับกับ F10 |
| F07 | check-then-act ทำให้ข้อมูลซ้ำ | ไม่เกี่ยวกับ vector · ใช้เวลาสร้าง load test นาน |
| F08 | เงินเพี้ยนเพราะใช้ float | schema เตรียมไว้แล้ว แต่เป็น static check ล้วน ไม่ต้องฉีด |
| F09 | เวลาเพี้ยนเพราะ timestamp ไม่มี timezone | เหมือน F08 |
| F11 | มี index แต่ใช้ไม่ได้ | ซ้อนทับกับ I01 |
| F12 | ตารางบวมเพราะ transaction ค้าง | ซ้อนทับกับ L02 |
| Q05 | สลับ cosine กับ inner product | ยืนยันได้แค่บางส่วน · ซ้อนทับกับ I01 |
| L01 | พารามิเตอร์ไม่ scale ตามขนาดข้อมูล | ต้องมีข้อมูล 4 ขนาด ต้นทุนสูง |
| V08 | index ร่วมกันหลาย tenant กระทบ recall กัน | ต้องสร้างระบบหลาย tenant ก่อน |

> `init/02_schema.sql` มีคอมเมนต์อ้าง F06–F09, F11 อยู่
> เป็นเศษของแผนเดิม ไม่ใช่ข้อผิดพลาด — schema เตรียมของไว้แล้ว
> ถ้าอนาคตรับเข้าชุดก็ทำได้ทันที

---

# บั๊กนอกแผนที่เจอระหว่างทาง

> **ที่นี่คือดัชนี ไม่ใช่ที่เก็บเนื้อหา** — บันทึกเต็มอยู่ใน `DECISIONS.md`
> คัดเนื้อหามาซ้ำเมื่อไหร่ มันจะเคลื่อนออกจากกันเมื่อนั้น ซึ่งคือ D15 ทั้งดุ้น
>
> **ไม่มีข้อไหนอยู่ในชุด 16** การรับเข้าชุดต้องตัดของเดิมออก (กฎเหล็กข้อ 4)
> และเป็นการตัดสินใจของเจ้าของโปรเจค

**กติกาการบันทึก:** เจอบั๊กที่ไม่อยู่ในแผนเมื่อไหร่ เพิ่มแถวที่นี่ทันที
พร้อมบันทึกเต็มใน `DECISIONS.md` — **ห้ามแก้เงียบๆ แล้วเดินต่อ**
เพราะบั๊กที่ไม่ได้บันทึกคือบั๊กที่จะเกิดซ้ำ และเป็นวัตถุดิบของหัวข้อ
"สิ่งที่ลองแล้วไม่ได้ผล" ใน README ซึ่งหายากมากในพอร์ตนิสิต

## ก) พฤติกรรมของ PostgreSQL / pgvector — อาจกลายเป็น fault ได้

| ID | เจออะไร | บันทึกเต็ม | เงียบไหม | สถานะ |
|---|---|---|---|---|
| **F13** | เพิ่ม index แล้วช้าลง 118% เพราะ lossy bitmap · **planner เลือกเอง** | E08 · EXP01c | ✅ เงียบสนิท | **ปิดแล้ว — อยู่ทะเบียนนี้ถาวร** (ดูข้างล่าง) |
| **U01** | uncorrelated subquery ถูกยกเป็น **InitPlan** คำนวณครั้งเดียว → 100,000 แถวได้ค่าซ้ำกันหมด | E11 | ✅ **เงียบสนิท ไม่มี error** | ยังไม่เสนอ |
| **U02** | `l2_norm(vector)` ไม่มีใน 0.8.5 แต่ error บอกว่า `is not unique` แล้ว HINT ชวนให้ใส่ cast → ถ้าทำตามจะไปลง `::halfvec` ซึ่ง**ลดความละเอียดข้อมูลเงียบๆ** | E10 | ⚠️ error ดัง แต่ชี้ผิดทาง | ยังไม่เสนอ |
| **U03** ⭐ | `pg_stat_activity` ถูก snapshot **ครั้งเดียวต่อ transaction** → ลูปเฝ้าระวังใน `DO` block เดียวเห็นภาพเดิมซ้ำตลอด แล้วรายงานว่า "ระบบปกติดี" ตลอดไป | E19 | ✅ **เงียบสนิท** | ยังไม่เสนอ |
| **U04** ⭐ | Qdrant 1.12.4 ที่ค่าเริ่มต้นล้วนๆ **ไม่สร้าง HNSW เลย** ที่ 100k จุด · `status: green` · `indexed_vectors_count: 0` · ค้นแบบ brute force โดยผู้ใช้ไม่รู้ | E24 | ✅ **เงียบสนิท** | ยังไม่เสนอ |

### F13 เต็มๆ — index ทำให้ query ช้าลง เพราะ lossy bitmap

**ตัดสินแล้ว 2026-07-27: อยู่ในทะเบียนนี้ถาวร ไม่พิจารณาเป็น fault หลักอีก**

เหตุผล: EXP01c ตอบเงื่อนไขที่ F13 ตั้งไว้เองแล้วว่า lossy **หายไป**เมื่อ `work_mem` พอ
และการรับเข้าชุด 12 ต้องตัดของเดิมออกหนึ่งข้อ (กฎเหล็กข้อ 4)
ซึ่งไม่คุ้มกับ fault ที่ config กลบได้

**ยังมีค่าในฐานะหลักฐาน** — มันคือที่มาของ **กฎเหล็กข้อ 7ก**
และเป็นตัวอย่างที่ดีที่สุดว่า cost model ของ planner พลาดตรงไหน

**ค้นพบเองจาก EXP01b ไม่ได้อยู่ในแผนเดิม** ดู `DECISIONS.md` E08


**ค้นพบเองจาก EXP01b ไม่ได้อยู่ในแผนเดิม** ดู `DECISIONS.md` E08

| | |
|---|---|
| **อาการ** | เพิ่ม index แล้วช้าลง **118%** ไม่มี error (เดิมวัดได้ 27% บน seed ที่พัง) |
| **ต้นเหตุ** | `work_mem` เล็กเกิน → bitmap ลดชั้นเป็น lossy → อ่านทั้งหน้าแล้วคัดใหม่ |
| **จุดสำคัญ** | **planner เลือกเอง** — cost บอกถูกกว่า 3.5% แต่จริงแพงกว่า 2.2 เท่า |

**เงื่อนไขที่ต้องตอบก่อนตัดสิน:** รัน EXP01b ซ้ำบนโปรไฟล์ `realistic` (`work_mem=4MB`)
- ถ้า lossy **หายไป** → เป็น "ปัญหาที่ config กลบไว้" คุณค่าลดลง อาจเก็บเป็นเชิงอรรถ
- ถ้า **ยังอยู่** → เป็น fault เต็มตัว ควรรับเข้าแทนข้อที่อ่อนกว่า

### ✅ ตอบแล้ว 2026-07-26 — EXP01c

**lossy หายไป** และหายตั้งแต่ `work_mem` ต่ำกว่า 4MB มาก
แยกตัวแปรแล้ว: container เดียว `shared_buffers` เดียว ข้อมูลชุดเดียว ปิด parallel
หลักฐาน `results/exp01c_work_mem_threshold.txt`

| work_mem | Heap Blocks | recheck rows | buffers |
|---|---|---|---|
| 64kB | exact=931 **lossy=1548** | 110,062 | 2501 |
| 96kB | exact=932 **lossy=1547** | 109,966 | 2501 |
| 128kB | exact=1446 **lossy=1033** | 73,470 | 2501 |
| 160kB | exact=2479 | 0 | 2501 |
| 4MB | exact=2479 | 0 | 2501 |

เส้นแบ่งอยู่ระหว่าง **128kB กับ 160kB** สำหรับชุดนี้ — **เท่าเดิมหลังรันใหม่บน seed ที่แก้แล้ว**
(200k แถว · 2,479 หน้า · ตรงเงื่อนไข 23,983 แถว) — ผูกกับขนาดข้อมูล ห้ามยกไปใช้ที่อื่น

**ตามเกณฑ์ที่เขียนไว้เอง → เก็บเป็นเชิงอรรถ ไม่รับเข้าชุด**

> ⚠️ **แต่เกณฑ์ข้อนี้อาจตีค่าต่ำเกินไป — ต้องตัดสินใหม่**
>
> `config/postgresql.realistic.conf` เขียนนิยามไว้เองว่า
> *"ข้อที่หายไป = ปัญหาที่ config กลบไว้ชั่วคราว (**ซึ่งอันตรายกว่า**)"*
> ซึ่งขัดกับ "คุณค่าลดลง" ในเกณฑ์ข้างบน
>
> และ `work_mem` เป็นโควตา **ต่อ operation ไม่ใช่ต่อเครื่อง** —
> ระบบที่ตั้งพอในภาวะปกติจะเจออาการนี้กลับมาตอนโหลดหนัก โดยไม่มีอะไรเปลี่ยนในโค้ด
>
> **ยังไม่ตัดสินให้** เพราะการรับเข้าชุดต้องตัดของเดิมออกหนึ่งข้อ (กฎเหล็กข้อ 4)
> ซึ่งเป็นการตัดสินใจของเจ้าของโปรเจค

**ของแถมที่มีค่ากว่าตัว F13:** buffers = 2501 **เท่ากันเป๊ะทั้ง 6 กรณี**
ตัวชี้วัดหลักของโปรเจคมองไม่เห็น fault นี้เลย → เกิดเป็น **กฎเหล็กข้อ 7ก**

---

---

**U03 ควรอ่านคู่กับวิทยานิพนธ์ของโปรเจค** — มันคือความล้มเหลวเงียบ
**ในเครื่องมือที่เขียนขึ้นมาเพื่อตรวจความล้มเหลวเงียบ** เครื่องมือเฝ้าระวัง
ที่เขียนเป็นลูปใน transaction เดียวจะบอกว่าทุกอย่างปกติไปตลอดกาล

**U01 คือข้อที่แรงที่สุดในตารางนี้** — ไม่มี error ไม่มี warning จำนวนแถวถูก
มิติถูก ชนิดถูก ครบทุกกลุ่ม แต่ข้อมูลใช้ไม่ได้เลย
และ assertion 4 ข้อที่เขียนไว้ตอนนั้น **ผ่านหมด**

**U02 อยู่ตระกูลเดียวกับ F03** — ข้อความ error ชี้ไปคนละทางกับต้นเหตุ
ต่างกันตรง F03 ทำให้แก้ผิดที่ ส่วน U02 ทำให้แก้แล้วเสียข้อมูลเพิ่ม

## ข) บั๊กของ harness ตัวเอง — ไม่ใช่ fault แต่ต้องจำ

ทุกข้อคือความล้มเหลวเงียบในโปรเจคที่ศึกษาความล้มเหลวเงียบ

| ID | เจออะไร | บันทึกเต็ม | จับได้เพราะ |
|---|---|---|---|
| **H01** | `make seed` ตั้ง default 2M แต่ผลทุกชุดมาจาก 200k | E09 | ไล่ตรวจอย่างเป็นระบบ (ยังไม่เคยแสดงอาการ) |
| **H02** | กลุ่ม vector มีอยู่แค่ในคอลัมน์ `cluster_id` ไม่ได้มีอยู่จริงเชิงเรขาคณิต (ในกลุ่ม 0.98 vs ข้ามกลุ่ม 1.00) | E12 | ตรวจ sanity ก่อนวัด ตามที่ `PROJECT.md` ข้อ 10 สั่งไว้เอง |
| **H03** | assertion ของ F01 ระบุข้อความ error ที่ไม่มีวันเกิดกับ role ธรรมดา | E13 | รันจริงครั้งแรก assertion ไม่ผ่าน |
| **H04** | ตัวฉีดเก็บกวาดตัวเองที่ ~5 วิ ตัวนับคะแนนที่รันแยกขั้นมาไม่ทัน → false negative | E14 | แยกขั้นตอนแล้วผลไม่ตรงกับที่เห็นตอนรันรวม |
| **H05** | buffers มองไม่เห็น lossy bitmap (2501 เท่ากันทั้ง 6 ค่า) | EXP01c | วัดหลายค่าแล้วเทียบ |
| **H06** | นิยาม F05 ไม่ระบุ role ของแต่ละ session · ยิง DDL ด้วย `app` ได้ `must be owner` → fault ไม่เกิดเลย | E15 | รันจริงครั้งแรก assertion ตกทั้ง 3 ข้อ |
| **H07** | ฆ่า psql ฝั่ง client ไม่ยกเลิก DDL ที่ค้างในคิว → พอ A ตาย ALTER ทำสำเร็จ **แก้ schema จริง** | E16 | ตาข่าย `DROP COLUMN IF EXISTS` ที่กันไว้ล่วงหน้า |
| **H08** 🔴 | `seed.sql` สร้างค่าเดียวซ้ำ 200,000 แถว (`created_at`, `total_satang`) ตั้งแต่ commit แรก — รูปแบบเดียวกับ U01 · **แก้แล้วและรัน EXP01/01b/01c ใหม่ทั้งหมด** | E17 | วัดก่อนเขียน F03b ว่า query ช้าพอไหม |
| **H09** | สเปก F03 ระบุ timeout 2s ทั้งที่ไม่มี query ไหนบนชุดนี้ช้าถึง | E18 | วัดทุกตัวเลือกก่อนเลือก |
| **H10** | ลอกโครงสร้างการวัดจาก F01/F05 มาใช้กับ F03 ทั้งที่หลักฐานคนละชนิด (สถานะสด vs ตาราง) | E21 | วัดผิดจังหวะซ้ำๆ จนต้องกลับไปถามว่าหลักฐานคืออะไร |
| **H11** | วัด `Plan Rows` จาก node `Aggregate` ซึ่งเป็น 1 เสมอ → ได้ "คลาด 1.0x" ตลอดไม่ว่าสถิติจะเพี้ยนแค่ไหน | E22 | assertion ข้อ 1 ตก |
| **H12** ⭐ | เข้าใจว่า "สถิติเก่า" = จำนวนแถวเก่า · ของจริง PostgreSQL ปรับสเกล `reltuples` ตามขนาดไฟล์ ตัวที่หลอกได้คือ MCV/histogram กับ **ค่าใหม่** | E22 | assertion ข้อ 1 ตกซ้ำ ทั้งที่ `reltuples` เก่าจริง |
| **H13** | ฆ่า `docker exec` ฝั่ง client แล้ว `DO` block ยังวิ่งต่อฝั่ง server 20 นาที · บล็อก TRUNCATE จนงานถัดไปได้ corpus ว่าง แล้วดูเหมือนบั๊กคนละสามตัว | E23 | assertion ของ qf12 (`ต้องได้ 500000 แถว แต่ได้ 0`) |
| **H14** ⭐ | ตัวตรวจ "planner ใช้ index ไหม" ค้นคำ `Index Scan` ทั้ง plan → ไปโดน pkey ของตารางอื่นใน subquery · ตอบว่าใช้ index ทุกครั้ง | E25 | assertion ตกทั้งที่ catalog บอกว่าเป็นไปไม่ได้ |
| **H15** | `count(*) = 2` บน `pg_opclass` ได้ 4 เพราะ opclass ชื่อเดียวกันมีทั้ง hnsw และ ivfflat | E25 | assertion ข้อ 3 ตก |
| **H16** 🔴 | `2>/dev/null` กลบ `CREATE INDEX` ที่ล้มเหลว **เป็นครั้งที่สามในวันเดียว** · ผล 12 ช่องของตัวตรวจผิดทั้งหมด | E26 | ตัวตรวจขึ้น CANNOT_CHECK ทั้งที่ทดสอบมือแล้วผ่าน |
| **H17** | `score.sql` สร้าง temp table โดยไม่ DROP ก่อน → เรียกซ้ำในเซสชันเดียวไม่ได้ | E26 | เจอทันทีที่เลิกกลบ stderr |

**H05 กลายเป็นกฎเหล็กข้อ 7ก แล้ว** ส่วน H01–H04, H06, H07 แก้แล้วทุกข้อ

**H06 เพิ่มบทเรียนใหม่ให้ทะเบียนนี้:** นิยาม fault ต้องระบุ **role ที่ใช้ยิงแต่ละ session**
ไม่ใช่แค่คำสั่ง SQL — สิทธิ์เป็นตัวแปรที่ทำให้ fault ไม่เกิดได้เงียบๆ

**H07 คือกรณีที่ตาข่ายกันเหนียวพิสูจน์คุณค่าตัวเอง** — มันไม่ได้แค่กันความเสียหาย
แต่เป็นตัวที่**บอกให้รู้ว่าการยกเลิกยังไม่ถูกชั้น** ถ้าไม่มีมัน ตาราง `orders`
จะมีคอลัมน์งอกทีละรอบที่รัน แล้วผลรอบถัดไปเทียบกับรอบก่อนไม่ได้โดยไม่มีอะไรเตือน

> **จุดร่วมที่ควรเขียนลงรายงาน:** H02, H03, H04 ถูกจับได้เพราะมี**ขั้นตอนตรวจสอบ
> ที่ออกแบบไว้ล่วงหน้า** (sanity check ก่อนวัด · assertion บังคับ · แยกขั้นวัดผล)
> ไม่ใช่เพราะสังเกตเห็นเอง
> ส่วน H01 ไม่เคยแสดงอาการเลย จับได้จากการไล่ตรวจความสอดคล้องล้วนๆ
>
> **นี่คือข้อโต้แย้งที่ตรงที่สุดว่าทำไมต้องมี `quietfail-check`**

---

# สรุปสถานะการเขียนสคริปต์

| fault | นิยาม | สคริปต์ฉีด | groundtruth | รันจริงแล้ว |
|---|---|---|---|---|
| **F01** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| **F03 + F03b** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| **F05** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| **F10** | ✅ | ✅ | ✅ ยืนยันแล้ว | ✅ **3 รอบติด** |
| I01–L02 (12 ข้อ) | ตาราง | ❌ | ❌ | ❌ |

ของกลางที่ใช้ได้กับทุก fault แล้ว: **`sql/score.sql`** (ตัวนับคะแนน 3 สถานะ
`DETECTED` / `NOT_DETECTED` / `CANNOT_CHECK` ตามกฎเหล็กข้อ 10)

**เฟส 1 ผ่านเมื่อ:** F01, F03, F05, F10 มีครบทั้ง 4 คอลัมน์ และฉีดซ้ำได้ 3 ครั้งติด
→ ✅ **ครบทุกข้อแล้ว 2026-07-27 — เฟส 1 ผ่าน**
