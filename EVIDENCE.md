# หลักฐานอ้างอิงจากเอกสารทางการ

> **กฎเหล็กข้อ 1 ของโปรเจค:** ผู้ช่วย AI ห้ามเป็นคนตัดสินว่าอะไรถูก
> ทุก fault ต้องมีเหตุผลอ้างอิงเอกสารทางการ
> ถ้า flag อะไรแล้วอธิบายไม่ได้ว่าทำไมผิด จะเสียหายหนักกว่าไม่ทำ
>
> ไฟล์นี้คือหลักฐานนั้น — **ไม่มีข้อไหนมาจากการเดาของ AI**

แหล่ง: README ทางการของ pgvector (github.com/pgvector/pgvector)
อ่านเมื่อ: 2026-07-26 · เวอร์ชันที่ติดตั้ง: 0.8.5 · PostgreSQL 17.10

---

## หลักฐานชิ้นสำคัญที่สุด — วิทยานิพนธ์ของโปรเจคถูกเขียนไว้ในเอกสารผู้พัฒนาเอง

เอกสารระบุว่าโดยค่าเริ่มต้น pgvector ทำ exact search ซึ่งให้ recall สมบูรณ์
และเมื่อเพิ่ม approximate index เข้าไป **ผลลัพธ์ของ query จะเปลี่ยนไป**
ต่างจาก index ทั่วไปที่เปลี่ยนแค่ความเร็ว

> ผู้พัฒนาเขียนเองว่า index แบบนี้แลก recall เพื่อความเร็ว
> QuietFail ไม่ได้ค้นพบเรื่องใหม่ — แต่วัดว่า**การแลกนั้นราคาเท่าไหร่**
> ซึ่งไม่มีใครวัดตอน deploy จริง

**ประโยคนี้เปลี่ยนน้ำหนักของโปรเจค:** จาก "นิสิตอ้างว่ามีปัญหา"
เป็น "ปัญหาที่ผู้พัฒนายอมรับไว้ในเอกสาร แต่ไม่มีเครื่องมือวัด"

---

## วิธีหา ground truth ที่เป็นทางการ ⭐

หัวข้อ Monitoring ของเอกสารแนะนำให้เฝ้าดู recall ด้วยการ
**เทียบผลจาก approximate search กับ exact search**

วิธีบังคับให้เป็น exact search:

```sql
BEGIN;
SET LOCAL enable_indexscan = off;
SELECT ...
COMMIT;
```

**นี่ดีกว่าแผนเดิมของเรามาก** — แผนเดิมคือ `DROP INDEX` เพื่อหาเฉลย
ซึ่งช้าและทำให้สภาพฐานข้อมูลเปลี่ยน

วิธีนี้ปิด index เฉพาะใน transaction เดียว index ยังอยู่ วัดสลับไปมาได้ทันที

**ผลต่อแผน:** วิธีวัด ground truth ของ Q01 = วิธีที่ผู้พัฒนาแนะนำเอง
ไม่ใช่วิธีที่เราคิดขึ้นมาเอง → เถียงไม่ได้

---

## ตารางยืนยัน fault ทีละข้อ

**ชุด ID ทางการอยู่ที่ [`FAULTS.md`](FAULTS.md) — ถ้าไฟล์นี้ขัดกับไฟล์นั้น ให้เชื่อ `FAULTS.md`**
ล็อกด้วย D15 · ไฟล์นี้ทำหน้าที่เดียวคือ **ผูก ID แต่ละข้อเข้ากับหลักฐานจากเอกสารทางการ**
ไม่ใช่ที่เก็บนิยาม

```
I01 I02 I03 I04 I05   Q01 Q02 Q03 Q04 Q06   V07 L02      = 12
```

ชั้นของ ID ตาม `FAULTS.md`: **I** ติดตั้ง · **Q** เรียกใช้ · **V** คุณภาพข้อมูล · **L** เสื่อมตามเวลา

| ID | สถานะ | หลักฐานจากเอกสาร |
|---|---|---|
| **Q01** recall collapse | ✅ ยืนยัน | เอกสารระบุตรงๆ ว่า approximate index ทำให้ผลลัพธ์ต่างจากเดิม และแลก recall เพื่อความเร็ว |
| **Q03** filter แล้วได้ไม่ครบ | ✅ ยืนยัน + มีตัวเลข | filter ทำงาน**หลัง**สแกน index — ถ้าเงื่อนไขตรง 10% ของแถว ด้วย `ef_search` ค่าเริ่มต้น 40 จะเหลือผลเฉลี่ยเพียง **4 แถว** |
| **Q04** ค่า default ต่ำเกิน | ✅ ยืนยัน + มีสูตร | `probes` เริ่มต้น = 1 แต่เอกสารแนะนำ `sqrt(lists)` และ `lists` แนะนำ `rows/1000` (≤1M แถว) หรือ `sqrt(rows)` (>1M) |
| **Q06** LIMIT > ef_search | ✅ ยืนยัน | FAQ อธิบายว่าจำนวนผลถูกจำกัดด้วยขนาด candidate list ซึ่งเริ่มต้นที่ 40 |
| **Q02** เขียน WHERE แทน ORDER BY | ✅ ยืนยัน | Troubleshooting ระบุว่า query ต้องมี `ORDER BY` + `LIMIT` และ `ORDER BY` ต้องเป็นผลของ distance operator โดยตรง เรียงจากน้อยไปมาก |
| **I01** opclass ไม่ตรง operator | ✅ ยืนยัน | ต้องสร้าง index แยกต่อหนึ่ง distance function — `vector_l2_ops`, `vector_ip_ops`, `vector_cosine_ops`, `vector_l1_ops` |
| **I02** สร้าง index ก่อนข้อมูลเป็นตัวแทน | ✅ ยืนยัน · ⚠️ **ตัวแปรที่ตัดสินแคบกว่าที่เอกสารระบุ** | เอกสารบอกให้สร้าง index **หลัง**ตารางมีข้อมูล (*"after the table has some data"*) และ FAQ อธิบายผลน้อยว่าเกิดจาก *"too little data for the number of lists"* · **ทำตามเอกสารตรงๆ ได้ recall ดีที่สุดบนข้อมูลจริง** → คำแนะนำถูกต้อง · สิ่งที่วัดเพิ่มได้คือ เมื่อคุมจำนวนแถวเท่ากันเป๊ะ 1,000 แถว **ความเป็นตัวแทนต่างหากที่ตัดสิน** (0.5974 vs 0.4151) ซึ่งไม่มีคำเตือนใดๆ (E33 · แก้การตีความ E42) |
| **I05** maintenance_work_mem ต่ำ | ✅ ยืนยัน + มีข้อความ NOTICE | ดูข้อความจริงข้างล่าง |
| **I03** CREATE INDEX ล็อกตาราง | ✅ ยืนยัน | เอกสารแนะนำให้ใช้ `CREATE INDEX CONCURRENTLY` ใน production เพื่อไม่บล็อกการเขียน |
| **L02** dead tuple ทำ recall ตก | ✅ ยืนยัน | FAQ ระบุว่าผลลัพธ์อาจน้อยลงได้เพราะ dead tuples · และ vacuum ของ HNSW ใช้เวลานาน |
| **V07** NULL / zero vector | ✅ ยืนยัน | เอกสารระบุสองที่ว่า NULL vector ไม่ถูก index และ zero vector ไม่ถูก index เมื่อใช้ cosine distance |
| **I04** สร้าง index ใหม่ได้คำตอบไม่เหมือนเดิม | ⚠️ เอกสารไม่พูดถึง · **พิสูจน์เองแล้ว 2026-07-27** | สร้าง index 5 รอบ ข้อมูลเดิมเป๊ะ → IVFFlat `probes=1` เปลี่ยนคำตอบ 196/200 query · **HNSW ก็เปลี่ยน 132/200 ทั้งที่ไม่มี k-means** → ชื่อเดิมที่โทษ k-means แคบเกินไป (E32) |

**11 จาก 12 ข้อยืนยันจากเอกสารได้แล้ว** · I04 เป็นข้อเดียวที่เอกสารเงียบ — **พิสูจน์ด้วยการรันเองเสร็จแล้ว 2026-07-27**

> 📌 **ขอบเขตของข้อความนี้ (E46)** — "ข้อเดียว" นับเฉพาะ **12 fault ฝั่ง vector**
> เมื่อรวม 4 fault ฝั่ง core ด้วย จะมี **2 ข้อที่เอกสารเงียบ** คือ I04 และ **F05**
> (เอกสาร PostgreSQL ให้ตารางความขัดแย้งของ lock ครบ **แต่ไม่ได้อธิบายผลของ
> การเข้าคิว** ซึ่งเป็นหัวใจของ F05) · `results/doc_crosscheck_f.txt`

> ความเงียบของเอกสารตรงนี้คือส่วนหนึ่งของ fault — ไม่มีที่ไหนรับประกันว่า
> สร้าง index ใหม่แล้วได้ผลเหมือนเดิม **และไม่มีที่ไหนเตือนว่าจะไม่เหมือน**

---

## ข้อความ NOTICE จริงสำหรับ I05

**รูปแบบข้อความตามเอกสาร** (ตัวเลขท้ายเปลี่ยนตามข้อมูลจริง):
```
NOTICE:  hnsw graph no longer fits into maintenance_work_mem after <N> tuples
DETAIL:  Building will take significantly more time.
HINT:  Increase maintenance_work_mem to speed up builds.
```

**✅ ยืนยันแล้ว 2026-07-27** — ข้อความตรงทุกตัวอักษรบน pgvector 0.8.5
เจอตอน build HNSW ใน Q01 (`results/q01_recall_collapse_100k.txt`):

```
NOTICE:  hnsw graph no longer fits into maintenance_work_mem after 28365 tuples
```

ได้ตัวเลขจริงมาด้วย: `maintenance_work_mem = 64MB` รับ HNSW graph ได้แค่
**28,365 จาก 100,000 tuples = 28%** · build ใช้เวลา 89.4 วินาที
เทียบกับ IVFFlat บนข้อมูลชุดเดียวกันที่ใช้ 2.2 วินาที = **ต่างกัน 40 เท่า**

ใช้เป็น assertion ของ I05 ได้ตรงๆ

---

## fault ใหม่ที่เจอจากเอกสาร (ไม่มีในแผนเดิม)

### V07 — vector ที่เป็น NULL หรือ zero ไม่ถูก index ⭐ · **รับเข้าชุดหลักแล้ว (D15)**

เอกสารระบุสองที่ว่า **NULL vector ไม่ถูก index** และ **zero vector ก็ไม่ถูก index
เมื่อใช้ cosine distance**

**ทำไมข้อนี้ดีมาก:**
- แถวหายไปจากผลค้นหา**อย่างถาวรและเงียบสนิท** ไม่มี error
- เกิดจากคุณภาพข้อมูล ไม่ใช่การตั้งค่า → ทีมที่จูน index เก่งแค่ไหนก็ไม่เจอ
- ตรวจแบบ static ได้ง่ายมาก: นับแถวที่ vector เป็น NULL หรือ norm = 0
- zero vector เกิดจริงเมื่อ embedding ล้มเหลวแล้วโค้ดใส่ศูนย์แทน — พบบ่อยใน pipeline จริง

**สถานะ:** รับเข้าชุด 12 ข้อแล้วในชั้น **V** (คุณภาพข้อมูล) **แทนที่ Q05** ซึ่งถูกตัดออก
— ดู D15 และ `FAULTS.md`

### V08 — index ร่วมกันหลาย tenant ทำให้ recall กระทบกัน · **ตัดออก (D15 · `FAULTS.md`)**

เอกสารเตือนว่าการใช้ approximate index ร่วมกันระหว่าง tenant
ทำให้ vector ของ tenant หนึ่งกระทบ recall และความเร็วของ tenant อื่น

ตรงกับระบบ SaaS จริงมาก และเป็นความล้มเหลวเงียบแบบข้ามผู้ใช้

**เหตุผลที่ตัด:** ต้องสร้างสถาปัตยกรรม multi-tenant ขึ้นมาก่อนถึงจะฉีดได้
= สร้างระบบใหม่เพื่อทดสอบข้อเดียว · เก็บหลักฐานไว้ตรงนี้เผื่อกลับมาทำ

### Q05 — สลับ cosine กับ inner product · **ตัดออก (D15 · `FAULTS.md`)**

`<#>` คืนค่า inner product แบบ**ติดลบ** เพราะ Postgres รองรับเฉพาะ index scan แบบ ASC
จุดนี้ทำให้เขียนผิดง่าย

**เหตุผลที่ตัด:** ยืนยันได้แค่บางส่วน (เอกสารอธิบายพฤติกรรม แต่ไม่ได้ระบุว่าเป็นความล้มเหลวเงียบ)
และอาการซ้อนทับกับ I01 ซึ่งยืนยันได้เต็มและตรวจจาก catalog ได้

---

## ข้อจำกัดของทางแก้ Q03 (ต้องเขียนในรายงาน)

ถ้าเปิด iterative scan เพื่อแก้ Q03 จะเจอเงื่อนไขเพิ่มทันที:

1. เลือกได้ระหว่าง `strict_order` (เรียงถูกต้องแน่นอน) กับ `relaxed_order` (recall ดีกว่าแต่ลำดับอาจคลาด)
2. มีเพดาน `hnsw.max_scan_tuples` = 20000 และ `ivfflat.max_probes`
3. ถ้าใช้ relaxed แล้วอยากได้ลำดับถูกต้อง ต้องเขียน materialized CTE ครอบ
4. **บน PostgreSQL 17+ ต้องเติม `+ 0` ใน ORDER BY ชั้นนอก** ไม่งั้นไม่ทำงานตามที่ตั้งใจ

ข้อ 4 สำคัญเพราะเราอยู่บน 17.10 พอดี

### 🔴 เคยอ้างว่ามี "ข้อ 5 ที่เอกสารไม่ได้บอก" — **ถอนแล้ว 2026-08-01 (E42 · E43)**

หัวข้อนี้เคยเขียนว่ามีข้อจำกัดข้อที่ 5 คือ *"ต้องเพิ่ม `work_mem` ด้วย ไม่งั้น
`iterative_scan` ไม่ช่วยอะไรเลย"* และนับเป็นช่องว่างของเอกสาร · **ไม่ถูกต้อง**

**เอกสารนิยาม `hnsw.scan_mem_multiplier` ว่าเป็นตัวคูณของ `work_mem` อยู่แล้ว**

> *"Specify the max amount of memory to use, as a multiple of `work_mem` (1 by default)"*

หน่วยความจำที่ใช้ได้ = `work_mem` × `multiplier` → **สองค่านี้เป็นปุ่มเดียวกัน**
การวัดเดิมหยุดที่ `multiplier = 8` ซึ่งไกลไม่พอ · ที่ **×32 ได้ครบ 40 แถว**
โดยไม่แตะ `work_mem` เลย

และเอกสารยังเขียนคำแนะนำสำหรับอาการนี้ไว้ตรงตัว

> *"Try increasing this if increasing `hnsw.max_scan_tuples` does not improve recall"*

→ **เราเจออาการที่เอกสารทำนายไว้ล่วงหน้า แล้วอ่านมันเป็นข้อบกพร่องของเอกสาร**

| คงค่า `work_mem` ไว้ที่ค่าเริ่มต้น 64kB | ได้ |
|---|---|
| `scan_mem_multiplier` = 1 | 5 / 40 |
| `scan_mem_multiplier` = 8 | 17 / 40 ← การวัดเดิมหยุดตรงนี้ |
| **`scan_mem_multiplier` = 32** | **40 / 40** |

**ข้อจำกัดของทางแก้ Q03 จึงมี 4 ข้อตามที่เอกสารระบุ ไม่ใช่ 5**

**สิ่งที่ยังยืนและควรเขียนในรายงาน** — ค่าเริ่มต้นของทางแก้ต่ำเกินไปมาก
(`multiplier = 1` ได้ 5 จาก 40) และ**ไม่มีสัญญาณใดบอกว่ายังหมุนไม่พอ**
ผู้ใช้เห็นแค่ผลลัพธ์น้อยกว่าที่ขอ ซึ่งตีความได้ทั้ง "หมุนไม่พอ" และ "ปุ่มไม่ทำงาน"

ผลเต็ม `results/q03_multiplier_probe.txt` · `results/doc_reread_probe.txt`
> ความล้มเหลวเงียบจึงไม่ได้แก้ด้วยการ "เปิดสวิตช์" แต่ต้องเข้าใจทั้งชุด

---

## เรื่องที่เคยต้องแก้ — แก้แล้ว

### ✅ image tag ไม่ล็อกเวอร์ชัน (แก้แล้ว 2026-07-26)

เอกสารระบุว่า tag `pg17` เท่ากับ `0.8.5-pg17` **ในตอนนี้**
แต่ `pg17` เป็น tag ที่**ขยับตาม** — พอ 0.8.6 ออก มันจะชี้ไปที่เวอร์ชันใหม่

**ผลกระทบถ้าไม่แก้:** ใครรัน `docker compose pull` หลังจากนั้นจะได้คนละเวอร์ชัน
โดยไม่มีอะไรเตือน → ผลการทดลองทั้งหมดเทียบกันไม่ได้

**นี่คือความล้มเหลวเงียบในโปรเจคที่ศึกษาความล้มเหลวเงียบ** (อันที่ 1 จาก 4 — ดู README)

**สถานะ:** `docker-compose.yml` ล็อกเป็น `pgvector/pgvector:0.8.5-pg17` แล้ว
บันทึกเหตุผลไว้ที่ D14 · **ห้ามเปลี่ยนกลับ**

---

## ชั้น 3 — ตารางจับคู่เอกสาร 6 เครื่องมือ (เฟส 2.5)

> **ชั้นนี้คือการทบทวนเอกสาร ไม่ใช่ผลการทดลอง** (`PROJECT.md` ข้อ 5)
> ทุกค่าในตารางนี้มาจากเอกสารทางการหรือซอร์สโค้ด อ่านเมื่อ 2026-07-27
> **ยกเว้นแถว pgvector และ Qdrant ซึ่งเรารันวัดเอง**
>
> ช่องไหนหาไม่เจอ เขียนว่า "ยังไม่ยืนยัน" — ห้ามเติมด้วยความจำ (กฎเหล็กข้อ 1 และ 10)

### ตาราง A — พารามิเตอร์ความพยายามค้นหา (แกนของ Q01 · Q04 · Q06)

| engine | พารามิเตอร์ HNSW | ค่าเริ่มต้น | พารามิเตอร์ IVF | ค่าเริ่มต้น |
|---|---|---|---|---|
| **pgvector 0.8.5** | `hnsw.ef_search` | **40** (คงที่) | `ivfflat.probes` | **1** |
| **Qdrant 1.12.4** | `hnsw_ef` | ไม่ใช่ค่าคงที่ต่ำ (วัดได้ recall@100 = 0.99 ที่ ef=40) | — | — |
| **Milvus** | `ef` | **= limit (TopK)** | `nprobe` | ยังไม่ยืนยัน |
| **Weaviate** | `ef` | **-1** = ปรับอัตโนมัติ (`dynamicEfMin` 100 · `dynamicEfMax` 500 · `dynamicEfFactor` 8) | — | — |
| **OpenSearch** | `ef_search` | **100** · engine `lucene` **ไม่สนใจค่านี้ แล้วตั้ง = k เอง** | `nprobes` | **1** |
| **FAISS** | `efSearch` | ยังไม่ยืนยัน | `nprobe` | **1** (`size_t nprobe = 1;` ในซอร์ส) |
| **Pinecone** | ไม่เปิดให้ตั้ง | — | ไม่เปิดให้ตั้ง | — |

**พารามิเตอร์ตอน build** (รองลงมา แต่บันทึกไว้)

| engine | `m` / maxConnections | `ef_construction` |
|---|---|---|
| pgvector 0.8.5 | 16 | 64 |
| Qdrant 1.12.4 | 16 | 100 |
| Milvus | **30** | **360** |
| Weaviate | 32 (`maxConnections`) | 128 |
| OpenSearch | 16 | 100 |

### 🔴 สิ่งที่ตารางนี้เปิดโปง — และมันสนับสนุน D16 โดยตรง

**`ef_search = 40` แบบคงที่ของ pgvector เป็นค่าที่แปลกแยกในกลุ่มนี้**

เครื่องมืออื่นที่ตรวจได้ **ไม่ปล่อยให้ ef ต่ำกว่าจำนวนผลที่ขอ**:
- **Milvus** — `ef` เริ่มต้น**เท่ากับ limit** โดยนิยาม
- **Weaviate** — `ef = -1` แล้วปรับอัตโนมัติในช่วง 100–500
- **OpenSearch** — เริ่มต้น 100 และ engine `lucene` **บังคับ `ef = k`**
- **Qdrant** — ตั้ง `hnsw_ef=40` กับ `limit=100` แล้ว recall ยัง 0.9895 (เราวัดเอง)

→ **recall@100 = 0.3903 ของ pgvector ไม่ใช่ธรรมชาติของ HNSW**
แต่เป็นผลของการที่ pgvector ปล่อยให้ candidate list เล็กกว่า `LIMIT` ได้เงียบๆ
**นี่คือ Q06 และเป็นเรื่องเฉพาะของ pgvector ในกลุ่มที่ตรวจ**

**แต่ `probes = 1` ไม่ใช่เรื่องเฉพาะตัว**
FAISS ตั้ง `nprobe = 1` ในซอร์ส และ OpenSearch ตั้ง `nprobes` เริ่มต้น 1 เหมือนกัน
→ **Q04 (ค่า default ต่ำเกินไปโดยไม่มีใครวัด) เป็นปัญหาร่วมของวงการ ไม่ใช่ของ pgvector**

**Pinecone ไม่เปิดให้ตั้งอะไรเลย** — ยืนยัน D05 อีกชั้น
ผู้ใช้ปรับไม่ได้ **และวัด recall ของตัวเองจากข้างในไม่ได้ด้วย**
ความล้มเหลวเงียบไม่ได้หายไปเพราะจ่ายเงิน แต่ตรวจจับยากขึ้น

### ตาราง B — fault ข้อไหนใช้ได้กับเครื่องมือไหน

> ⚠️ **คอลัมน์นี้เป็นการให้เหตุผลเชิงโครงสร้าง ไม่ใช่ค่าที่คัดจากเอกสาร**
> เกณฑ์: fault ข้อนั้นต้องมี "กลไกที่ทำให้เกิด" อยู่ในสถาปัตยกรรมของเครื่องมือนั้น

| fault | pgvector | Qdrant | Milvus | Weaviate | OpenSearch | FAISS | Pinecone |
|---|---|---|---|---|---|---|---|
| **Q01** recall collapse | ✅ วัดแล้ว | ✅ **วัดแล้ว** | ✅ | ✅ | ✅ | ✅ | ✅ แต่วัดเองไม่ได้ |
| **Q04** default ต่ำเกิน | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ ไม่มีให้ตั้ง |
| **Q06** `LIMIT` > ef | ✅ **เฉพาะตัว** | ❌ | ❌ `ef`=limit | ❌ dynamic | ❌ lucene บังคับ =k | ⚠️ ยังไม่ยืนยัน | ❌ |
| **Q02** เขียน WHERE แทน ORDER BY | ✅ SQL เท่านั้น | ❌ | ❌ | ❌ | ⚠️ DSL คนละแบบ | ❌ | ❌ |
| **Q03** filter แล้วได้ไม่ครบ | ✅ | ✅ | ✅ | ✅ | ✅ | ⚠️ ต้องทำเอง | ✅ |
| **I01** opclass ไม่ตรง operator | ✅ **เฉพาะ SQL** | ❌ ตั้ง distance ตอนสร้าง collection | ❌ | ❌ | ❌ | ❌ | ❌ |
| **I02** สร้าง index ตอนข้อมูลน้อย | ✅ IVF | ⚠️ Qdrant สร้างเองตาม threshold | ✅ IVF | ❌ HNSW ล้วน | ✅ IVF | ✅ IVF | ❌ |
| **I03** `CREATE INDEX` ล็อกตาราง | ✅ **เฉพาะ SQL DDL** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **I04** k-means สุ่ม | ✅ IVF | ❌ ไม่มี IVF | ✅ IVF | ❌ | ✅ IVF | ✅ IVF | ⚠️ ไม่รู้ข้างใน |
| **I05** หน่วยความจำ build ต่ำ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| **V07** NULL / zero vector | ✅ | ⚠️ ยังไม่ยืนยัน | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ⚠️ |
| **L02** dead tuple | ✅ **เฉพาะ MVCC** | ❌ คนละกลไก | ❌ | ❌ | ❌ | ❌ | ❌ |

**สิ่งที่ตาราง B บอก:**
- **Q01 · Q04 · I05 ใช้ได้แทบทุกตัว** → เป็นปัญหาของ ANN ไม่ใช่ของยี่ห้อ
- **I01 · I03 · L02 · Q02 เป็นของ pgvector โดยเฉพาะ** เพราะผูกกับ SQL/MVCC
  → ยืนยันว่าการเลือก pgvector เป็นชั้น 1 ได้ fault มากกว่าที่เครื่องมืออื่นให้ได้
- **Q06 กลับกัน** — เป็นของ pgvector เฉพาะตัวเพราะเครื่องมืออื่น**ป้องกันไว้ให้แล้ว**

  > ⚠️ **ผ่อนถ้อยคำ 2026-08-01 (E44)** — pgvector **มีกลไกป้องกันตั้งแต่ 0.8.0**
  > คือ `hnsw.iterative_scan` ซึ่งวัดแล้วยกเพดานได้จริง (40 → 100 ครบทั้ง 200 query
  > จ่าย buffers เพิ่มเพียง 6%) · **สิ่งที่ต่างจากเจ้าอื่นคือตั้งค่าปริยายเป็นปิด
  > ไม่ใช่ "ไม่มีกลไก"** ซึ่งแคบกว่าและตรงกว่า
  >
  > ⚠️ และการยกเพดานไม่ได้แก้คุณภาพ — `recall@100` ยังได้แค่ **0.7572**
  > `results/q06_iterative_probe.txt`

### แหล่งอ้างอิง (อ่าน 2026-07-27)

- Milvus HNSW — `milvus.io/docs/hnsw.md`
- Weaviate vector index — `docs.weaviate.io/weaviate/config-refs/schema/vector-index`
- OpenSearch k-NN index — `docs.opensearch.org/2.12/search-plugins/knn/knn-index/`
- FAISS `IndexIVF` — `faiss.ai/cpp_api/struct/structfaiss_1_1IndexIVF.html`
- FAISS indexes wiki — `github.com/facebookresearch/faiss/wiki/Faiss-indexes`
- Pinecone indexing overview — `docs.pinecone.io/guides/index-data/indexing-overview`

---

## สิ่งที่ยังต้องรันเองเพื่อยืนยัน

เอกสารบอกว่า "มีปัญหา" แต่ไม่ได้บอกว่า "รุนแรงแค่ไหน"
ตัวเลขต่อไปนี้ไม่มีในเอกสาร ต้องวัดเอง — **และนั่นคือคุณค่าของโปรเจคนี้**

- [x] ~~recall@10 ตกลงเท่าไหร่จริง (Q01)~~ → **วัดแล้ว 2 ขนาด**
      100k: HNSW 0.8565 · IVFFlat 0.7870
      500k: HNSW **0.5885** · IVFFlat 0.7820 · แย่สุดต่อ query = 0.0000 ทุกกรณี
      **ยังไม่ได้วัดที่ 2M** — เกณฑ์ผ่านเฟส 2 ต้องการ 2 ขนาด ซึ่งครบแล้ว
- [ ] `probes = 1` เทียบกับ `sqrt(lists)` ต่างกันกี่ % (Q04)
- [x] สร้าง IVFFlat index 5 รอบ ได้ recall ต่างกันไหม (I04) — **ต่างกัน** 0.7415-0.8145
      และ 196/200 query เปลี่ยนคำตอบ · HNSW ก็ต่าง 132/200 · ปิด parallel แล้วยังต่าง
- [x] ~~ข้อความ NOTICE ของ 0.8.5 ตรงกับที่เอกสารเขียนไหม (I05)~~ → **ตรง** ยืนยัน 2026-07-27
- [ ] เปิด iterative scan แล้ว recall ดีขึ้นเท่าไหร่ แลกกับความเร็วเท่าไหร่
- [ ] `LIMIT` เกิน `ef_search` แล้วได้ผลไม่ครบจริงไหม ขาดไปกี่แถว (Q06)
- [ ] NULL / zero vector หายจากผลค้นหาจริงไหม และ `CREATE INDEX` เตือนหรือเงียบ (V07)
