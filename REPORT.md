# QuietFail — รายงานผล

**ANN index ทำให้ vector search เร็วขึ้นหลายสิบเท่า แล้วทิ้งคำตอบที่ควรเจอไปส่วนหนึ่ง โดยไม่มี error ใดๆ แจ้ง**
รายงานนี้วัดว่าหายไปเท่าไหร่ และตรวจจับล่วงหน้าได้กี่ข้อ

```
PostgreSQL 17.10 · pgvector 0.8.5 · image pgvector/pgvector:0.8.5-pg17
corpus 100,000 และ 500,000 แถว · 384 มิติ · 200 query · cosine
เฉลย = exact search บังคับด้วย SET LOCAL enable_indexscan = off
วันที่วัด 2026-07-26 ถึง 2026-07-28 · เครื่องเดียว · โปรไฟล์ fragile
```

> **ทุกตัวเลขในรายงานนี้มาจากการรันจริง** ไม่มีตัวเลขที่ประมาณหรือคัดลอกมาจากเอกสาร
> หลักฐานดิบอยู่ใน `results/` (35 ไฟล์) · ตรวจซ้ำได้ด้วย `python scripts/audit.py --reproduce`

---

## 1. คำตอบสั้นที่สุด

| คำถาม | คำตอบ |
|---|---|
| **RQ1** ผลผิดไปกี่ % | **14–41% ของ top-10 หายไป** ที่ค่าเริ่มต้น · และ **มี query ที่ได้ของถูก 0 อัน ในทุกการตั้งค่าที่วัด** |
| **RQ2** ตรวจจับอัตโนมัติได้กี่ข้อ | **11 จาก 12 ข้อ** มีตัวตรวจที่พิสูจน์แล้วว่าใช้ได้ (ข้อที่ 12 คือ Q01 ซึ่ง*เป็น*การวัดเอง) |
| RQ3 โค้ดที่ AI สร้างติดกับดักบ่อยแค่ไหน | **ไม่ได้ทำ** — `PROJECT.md` ระบุไว้เองว่าตัดได้โดยโปรเจคยังสมบูรณ์ |

**สิ่งที่ไม่ได้อยู่ในคำถามวิจัย แต่เป็นผลที่แรงที่สุด**

> **เอกสารทางการของ pgvector ผิดหรือไม่ครบ 3 ที่ ซึ่งวัดเจอทั้งหมด** (หัวข้อ 4)
> และ **ชื่อ fault ที่โปรเจคนี้ตั้งเอง ถูกการวัดหักล้าง 4 จาก 12 ข้อ** (หัวข้อ 5)

---

## 2. RQ1 — ใส่ index แล้วผลหายไปเท่าไหร่

`ef_search = 40` · `probes = 1` — **ค่าเริ่มต้นล้วนๆ ไม่ได้จูนอะไรเลย** ซึ่งคือสิ่งที่ทีมส่วนใหญ่ใช้

| ขนาด | | recall@10 | recall@100 | **แย่สุดต่อ query** | ครบ 10/10 | เร็วขึ้น |
|---|---|---|---|---|---|---|
| 100k | exact | 1.0000 | 1.0000 | 1.0000 | 200/200 | 1.0× |
| 100k | HNSW | 0.8565 | 0.3903 | **0.0000** | 52/200 | 32.6× |
| 100k | IVFFlat | 0.7870 | 0.7793 | **0.0000** | 100/200 | 44.6× |
| **500k** | exact | 1.0000 | 1.0000 | 1.0000 | 200/200 | 1.0× |
| **500k** | **HNSW** | **0.5885** | 0.3871 | **0.0000** | **2/200** | **117.1×** |
| **500k** | IVFFlat | 0.7820 | 0.7857 | **0.0000** | 108/200 | 51.6× |

### สามอย่างที่ตารางนี้บอก

**ก) ค่าเฉลี่ยไม่ใช่ความจริงทั้งหมด**
recall@10 = 0.8565 ฟังดูรับได้ แต่ **มี query ที่ได้ของถูก 0 จาก 10 อัน** และมีแค่
52 จาก 200 query ที่ได้ครบ · ที่ 500k เหลือ **2 จาก 200**

**ข) index สองแบบเสื่อมตามขนาดข้อมูลคนละแบบ**
HNSW ตกจาก 0.8565 → **0.5885** (ตกลง 31%) ส่วน IVFFlat แทบไม่ขยับ
เพราะ `ef_search = 40` เป็น**ค่าสัมบูรณ์** ส่วน `probes = 1` จาก `lists = 100` เป็น**ค่าสัมพัทธ์**

**ค) HNSW เร็วขึ้นพร้อมกับที่มันพังลง** — 32.6× → **117.1×**
ทีมที่เฝ้าดูแต่ latency จะเห็นกราฟที่ "ดีขึ้น" ตอนที่คุณภาพกำลังพัง

### recall@100 = 0.39 ไม่ใช่ "คุณภาพแย่" แต่เป็นเพดาน

`ef_search = 40` ทำให้ **คืนได้ไม่เกิน 40 แถว** ต่อให้ขอ 100 → 40/100 = 0.40 คือเพดานตั้งแต่แรก
วัดแล้วว่าหน้าผาอยู่ที่ `k = ef_search + 1` **พอดีเป๊ะ** ไม่ใช่ใกล้เคียง (Q06)

| ขอ | 39 | 40 | **41** | 100 |
|---|---|---|---|---|
| ได้ | 39 | 40 | **40** | **40** |

→ **แก้ Q06 ไม่ได้แก้ Q01** ตั้ง `ef_search = 100` ได้แถวครบ แต่ถูกแค่ 90.6%

### เทียบข้ามเครื่องมือ — เพื่อไม่ให้กล่าวหา pgvector เกินจริง

ข้อมูลชุดเดียวกัน เฉลยชุดเดียวกัน (`ef` เท่ากันในสองแถวแรก)

| engine | recall@10 | recall@100 |
|---|---|---|
| pgvector HNSW `ef_search=40` | 0.8565 | **0.3903** |
| Qdrant HNSW `hnsw_ef=40` | 0.9945 | **0.9895** |
| Qdrant ค่าเริ่มต้นของตัวเอง | 1.0000 | 0.9996 |

**recall loss มีจริงทั้งสอง engine** (Qdrant แย่สุดต่อ query = 0.90 · ที่ k=100 ครบแค่ 69/200)
→ เป็นธรรมชาติของ ANN ไม่ใช่บั๊กของ pgvector

**แต่ขนาดต่างกันมหาศาล** — pgvector ได้ 0.39 เพราะปล่อยให้ `ef_search < LIMIT`
ส่วน Milvus ตั้ง `ef = limit` · Weaviate ปรับอัตโนมัติ · OpenSearch+lucene บังคับ `= k`
→ **Q06 เป็นของ pgvector เฉพาะตัว เพราะเจ้าอื่นป้องกันไว้ให้แล้ว**

---

## 3. RQ2 — ตรวจจับอัตโนมัติได้กี่ข้อ จากหลักฐานอะไร

**15 fault มีตัวฉีดและตัวตรวจครบวงจร** — 11 จาก 12 ข้อของชุด vector (Q01 คือการวัดเอง ไม่ใช่ข้อที่ต้องมีตัวตรวจแยก) บวก 4 ข้อพื้นฐานของ PostgreSQL
ตัวตรวจทุกตัว**พิสูจน์แล้วว่าพลิกสถานะได้จริง** ไม่ใช่แค่ตอบบวกอย่างเดียว

### แบ่งตามชนิดหลักฐาน — นี่คือคำตอบของ "จากหลักฐานอะไร"

| ชนิดหลักฐาน | ตรวจได้เมื่อไหร่ | fault | ต้นทุน |
|---|---|---|---|
| **catalog ล้วน** | เมื่อไหร่ก็ได้ · **ไม่ต้องมีข้อมูลด้วยซ้ำ** | I01 I04 I05 Q04 Q06 | ~0 |
| **วัดสดด้วย query เดียว** | เมื่อไหร่ก็ได้หลังมีข้อมูล | I02 L02 Q03 V07 | วินาที |
| **สถานะสดของ DB** | **ต้องจับตอนกำลังเกิด** | F01 F05 I03 | ต้องเฝ้า |
| **ตารางที่ตัวฉีดบันทึกไว้** | หลังตัวฉีดจบ | F03 F10 | ต้องเตรียม |
| **ประวัติ query ที่เคยรัน** | ไม่ต้องมีซอร์สโค้ด | Q02 | ~0 |

**5 ข้อตรวจได้จาก catalog ล้วน** คือผลที่ใช้งานได้จริงที่สุด — เอาไปใส่ CI ได้เลย
โดยไม่ต้องมีข้อมูล production ไม่ต้องรัน query และไม่ต้องรอให้เกิดปัญหาก่อน

**3 ข้อต้องจับตอนกำลังเกิด** (F01 F05 I03) — เครื่องมือแบบสแกนครั้งเดียวจับไม่ได้เลย
นี่คือข้อจำกัดที่ต้องพูดให้ชัด ไม่ใช่ซ่อน

### ตัวตรวจตอบ 3 อย่าง ไม่ใช่ 2

```
DETECTED / NOT_DETECTED / CANNOT_CHECK
```

**`CANNOT_CHECK` ไม่นับว่าผ่าน** — เครื่องมือที่บอกว่า "ปลอดภัย" ทั้งที่ไม่ได้ดูอะไรเลย
อันตรายกว่าไม่มีเครื่องมือ · ตัวอย่างที่ตัวตรวจ**ปฏิเสธที่จะตอบ**:

- Q03 และ L02 ปฏิเสธถ้า `limit_k >= ef_search` เพราะจะแยกจาก Q06 ไม่ออก
- I04 ปฏิเสธถ้าไม่เคยจดว่าวัด recall บน build ไหน — **ทีมที่ไม่เคยจดคือทีมที่เสี่ยงที่สุด ไม่ใช่ปลอดภัยที่สุด**
- I05 ปฏิเสธถ้ามิติไม่ใช่ 384 เพราะค่าความจุ calibrate ไว้ที่มิตินั้นเท่านั้น

### ต้นทุนของการตรวจ — วัดแล้ว

| | ข้อ | เวลา |
|---|---|---|
| รันตัวฉีดจริงซ้ำอัตโนมัติได้ | 9 ข้อ | ~40 นาที (วัดจริงแล้ว 6 ข้อ = **21.7 นาที · assertion 28/28**) |
| ต้องรันด้วยมือ | 6 ข้อ | ต้องหลาย session หรือต้องตั้ง env |
| ตรวจว่าหลักฐานครบรูปแบบ | 24 หัวข้อ | **~30 วินาที** (`python scripts/audit.py`) |

---

## 4. ⭐ เอกสารของผู้พัฒนาเองผิดหรือไม่ครบ 3 ที่

นี่เป็นผลที่ไม่ได้ตั้งใจหา — โผล่มาจากการวัดตามปกติ

### 4.1 Q04 — สูตรที่เอกสารแนะนำ ช้ากว่าที่จำเป็น 2.2 เท่า

IVFFlat `lists = 100` · เอกสารแนะนำ `probes = sqrt(lists) = 10`

| `probes` | | recall@10 | ครบ 10/10 | 200 query |
|---|---|---|---|---|
| **1** | ค่าเริ่มต้น | **0.7370** | 78/200 | 277 ms |
| **5** | | **1.0000** | **200/200** | 495 ms |
| 10 | **เอกสารแนะนำ** | 1.0000 | 200/200 | **1,101 ms** |

**`probes = 5` ได้ recall สมบูรณ์แล้ว** — 10 ช้ากว่า 2.2 เท่าโดยไม่ได้ recall เพิ่มเลยสักนิด

→ **ทั้งค่าที่ผู้พัฒนาตั้งมาให้ และสูตรที่ผู้พัฒนาแนะนำ ต่างก็ไม่ใช่คำตอบที่ถูกสำหรับข้อมูลชุดนี้**
คำตอบมาจากการวัดเท่านั้น — ซึ่งเป็นวิทยานิพนธ์ของโปรเจคตรงตัว

### 4.2 I02 — คำเตือนออกกับกรณีที่ดีที่สุด และเงียบกับกรณีที่แย่ที่สุด

4 เงื่อนไข · ข้อมูลปลายทางเหมือนกันเป๊ะ 100,000 แถว · เปลี่ยนแค่ **ตอนที่ build**

| | build ตอนมี | กลุ่มที่เห็นตอน build | **pgvector เตือน** | recall |
|---|---|---|---|---|
| **A** | 50 แถว | 50 จาก 50 | **✅ เตือน** | **1.0000** |
| C | 100,000 แถว (วิธีที่เอกสารบอก) | 50 จาก 50 | ❌ เงียบ | 0.7682 |
| **D** | 1,000 แถว | **5 จาก 50** | ❌ **เงียบ** | **0.2278** |

NOTICE ออกเมื่อ `rows < lists` เท่านั้น (วัดแล้ว: ออกที่ 50 ไม่ออกที่ 100)
และ `DETAIL` เขียนว่า *This will cause low recall* ซึ่ง **วัดแล้วเป็นเท็จบนชุดนี้**

**ตัวแปรจริงไม่ใช่จำนวนแถว แต่คือความเป็นตัวแทน** — 50 แถวครบ 50 กลุ่ม
ดีกว่า 1,000 แถวจาก 5 กลุ่ม **20 เท่า**

### 4.3 Q03 — ทางแก้ที่เอกสารบอก ไม่ทำงานเลยจนกว่าจะแตะค่าที่เอกสารไม่ได้พูดถึง

`EVIDENCE.md` จดข้อจำกัดของ `iterative_scan` ไว้ 4 ข้อจากเอกสาร — **มีข้อที่ 5**

ที่ filter ตรง 1% · ขอ 40 แถว

| เปลี่ยนอะไร | ได้ | buffers |
|---|---|---|
| ค่าเริ่มต้น | 0 | 977 |
| เปิด `hnsw.iterative_scan` | **0** | 1,743 |
| `+ max_scan_tuples` ×10 | **0** | 1,743 |
| `+ scan_mem_multiplier` ×8 | **0** | 3,035 |
| **`+ work_mem` 64kB → 4MB** | **40** | 27,145 |

**พารามิเตอร์ของ pgvector ทุกตัวให้ผลเป็นศูนย์** ตัวปลดล็อกคือ `work_mem`
ค่าทั่วไปของ PostgreSQL ที่ไม่เกี่ยวกับ vector เลย และไม่มีในรายการที่เอกสารให้ไว้

**และสามขั้นแรกจ่าย I/O เพิ่มจริงโดยไม่ได้แถวเพิ่มเลย** — คนที่จูนตามเอกสารครบทุกข้อ
จะเห็นระบบช้าลง ผลยังเป็นศูนย์ แล้วสรุปว่า iterative scan ใช้ไม่ได้

---

## 5. ⭐ ชื่อ fault ที่เราตั้งเอง ถูกการวัดหักล้าง 4 ข้อ

**ทุกครั้งจับได้ด้วยกลุ่มควบคุม ไม่ใช่ด้วยผลหลัก** — ถ้าวัดแต่สิ่งที่ตั้งใจวัด จะยืนยันชื่อผิดได้อย่างสวยงามทุกครั้ง

| ข้อ | ชื่อเดิม | สิ่งที่กลุ่มควบคุมพบ |
|---|---|---|
| **Q02** | "เขียน `WHERE` แทน `ORDER BY`" | `WHERE` + `ORDER BY` + `LIMIT` ใช้ index ได้ → **`WHERE` ไม่ใช่ตัวปัญหา** การขาด `ORDER BY`+`LIMIT` ต่างหาก |
| **I04** | "k-means ของ IVFFlat สุ่ม" | **HNSW ไม่มี k-means เลยก็แกว่ง** 132/200 · ปิด parallel ก็ยังแกว่ง → ชื่อเดิมแคบเกินไป |
| **I02** | "สร้าง index ตอนข้อมูลน้อย" | 50 แถวได้ **recall 1.0000** → ตัวแปรคือ**ความเป็นตัวแทน** ไม่ใช่จำนวนแถว |
| **L02** | "dead tuple สะสม → index บวม recall ตก" | `UPDATE` ทำ index บวม 195→**202 MB** แต่ **ผลครบทุกแถว recall ไม่ตก** → บวมกับผลหายเป็นคนละเรื่อง |

**กฎที่ได้จากเรื่องนี้:** fault ที่บอกว่า "เพราะกลไก X" ต้องหาวิธีสร้าง X **โดยไม่ให้เกิดอาการ**
เป็นกลุ่มควบคุมเสมอ · ถ้าทำได้ แปลว่า X ไม่ใช่ต้นเหตุ

---

## 6. หน้าผา — ความล้มเหลวเงียบไม่ค่อยมาแบบค่อยเป็นค่อยไป

สามข้อที่วัดได้ว่าเป็น **step function ไม่ใช่ทางลาด** — ตัววัดแบบ threshold จับไม่ทัน

| | เส้นแบ่ง | ก่อนเส้น | หลังเส้น |
|---|---|---|---|
| **Q06** | `k = ef_search + 1` | ขอ 40 ได้ 40 | ขอ 41 ได้ **40** |
| **Q03** | filter ตรง ~20% → 2% | ขอ 10 ได้ 10 | ขอ 10 ได้ **0** |
| **L02** | dead 40% → 50% | ขอ 10 ได้ 10 | ขอ 10 ได้ **0** |

**Q03 กับ L02 เป็นกลไกเดียวกันเป๊ะ** — index คืน candidate ตาม `ef_search` แล้วมีอะไรมาคัดออกทีหลัง

| | ตัวกรองคืออะไร | เห็นในโค้ดไหม |
|---|---|---|
| Q03 | `WHERE` ที่เขียนเอง | ✅ เห็น |
| **L02** | **MVCC** ตัดแถวที่ตายแล้ว | ❌ **ไม่มีอะไรในคำสั่งบอกเลย** |

→ L02 หายากกว่ามาก เพราะ**คำสั่งที่พังหน้าตาเหมือนคำสั่งที่ถูกทุกประการ**

---

## 7. fault เกี่ยวโยงกันเอง ไม่ได้แยกเป็นข้อๆ

- **Q06 อธิบาย recall@100 ของ Q01** — 0.3903 คือ 40/100 ซึ่งเป็นเพดาน ไม่ใช่คุณภาพ
- **Q02 รูปแบบ `+ 0` คือทางแก้ของ Q03** — ทางแก้ของ fault หนึ่ง คือวิธีฉีดของอีก fault หนึ่ง
- **I04 บังคับวิธีออกแบบของ I02** — เพราะ rebuild ไม่ reproducible จึงเทียบ A/B ครั้งเดียวไม่ได้ ต้อง 5 รอบ
- **I03 กินเวลาเท่ากับ build ของ I05** — ระยะที่เขียนไม่ได้ = ระยะที่ `CREATE INDEX` ทำงาน
- **`probes = 5` แก้ทั้ง Q04 และ I04** — ค่าที่พอเพียงไม่ได้แค่ยก recall แต่ทำให้ผลนิ่งด้วย

---

## 8. ข้อจำกัด — สิ่งที่รายงานนี้ตอบไม่ได้

**ต้องอ่านหัวข้อนี้ก่อนอ้างตัวเลขใดๆ ข้างบน**

1. **เครื่องเดียว โปรไฟล์เดียว** — ทุกตัวเลขมาจาก `fragile` (`work_mem=64kB`, `shared_buffers=128MB`)
   บนเครื่องเดียว · latency ต่างกันได้ 10 เท่าตามฮาร์ดแวร์ · **`buffers` ต่างหากที่นิ่ง** จึงใช้เป็นตัวชี้วัดหลัก
2. **corpus สังเคราะห์** 50 กลุ่มสะอาด · ผลของ **I02 ผูกกับคุณสมบัตินี้โดยตรง** (corpus เรียง id สลับกลุ่ม
   50 แถวแรกจึงครบ 50 กลุ่มพอดี) — ข้อมูลจริงยังไม่ได้ทดสอบ
3. **กลไกที่ทำให้ HNSW rebuild ไม่นิ่ง ยังไม่รู้** — ตัดออกได้แค่ฝั่ง query กับ parallel build เท่านั้น
4. **ตัวเลข recall บางตัวไม่มีวันซ้ำเป๊ะ** เพราะ build ไม่ reproducible (I04)
   → การตรวจซ้ำยืนยันว่า **assertion ยังผ่าน** ไม่ใช่ว่าตัวเลขเท่าเดิม
5. **RQ3 ไม่ได้ทำ** — ชุดโจทย์ AI และอัตราการพลาด
6. **3 fault ที่ตัดออก** (Q05 · L01 · V08) และ 3 ข้อที่ตรวจได้เฉพาะตอนกำลังเกิด (F01 F05 I03)
7. **ตัวตรวจของ Q02 อ่านข้อความ query** จึงจับได้แค่ "ขาด `ORDER BY`/`LIMIT`" และ "เรียง DESC"
   รูปแบบที่ห่อ operator ด้วยนิพจน์ ตรวจจากข้อความไม่ได้ ต้องดู plan — **ระบุไว้ตรงๆ ไม่อ้างว่าครอบคลุม**

---

## 9. ความน่าเชื่อถือของตัวเลขในรายงานนี้

| | |
|---|---|
| fault ที่มีตัวฉีด + ตัวตรวจครบวงจร | **15** |
| ตัวตรวจที่พิสูจน์แล้วว่าพลิกสถานะได้ | **15 / 15** |
| ตัดสินใจที่บันทึกไว้ | **19 รายการ** (D01–D19) |
| ทุก fault รันติดกัน | **3 รอบ** ผ่านทุกรอบ |
| หลักฐานดิบที่เก็บไว้ | **35 ไฟล์** ใน `results/` |
| บันทึกความผิดพลาดระหว่างทาง | **34 รายการ** (E01–E35 · เลข E33 เคยซ้ำ แก้แล้ว) |
| บั๊กของเครื่องมือตัวเอง | **27 รายการ** (H01–H27) |
| ตรวจความพร้อมทั้ง repo | **24 หัวข้อ ผ่านหมด** · `python scripts/audit.py` |

### ทำไมถึงนับบั๊กของตัวเองไว้ 27 ข้อ

**ทุกข้อคือความล้มเหลวเงียบ — ในเครื่องมือที่เขียนขึ้นมาเพื่อตรวจความล้มเหลวเงียบ**
และมี 3 ตระกูลที่เกิดซ้ำจนต้องกลายเป็นกฎ:

| ตระกูล | เกิดกี่ครั้ง |
|---|---|
| ฝัง SQL ที่มี quote ใน `bash -c` แล้วคำสั่งไม่ทำงานแบบเงียบๆ | **5** |
| match ข้อความแทนอ่านโครงสร้าง | **3** |
| ตัวตรวจวัดคนละอย่างกับการทดลอง | **2** (I05 · L02) |

**สองครั้งหลังนี้อันตรายที่สุด** — ตัวตรวจรายงานข่าวดีโดยไม่มี error อะไรเลย
I05 เคยผ่านทุกอย่างยกเว้นการพิสูจน์ว่าตัวตรวจพลิกได้ แล้วไม่มีใครสังเกตจนถึง audit
ผลรัน 102 บรรทัดของมันไม่มีคำว่า `DETECTED` เลยสักครั้ง

→ **นี่คือเหตุผลที่ทุก fault ต้องพิสูจน์ว่าตัวตรวจตอบลบได้** ไม่ใช่แค่ assertion ผ่าน

---

## 10. ทำซ้ำได้อย่างไร

```bash
docker compose up -d
python scripts/audit.py                 # 24 หัวข้อ ~30 วินาที
python scripts/audit.py --reproduce     # รันตัวฉีดจริงซ้ำ 9 ข้อ ~40 นาที
```

`exit 0` = ผ่านหมด · `exit 1` = มีข้อไม่ผ่าน **หรือตรวจไม่ได้** แล้วมันจะบอกว่าข้อไหน

**ตัวชี้ขาดคือ exit code ไม่ใช่ข้อความใน output** — ทุกสคริปต์ใช้ `ON_ERROR_STOP`
และ `RAISE EXCEPTION` เมื่อ assertion ตก

| อ่านอะไรต่อ | ที่ไหน |
|---|---|
| นิยาม fault ครบทุกข้อ | `FAULTS.md` — แหล่งอ้างอิงเดียว |
| ตัดสินใจอะไรไป และปฏิเสธอะไรเพราะอะไร | `DECISIONS.md` |
| หลักฐานอ้างอิงเอกสารทางการต่อ fault | `EVIDENCE.md` |
| ผลการทดลองดิบ | `results/` |
