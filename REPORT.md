# QuietFail — รายงานผล

**ANN index ทำให้ vector search เร็วขึ้นหลายสิบเท่า แล้วทิ้งคำตอบที่ควรเจอไปส่วนหนึ่ง โดยไม่มี error ใดๆ แจ้ง**
รายงานนี้วัดว่าหายไปเท่าไหร่ และตรวจจับล่วงหน้าได้กี่ข้อ

```
PostgreSQL 17.10 · pgvector 0.8.5 · image pgvector/pgvector:0.8.5-pg17
corpus 100,000 และ 500,000 แถว · 384 มิติ · 200 query · cosine
เฉลย = exact search บังคับด้วย SET LOCAL enable_indexscan = off
วันที่วัด 2026-07-26 ถึง 2026-07-28 · เครื่องเดียว · โปรไฟล์ fragile
```

> **ทุกตัวเลขในรายงานนี้มาจากการรันจริง** ไม่มีตัวเลขที่ประมาณหรือคัดลอกมาจากเอกสาร
> หลักฐานดิบอยู่ใน `results/` (68 ไฟล์) · ตรวจซ้ำได้ด้วย `python scripts/audit.py --reproduce`

---

## 1. คำตอบสั้นที่สุด

| คำถาม | คำตอบ |
|---|---|
| **RQ1** ผลผิดไปกี่ % | **ขึ้นกับข้อมูลมาก — 0.65% ถึง 41% ของ top-10** ที่ค่าเริ่มต้น · **IVFFlat `probes=1` มี query ที่ได้ของถูก 0 อัน ทั้งสองชุดข้อมูล** (หัวข้อ 2) |
| **RQ2** ตรวจจับอัตโนมัติได้กี่ข้อ | **11 จาก 12 ข้อ** มีตัวตรวจที่พิสูจน์แล้วว่าใช้ได้ (ข้อที่ 12 คือ Q01 ซึ่ง*เป็น*การวัดเอง) |
| **RQ3** โค้ดที่ AI สร้าง ล้มเหลวเงียบใน**รูปแบบใด** และเกิดซ้ำข้ามโมเดลไหม | **เจอ 2 รูปแบบใหญ่ที่ต่างกันคนละขั้ว** — *เร็วแต่ผิด* กับ *ถูกแต่ช้า* · **"เขียนจนปิด index" เกิดกับทั้งสองโมเดล** · และ**ผลไม่นิ่งเมื่อถามซ้ำ** (Opus 5 = 5 กับ 7 จาก 10 บนโจทย์เดียวกัน · หัวข้อ 9.1) |

> 🔴 **อ่านตัวเลข RQ1 พร้อมชุดข้อมูลเสมอ** — ทดสอบซ้ำบน embedding จริงแล้วพบว่า
> **ขนาดของ recall loss ยกข้ามชุดข้อมูลไม่ได้**
>
> | ที่ค่าเริ่มต้น · 100k แถว | สังเคราะห์ | **จริง** |
> |---|---|---|
> | HNSW `ef_search=40` เสีย top-10 | 14.4% | **0.65%** |
> | IVFFlat `probes=1` เสีย top-10 | 21.3% | **33.5%** |
> | HNSW แย่สุดต่อ query | **0.0000** | 0.4000 |
> | IVFFlat แย่สุดต่อ query | **0.0000** | **0.0000** |
>
> **ที่ยกข้ามชุดข้อมูลได้:** ค่าเริ่มต้นไม่พอ · IVFFlat มี query ที่ได้ของถูกศูนย์อัน
> **ที่ยกไม่ได้:** ตัวเลขเปอร์เซ็นต์ · และ **HNSW ไม่เคยได้ศูนย์บนข้อมูลจริง**

**สิ่งที่ไม่ได้อยู่ในคำถามวิจัย แต่เป็นผลที่แรงที่สุด**

> **การทดสอบซ้ำบน embedding จริงหักล้างข้ออ้างเรื่องเอกสารของงานนี้เอง ครบ 3 ใน 3 ข้อ**
> (หัวข้อ 4) และ **ชื่อ fault ที่โปรเจคนี้ตั้งเอง ถูกการวัดหักล้าง 4 จาก 12 ข้อ** (หัวข้อ 5)
>
> รวมแล้ว **ข้อสรุปของงานนี้ถูกหักล้างหรือแก้ด้วยกระบวนการของตัวเอง 14 ครั้ง**
> — 7 ครั้งจากการทดสอบข้ามชุดข้อมูล และ 7 ครั้งจากการไล่เทียบเอกสารทางการ
>
> 🔴 **สองแหล่งนี้ต่างกันตรงที่ใครเป็นคนจับ** — การทดสอบข้ามชุดข้อมูลเป็นกลุ่มควบคุม
> ที่ออกแบบไว้ล่วงหน้า จับได้เอง · ส่วนการไล่เทียบเอกสาร **ไม่มีเครื่องมือใดจับได้
> ต้องมีคนตั้งคำถามให้ตรวจซ้ำ**

### โครงของรายงาน

| หัวข้อ | อะไร |
|---|---|
| 2–3 | **ผล** — ตอบ RQ1 และ RQ2 ตรงๆ |
| 4–7 | **อภิปราย** — สิ่งที่โผล่มาจากการวัด ไม่ได้ตั้งใจหา |
| **8** | **ภัยคุกคามต่อความสมเหตุสมผล** — 4 แกน · รวมความเชื่อถือได้ของ**เครื่องมือวัดเอง** |
| 9 | **RQ3 และงานต่อ** — ผลการทดลองกับโค้ดที่ AI สร้าง และสิ่งที่ยังเปิดอยู่ |
| 10 | วิธีทำซ้ำ |

**หัวข้อ 8.4 คือส่วนที่ผู้อ่านควรใช้ตัดสินว่าจะเชื่อตัวเลขข้างบนแค่ไหน** —
มันบันทึกไว้ว่าเครื่องมือวัดของโปรเจคนี้เคยให้ผลผิดเงียบๆ กี่ครั้ง และจับได้อย่างไร

---

## 2. RQ1 — ใส่ index แล้วผลหายไปเท่าไหร่

`ef_search = 40` · `probes = 1` — **ค่าเริ่มต้นล้วนๆ ไม่ได้จูนอะไรเลย** ซึ่งคือสิ่งที่ทีมส่วนใหญ่ใช้

| ขนาด | | recall@10 | recall@100 | **แย่สุดต่อ query** | ครบ 10/10 | เร็วขึ้น |
|---|---|---|---|---|---|---|
| 100k | exact | 1.0000 | 1.0000 | 1.0000 | 200/200 | 1.0× |
| 100k | HNSW | 0.8565 | 0.3903 | **0.0000** | 52/200 | 32.6× |
| 100k | IVFFlat | 0.7870 | 0.7793 | **0.0000** | 100/200 | 44.6× |
| **500k** | exact | 1.0000 | 1.0000 | 1.0000 | 200/200 | 1.0× |
| **500k** | **HNSW** | **0.5885** | 0.3871 | **0.0000** | **2/200** | **117.1×** |
| **500k** | IVFFlat | 0.7820 | 0.7857 | **0.0000** | 108/200 | 51.6× |

### สามอย่างที่ตารางนี้บอก

**ก) ค่าเฉลี่ยไม่ใช่ความจริงทั้งหมด**
recall@10 = 0.8565 ฟังดูรับได้ แต่ **มี query ที่ได้ของถูก 0 จาก 10 อัน** และมีแค่
52 จาก 200 query ที่ได้ครบ · ที่ 500k เหลือ **2 จาก 200**

**ข) index สองแบบเสื่อมตามขนาดข้อมูลคนละแบบ**
HNSW ตกจาก 0.8565 → **0.5885** (ตกลง 31%) ส่วน IVFFlat แทบไม่ขยับ
เพราะ `ef_search = 40` เป็น**ค่าสัมบูรณ์** ส่วน `probes = 1` จาก `lists = 100` เป็น**ค่าสัมพัทธ์**

**ค) HNSW เร็วขึ้นพร้อมกับที่มันพังลง** — 32.6× → **117.1×**
ทีมที่เฝ้าดูแต่ latency จะเห็นกราฟที่ "ดีขึ้น" ตอนที่คุณภาพกำลังพัง

### recall@100 = 0.39 ไม่ใช่ "คุณภาพแย่" แต่เป็นเพดาน

`ef_search = 40` ทำให้ **คืนได้ไม่เกิน 40 แถว** ต่อให้ขอ 100 → 40/100 = 0.40 คือเพดานตั้งแต่แรก
วัดแล้วว่าหน้าผาอยู่ที่ `k = ef_search + 1` **พอดีเป๊ะ** ไม่ใช่ใกล้เคียง (Q06)

> ⚠️ **ตัวเลขในตารางนี้เป็นของ corpus สังเคราะห์** — บน embedding จริงเพดานอยู่ที่ **41**
> หน้าผาจึงอยู่ที่ `k = ef_search + 2` · **กลไกยกข้ามชุดข้อมูลได้ ตำแหน่งยกไม่ได้**
> ดูหัวข้อ 6

| ขอ | 39 | 40 | **41** | 100 |
|---|---|---|---|---|
| ได้ | 39 | 40 | **40** | **40** |

→ **แก้ Q06 ไม่ได้แก้ Q01** ตั้ง `ef_search = 100` ได้แถวครบ แต่ถูกแค่ 90.6%

> ⭐ **หลักฐานที่ตรงกว่านี้ วัดได้ 2026-08-01 (E44)** — `hnsw.iterative_scan`
> ซึ่งไม่เคยทดสอบมาก่อน ยกเพดานได้**สมบูรณ์** 40 → 100 ครบทั้ง 200 query
> โดยจ่าย buffers เพิ่มเพียง 6% · แต่ `recall@100` ได้แค่ **0.7572**
>
> ```
> ได้ครบ 100 แถวทั้ง 200 query   แต่ 1 ใน 4 ของแถวที่ได้ เป็นของผิด
> ```
>
> **จำนวนแถวกับความถูกต้องคุมด้วยปุ่มคนละตัว** และนี่เป็นกับดักที่อันตราย
> กว่าเดิม เพราะตัวชี้วัดที่มองเห็นง่ายที่สุด (จำนวนแถว) กลับมาปกติสนิท
> `results/q06_iterative_probe.txt`

### เทียบข้ามเครื่องมือ — เพื่อไม่ให้กล่าวหา pgvector เกินจริง

ข้อมูลชุดเดียวกัน เฉลยชุดเดียวกัน (`ef` เท่ากันในสองแถวแรก)

| engine | recall@10 | recall@100 |
|---|---|---|
| pgvector HNSW `ef_search=40` | 0.8565 | **0.3903** |
| Qdrant HNSW `hnsw_ef=40` | 0.9945 | **0.9895** |
| Qdrant ค่าเริ่มต้นของตัวเอง | 1.0000 | 0.9996 |

**recall loss มีจริงทั้งสอง engine** (Qdrant แย่สุดต่อ query = 0.90 · ที่ k=100 ครบแค่ 69/200)
→ เป็นธรรมชาติของ ANN ไม่ใช่บั๊กของ pgvector

**แต่ขนาดต่างกันมหาศาล** — pgvector ได้ 0.39 เพราะปล่อยให้ `ef_search < LIMIT`
ส่วน Milvus ตั้ง `ef = limit` · Weaviate ปรับอัตโนมัติ · OpenSearch+lucene บังคับ `= k`
→ **Q06 เป็นของ pgvector เฉพาะตัว เพราะเจ้าอื่นป้องกันไว้ให้แล้ว**

> ⚠️ **ผ่อนถ้อยคำ 2026-08-01 (E44)** — pgvector **มีกลไกป้องกันตั้งแต่ 0.8.0**
> คือ `hnsw.iterative_scan` ซึ่งวัดแล้วยกเพดานได้จริง (40 → 100 ครบทั้ง 200 query
> จ่าย buffers เพิ่ม 6%) · **สิ่งที่ต่างจากเจ้าอื่นคือตั้งค่าปริยายเป็นปิด
> ไม่ใช่ "ไม่มีกลไก"** ซึ่งเป็นคำอธิบายที่แคบกว่าและตรงกว่า
>
> ข้อสรุปเรื่องค่าเริ่มต้นไม่กระทบ เพราะปริยายยังเป็น `off` จริง

---

## 3. RQ2 — ตรวจจับอัตโนมัติได้กี่ข้อ จากหลักฐานอะไร

**15 fault มีตัวฉีดและตัวตรวจครบวงจร** — 11 จาก 12 ข้อของชุด vector (Q01 คือการวัดเอง ไม่ใช่ข้อที่ต้องมีตัวตรวจแยก) บวก 4 ข้อพื้นฐานของ PostgreSQL
ตัวตรวจทุกตัว**พิสูจน์แล้วว่าพลิกสถานะได้จริง** ไม่ใช่แค่ตอบบวกอย่างเดียว

### แบ่งตามชนิดหลักฐาน — นี่คือคำตอบของ "จากหลักฐานอะไร"

| ชนิดหลักฐาน | ตรวจได้เมื่อไหร่ | fault | ต้นทุน |
|---|---|---|---|
| **catalog ล้วน** | เมื่อไหร่ก็ได้ · **ไม่ต้องมีข้อมูลด้วยซ้ำ** | I01 I04 I05 Q04 Q06 | ~0 |
| **วัดสดด้วย query เดียว** | เมื่อไหร่ก็ได้หลังมีข้อมูล | I02 L02 Q03 V07 | วินาที |
| **สถานะสดของ DB** | **ต้องจับตอนกำลังเกิด** | F01 F05 I03 | ต้องเฝ้า |
| **ตารางที่ตัวฉีดบันทึกไว้** | หลังตัวฉีดจบ | F03 F10 | ต้องเตรียม |
| **ประวัติ query ที่เคยรัน** | ไม่ต้องมีซอร์สโค้ด | Q02 | ~0 |

**5 ข้อตรวจได้จาก catalog ล้วน** คือผลที่ใช้งานได้จริงที่สุด — เอาไปใส่ CI ได้เลย
โดยไม่ต้องมีข้อมูล production ไม่ต้องรัน query และไม่ต้องรอให้เกิดปัญหาก่อน

**3 ข้อต้องจับตอนกำลังเกิด** (F01 F05 I03) — เครื่องมือแบบสแกนครั้งเดียวจับไม่ได้เลย
นี่คือข้อจำกัดที่ต้องพูดให้ชัด ไม่ใช่ซ่อน

### ตัวตรวจตอบ 3 อย่าง ไม่ใช่ 2

```
DETECTED / NOT_DETECTED / CANNOT_CHECK
```

**`CANNOT_CHECK` ไม่นับว่าผ่าน** — เครื่องมือที่บอกว่า "ปลอดภัย" ทั้งที่ไม่ได้ดูอะไรเลย
อันตรายกว่าไม่มีเครื่องมือ · ตัวอย่างที่ตัวตรวจ**ปฏิเสธที่จะตอบ**:

- Q03 และ L02 ปฏิเสธถ้า `limit_k >= ef_search` เพราะจะแยกจาก Q06 ไม่ออก
- I04 ปฏิเสธถ้าไม่เคยจดว่าวัด recall บน build ไหน — **ทีมที่ไม่เคยจดคือทีมที่เสี่ยงที่สุด ไม่ใช่ปลอดภัยที่สุด**
- I05 ปฏิเสธถ้ามิติไม่ใช่ 384 เพราะค่าความจุ calibrate ไว้ที่มิตินั้นเท่านั้น

### ต้นทุนของการตรวจ — วัดแล้ว

| | ข้อ | เวลา |
|---|---|---|
| รันตัวฉีดจริงซ้ำอัตโนมัติได้ | 9 ข้อ | **33.8 นาที · assertion 46/46 · ผ่านครบทั้ง 9 ข้อ** (วัดจริง 2026-07-28) |
| ต้องรันด้วยมือ | 6 ข้อ | **รันครบแล้ว 2026-08-01 ผ่านหมด** · ต้องหลาย session หรือตั้ง env (`results/manual_faults_run.txt`) |
| ตรวจว่าหลักฐานครบรูปแบบ | 36 หัวข้อ | **~30 วินาที** (`python scripts/audit.py`) |

---

## 4. ค่าเริ่มต้นของ pgvector ไม่พอ — **แต่เอกสารไม่ได้ผิด**

> ### ⭐ รูปแบบร่วมที่พบตอนตรวจเอกสาร — ครอบคลุมทั้งสองฝั่งของโครงงาน (E46)
>
> ทั้ง pgvector และ PostgreSQL **มีเครื่องมือที่แก้ปัญหาได้อยู่แล้ว
> แต่ตั้งค่าปริยายเป็นปิด**
>
> | ความผิดพลาด | กลไกที่ช่วยได้ | ค่าปริยาย | ผลเมื่อไม่ได้ตั้ง |
> |---|---|---|---|
> | **Q03 · L02 · Q06** (pgvector) | `hnsw.iterative_scan` | **off** | ผลลัพธ์ขาดเงียบๆ |
> | **F03** (PostgreSQL) | `lock_timeout` | **0** | วินิจฉัยสาเหตุผิดเงียบๆ |
> | **F01** (PostgreSQL) | `idle_in_transaction_session_timeout` | **0** | connection หมดเงียบๆ |
>
> **ข้อสรุปหลักที่ว่า "ค่าเริ่มต้นคือปัญหา" จึงไม่ใช่ข้อสังเกตเฉพาะ pgvector**
> แต่เป็นรูปแบบร่วมที่พบทั้งในส่วนขยายและในตัวฐานข้อมูลเอง
>
> เป็นข้อสังเกตที่**ได้จากการไล่เทียบเอกสารย้อนหลัง ไม่ใช่จากการออกแบบการทดลอง**
> — ไม่มีเครื่องมือใดในโครงงานจับให้ได้ · `results/doc_crosscheck_f.txt`


> 🔴 **หัวข้อนี้เขียนใหม่ทั้งหมด 2026-07-30** หลังทดสอบซ้ำบน embedding จริง
> เดิมชื่อ *"เอกสารของผู้พัฒนาเองผิดหรือไม่ครบ 3 ที่"* — **ทั้ง 3 ข้ออ้างถูกถอน**
> (2 ข้อจากการทดสอบข้ามชุดข้อมูล · ข้อที่ 3 จากการไล่เทียบเอกสาร ดู E42)
>
> | | ข้ออ้างเดิม | ผลหลังทดสอบข้ามชุดข้อมูล |
> |---|---|---|
> | 4.1 | Q04 "สูตรเอกสารเกินจำเป็น" | ❌ **ถอนข้ออ้าง** |
> | 4.2 | I02 "คำเตือนออกผิดที่" | ❌ **ถอนข้ออ้าง** — เอกสารแนะนำถูก |
> | 4.3 | Q03 "`work_mem` ไม่อยู่ในรายการข้อจำกัด" | ❌ **ถอนข้ออ้าง** |
>
> 🔴 **แก้อีกครั้ง 2026-08-01 (E42)** — หลังเจ้าของโครงงานท้วงว่าข้ออ้างเรื่อง
> *"เอกสารไม่ครบ"* เป็นช่องโหว่ใหญ่ และสั่งให้ตรวจกับเอกสารต้นทางให้ถี่ถ้วน
>
> **ผลคือข้ออ้างเรื่องเอกสารถูกถอนครบทั้งสามข้อ**
>
> ### ⭐⭐ และการไล่เทียบยังพบหลักฐานที่ **ยืนยันว่าเอกสารแม่นในเชิงปริมาณ**
>
> เอกสารหัวข้อ *Filtering* ทำนายผลของ Q03 ไว้เป็นตัวเลข
> *"If a condition matches 10% of rows, with HNSW and the default `hnsw.ef_search`
> of 40, **only 4 rows will match on average**"*
>
> ```
> filter 10% · ef_search 40    เอกสารทำนาย 4    วัดบน embedding จริง = 4
>                                               วัดบนสังเคราะห์      = 2
> ```
>
> **ตรงทุกหลักบนข้อมูลจริง** และเข้ากับเส้นแบ่งกลไก/ปริมาณพอดี
> `results/real_q03.txt` แถว filter 10.0
>
> 🔴 **ตัวเลขนี้อยู่ในไฟล์ผลมาตั้งแต่ 30 กรกฎาคม และไม่มีใครสังเกตจนถึง 2 สิงหาคม**
> เพราะรอบนั้นตั้งคำถามว่า *เอกสารบกพร่องตรงไหน* จึงอ่านข้ามหลักฐานที่บอกว่า
> เอกสารถูก · **คำถามที่ตั้งเป็นตัวกำหนดว่าจะเห็นอะไรในข้อมูลชุดเดียวกัน**
> **เอกสารทางการของ pgvector ไม่มีข้อบกพร่องที่รายงานนี้พิสูจน์ได้**
>
> **ข้อสรุปหลักของหัวข้อนี้ไม่เปลี่ยน และไม่เคยขึ้นกับข้ออ้างที่ถอนไป**
>
> ```
> ค่าเริ่มต้นของ pgvector ไม่เพียงพอ  และเมื่อไม่พอ  มันล้มเหลวโดยไม่มี error
> ```
>
> ยืนยันแล้ว **3 ชุดข้อมูล** (สังเคราะห์ · 384 มิติ · 768 มิติ) ทั้ง `probes=1`
> `ef_search=40` และ `scan_mem_multiplier=1` · สิ่งที่หายไปคือ**ข้อกล่าวหาต่อเอกสาร**
> ซึ่งเป็นข้อรองที่งอกขึ้นมาระหว่างทาง ไม่ใช่คำถามวิจัยข้อใด
>
> #### 🔴 ทำไมทั้งสามข้อถึงล้ม — สาเหตุคนละแบบ และควรอ่านแยกกัน
>
> | | ล้มเพราะ |
> |---|---|
> | Q04 · I02 | **ผูกกับ corpus สังเคราะห์** ซึ่งเล่มยอมรับไว้เองว่าเป็นข้อจำกัด · การทดสอบข้ามชุดข้อมูลจับได้ |
> | **Q03** | **ไล่ค่าพารามิเตอร์ไม่ไกลพอ แล้วสรุปว่าปุ่มไม่ทำงาน** · ไม่มีเครื่องมือใดจับได้ — คนถามจับได้ |
>
> **กรณี Q03 ร้ายแรงกว่าในเชิงระเบียบวิธี** เพราะไม่ใช่ข้อจำกัดที่ประกาศไว้แล้ว
> แต่เป็นการวัดที่หยุดก่อนถึงคำตอบ · เป็น**ความล้มเหลวเงียบชนิดเดียวกับที่รายงานนี้ศึกษา**
> คือไม่มี error ไม่มีสัญญาณเตือน มีแต่ข้อสรุปที่ผิดซึ่งดูสมเหตุสมผลทุกประการ
>
> #### เส้นแบ่งกลไก/ปริมาณยังยืน — แต่ต้องอ่านใหม่
>
> Q03 ไม่ใช่ตัวอย่างของ "ข้อสรุปเชิงกลไกยกข้ามชุดข้อมูลได้" อีกต่อไป
> เพราะข้ออ้างเรื่องเอกสารของมันถูกถอน · เส้นแบ่งยังยืนด้วยหลักฐานที่เหลือ
> (Q06 เพดาน `ef_search` · I05 ความจุ `maintenance_work_mem` · V07 · I01 · Q02)
> ซึ่งได้รับการทำนายล่วงหน้าและยืนยันครบ 6 ข้อ — ดูหัวข้อ 5

### 4.1 Q04 — ค่าเริ่มต้นไม่พอ · **แต่สูตรของเอกสารสมเหตุสมผล**

IVFFlat `lists = 100` · เอกสารแนะนำ `probes = sqrt(lists) = 10`

| `probes` | | สังเคราะห์ | **จริง** | ครบ 10/10 (จริง) | 200 query (จริง) |
|---|---|---|---|---|---|
| **1** | ค่าเริ่มต้น | 0.7370 | **0.6607** | **58/200** | 283 ms |
| 2 | | 0.9525 | 0.8017 | 87/200 | 530 ms |
| **5** | | **1.0000** | **0.9068** | 125/200 | 1,076 ms |
| **10** | **เอกสารแนะนำ** | 1.0000 | **0.9527** | 150/200 | 2,102 ms |
| 50 | | — | 0.9930 | 191/200 | 9,959 ms |
| 100 | สแกนทุกกลุ่ม | 1.0000 | 1.0000 | 200/200 | 20,331 ms |

> 🔴 **แถว `probes = 100` ไม่ได้ใช้ดัชนี — ตรวจพบ 1 สิงหาคม 2569 (E45)**
>
> เอกสารของ pgvector ระบุไว้ว่าค่า probes ที่เท่ากับจำนวนกลุ่มให้ผลเทียบเท่า
> การค้นแบบตรง *"at which point the planner won't use the index"* ผู้พัฒนา
> ตรวจแผนการทำงานย้อนหลังแล้วพบว่าเป็นเช่นนั้นจริง
>
> | `probes` | 384 มิติ | 768 มิติ |
> |---|---|---|
> | 1 ถึง 40 | ใช้ดัชนี | ใช้ดัชนี |
> | 50 | ใช้ดัชนี | **ไม่ใช้** |
> | 100 | **ไม่ใช้** | **ไม่ใช้** |
>
> ค่าเรียกคืน 1.0000 ที่แถวดังกล่าวจึงถูกต้องในแง่ผลลัพธ์ที่ผู้ใช้ได้รับ
> **แต่มิใช่ผลของการปรับ `probes`** และเวลาที่บันทึกไว้เป็นเวลาของการค้นแบบตรง
>
> **ข้อสรุปหลักไม่กระทบ** เนื่องจากอยู่ที่ปลายล่างของตาราง (`probes` = 1 และ 10)
> ซึ่งตรวจยืนยันแล้วว่าใช้ดัชนีจริงทั้งสองแถว


*ข้อมูลจริง = BEIR/Quora · all-MiniLM-L6-v2 · 3 build ต่อค่า · `results/real_q04.txt`*

#### 🔴 ข้ออ้างที่ถอน

เคยอ้างว่า *"`probes=5` ได้ recall สมบูรณ์แล้ว · เอกสารแนะนำ 10 ซึ่งช้ากว่า 2.2 เท่า
โดยไม่ได้ recall เพิ่มเลยสักนิด"*

บนข้อมูลจริง `probes=5` ได้แค่ **0.9068** และ `probes=10` ได้ **0.9527** — **ดีขึ้นจริง**

→ **คำแนะนำของเอกสารสมเหตุสมผลบนข้อมูลจริง · corpus สังเคราะห์ต่างหากที่ทำให้มันดูสิ้นเปลือง**

#### ⭐ ข้อที่รอด — และแรงขึ้นบนข้อมูลจริง

**ค่าเริ่มต้น `probes = 1` ไม่พอ** และแย่กว่าบนสังเคราะห์ด้วยซ้ำ
(0.6607 vs 0.7370 · ครบ 10/10 เหลือ 58 จาก 200) · **แย่สุดต่อ query = 0.0000 ทั้งสองชุด**

| ยกไปใช้ได้ | ยกไปใช้ไม่ได้ |
|---|---|
| "ค่าเริ่มต้นของ pgvector ไม่พอสำหรับงานจริง" | "สูตรของเอกสารเกินจำเป็น" |

**และนี่คือวิทยานิพนธ์ของโปรเจคตรงตัวยิ่งกว่าเดิม** — ค่าที่เหมาะสมมาจากการวัด
**กับข้อมูลของคุณเอง** ไม่ใช่จากสูตร และไม่ใช่จากการวัดกับข้อมูลชุดอื่น

### 4.2 I02 — pgvector ไม่มีคำเตือนสำหรับตัวอย่างที่ไม่เป็นตัวแทน

4 เงื่อนไข · ข้อมูลปลายทางเหมือนกันเป๊ะ 100,000 แถว · เปลี่ยนแค่ **ตอนที่ build**

> 🔴 **หัวข้อนี้ถูกลดระดับข้ออ้างแล้ว 2026-07-30** หลังวัดซ้ำบน embedding จริง
> เดิมชื่อ *"คำเตือนออกกับกรณีที่ดีที่สุด"* ซึ่ง **จริงเฉพาะบน corpus สังเคราะห์**

| | build ตอนมี · ลักษณะตัวอย่าง | **pgvector เตือน** | สังเคราะห์ | **จริง** |
|---|---|---|---|---|
| **A** | 50 แถว · กระจาย | **✅ เตือน** | **1.0000** | **0.4520** |
| B | 1,000 แถว · กระจาย | ❌ เงียบ | 0.8798 | 0.5974 |
| C | 100,000 แถว (วิธีที่เอกสารบอก) | ❌ เงียบ | 0.7682 | **0.6653** |
| **D** | 1,000 แถว · **กระจุกในบริเวณแคบ** | ❌ **เงียบ** | **0.2278** | **0.4151** |

*ข้อมูลจริง = BEIR/Quora · all-MiniLM-L6-v2 · 5 รอบต่อเงื่อนไข · `results/real_i02.txt`*
*เงื่อนไข D บนข้อมูลจริงแปลเป็น "1,000 เพื่อนบ้านที่ใกล้จุดยึดที่สุด" เพราะไม่มีกลุ่มที่ออกแบบไว้*

#### สิ่งที่รอดการทดสอบข้ามชุดข้อมูล

**⭐ ความเป็นตัวแทนสำคัญกว่าจำนวนแถว** — คู่ที่คุมจำนวนแถวเท่ากันเป๊ะ:

| 1,000 แถวเท่ากัน | recall | query ที่ได้ 0 อัน |
|---|---|---|
| B กระจาย | 0.5974 | 44 / 1,000 |
| **D กระจุก** | **0.4151** | **104 / 1,000** |

และ **A (50 แถวกระจาย) ยังชนะ D (1,000 แถวกระจุก)** ทั้งสองชุดข้อมูล

→ **ช่องว่างที่ยังยืนอยู่: pgvector ไม่มีคำเตือนใดๆ สำหรับตัวอย่างที่ไม่เป็นตัวแทน**
ซึ่งเป็นกรณีที่แย่ที่สุดในทั้งสองชุดข้อมูล
NOTICE ผูกกับ `rows < lists` อย่างเดียว ซึ่งไม่ใช่ตัวแปรที่ตัดสินผล

> 🔴 **ต้องระบุให้ชัด — คำแนะนำของเอกสารถูกต้อง** (ตรวจซ้ำ 2026-08-01)
>
> เอกสาร pgvector เขียนไว้ตรงๆ ว่าให้สร้าง index *"after the table has some data"*
> และ FAQ ข้อ *"Why are there less results for a query after adding an IVFFlat index?"*
> ตอบว่า *"The index was likely created with too little data for the number of lists."*
>
> **ทำตามเอกสารตรงๆ คือเงื่อนไข C ซึ่งได้ recall ดีที่สุดบนข้อมูลจริงทั้งสองแบบจำลอง**
> (384 มิติ 0.6653 · 768 มิติ 0.6856) → **ผู้ที่ทำตามเอกสารจะไม่เจอปัญหานี้**
>
> สิ่งที่รายงานนี้เพิ่มได้จึงไม่ใช่ *"เอกสารบกพร่อง"* แต่คือ **การระบุตัวแปรให้แคบลง**
> เอกสารอธิบายสาเหตุว่าเป็นเรื่อง *ปริมาณ* ("too little data") ขณะที่การเทียบแบบคุม
> จำนวนแถวเท่ากันเป๊ะ 1,000 แถว (B 0.5974 · D 0.4151) ชี้ว่าตัวแปรที่ตัดสินคือ
> **ความเป็นตัวแทน** ซึ่งปริมาณเป็นเพียงตัวแทนหยาบๆ ของมัน

#### 🔴 สิ่งที่ **ไม่** รอด — ต้องเลิกอ้าง

| เคยอ้าง | ความจริง |
|---|---|
| "คำเตือนออกกับกรณีที่ดีที่สุด" | บนข้อมูลจริง A **แย่เป็นอันดับสอง** → คำเตือนออก**ถูกที่** |
| "`DETAIL: This will cause low recall` เป็นเท็จ" | บนข้อมูลจริง **เป็นจริง** |
| "ยิ่ง build ตอนข้อมูลมาก ยิ่งแย่" | บนข้อมูลจริง **กลับกัน** (0.45 → 0.60 → 0.67) |

สาเหตุระบุได้ชัด และ `CLAUDE.md` เตือนไว้เองตั้งแต่ต้น: A ได้ 1.0000 เพราะ
corpus สังเคราะห์เรียง id สลับกลุ่ม **50 แถวแรกจึงครบ 50 กลุ่มพอดี**
— คุณสมบัติของข้อมูลชุดนั้น ไม่ใช่กฎทั่วไป · ตอนนี้พิสูจน์แล้วด้วยข้อมูลคนละชุด

#### ⭐ ผลใหม่ที่ corpus สังเคราะห์มองไม่เห็น

ช่วงของ recall จาก 5 build บนข้อมูลจริง: **A = 0.0000** · B 0.0495 · C 0.0490 · D 0.0225

A มี 50 แถวแต่ `lists = 100` → แถวน้อยกว่ากลุ่มที่ขอ k-means จึงไม่มีอะไรให้สุ่ม
แต่ละแถวเป็นจุดศูนย์กลางของตัวเอง = **กำหนดได้แน่นอน**

→ **เงื่อนไขเดียวกับที่ pgvector เตือน คือเงื่อนไขเดียวที่ผล reproduce ได้ 100%**
ความไม่นิ่งของ I04 หายไปพอดีตรงจุดที่เอกสารบอกว่าอย่าทำ

#### ⚠️ แก้ข้อสรุปหลังวัดต้นทุน (2026-07-28) — เดิมเทียบแต่ recall

การทดลอง **I02b** สร้าง corpus ที่ยากขึ้น (กลุ่มขนาดไม่เท่ากันแบบ power law ·
id เรียงตามกลุ่มไม่สลับ · noise สูงขึ้น · outlier 2%) แล้ววัดซ้ำ พบว่า
**เงื่อนไข A ที่ได้ recall สูงกว่า จ่ายด้วยต้นทุนที่ไม่เคยวัดมาก่อน**

วัด **3 รอบ** ทั้งสอง corpus (ค่าเดี่ยวรอบแรกทำให้เขียนตัวเลขผิดไปครั้งหนึ่ง — ดู E36)

**corpus ชุดหลัก** — A นิ่งสนิททั้งสามรอบ ส่วน C แกว่งตาม I04

| | รอบ 1 | รอบ 2 | รอบ 3 |
|---|---|---|---|
| A (50 แถว) recall / buffers | 1.0000 / 433 | 1.0000 / 433 | 1.0000 / 433 |
| C (ครบ) recall / buffers | 0.8285 / 364 | 0.8165 / 341 | 0.7880 / 345 |
| **A อ่านมากกว่า C** | **1.19×** | **1.27×** | **1.26×** |

**corpus ที่ยากขึ้น** — 🔴 **ทิศทางไม่นิ่ง รอบที่ 2 C ชนะ A**

| | รอบ 1 | รอบ 2 | รอบ 3 |
|---|---|---|---|
| A (50 แถว) recall | 0.8615 | **0.8420** | 0.8365 |
| C (ครบ) recall | 0.7655 | **0.8495** | 0.7850 |

**สิ่งที่พูดได้จากสามรอบ**

1. บนชุดหลัก A ได้ recall เต็มจริงและ**ทำซ้ำได้เป๊ะ** โดยอ่านมากกว่า **1.2 เท่า**
   — เป็นการแลกที่**คุ้ม** ไม่ใช่ของฟรี และไม่ใช่ของแพง
2. **บน corpus ที่ยากขึ้น ความได้เปรียบหายไปจนกลับทิศได้** — ต้องเลิกอ้างว่า A ชนะ
3. ข้อสรุปที่ยังยืนคือ **ความเป็นตัวแทนสำคัญกว่าจำนวนแถว** ซึ่งการวัดต้นทุน
   ไม่กระทบ · ส่วนข้ออ้างเรื่อง "คำเตือนออกผิดที่" ถูกถอนไปแล้วในรอบข้อมูลจริง

**สิ่งที่ต้องเลิกพูด:** *"build ตอนข้อมูลน้อยให้ผลดีกว่า"* เป็นข้อสรุปทั่วไป
มันจริงเฉพาะบน corpus ที่กลุ่มสลับกันสม่ำเสมอ และ**ไม่ควรแนะนำเป็นวิธีทำ**
เพราะได้ recall เต็มเหมือนกันจากการเพิ่ม `probes` ซึ่งปรับกลับได้และไม่ทำให้ index บวม

หลักฐาน: `results/i02b_run.txt` · `results/i02b_buffers.txt` · `results/i02_cost_control.txt`

### 4.3 ❌ Q03 — ข้ออ้างเรื่องเอกสาร **ถอนแล้ว** · ตัว fault ยังมีจริง

> 🔴 **ถอน 2026-08-01 (E42)** — เดิมหัวข้อนี้ชื่อ *"ทางแก้ที่เอกสารระบุไม่เพียงพอ
> แม้ทำครบทุกข้อ"* และอ้างว่ามีข้อจำกัดข้อที่ 5 ที่เอกสารไม่ได้เขียน
> **ตรวจแล้วไม่จริงทั้งสองส่วน**
>
> **1. เอกสารนิยาม `scan_mem_multiplier` ว่าเป็นตัวคูณของ `work_mem` อยู่แล้ว**
>
> > *"Specify the max amount of memory to use, as a multiple of `work_mem` (1 by default)"*
>
> หน่วยความจำที่ใช้ได้ = `work_mem` × `multiplier` — **สองปุ่มคูณกัน ไม่ใช่คนละเรื่อง**
> ข้ออ้างที่ว่า *"เอกสารไม่พูดถึง `work_mem`"* จึงไม่ถูกต้องตั้งแต่ต้น
>
> **2. การทดลองเดิมไล่ค่าไม่ไกลพอ** — หยุดที่ `multiplier = 8` แล้วสรุปว่าปุ่มไม่ทำงาน
>
> | คงค่า `work_mem` ไว้ที่ค่าเริ่มต้น 64kB | ได้ |
> |---|---|
> | `scan_mem_multiplier` = 1 | 5 / 40 |
> | `scan_mem_multiplier` = 8 | 17 / 40 ← **ของเดิมหยุดตรงนี้** |
> | **`scan_mem_multiplier` = 32** | **40 / 40** |
> | `scan_mem_multiplier` = 64 · 128 | 40 / 40 |
> | *(อ้างอิง)* `work_mem` = 4MB · multiplier 1 | 40 / 40 |
>
> **`multiplier = 32` ให้ผลเท่ากับการเพิ่ม `work_mem` เป๊ะ** — ปุ่มที่เอกสารให้ใช้ได้จริง
> `results/q03_multiplier_probe.txt`

**สิ่งที่ยังยืน และเป็นข้อสรุปที่ควรใช้ต่อไป**

1. **ตัว fault มีจริง** — filter ทำงานหลัง index คืนผู้เข้ารอบ ทำให้แถวหายเงียบๆ
   ยืนยันแล้ว **3 ชุดข้อมูล** ไม่มี error สักครั้ง
2. **ค่าเริ่มต้นไม่พอ** — `multiplier = 1` ได้ **5 จาก 40**
3. **ต้องหมุนไกลกว่าที่ใครจะเดา** — เอกสารยกตัวอย่างค่า 2 · ที่ 8 ยังได้แค่ 17/40
   ต้องถึง **32** จึงครบ · เป็นเรื่อง**ค่าที่เหมาะสมต้องมาจากการวัด**
   ซึ่งเป็นวิทยานิพนธ์ของโครงงานอยู่แล้ว ไม่ใช่เรื่องเอกสารบกพร่อง

ที่ filter ตรง 1% · ขอ 40 แถว

| เปลี่ยนอะไร | สังเคราะห์ | **จริง** | buffers (จริง) |
|---|---|---|---|
| ค่าเริ่มต้น | 0 | 1 | 530 |
| เปิด `hnsw.iterative_scan` | **0** | 5 | 873 |
| `+ max_scan_tuples` ×10 | **0** | **5** | **873** |
| `+ scan_mem_multiplier` ×8 | **0** | 13 | 3,005 |
| **`+ work_mem` 64kB → 4MB** | **40** | **40** | 27,917 |

*`results/real_q03.txt`*

ตารางนี้เป็นการไล่ทีละขั้นแบบสะสม โดย `scan_mem_multiplier` หยุดที่ 8
**ซึ่งตอนนี้ทราบแล้วว่าไกลไม่พอ** — การเพิ่ม `work_mem` ให้ผลเหมือนกับการเพิ่ม
`multiplier` ต่อไปถึง 32 เพราะทั้งคู่คูณเข้าตัวเดียวกัน

#### ⭐ ผลที่แข็งขึ้นจากการทดสอบข้ามชุดข้อมูล

> 🔴 **ข้อสรุปนี้ถอนแล้ว 2026-08-01 (E43)** — เป็นความผิดพลาดชนิดเดียวกับ E42
> ครั้งที่สอง คือวัดใต้เงื่อนไขที่มีตัวอื่นผูกอยู่ แล้วอ่านว่าตัวที่ปรับไม่มีผล
>
> **เอกสารเขียนคำเตือนเรื่องนี้ไว้ตรงๆ และเราไม่เคยยกมาอ้าง**
>
> > *"Try increasing this [`scan_mem_multiplier`] if increasing
> > `hnsw.max_scan_tuples` does not improve recall."*
>
> เมื่อทำให้หน่วยความจำเหลือเฟือก่อน (`scan_mem_multiplier = 32`) แล้วไล่ค่า
> `max_scan_tuples` ปุ่มตอบสนองเป็นลำดับชัดเจน
>
> | `max_scan_tuples` (ที่ `mult = 32`) | ได้/40 | buffers |
> |---|---|---|
> | 100 | 4 | 781 |
> | 1,000 | **7** | 2,397 |
> | 5,000 | **40** | 11,114 |
> | 20,000 (ปริยาย) | 40 | 16,064 |
>
> ที่เห็นว่า *"buffers ไม่ขยับ"* เพราะการสแกนหยุดด้วยเหตุผลอื่นก่อนจะแตะเพดาน
> จำนวน tuple — **การเพิ่มเพดานที่ไม่เคยถูกแตะ ย่อมไม่มีอะไรเปลี่ยน**
>
> 🔴 **และการวัดซ้ำสองชุดข้อมูลไม่ได้ทำให้ข้อสรุปนี้แข็งขึ้นอย่างที่เคยเขียน**
> เพราะทั้งสองชุดวัดใต้เงื่อนไขที่ผิดเหมือนกัน — **การทำซ้ำยืนยันความสม่ำเสมอ
> ไม่ได้ยืนยันความถูกต้อง** · `results/doc_reread_probe.txt`

**สิ่งที่การวัดรอบนี้ให้เพิ่ม และใช้ได้จริง** — ที่ `mult = 32` ค่า `max_scan_tuples`
เพียง 5,000 ก็ได้ครบ 40 แถวแล้ว ขณะที่ค่าปริยาย 20,000 ได้เท่ากันแต่อ่านมากกว่า
**1.45 เท่า** (วัดครั้งเดียวบนชุดข้อมูลเดียว — เป็นข้อสังเกต ยังไม่ใช่ข้อสรุป)

#### 🔴 ประวัติการแก้ถ้อยคำของหัวข้อนี้ — เก็บไว้เพราะเป็นบทเรียน

| รอบ | ข้อความ | ผล |
|---|---|---|
| 1 | "ทางแก้ไม่ทำงานเลย" | ผิด — บนข้อมูลจริงช่วยบางส่วน (1 → 5 → 5 → 13) |
| 2 | "ไม่เพียงพอ แม้ทำครบทุกข้อ" | ผิด — **ไม่ได้ทำครบ ทำแค่ไม่แรงพอ** |
| **3** | **"ค่าเริ่มต้นไม่พอ · ค่าที่พอต้องมาจากการวัด"** | ✅ ยืนกับหลักฐานที่มี |

**สองรอบแรกผิดไปทางเดียวกันคือกล่าวหาเอกสาร** ทั้งที่หลักฐานพาไปได้แค่
"เราตั้งค่าไม่พอ" · เป็นตัวอย่างของการที่ข้อสรุปเคลื่อนไปตามเรื่องเล่าที่อยากได้

#### อาการหลักเกิดซ้ำ · เส้นโค้งต่างกัน

| filter ตรง % | สังเคราะห์ได้ | **จริงได้** |
|---|---|---|
| 20 | 10 | **6** |
| 10 | 2 | 4 |
| 2 | **0** | 1 |
| 0.5 | — | **0** |
| 0.1 | **0** | **0** |

*ขอ 10 · เฉลย 10 ทุกระดับ · ใช้ index ทุกกรณี · **ไม่มี error สักครั้ง***

**buffers นิ่งที่ 530 ตั้งแต่ 20% ลงไป ทั้งที่ผลลัพธ์ลดจาก 6 เหลือ 0**
(สังเคราะห์นิ่งที่ 977 เหมือนกัน) → **จ่าย I/O เท่าเดิม แต่ได้ของน้อยลงเรื่อยๆ**
ทีมที่เฝ้าดู I/O จะไม่เห็นอะไรผิดปกติเลย

---

## 4.4 ⭐⭐ ทดสอบข้ามชุดข้อมูลครบ 10 fault — เส้นแบ่งที่ชัดเจน

**คุมทุกอย่างเท่าเดิม เปลี่ยนแค่ที่มาของเวกเตอร์** — BEIR/Quora · all-MiniLM-L6-v2 ·
100,000 แถว · 384 มิติ · 200 query · โปรไฟล์เดียวกัน

| fault | ข้อสรุป**เชิงกลไก** | ข้อสรุป**เชิงปริมาณ** |
|---|---|---|
| **I01** opclass ไม่ตรง | ✅ รอด | ✅ **buffers เท่ากับไม่มี index ทุกบล็อก** |
| **Q02** รูปแบบ query | ✅ รอด | ✅ **ใช้ index ได้ 2 จาก 6 แบบเท่ากันเป๊ะ** |
| **Q06** เพดาน `ef_search` | ✅ รอด | ⚠️ เพดานขยับ 40 → 41 |
| **Q03** filter หลัง index | ✅ รอด | ⚠️ ค่าที่ทำให้ผลครบต่างกันตามชุดข้อมูล |
| **V07** NULL/zero vector | ✅ รอด | ✅ **หาย 10 แถวพอดีทั้งคู่** |
| **I05** ความจุ `maintenance_work_mem` | ✅ รอด | ✅ 28,352 vs 28,364 |
| **L02** แถวที่ถูกลบ | ✅ รอด | ❌ ไม่มีหน้าผา |
| **I04** build ไม่นิ่ง | ✅ รอด | ❌ กลุ่มควบคุมพลิก 2 ข้อ |
| **I02** ตัวอย่างตอน build | ⚠️ บางส่วน | ❌ ทิศทางกลับด้าน |
| **Q04** ค่า `probes` | ✅ รอด | ❌ **ถอนข้ออ้าง** |

### ⭐⭐ ผลเชิงระเบียบวิธี — ทำนายล่วงหน้าแล้วถูก

> **ข้อสรุปเชิงกลไกยกข้ามชุดข้อมูลได้ทุกข้อ · ข้อสรุปเชิงปริมาณยกไม่ได้เลยสักข้อ**

เส้นแบ่งนี้ถูกเขียนไว้**ก่อน**รัน L02 · V07 · Q06 · I04 · I01 · Q02
(หลังทดสอบ Q01 · I02 · Q04 · Q03) แล้วผลออกมาตามนั้น **ครบทั้ง 6 ข้อ**
→ **เป็นข้อค้นพบที่ทำนายได้ ไม่ใช่การอธิบายย้อนหลัง**

### ⭐ I01 ยืนยันว่าเป็นภาพสะท้อนกลับด้านของ Q01

| | ใช้ index | buffers | recall@10 |
|---|---|---|---|
| ไม่มี index | — | 23,388 | 1.0000 |
| **opclass ผิด** (l2 แต่ค้น cosine) | ❌ | **23,388** | **1.0000** |
| opclass ถูก | ✅ | **473** | 0.9930 |

**buffers ของ "opclass ผิด" เท่ากับ "ไม่มี index เลย" ทุกบล็อก** — planner ไม่แตะ index นั้นเลย

```
Q01  index ถูกใช้     → เร็วขึ้น 49 เท่า   แต่ recall ตก
I01  index ไม่ถูกใช้   → recall สมบูรณ์     แต่ไม่เร็วขึ้นเลย
```

**ทั้งคู่ไม่มี error** · ทีมที่เฝ้าดู recall จะไม่เห็น I01 · ทีมที่เฝ้าดู latency จะไม่เห็น Q01

**ข้อเสนอสำหรับผู้อ่านและผู้ทำงานต่อ:** ตัวเลขทุกตัวในรายงานนี้ต้องอ่านคู่กับชุดข้อมูล
ที่ใช้วัดเสมอ ส่วนคำอธิบาย*ว่าเกิดอะไรขึ้นและทำไม* ยกไปใช้ได้

### ⭐⭐ 4.5 ทดสอบซ้ำด้วยแบบจำลองที่สอง — 768 มิติ

**เปลี่ยนทั้งโมเดลและมิติ ข้อความชุดเดิมเป๊ะ** — `all-mpnet-base-v2` · 768 มิติ ·
100,000 แถว · 200 query · ครบ 10 fault เหมือนรอบแรก · `results/real2_model2.txt`

#### ⭐ ข้อค้นพบที่โมเดลเดียวตอบไม่ได้

| ที่ค่าเริ่มต้น | HNSW `ef_search=40` | IVFFlat `probes=1` |
|---|---|---|
| **768 มิติ** (mpnet) | **0.9974** | 0.6896 |
| **384 มิติ** (MiniLM) | **0.9935** | 0.6653 |
| สังเคราะห์ | 0.8565 | 0.7870 |

| | ต่างกันระหว่างสองโมเดลจริง | ต่างจากสังเคราะห์ | อัตราส่วน |
|---|---|---|---|
| HNSW | **0.0039** | 0.1370 | **35×** |
| IVFFlat | **0.0243** | 0.1217 | **5×** |

**ทิศทางเดียวกันทั้งสอง index** — ช่องว่างระหว่างโมเดลจริงสองตัว
เล็กกว่าช่องว่างระหว่างสังเคราะห์กับจริง **อย่างน้อย 5 เท่า**

> ⚠️ **ห้ามอ้าง "35 เท่า" ลอยๆ** — เป็นของ HNSW เท่านั้น · IVFFlat ได้ 5 เท่า
> ตัวเลขที่ยกไปใช้ได้อย่างปลอดภัยคือ **"อย่างน้อย 5 เท่า"**

→ **ความต่างทั้งหมดที่รายงานนี้พบ มาจาก "สังเคราะห์ vs จริง" ไม่ใช่จาก "โมเดลไหน"**
ซึ่งเป็นข้อสรุปที่ต้องมีสองโมเดลถึงจะพูดได้

#### เส้นแบ่งยืนยันครั้งที่สาม — คราวนี้มีมิติเป็นตัวแปร

| | ยกข้ามได้ |
|---|---|
| **กลไก** | V07 หาย 10 แถวพอดีทั้ง 3 ชุด · Q06 เพดาน 41 ทั้งสองมิติ · I02 ความเป็นตัวแทน · L02 ขอเยอะไม่ช่วย + `VACUUM` แก้ได้ · I04 รูปแบบการแกว่ง |
| **ปริมาณ** | ❌ Q04 ค่า `probes` ที่พอ (5 → 100 → 50) · I04 สัดส่วน · L02 จุดที่เริ่มขาด · I05 ความจุ |

#### 🔴 I05 — คำทำนายที่บันทึกไว้ก่อนรัน และมันผิด

ก่อนรัน รายงานนี้ทำนายว่าที่ 768 มิติ ความจุควรเหลือครึ่งเดียว = **14,000–14,500 tuples**

```
384 มิติ  28,364 tuples
768 มิติ  16,759 tuples      อัตราส่วน 1.69 ไม่ใช่ 2
```

**ทิศทางถูก ขนาดผิด** — ถ้าความจุแปรผกผันกับขนาดเวกเตอร์ล้วนต้องได้ 2 เท่า
การได้ 1.69 บ่งชี้ว่ามี**ต้นทุนต่อ element ที่ไม่ขึ้นกับมิติ**

> ### ✅ ตรวจกับรหัสต้นฉบับแล้ว 2026-08-01 — **674 ไบต์** ไม่ใช่ "ราว 700–1,000"
>
> ย่อหน้านี้เคยกำกับไว้ว่า *ยังไม่ได้ยืนยันกับรหัสต้นฉบับ* · ตอนนี้ยืนยันแล้ว
> และแยกเป็นรายการได้ครบทุกไบต์จาก pgvector v0.8.5
>
> | ที่มา | ไบต์ |
> |---|---|
> | `sizeof(HnswElementData)` | 128 |
> | `HnswNeighborArrayPtr[1]` | 8 |
> | `HNSW_NEIGHBOR_ARRAY_SIZE(m×2=32)` ชั้น 0 | 520 |
> | ชั้นสูงกว่า 0 เฉลี่ย (`E[level]` = 1/15) | 18 |
> | **รวมส่วนที่ไม่ขึ้นกับมิติ** | **674** |
> | เวกเตอร์ `MAXALIGN(8 + 4×dim)` | 384 → 1,544 · 768 → 3,080 |
>
> **อธิบายอัตราส่วนได้เป๊ะ** — (674+3,080) / (674+1,544) = **1.692**
>
> 🔴 **ช่วงที่เคยเดาไว้ไม่ครอบคลุมค่าจริง** — ทิศทางถูก แต่ขอบล่างสูงเกินไป
> เป็นเหตุผลที่ย่อหน้านี้ถูกกำกับไว้ตั้งแต่ต้นว่าห้ามอ้างจนกว่าจะตรวจ
>
> **ยืนยันด้วยการทำนายล่วงหน้า 5 เงื่อนไข คลาดไม่เกิน 16 tuple**
> `results/i05_capacity_model.txt`

#### ⭐⭐ และการตรวจนี้เปิดข้อค้นพบใหม่ที่เข้าธีมของรายงานพอดี

```
maintenance_work_mem 64MB · parallel  ->  28,364 tuples
maintenance_work_mem 64MB · serial    ->  29,798 tuples
```

**ค่าเดียวกันเป๊ะ ให้ความจุต่างกัน 5%** เพราะเส้นทาง parallel หัก `estother`
3 MiB แล้วหัก `memoryMargin` อีก 1 MiB ขณะที่เส้นทาง serial ไม่หักทั้งสองอย่าง

**และผู้ใช้ไม่ได้เป็นคนเลือกเส้นทาง** — PostgreSQL เลือกให้ผ่าน
`plan_create_index_workers()` ซึ่งบังคับให้ผู้ร่วม build แต่ละคนได้ไม่น้อยกว่า 32MB
ที่ `maintenance_work_mem = 32MB` จึงเหลือ worker = 0 กลายเป็น serial เอง

→ **ตัวแปรที่ผู้ใช้มองไม่เห็นและไม่ได้ตั้ง เปลี่ยนความจุไป 5% โดยไม่มีอะไรแจ้ง**

**ข้ออ้างหลักของ I05 ยังยืนและแข็งขึ้น** — ความจุขึ้นกับมิติจริง ลดลง 41% เมื่อมิติเพิ่มเท่าตัว

#### ⭐⭐ ข้อค้นพบใหม่ — Q03 · ขนาดของ index เปลี่ยนว่า fault จะเกิดไหม

| filter ตรง % | ได้ | เฉลย | **ใช้ index** | buffers |
|---|---|---|---|---|
| 20% | **5** | 10 | ✅ | 537 |
| 10% | **4** | 10 | ✅ | 531 |
| 5% | **2** | 10 | ✅ | 519 |
| **2%** | 10 | 10 | ❌ | 12,740 |
| 0.1% | 10 | 10 | ❌ | 1,340 |

**อาการเกิดที่ 20–5% เหมือนเดิม แต่ตั้งแต่ 2% ลงไป planner เลิกใช้ index เอง**
แล้วไปทำ Seq Scan บนแถวที่กรองแล้ว จึงได้ครบ 10 — **ไม่ใช่เพราะ fault หายไป**

สาเหตุที่อนุมานได้: index 768 มิติใหญ่เท่าตัว (**390 MB vs 195 MB**)
planner จึงประเมินว่าการสแกนแถวที่กรองแล้วถูกกว่า

> ⚠️ **ตารางทางแก้ของ Q03 ในรอบ 768 มิติใช้เทียบไม่ได้** — วัดที่ filter 1%
> ซึ่งเป็นจุดที่ planner ไม่ใช้ index อยู่แล้ว จึงได้ 40/40 ทั้ง 5 ขั้น
> **ไม่ได้แปลว่าทางแก้ใช้ได้** · ต้อง `enable_seqscan = off` ก่อนถึงจะทดสอบได้
> รายงานไว้เป็นข้อจำกัดของการวัด ไม่ใช่ผล

**ข้อค้นพบ: ขนาดของ index มีผลว่า planner จะใช้มันหรือไม่ ซึ่งเปลี่ยนว่า
ความล้มเหลวเงียบจะเกิดหรือไม่ — โดยที่ผู้ใช้ไม่ได้เปลี่ยนอะไรเลย**
เป็นตัวแปรที่การทดสอบสองชุดแรกมองไม่เห็น

---

### สิ่งที่ยืนยันได้เหมือนกันทุกชุดข้อมูล

- **ไม่มี error ใดๆ ในทุกการทดลอง** — ความล้มเหลวเงียบสนิททั้งหมด
- **ค่าเริ่มต้นของ pgvector ไม่พอ** ทั้ง `probes=1` และ `ef_search=40`
- **`VACUUM` แก้ L02 ได้ แต่ index ไม่เล็กลง** (195 → 195 MB)

  > 🔴 **รายการทางแก้นี้ไม่ครบ — แก้ 2026-08-01** · มีอีกสองทางที่ไม่เคยวัด
  >
  > | ทางแก้ | เวลา | index หลังแก้ | ได้/10 |
  > |---|---|---|---|
  > | `VACUUM` | 12.8 วิ | 195.3 MB | 10 |
  > | **`REINDEX`** | **3.4 วิ** | **19.5 MB** | 10 |
  > | `iterative_scan` + `mult=32` | — *(จ่ายทุก query)* | ไม่แตะ | 10 |
  >
  > **`REINDEX` เร็วกว่า 3.8 เท่าและลดขนาด 10 เท่า** — และ**เอกสารแนะนำไว้ตรงๆ**
  > (*"Speed it up by reindexing first"*) ขณะที่ทะเบียนของโครงงานเคยจัดมันไว้ใน
  > กลุ่ม *"คนมักแก้ผิด"* ว่าแพงกว่า `VACUUM` โดยไม่จำเป็น **ทั้งที่ไม่เคยวัด** (E45)
  > ⚠️ ข้อแลกคือ `REINDEX` ล็อกตาราง จึงต้องใช้ `CONCURRENTLY` ตามที่เอกสารระบุ
  >
  > ส่วน `iterative_scan` แก้ได้โดยไม่ต้อง `VACUUM` เลย (E43) — แพงกว่า 11 เท่า
  > ต่อ query จึงเป็น**หลักฐานเชิงกลไก ไม่ใช่คำแนะนำ** เพราะมันพิสูจน์ว่า
  > ทางแก้เดียวกันใช้ได้ทั้ง L02 และ Q03 ตามที่ทฤษฎี "กลไกเดียวกัน" บังคับ
- **ขอแถวเยอะขึ้นไม่ช่วย** ทั้ง Q06 และ L02
- **ค่าเฉลี่ยซ่อนความจริงราย query** — I04 เปลี่ยนคำตอบ 183/200 ทั้งที่ค่าเฉลี่ยแกว่งแค่ 0.096

---

## 5. ⭐ ชื่อ fault ที่เราตั้งเอง ถูกการวัดหักล้าง 4 ข้อ

**ทุกครั้งจับได้ด้วยกลุ่มควบคุม ไม่ใช่ด้วยผลหลัก** — ถ้าวัดแต่สิ่งที่ตั้งใจวัด จะยืนยันชื่อผิดได้อย่างสวยงามทุกครั้ง

| ข้อ | ชื่อเดิม | สิ่งที่กลุ่มควบคุมพบ |
|---|---|---|
| **Q02** | "เขียน `WHERE` แทน `ORDER BY`" | `WHERE` + `ORDER BY` + `LIMIT` ใช้ index ได้ → **`WHERE` ไม่ใช่ตัวปัญหา** การขาด `ORDER BY`+`LIMIT` ต่างหาก |
| **I04** | "k-means ของ IVFFlat สุ่ม" | **HNSW ไม่มี k-means เลยก็แกว่ง** 132/200 · ปิด parallel ก็ยังแกว่ง → ชื่อเดิมแคบเกินไป · ⚠️ **บนข้อมูลจริง HNSW แกว่งแค่ 6/200** ขณะที่ IVFFlat แกว่ง 91.5% → ข้อโต้แย้งนี้**ยังถูกสำหรับ corpus สังเคราะห์ แต่ยกไปใช้ทั่วไปไม่ได้** · กลไกทราบแล้วจากซอร์ส (E41) |
| **I02** | "สร้าง index ตอนข้อมูลน้อย" | 50 แถวได้ **recall 1.0000** → ตัวแปรคือ**ความเป็นตัวแทน** ไม่ใช่จำนวนแถว |
| **L02** | "dead tuple สะสม → index บวม recall ตก" | `UPDATE` ทำ index บวม 195→**202 MB** แต่ **ผลครบทุกแถว recall ไม่ตก** → บวมกับผลหายเป็นคนละเรื่อง |

**กฎที่ได้จากเรื่องนี้:** fault ที่บอกว่า "เพราะกลไก X" ต้องหาวิธีสร้าง X **โดยไม่ให้เกิดอาการ**
เป็นกลุ่มควบคุมเสมอ · ถ้าทำได้ แปลว่า X ไม่ใช่ต้นเหตุ

---

## 6. หน้าผา — ความล้มเหลวเงียบไม่ค่อยมาแบบค่อยเป็นค่อยไป

**สองข้อที่วัดได้ว่าเป็น step function ไม่ใช่ทางลาด** — ตัววัดแบบ threshold จับไม่ทัน

| | เส้นแบ่ง (สังเคราะห์) | หลังเส้น | **เส้นแบ่ง (จริง)** | **หลังเส้น** |
|---|---|---|---|---|
| **Q06** | k เกิน `ef_search` | ขอ 41 ได้ **40** | k เกิน 41 | ขอ 50 ได้ **41** |
| **Q03** | filter ~20% → 2% | ขอ 10 ได้ **0** | filter 20% → 0.5% | ขอ 10 ได้ **0** |

**เพดานตายตัวของ Q06 ยืนยันทั้งสองชุด** — บนข้อมูลจริง ขอ 50 · 100 · 1000 ได้ **41 เท่ากันหมด**

> 🔴 **L02 ถูกถอนออกจากตารางนี้ 2026-07-30** — บน corpus สังเคราะห์ผลตกจาก 10 เป็น 0
> ในขั้นเดียวที่ dead 50% แต่**บนข้อมูลจริงได้ครบ 10 จนถึง dead 80%** และที่ 90%
> ยังได้ 5 · **ไม่เคยถึง 0 เลย** → ลักษณะขั้นบันไดของ L02 เป็นของ corpus สังเคราะห์
> ส่วน**กลไก**ของมันยังยืนยันได้ (ดูข้างล่าง)

**Q03 กับ L02 เป็นกลไกเดียวกันเป๊ะ — และข้อนี้ยืนยันได้ทั้งสองชุดข้อมูล**
index คืน candidate ตาม `ef_search` แล้วมีอะไรมาคัดออกทีหลัง

| | ตัวกรองคืออะไร | เห็นในโค้ดไหม |
|---|---|---|
| Q03 | `WHERE` ที่เขียนเอง | ✅ เห็น |
| **L02** | **MVCC** ตัดแถวที่ตายแล้ว | ❌ **ไม่มีอะไรในคำสั่งบอกเลย** |

→ L02 หายากกว่ามาก เพราะ**คำสั่งที่พังหน้าตาเหมือนคำสั่งที่ถูกทุกประการ**

---

## 7. fault เกี่ยวโยงกันเอง ไม่ได้แยกเป็นข้อๆ

- **Q06 อธิบาย recall@100 ของ Q01** — 0.3903 คือ 40/100 ซึ่งเป็นเพดาน ไม่ใช่คุณภาพ
- **Q02 รูปแบบ `+ 0` คือทางแก้ของ Q03** — ทางแก้ของ fault หนึ่ง คือวิธีฉีดของอีก fault หนึ่ง
- **I04 บังคับวิธีออกแบบของ I02** — เพราะ rebuild ไม่ reproducible จึงเทียบ A/B ครั้งเดียวไม่ได้ ต้อง 5 รอบ
- **I03 กินเวลาเท่ากับ build ของ I05** — ระยะที่เขียนไม่ได้ = ระยะที่ `CREATE INDEX` ทำงาน
- **`probes = 5` แก้ทั้ง Q04 และ I04** — ค่าที่พอเพียงไม่ได้แค่ยก recall แต่ทำให้ผลนิ่งด้วย

---

## 8. ภัยคุกคามต่อความสมเหตุสมผล

**ต้องอ่านหัวข้อนี้ก่อนอ้างตัวเลขใดๆ ข้างบน**

### 8.1 ความสมเหตุสมผลเชิงมโนทัศน์ — วัดสิ่งที่อ้างว่าวัดจริงไหม

**จุดแข็ง:** โปรเจคนี้ใช้ **fault injection** จึงรู้เฉลย 100% ไม่ใช่ประมาณเอา
และ recall วัดเทียบ **exact search** ที่บังคับด้วยวิธีที่เอกสาร pgvector แนะนำเอง
(`SET LOCAL enable_indexscan = off`) ไม่ใช่เทียบกับ index อีกตัว

**จุดอ่อนที่พบเอง:** ชื่อ fault 4 จาก 12 ข้อ **วัดแล้วพบว่าอธิบายผิด** (หัวข้อ 5)
แปลว่าสิ่งที่คิดว่ากำลังวัด กับสิ่งที่วัดจริง เคยไม่ตรงกันมาแล้ว 4 ครั้ง
ทุกครั้งจับได้ด้วยกลุ่มควบคุม — ไม่ใช่ด้วยผลหลัก

**จุดอ่อนที่ยังอยู่:** ตัวตรวจของ Q02 อ่าน**ข้อความ** query จึงจับได้แค่
"ขาด `ORDER BY`/`LIMIT`" กับ "เรียง DESC" · รูปแบบที่ห่อ operator ด้วยนิพจน์
ตรวจจากข้อความไม่ได้ ต้องดู plan — **ระบุไว้ตรงๆ ไม่อ้างว่าครอบคลุมทุกแบบ**

### 8.2 ความสมเหตุสมผลภายใน — มีคำอธิบายอื่นไหม

ทุกข้อที่อ้างสาเหตุ มีกลุ่มควบคุมตัดคำอธิบายอื่นออก

| ข้อ | คำอธิบายอื่นที่ถูกตัดออก | ด้วยอะไร |
|---|---|---|
| **I04** | ฝั่ง query ไม่นิ่ง | วัดซ้ำบน index ตัวเดิม — ตรงกันทุก query |
| **I04** | k-means ของ IVFFlat | HNSW ไม่มี k-means **แต่ก็แกว่ง** |
| **I04** | parallel build | ปิดแล้ว **ยังแกว่งทั้งคู่** |
| **L02** | dead tuple ทุกชนิด | `UPDATE` สร้าง dead tuple **แต่ผลไม่หาย** |
| **I02** | จำนวนแถวตอน build | 50 แถวได้ **recall 1.0000** → ตัวแปรคือความเป็นตัวแทน |
| **V07** | ข้อมูลหายจากตาราง | มี index แต่ปิด index scan → **แถวกลับมาครบ** |
| **Q03 · L02** | เพดาน `ef_search` (Q06) | บังคับ `LIMIT` ต่ำกว่า `ef_search` ชัดเจน |

**ตัวรบกวนที่ควบคุมไม่ได้และต้องยอมรับ:** I04 พิสูจน์แล้วว่าการสร้าง index
ไม่ reproducible (recall แกว่ง 0.0515–0.0885 ข้ามรอบ) จึงเป็นสัญญาณรบกวนพื้นหลัง
ของทุกข้อที่ต้องสร้าง index ใหม่ · **แก้ด้วยการออกแบบ** — สร้าง 5 รอบต่อการตั้งค่า
และเทียบเป็นคู่ในรอบเดียวกัน แทนการเทียบข้ามรอบ

### 8.3 ความสมเหตุสมผลภายนอก — ใช้ได้กว้างแค่ไหน

**ข้อจำกัดที่ต้องพูดก่อน:**

1. **เครื่องเดียว โปรไฟล์เดียว** — `fragile` (`work_mem=64kB` · `shared_buffers=128MB`)
   latency ต่างกันได้ 10 เท่าตามฮาร์ดแวร์ → **ใช้ `buffers` เป็นตัวชี้วัดหลัก** เพราะนิ่ง
2. **corpus สังเคราะห์ 50 กลุ่มสะอาด** · ผลของ **I02 ผูกกับคุณสมบัตินี้โดยตรง**
   (corpus เรียง id สลับกลุ่ม 50 แถวแรกจึงครบ 50 กลุ่มพอดี) — ข้อมูลจริงยังไม่ได้ทดสอบ
3. **pgvector 0.8.5 · PostgreSQL 17.10 เวอร์ชันเดียว** — ล็อก image tag ไว้เพราะ
   tag `pg17` ขยับตามเวอร์ชันใหม่ได้โดยไม่มีอะไรเตือน
4. **ขนาดข้อมูล 2 ระดับ** (100k · 500k) ตามเกณฑ์ผ่านเฟส 2 — ไม่ได้ทดสอบระดับล้าน

**หลักฐานที่ช่วยยืนยันภายนอกได้บางส่วน:** เฟส 2.5 วัด Q01 ซ้ำบน **Qdrant**
ด้วยข้อมูลและเฉลยชุดเดียวกัน → recall loss มีจริงทั้งสอง engine
แปลว่าเป็นธรรมชาติของ ANN ไม่ใช่ของ pgvector · **แต่ขนาดต่างกันมหาศาล**
และการจับคู่กับเอกสารของ 5 engine ยืนยันว่า Q06 เป็นของ pgvector เฉพาะตัว

### 8.4 ความเชื่อถือได้ — ทำซ้ำได้แค่ไหน และเครื่องมือวัดเชื่อได้แค่ไหน

**ฝั่งการทดลอง**

| | |
|---|---|
| fault ที่มีตัวฉีด + ตัวตรวจครบวงจร | **15** |
| ตัวตรวจที่พิสูจน์แล้วว่าพลิกสถานะได้ | **15 / 15** |
| ทุก fault รันติดกัน | **3 รอบ** ผ่านทุกรอบ |
| หลักฐานดิบที่เก็บไว้ | **68 ไฟล์** ใน `results/` |
| รันตัวฉีดจริงซ้ำ | **ครบ 9 ข้อ · 33.8 นาที · assertion 46/46 ผ่านหมด** (วัดจริง 2026-07-28) |
| ตรวจความพร้อมทั้ง repo | **36 หัวข้อ ผ่านหมด** |

**ข้อยกเว้นที่ต้องระบุ:** ตัวเลข recall บางตัว **ไม่มีวันซ้ำเป๊ะ** เพราะ build
ไม่ reproducible (I04) → การตรวจซ้ำยืนยันว่า **assertion ยังผ่าน** ไม่ใช่ว่าตัวเลขเท่าเดิม

#### ฝั่งเครื่องมือวัด — ความล้มเหลวเงียบในเครื่องมือที่สร้างมาจับความล้มเหลวเงียบ

ระหว่างสร้าง harness เกิดบั๊กที่บันทึกไว้ **32 รายการ** (H01–H32)
และ **9 ในนั้นเป็นชนิด "ให้คำตอบผิดโดยไม่มี error"** ไม่ใช่แค่ทำงานไม่สะดวก

**สองครั้งที่อันตรายที่สุด คือครั้งที่ตัวตรวจเองรายงานว่าไม่พบปัญหา ทั้งที่ fault เกิดอยู่**

| | เกิดอะไร | จับได้เพราะ |
|---|---|---|
| **I05** | ตัวตรวจถูกเขียนแล้ว **ไม่เคยถูกรันเลยสักครั้ง** · ผลรัน 102 บรรทัดไม่มีคำว่า `DETECTED` | audit ก่อนส่งงาน (E30) |
| **L02** | ตัวตรวจใช้ **แถวในตารางเองเป็น query vector** ซึ่งเป็นแถวที่ยังไม่ตาย จึงได้ผลครบ | กฎบังคับพิสูจน์ 3 สถานะ (E35) |

ทั้งสองข้อ **assertion ผ่านครบทุกรอบตั้งแต่แรก** — สิ่งที่จับได้คือกฎที่แยก
*"fault เกิดจริงไหม"* ออกจาก *"ตัวตรวจใช้ได้ไหม"* และบังคับให้พิสูจน์อย่างหลังแยกต่างหาก

**3 ตระกูลที่เกิดซ้ำจนต้องกลายเป็นกฎถาวร**

| ตระกูล | เกิดกี่ครั้ง | กฎที่ออกมา |
|---|---|---|
| ฝัง SQL ที่มี quote ใน `bash -c` แล้วคำสั่งไม่ทำงานแบบเงียบๆ | **5** | SQL ที่มี `'` ต้องอยู่ในไฟล์ `.sql` |
| match ข้อความแทนอ่านโครงสร้าง | **3** | อ่าน catalog / plan node ที่ระบุชื่อ ห้ามค้นคำ |
| ตัวตรวจวัดคนละอย่างกับการทดลอง | **2** | ตัวตรวจต้องรับ input จากแหล่งเดียวกับการทดลอง |

> **⚠️ ข้อจำกัดของตัวเลข 27 นี้ — ต้องอ่านก่อนตีความ**
>
> **27 คือขอบล่างที่ไม่รู้ว่าหลวมแค่ไหน ไม่ใช่อัตราการเกิด**
> เพราะบันทึกเฉพาะบั๊กที่**สังเกตเห็น** และไม่มีตัวหารว่า "จากกี่โอกาส"
> จึงอ้างได้แค่ว่า **ความผิดพลาดแบบนี้เกิดขึ้นได้จริงและเกิดซ้ำเป็นรูปแบบ**
> **อ้างไม่ได้ว่าเกิดบ่อยแค่ไหน**
>
> และ **RQ3 ก็ตอบแทนไม่ได้** — โจทย์ 10 ข้อของ RQ3 มีตัวหารก็จริง แต่เป็นตัวหาร
> ที่ผู้วิจัยเลือกเอง จึงตอบได้แค่ว่า *ล้มเหลวรูปแบบใด* ไม่ใช่ *บ่อยแค่ไหน* (หัวข้อ 9.1)
> **คำถามเรื่องความถี่จึงยังเปิดอยู่ทั้งงาน** และระบุไว้เป็นงานต่อในหัวข้อ 9.2

**ทำไมถึงบันทึกไว้แทนที่จะแก้เงียบๆ:** เพราะบั๊กที่ไม่ได้บันทึกคือบั๊กที่จะเกิดซ้ำ
และเพราะรายงานที่อ้างว่าเครื่องมือไม่เคยผิด เป็นรายงานที่ตรวจสอบตัวเองไม่พอ

---

## 9. RQ3 และงานต่อ

### 9.1 RQ3 — ทำแล้ว · เก็บ 3 ครั้ง จาก 2 โมเดล

10 โจทย์ · แชทแยกต่อข้อ · ไม่ให้เห็น repo นี้ · ไม่ใบ้เรื่อง index
ให้คะแนนด้วยการ**รัน SQL จริง** เทียบ exact search · เกณฑ์คือความครบของแถว
**และต้องยืนยันจาก query plan ว่าใช้ vector index จริง** (ดูหัวข้อ 9.1.2)

| การเก็บ | ติดกับดัก | ข้อที่**ไม่ได้ใช้ vector index เลย** |
|---|---|---|
| **Opus 5** รอบ 1 (high) | **5 / 10** | 0 |
| **Opus 5** รอบ 2 (high · โจทย์เดิมเป๊ะ) | **7 / 10** | **3** |
| **ChatGPT** (เว็บปกติ) | **10 / 10** | **8** |

**ห้ามรายงาน RQ3 เป็นเลขเดี่ยว** — ตัวเลขที่ถูกต้องคือ **Opus 5 = 5–7 จาก 10**

รายละเอียดรายข้อของทั้งสามการเก็บอยู่ใน `results/rq3_ai_traps.txt`

#### 9.1.1 ⭐ การถามครั้งเดียวให้ผลไม่นิ่ง — วัดแล้ว ไม่ได้เดา

โมเดลเดียวกัน โจทย์เดียวกันทุกตัวอักษร แชทใหม่ → **5/10 กับ 7/10**

RQ3 เป็นการวัดเดียวในงานนี้ที่เคยทำครั้งเดียว ทั้งที่ fault ทุกข้อรัน 3 รอบ
และ I04 บังคับ 5 build ต่อการตั้งค่า — **การทำซ้ำจึงเปิดเผยสิ่งเดียวกับที่ I04 เปิดเผย**
คือค่าที่ดูเหมือนเสถียรพอจะรายงานได้ กลับแกว่งเมื่อวัดซ้ำ

ตัวอย่างที่ชัดที่สุด: P09 รอบแรกใช้ `vector_norm()` ซึ่งถูกต้อง
รอบสองใช้ `l2_norm()` ซึ่ง**ไม่มีสำหรับชนิด `vector`** แล้วรันไม่ผ่าน

#### 9.1.2 ⭐⭐ "เขียนจนปิด index" เป็นรูปแบบร่วม ไม่ใช่นิสัยของค่ายใดค่ายหนึ่ง

ข้อสรุปเดิมหลังเก็บสองครั้งคือ *ChatGPT ไม่ใช้ index · Opus 5 ใช้*
ซึ่งอ่านได้ง่ายๆ ว่าเป็นลักษณะเฉพาะเจ้า **รอบสามหักล้างข้อนั้น**

Opus 5 รอบ 2 ไม่ใช้ vector index ใน 3 ข้อ (P02 · P08 · P10) ด้วยรูปแบบเดียวกับ ChatGPT
คือดึง query vector เข้ามาทาง `JOIN` หรือ CTE แทนที่จะเป็น subquery ตรงใน `ORDER BY`

```sql
-- รูปแบบที่ปิด index (Seq Scan 20,043 block)
... CROSS JOIN (SELECT embedding FROM search_queries WHERE id = 1) AS q
    ORDER BY d.embedding <=> q.embedding LIMIT 10;

-- รูปแบบที่ใช้ index ได้ (981 block)
... ORDER BY d.embedding <=> (SELECT embedding FROM search_queries WHERE id = 1) LIMIT 10;
```

ผลลัพธ์ของสองรูปแบบนี้ **ถูกต้องเท่ากันทุกแถว** ต่างกันแค่ I/O 20 เท่า
→ เป็นกลไกเดียวกับ **Q02** และมองไม่เห็นถ้าไม่ดู plan

**นี่คือเหตุผลที่ n=2 มีค่ากว่า n=1 มาก** — มันเปลี่ยนคำอธิบายจาก
"นิสัยของโมเดลหนึ่ง" เป็น "**รูปแบบการเขียนที่โมเดลภาษาเลือกใช้ร่วมกัน**"

#### 9.1.3 ⭐ โมเดลตกหลุมที่งานนี้เคยตกเอง

P09 รอบสองเขียน `l2_norm(embedding)` เพื่อหา zero vector แล้วได้

```
ERROR:  function l2_norm(vector) is not unique
HINT:   Could not choose a best candidate function.
        You might need to add explicit type casts.
```

**นี่คือ E10 ในทะเบียนความผิดพลาดของงานนี้ตรงตัว** — `l2_norm` ใน pgvector 0.8.5
มีเฉพาะ `halfvec` และ `sparsevec` ไม่มีสำหรับ `vector`
และ `HINT` ชวนให้เติม cast ซึ่งถ้าทำตามจะไปลงที่ `::halfvec`
แล้ว**ลดความละเอียดของข้อมูลโดยไม่มีอะไรเตือน**

งานนี้ค้นพบ E10 จากการที่ผู้ช่วย AI เดาผิดระหว่างทาง — และตอนนี้โมเดลเดียวกัน
เดาผิดแบบเดิมซ้ำในการวัดที่ควบคุมแล้ว → **เป็นหลักฐานอิสระว่า E10 ไม่ใช่เหตุบังเอิญครั้งเดียว**
และเป็นตัวอย่างว่าข้อความ error ที่ *มี* อยู่ ก็ยังชี้ทางผิดได้

#### 9.1.4 ⭐ ผลที่สำคัญที่สุด — โมเดลทำตามเอกสารครบทุกข้อ แล้วยังได้ศูนย์แถว

โจทย์ P08 บอกเงื่อนไขตรงๆ ว่า *"ต้องได้ครบ 10 แถวเสมอ"* · โมเดลตั้งค่าครบ

```
SET hnsw.iterative_scan      = strict_order;
SET hnsw.ef_search           = 100;
SET hnsw.max_scan_tuples     = 100000;   -- ปุ่มถูกตัว แต่ไม่ใช่ตัวที่ผูกอยู่
SET hnsw.scan_mem_multiplier = 4;      ->  ได้ 0 แถว

SET work_mem = '16MB';                 ->  ได้ 10 แถว
```

> 🔴 **แก้การตีความ 2026-08-01 (E42)** — เดิมอ่านผลนี้ว่า *"เป็นข้อจำกัดข้อที่ 5
> ที่ไม่มีอยู่ในเอกสาร"* · ตอนนี้ทราบแล้วว่า `scan_mem_multiplier` คูณกับ `work_mem`
> และค่าที่พอคือราว 32 · **โมเดลตั้งไว้ 4 ซึ่งไกลไม่พอ** ไม่ใช่ว่าเอกสารไม่ได้เขียน

**การตีความที่ถูกต้อง และแข็งกว่าเดิม** — โมเดลอ่านเอกสาร ตั้งค่าถูกตัว
แล้ว**ตั้งไม่แรงพอ** จึงได้ศูนย์แถวโดยไม่มี error

**และเป็นความล้มเหลวรูปแบบเดียวกับที่โครงงานนี้ทำเอง** — การวัดของ Q03 เดิม
ก็หยุดที่ `multiplier = 8` แล้วสรุปว่าปุ่มไม่ทำงาน (E42)

```
โมเดล      ตั้ง scan_mem_multiplier = 4    ->  0 แถว  ->  สรุปว่าแก้ไม่ได้
โครงงานนี้  ตั้ง scan_mem_multiplier = 8    ->  0 แถว  ->  สรุปว่าเอกสารไม่ครบ
ความจริง   ต้องราว 32                      ->  ครบ
```

**ทั้งคู่ไม่มีอะไรบอกว่ายังไม่ถึง** — ไม่มี error ไม่มีคำเตือน ไม่มีเส้นบอกระยะ
มีแต่ผลลัพธ์ที่น้อยกว่าที่ควร ซึ่งตีความได้ทั้ง *"ปุ่มนี้ไม่ทำงาน"* และ *"ยังหมุนไม่พอ"*
→ **RQ3 จึงยืนยันตัว fault ด้วยหลักฐานอิสระ และยืนยันด้วยว่ามันหลอกได้ทั้งคนและโมเดล**

#### ⭐ โมเดลอธิบายกลไกได้ถูก แล้วยังแก้ไม่ได้

ภาพหน้าจอต้นฉบับของ P08 มีคอมเมนต์ที่โมเดลเขียนเอง:

> *"ป้องกันปัญหา over-filtering: HNSW คืน ef_search แถวก่อน แล้วค่อยกรอง
> ทำให้หมวดหายาก เช่น archive (1% = ~1,000 แถว) ได้ไม่ครบ 10"*

**เป็นคำอธิบายกลไก Q03 ที่ถูกต้องทุกตัวอักษร** — แล้วมันตั้งค่าถูกตัวทุกตัว
และยังได้ 0 แถว → **รู้กลไก อ่านเอกสารถูก ตั้งค่าถูกตัว ก็ยังพลาดได้
ถ้าไม่วัดว่าค่าที่ตั้งไปนั้นพอหรือยัง**

และ P02 มีคอมเมนต์ว่า *"archive มีแค่ ~1,000 แถว → exact search เร็วและได้ผลครบ 100%"*
วัดจริงแล้ว planner **ไม่เลือก exact search** — ยังใช้ HNSW แล้วกรอง `category` ทีหลัง ได้ 0 แถว
→ ตรงกับรายการ *"สิ่งที่ผู้ช่วย AI เชื่อถือไม่ได้"* ข้อแรกใน `CLAUDE.md` ซึ่งโปรเจคนี้เคยพลาดเอง (E07)

#### หมายเหตุที่ทำให้ตัวเลขนี้อ่อนลง — ต้องพูดก่อน

**P06 ถูกนับว่าติดกับดัก แต่สาเหตุไม่ใช่ I01/Q04** — มันล้มด้วย
`could not resize shared memory segment` จาก `SET maintenance_work_mem = '2GB'`
ซึ่งเกิน `/dev/shm` ของคอนเทนเนอร์ = **U05** ในทะเบียนบั๊กระหว่างทาง

ตรวจแยกแล้ว: ลดเป็น 256MB คำตอบเดิมรันผ่านและสร้าง index เป็น `vector_cosine_ops`
ครบทุกตัว → **โมเดลไม่ติด I01 และไม่ติด Q04**
ดังนั้นตัวเลข `I01 1/1` กับ `Q04 1/1` ในไฟล์ผลดิบ **นับสูงเกินจริง**

และ P06 เป็นความล้มเหลว**แบบดัง** ไม่ใช่แบบเงียบ — จึงแยกออกจากตัวเลข 4/10 ข้างบน

#### สิ่งที่โมเดลทำได้ดี

- **P04 ใช้ partial index** เฉพาะหมวด `legal` เลี่ยง over-filtering ได้ครบ 20/20 recall 1.00
  — เป็นทางแก้ที่โปรเจคนี้ไม่ได้ทดสอบไว้ด้วยซ้ำ
- **P09 จับทั้ง `NULL` และ zero vector** และรู้ว่า cosine ของ zero vector เป็น NaN
- โมเดลพูดถึง over-filtering และ `iterative_scan` เองหลายข้อ **โดยโจทย์ไม่ได้ใบ้**

→ **มันรู้ว่าปัญหามีอยู่ แต่แก้ไม่ครบในข้อที่ต้องแก้จริง**

#### ความไวต่อลำดับที่วางบล็อกคำตอบ — ตรวจแล้ว

โมเดลตอบเป็นหลายบล็อก (query บล็อกหนึ่ง · คำแนะนำเรื่อง index อีกบล็อก)
**เรียงตามลำดับที่โมเดลแสดงจริง** ผู้เก็บยืนยันว่าไม่ได้สลับ ทำแค่ต่อบล็อกที่ติดกัน
→ ลำดับเป็นส่วนหนึ่งของคำตอบ และตรงกับสิ่งที่ developer ที่ copy จากบนลงล่างจะเจอ

หัวข้อนี้จึงเป็น**การตรวจความทนทาน** ว่าถ้าตีความอีกแบบ ตัวเลขจะขยับแค่ไหน

| ข้อ | ลำดับที่โมเดลเลือกเอง | ถ้านับคำแนะนำท้ายด้วย |
|---|---|---|
| P04 | index มาก่อน → **20/20 ผ่าน** | (ไม่มีอะไรท้ายให้นับ) |
| P07 | index มาทีหลัง → **5/50 ตก** | นับด้วย → 40/50 **ยังตก** |
| P08 | ทางเลือก B ท้าย → **0/10 ตก** | นับด้วย → 10/10 ผ่าน |

**โมเดลเองเป็นคนเลือกว่าจะวาง index ไว้หน้าหรือหลัง** — P04 วางไว้หน้าในฐานะสิ่งที่ต้องมีก่อน
ส่วน P07 วางไว้หลังในฐานะคำแนะนำเพิ่ม ทั้งที่เป็นปัญหาชนิดเดียวกัน · **ความไม่สม่ำเสมอนี้เป็นผล**

> **รอบสองยืนยันว่าลำดับเป็นของโมเดลจริง** — P07 รอบแรกวาง index **หลัง** query
> รอบสองวาง **ก่อน** โดยไม่มีใครไปยุ่งกับคำตอบเลย (5/50 → 40/50)
> → ที่เคยสงสัยว่าลำดับอาจเป็นสิ่งประดิษฐ์ของวิธีเก็บ **ตัดออกได้แล้ว**
> มันคือความแปรปรวนของโมเดล และเป็นอีกหน้าหนึ่งของหัวข้อ 9.1.1

**ตัวเลขรวม 5/10 ของรอบแรกไม่เปลี่ยน** เมื่อตัดสินตามเนื้อหา ไม่ใช่ตามลำดับ —
P04 โจทย์ถามเฉพาะ `legal` partial index จึงตอบครบ · P07 ตกอยู่ดีแต่เป็น **Q06 ไม่ใช่ Q03**
(40/50 — ตรงกับ `ef_search` พอดีในการวัดครั้งนั้น) · **P08 ตกอยู่ดี** เพราะทางเลือก B เป็น partial index
เฉพาะบางหมวด แต่โจทย์บอกว่า *"ไม่ว่าจะกรองด้วยหมวดไหน"*

→ **ข้อสรุปหลักไม่ขึ้นกับลำดับ** เพราะทางเลือกสำรองของโมเดลเองก็ไม่ตอบโจทย์ที่ถาม

#### ⭐ ทำไมงานนี้ขยาย RQ3 เองไม่ได้ — ทดสอบแล้ว ไม่ได้เดา

คำถามที่ตามมาทันทีคือ *"ทำไมไม่ให้ผู้ช่วย AI เรียกโมเดลอื่นมาตอบเองเลย"*
ทดสอบแล้วเมื่อ 2026-07-28: เรียก subagent แล้วถามตรงๆ ว่าเห็นอะไรบ้าง

subagent เห็น **`CLAUDE.md` ทั้งไฟล์** ซึ่งเป็นไฟล์บริบทของโปรเจค และตอบเองว่า

> *"ความรู้ว่า `ef_search=40` สร้างเพดานตายตัว · `probes=1` ทำ recall หายมหาศาล ·
> `iterative_scan=off` บล็อกการ filter — มาจาก context ที่ได้รับ ไม่ใช่จาก training"*

→ **โมเดลถูกยื่นเฉลยพร้อมทางแก้ครบ 12 ข้อ ก่อนตอบโจทย์**
ตัวเลขที่ได้จะสวยและไร้ความหมาย และจะลากตัวเลขข้างบนให้ถูกสงสัยไปด้วย

**ข้อสังเกตเชิงวิธีวิจัย:** ไฟล์ที่ทำให้งานเดินต่อได้เมื่อ context หมด
คือไฟล์เดียวกับที่ทำให้วัด RQ3 จากภายในไม่ได้ —
**บริบทที่ดีสำหรับการทำงาน คือการปนเปื้อนสำหรับการวัด**

**การเก็บทั้ง 3 ครั้งจึงทำจากภายนอกทั้งหมด** โดยเจ้าของโปรเจคเป็นผู้เก็บ
ชุดโจทย์อยู่ที่ `rq3/assignments.md` (รายละเอียดใน E37)

#### ข้อจำกัด

| | |
|---|---|
| **ขนาดตัวอย่าง** | 2 โมเดล · 3 การเก็บ · 10 โจทย์ต่อการเก็บ |
| **วิธีเลือกโจทย์** | ผู้วิจัยออกแบบเองให้เล็งกับดักที่วัดไว้ — **ไม่ใช่ตัวอย่างสุ่มจากงานจริง** |
| **การทำซ้ำ** | โมเดล ก เก็บ 2 รอบ · **โมเดล ข เก็บรอบเดียว** จึงยังไม่รู้ว่านิ่งไหม |
| **ความจำเพาะ** | ผูกกับ pgvector 0.8.5 และสภาพแวดล้อมนี้ |
| **การระบุรุ่น** | โมเดล ข ตอบผ่านหน้าเว็บปกติ **ล็อกเวอร์ชันไม่ได้** ทำซ้ำเป๊ะไม่ได้ |

**อ้างได้:** รูปแบบความล้มเหลวเหล่านี้เกิดขึ้นจริงกับโมเดลระดับบนสุด
และ "เขียนจนปิด index" เกิดกับทั้งสองโมเดล
**อ้างไม่ได้:** อัตราของ AI โดยทั่วไป · ว่าโมเดลไหนดีกว่า · ตัวเลขเดี่ยวใดๆ

รายละเอียดเต็ม: `results/rq3_ai_traps.txt` · เครื่องมือและวิธีทำซ้ำ: `rq3/README.md`

### 9.2 ⭐ คำถามเรื่อง "บ่อยแค่ไหน" — ยังเปิดอยู่ทั้งงาน

**ไม่มีตัวเลขไหนในรายงานนี้ตอบคำถามนี้ได้ รวมทั้ง RQ3**

| แหล่ง | มีตัวหารไหม | ทำไมยังไม่ใช่อัตรา |
|---|---|---|
| ทะเบียนบั๊ก H01–H32 | ❌ | จดเฉพาะที่สังเกตเห็น ไม่รู้ว่า "จากกี่โอกาส" |
| **RQ3 โจทย์ 10 ข้อ** | ✅ | **แต่เป็นตัวหารที่เราเลือกเอง** — โจทย์ออกแบบมาเล็งกับดักที่รู้อยู่แล้ว |

**เพิ่มจำนวนโจทย์ไม่ช่วย** เพราะปัญหาอยู่ที่**วิธีเลือกโจทย์** ไม่ใช่ขนาดตัวอย่าง
งานที่จะตอบเรื่องความถี่ได้ต้องเปลี่ยนวิธีเก็บทั้งหมด เช่นสุ่มจาก query จริงใน production
หรือสุ่มจาก repo สาธารณะที่ใช้ pgvector ซึ่งอยู่นอกขอบเขตของงานนี้

### 9.3 กลไกที่ทำให้ HNSW สร้างใหม่แล้วได้คำตอบไม่เหมือนเดิม

**ตัดออกได้แล้ว 3 สาเหตุ**

| | สาเหตุที่ตัดออก | หลักฐาน |
|---|---|---|
| 1 | ฝั่ง query | วัดซ้ำบน index ตัวเดิม → ตรงกันทุก query |
| 2 | parallel build | ปิดแล้วยังแกว่ง 127/200 |
| 3 | **PRNG ของ session** | `setseed` ค่าเดิมทุก build → **ยังแกว่ง 101/200** |

ข้อ 3 วัดเมื่อ 2026-07-31 · `results/i04_seed_probe.txt` · ปิด parallel build ไปพร้อมกัน
จึงไม่ใช่กรณีที่ worker มี PRNG แยกแล้ว `setseed` ไปไม่ถึง

> ⚠️ **ห้ามอ่านว่า `setseed` ช่วยลดจาก 117 เหลือ 101** — ค่าฐานเองแกว่งข้ามรอบ
> (เคยวัดได้ 132 · รอบนี้ 117) ส่วนต่าง 16 จึงอยู่ในระดับเดียวกับ noise
> และมีเงื่อนไขละรอบเดียว · ที่พูดได้คือ **`setseed` ไม่ได้ทำให้ผลนิ่ง**

### 🔴 อ่านซอร์สแล้ว — กลไก**ทราบแล้ว** และการทดลอง seed เล็งผิดตัว

```c
// pgvector src/hnsw.h
#define RandomDouble() pg_prng_double(&pg_global_prng_state)

// pgvector src/hnswutils.c — HnswInitElement
int level = (int) (-log(RandomDouble()) * ml);

// PostgreSQL — random() และ setseed() ใช้ static pg_prng_state คนละตัว
```

**HNSW สุ่มระดับชั้นให้ทุกจุดตอน build** ตามอัลกอริทึมมาตรฐาน (Malkov & Yashunin [6])
กราฟจึงต่างกันทุก build **โดยการออกแบบ** ไม่ใช่ข้อบกพร่อง

และ **`setseed()` คุม PRNG คนละตัว** จึงไม่มีทางมีผลตั้งแต่แรก —
ผลลบของการทดลองข้างบนจึงอธิบายได้ครบ

| ข้อสรุปที่ถูกต้อง | |
|---|---|
| กลไก | **ทราบแล้ว** — เป็นการสุ่มระดับชั้นตามอัลกอริทึม |
| สิ่งที่ยังทำไม่ได้ | **ทำให้ build ซ้ำได้จากฝั่ง SQL** — `pg_global_prng_state` ไม่มีอินเทอร์เฟซให้ตั้งค่า |

**ข้อนี้มีค่าในทางปฏิบัติมากกว่าเดิม** — ทีมที่ต้องการผลค้นหาที่ทำซ้ำได้เป๊ะ
**ทำไม่ได้ด้วย HNSW ของ pgvector** ต้องใช้ exact search หรือเก็บ index เดิมไว้ไม่ rebuild

### 9.4 ทดสอบบนข้อมูลจริง

ผลของ I02 ผูกกับโครงสร้าง corpus สังเคราะห์โดยตรง
embedding จริงจากโมเดลจริงอาจให้เส้นโค้งคนละแบบ

### 9.5 fault ที่ตัดออกและยังไม่ได้ตัดสิน

**ตัดแล้ว:** Q05 · L01 · V08 (D15) · F13 ปิดถาวรเป็นบั๊กระหว่างทาง (D18)
**ยังไม่รับเข้าชุด:** U01–U05 ในทะเบียนบั๊กระหว่างทาง — การรับเข้าต้องตัดของเดิมออก

---

## 10. ทำซ้ำได้อย่างไร

```bash
docker compose up -d
python scripts/audit.py                 # 36 หัวข้อ ~30 วินาที
python scripts/audit.py --reproduce     # รันตัวฉีดจริงซ้ำ 9 ข้อ ~40 นาที
```

`exit 0` = ผ่านหมด · `exit 1` = มีข้อไม่ผ่าน **หรือตรวจไม่ได้** แล้วมันจะบอกว่าข้อไหน

**ตัวชี้ขาดคือ exit code ไม่ใช่ข้อความใน output** — ทุกสคริปต์ใช้ `ON_ERROR_STOP`
และ `RAISE EXCEPTION` เมื่อ assertion ตก

| อ่านอะไรต่อ | ที่ไหน |
|---|---|
| นิยาม fault ครบทุกข้อ | `FAULTS.md` — แหล่งอ้างอิงเดียว |
| ตัดสินใจอะไรไป และปฏิเสธอะไรเพราะอะไร | `DECISIONS.md` |
| หลักฐานอ้างอิงเอกสารทางการต่อ fault | `EVIDENCE.md` |
| ผลการทดลองดิบ | `results/` |
