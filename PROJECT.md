# QuietFail

**ชุดทดสอบความล้มเหลวเงียบของระบบ PostgreSQL + pgvector**
*A fault-injection benchmark for silent failures in PostgreSQL vector search*

> เอกสารนี้คือขอบเขตงานที่ล็อกแล้ว
> การเพิ่มอะไรที่ไม่อยู่ในนี้ ต้องตัดของเดิมออกเท่ากัน

---

## 1. ประโยคเดียว

> ใส่ index ให้ vector search แล้วเร็วขึ้นหลายสิบเท่า
> แต่ผลลัพธ์ที่ควรเจอหายไปส่วนหนึ่ง **โดยไม่มี error ใดๆ แจ้ง**
> QuietFail วัดว่าหายไปเท่าไหร่ และเตือนก่อนโค้ดขึ้น production

ทดสอบกับคนไม่ใช่สายเทคได้: เข้าใจใน 15 วินาที ไม่ต้องรู้ว่า HNSW คืออะไร

---

## 2. ปัญหาที่แก้

ระบบข้อมูลยุคนี้เกือบทุกที่ต่อ vector search เข้าไป (RAG, recommendation, ค้นหาความหมาย)
แต่ vector search ต่างจาก SQL ปกติในจุดที่อันตรายที่สุด:

**SQL ปกติ** — ถ้าเขียนผิด จะได้ error หรือได้ผลผิดที่สังเกตเห็นได้
**ANN search** — ถ้าตั้งค่าผิด **ได้ผลลัพธ์ที่ดูสมเหตุสมผลเสมอ** แค่ไม่ใช่ผลที่ถูก

ไม่มี exception ไม่มี log ไม่มี alert dashboard เขียวหมด
ทีมงานรู้ตัวอีกทีเมื่อผู้ใช้บ่นว่า "ค้นแล้วไม่เจอ"

QuietFail สร้างความล้มเหลวประเภทนี้ขึ้นมาแบบจงใจ วัดขนาดของมันด้วยเฉลยที่เถียงไม่ได้
แล้วส่งมอบเครื่องมือที่ตรวจจับได้ก่อนขึ้นระบบจริง

---

## 3. คำถามวิจัย

ทั้งโปรเจคตอบแค่ 3 ข้อนี้ ถ้าคำถามไหนตอบไม่ได้ ให้ตัดทิ้ง อย่าดันต่อ

| # | คำถาม | ตอบด้วยอะไร |
|---|---|---|
| RQ1 | การตั้งค่า ANN index ผิดพลาดแบบที่ **ไม่มี error** ทำให้ผลลัพธ์ผิดไปกี่เปอร์เซ็นต์ | recall@k เทียบกับ exact search |
| RQ2 | ความผิดพลาดเหล่านี้ตรวจจับอัตโนมัติได้กี่ข้อ และจากหลักฐานอะไร | จำนวนข้อที่ checker จับได้ / 12 |
| RQ3 | โค้ดที่ AI สร้างให้ ตกอยู่ในความล้มเหลวเงียบ **รูปแบบใดบ้าง** และรูปแบบนั้น **เกิดซ้ำข้ามโมเดลและข้ามการถามหรือไม่** | จำแนกชนิดความล้มเหลวจากชุดโจทย์ควบคุม 10 ข้อ · รันจริงและอ่าน query plan · **เก็บซ้ำ 2 รอบต่อโมเดล** |

**RQ1 คือแกน** RQ2 คือสิ่งที่ทำให้เป็นซอฟต์แวร์ไม่ใช่บทความ
RQ3 เป็นส่วนเสริม — ถ้าเวลาไม่พอ ตัดได้โดยโปรเจคยังสมบูรณ์

> ### ⚠️ RQ3 ตอบเป็น "รูปแบบ" ไม่ใช่ "อัตรา" — และเป็นการตัดสินใจ ไม่ใช่ข้อจำกัดที่ยอมรับ
>
> ถ้อยคำเดิมคือ *"ติดกับดักบ่อยแค่ไหน"* กับ *"อัตราการพลาด"* ซึ่ง **อ้างเกินกว่าที่วิธีการรองรับ**
> อัตราต้องการให้โจทย์ทั้ง 10 ข้อเป็น **ตัวอย่างสุ่มจากประชากรที่นิยามได้**
> แต่โจทย์ชุดนี้**ออกแบบมาให้เล็งกับดักที่รู้อยู่แล้ว** ตัวหารจึงเป็นจำนวนโจทย์ที่เราเลือกเอง
> ไม่ใช่โอกาสที่เกิดขึ้นในงานจริง · การเพิ่ม n ไม่ช่วย เพราะปัญหาอยู่ที่**วิธีเลือกโจทย์**
>
> **สิ่งที่วิธีนี้รองรับจริง** คือคำถามเชิงจำแนก ซึ่งตอบได้หนักแน่นด้วย n ที่มี:
> รูปแบบความล้มเหลวมีกี่ชนิด · ชนิดไหนเกิดซ้ำข้ามโมเดล · ผลนิ่งเมื่อถามซ้ำหรือไม่
>
> **การเก็บซ้ำเปลี่ยนข้อสรุปมาแล้วจริง** — Opus 5 ได้ 5/10 กับ 7/10 บนโจทย์เดียวกัน
> และรอบสองเปิดเผยว่ากับดัก "เขียนจนปิด index" เกิดกับทั้งสองโมเดล
> ไม่ใช่ลักษณะเฉพาะเจ้าอย่างที่รอบแรกทำให้เข้าใจ (ดู `DECISIONS.md` D20 · E38)

---

## 4. ขอบเขต

### ทำ

- PostgreSQL **17.10** + pgvector (ล็อกเวอร์ชันเดียว บันทึกไว้ในผลทุกชุด)
- index สองแบบ: **HNSW** และ **IVFFlat** — การเทียบกันเองคือส่วนหนึ่งของผลลัพธ์
- fault 12 ข้อ แบ่ง 3 ชั้น: ติดตั้ง / เรียกใช้ / ข้อจำกัด
- fault พื้นฐานเดิม 4 ข้อ (F01, F03, F05, F10) เพื่อแสดงว่ากลไกวัดผลใช้ได้ทั้งกับ DB ธรรมดาและ vector
- ข้อมูลสังเคราะห์ที่คุมการกระจายตัวได้
- checker ที่รันใน CI แล้ว fail build ได้จริง

### ไม่ทำ (เขียนไว้เพื่อกันตัวเอง)

| ไม่ทำ | เหตุผล |
|---|---|
| วัด recall เต็มรูปแบบกับ **ทุก** vector engine | 5 container 5 API = โปรเจคไม่จบ (ดูโครงสร้าง 3 ชั้นในข้อ 5 แทน) |
| Pinecone | fault ชั้นติดตั้ง (I01, I02, I04, I05) หายไปทั้งหมดเพราะเขาสร้าง index ให้ · ทำซ้ำไม่ได้เพราะเขาอัปเดตระบบเมื่อไหร่ก็ได้ · ต้องจ่ายเงิน คนอื่นทำซ้ำฟรีไม่ได้ (**ไม่ใช่**เพราะไม่มี ground truth — ดู D05/E03) |
| เทียบ embedding model หลายตัว | คนละคำถามวิจัย |
| เทรนหรือ fine-tune อะไร | ไม่เกี่ยวกับคำถาม |
| ระบบแบบกระจาย / sharding | เกินกำลังและวัดยาก |
| ข้อมูล production จริง | ground truth ต้องคุมได้ |
| หน้าเว็บ / dashboard | ไม่เพิ่มค่าให้คำถามวิจัย |
| เทียบผู้ให้บริการ LLM | คนละโปรเจค |
| **Q05** สลับ cosine กับ inner product | เอกสารยืนยันได้แค่บางส่วน (บอกว่า `<#>` คืนค่าติดลบ แต่ไม่ได้บอกว่าเป็นความล้มเหลวเงียบ) · และอาการซ้อนทับกับ **I01** ซึ่งยืนยันได้เต็มและตรวจจาก catalog ได้ → ตัดออกเพื่อรับ V07 เข้าแทน (D15) |
| **L01** พารามิเตอร์ไม่ scale ตามขนาดข้อมูล | ต้องมีข้อมูลครบ 4 ขนาด (100k/500k/2M/5M) และสร้าง index ใหม่ทุกขนาด = ต้นทุนเวลาสูงที่สุดในบรรดา fault ทั้งหมด · **ตัดเพื่อคุมขอบเขตให้จบทัน ไม่ใช่เพราะไม่สำคัญ** (D15) |
| **V08** recall กระทบกันข้าม tenant | ต้องสร้างสถาปัตยกรรม multi-tenant ขึ้นมาก่อนถึงจะฉีดได้ = สร้างระบบใหม่เพื่อทดสอบข้อเดียว (D15) |

> **กฎเหล็ก:** ถ้าอยากเพิ่มอะไรเข้ามา ต้องเขียนก่อนว่ามันตอบ RQ ข้อไหน
> ถ้าตอบไม่ได้ = ไม่ทำ

---

## 5. เลือกอะไรทดสอบ และทำไม

### สิ่งที่ค้นแล้วไม่เจอ (ต้องเขียนไว้ตรงๆ)

ค้นข้อมูลตลาดงานไทยแล้ว **ไม่พบข้อมูลที่ยืนยันได้ว่า vector engine ตัวไหนครองตลาดไทย**
ประกาศงานไทยที่ระบุ vector database ชัดเจนมีน้อยมาก และที่เจอก็ระบุ pgvector คู่กับ PostgreSQL

**ห้ามเขียนในรายงานว่า "X คือตัวที่บริษัทไทยใช้มากที่สุด"** เพราะพิสูจน์ไม่ได้
สิ่งที่เขียนได้คือเหตุผลเชิงโครงสร้างและวิธีวัดผล

### สิ่งที่ข้อมูลบอกจริง

ประกาศงานเกือบทั้งหมดขอ **"ประสบการณ์กับ vector database อย่างน้อยหนึ่งตัว"**
แล้วไล่ชื่อมาเป็นพวง — ไม่ได้บังคับตัวใดตัวหนึ่ง
บางประกาศถึงกับเขียนว่า "หรือเข้าใจแนวคิดของการเก็บ embedding"

**แปลว่าสิ่งที่ถูกประเมินคือความเข้าใจ ANN ไม่ใช่ยี่ห้อ**

### โครงสร้าง 3 ชั้น — ครอบคลุมโดยไม่ระเบิดขอบเขต

| ชั้น | เครื่องมือ | ทำอะไร | ต้นทุน |
|---|---|---|---|
| **1 — วัดลึก** | **pgvector** (HNSW + IVFFlat) | fault ครบ 12 ข้อ + recall เต็มรูปแบบ | สูง |
| **2 — พิสูจน์ว่าเป็นเรื่องทั่วไป** | **Qdrant** | รันเฉพาะ **Q01** (recall collapse) | กลาง |
| **3 — จับคู่เอกสาร** | Milvus, Weaviate, OpenSearch, FAISS, Pinecone | ตารางเทียบว่า fault ข้อไหนใช้ได้ พารามิเตอร์ชื่ออะไร | ต่ำ |

### ทำไมชั้น 1 เป็น pgvector

1. **อยู่ในฐานข้อมูลเดียวกับ harness ที่มีอยู่แล้ว** — `EXPLAIN`, `pg_locks`, สอง config profile ใช้ต่อได้ทันที
2. **เส้นทางที่บริษัทเลือกจริงเมื่อมี PostgreSQL อยู่แล้ว** — เพิ่ม extension ถูกกว่าเพิ่มระบบใหม่ทั้งระบบ
3. **ตรวจได้จาก catalog** — opclass, พารามิเตอร์ index อ่านด้วย SQL → เขียน checker ได้จริง
4. **เฉลยอยู่ในตัว** — ปิด index แล้วค้น exact = คำตอบถูก 100% ตามนิยาม

### ทำไมชั้น 2 สำคัญกว่าที่คิด (และไม่ใช่ scope creep)

ถ้าวัดแค่ pgvector ตัวเดียว จะโดนถามทันทีว่า
**"นี่มันบั๊กของ pgvector หรือเปล่า"**

การรัน Q01 ซ้ำบน Qdrant แล้วเจอ recall collapse เหมือนกัน เปลี่ยนข้อสรุปจาก
"pgvector มีปัญหา" เป็น **"ANN ทุกตัวมีปัญหานี้ เพราะมันคือธรรมชาติของการค้นแบบประมาณ"**

นี่คือการอัปเกรดคุณภาพของข้อสรุป ไม่ใช่การเพิ่มงาน — และเป็นคำตอบที่ดีที่สุด
สำหรับคำถามว่า "โปรเจคนี้ครอบคลุมบริษัทที่ใช้เครื่องมืออื่นไหม"

เลือก Qdrant เพราะ: open source, container เดียว, REST API ตรงไปตรงมา,
ตั้ง `ef` / `m` ได้ชัดเจน → เทียบกับ HNSW ของ pgvector ได้ตรงตัว

### ชั้น 3 คืออะไร

ตาราง 1 หน้าใน README: fault 12 ข้อ × 6 เครื่องมือ ระบุว่าข้อไหนใช้ได้
และพารามิเตอร์เทียบเท่าชื่ออะไร (เช่น `ef_search` ↔ `ef` ↔ `search_ef`)

**ใช้เอกสารทางการเป็นแหล่งอ้างอิง ห้ามเดา ห้ามวัด** และเขียนกำกับให้ชัดว่า
ชั้นนี้คือการทบทวนเอกสาร ไม่ใช่ผลการทดลอง

ต้นทุนต่ำมาก แต่ตอบคำถาม "ครอบคลุมไหม" ได้ตรง และแสดงว่าคุณรู้จักภูมิทัศน์ทั้งหมด

### 5.1 งานที่เกี่ยวข้อง และเราต่างจากเขาตรงไหน

ต้องรู้จักงานพวกนี้ก่อนเริ่ม ไม่ใช่เพื่อลอก แต่เพราะ**คำถามแรกที่จะโดนคือ
"อันนี้มันต่างจาก X ยังไง"** ถ้าตอบไม่ได้ = จบ

| งาน | เขาทำอะไร | เราต่างตรงไหน |
|---|---|---|
| **Jepsen** | ฉีด fault ใส่ระบบฐานข้อมูลแบบกระจาย แล้วตรวจว่าละเมิดสิ่งที่ผู้ขายโฆษณาไว้ไหม | **บรรพบุรุษทางความคิดที่ใกล้ที่สุด** เราใช้วิธีเดียวกันแต่กับ ANN และส่งมอบเป็นเครื่องมือที่ทีมอื่นรันเองได้ |
| **ann-benchmarks** | วัด recall เทียบความเร็วของไลบรารี ANN หลายตัว **โดยจูนพารามิเตอร์มาแล้ว** | เขาตอบ "ตัวไหนเร็วที่สุดที่ recall เท่านี้" (ใช้เลือกของ) เราตอบ "ระบบ**ที่คุณ deploy อยู่**กำลังทำผลลัพธ์หายอยู่หรือเปล่า" (ใช้ตรวจของ) |
| **VectorDBBench** | เทียบสมรรถนะข้าม vector database | เทียบยี่ห้อ ≠ ตรวจการตั้งค่าผิด |
| **SQLancer** | หา logic bug ใน DBMS ด้วย metamorphic testing | หาบั๊กใน**ตัว DB** เราหาการ**ใช้ผิด**ของทีมที่ deploy |
| **RAGAS / BEIR** | วัดคุณภาพการค้นคืนเชิงความหมาย | คนละชั้น เขาวัดว่า "คำตอบดีไหม" เราวัดว่า "index ทำผลหายไปกี่ %" ซึ่งเป็นชั้นที่อยู่ใต้เขาอีกที |

**ประโยคที่ต้องท่องให้ได้:**

> ann-benchmarks ช่วยคุณ**เลือก**เครื่องมือ QuietFail ช่วยคุณ**ตรวจ**ว่าเครื่องมือที่เลือกไปแล้ว
> ตั้งค่าถูกหรือเปล่า — งานแรกทำครั้งเดียวตอนเริ่มโปรเจค งานหลังต้องทำทุก PR

**สิ่งที่ควรเอาแบบมาจาก Jepsen:**
- ประกาศขอบเขตและเวอร์ชันที่ทดสอบให้ชัดตั้งแต่ต้น
- มี "แบบจำลองที่ถูกต้อง" เป็นตัวเทียบเสมอ (ของเรา = exact search)
- รายงานผลลบและสิ่งที่ทดสอบไม่ได้ด้วย ไม่ใช่เฉพาะที่เจอ

**สิ่งที่ห้ามเอาแบบ:** ขนาดของงาน Jepsen หนึ่งรายงานใช้เวลาเป็นเดือนโดยผู้เชี่ยวชาญ
อย่าพยายามเทียบ ให้เทียบที่**วิธีคิด** ไม่ใช่ปริมาณ

### ตัวสอบทาน (ทำเมื่อเหลือเวลา)

`hnswlib` หรือ `faiss` ใช้เป็น**ตัวสอบทานวิธีวัด** ไม่ใช่ระบบที่ทดสอบ
ถ้าคำนวณ recall บนข้อมูลชุดเดียวกันแล้วได้เลขใกล้กัน = ยืนยันว่าสูตรวัดของเราไม่ได้ผิดเอง

### เหตุผลที่ไม่ทำแบบ "ทดสอบ 5 ตัวเท่าๆ กัน"

| ทดสอบ 5 ตัว ตื้นๆ | ทดสอบ 1 ตัว ลึก + พิสูจน์ว่าทั่วไป |
|---|---|
| ได้บทความเทียบยี่ห้อ | ได้วิธีการที่ใช้ซ้ำได้ |
| หมดอายุใน 3 เดือน | ข้อสรุปอยู่ได้นาน |
| ตอบคำถามลึกไม่ได้เวลาถูกจี้ | ตอบได้ทุกคำถาม |
| ทุกคนทำได้ | ต้องมีวิธีวัดผลถึงจะทำได้ |

การวิเคราะห์เรซูเม่สายนี้ชี้ตรงกันว่า สิ่งที่แยกใบสมัครกลุ่มบนออกจากกลุ่มล่าง
**ไม่ใช่จำนวนเครื่องมือที่รู้ แต่คือความเฉพาะเจาะจงและหลักฐาน**

---

## 6. รายการ fault

> **รายการนิยาม fault ทั้งหมดย้ายไปอยู่ที่ [`FAULTS.md`](FAULTS.md) แล้ว**
> ไฟล์นั้นคือแหล่งอ้างอิงเดียว — ถ้าไฟล์นี้ขัดกับ `FAULTS.md` **ให้เชื่อ `FAULTS.md`**
>
> เหตุผลที่ย้าย: เคยมีรายการอยู่สามที่ (`PROJECT.md`, `EVIDENCE.md`, `CLAUDE.md`)
> แล้วมันเคลื่อนออกจากกันจนนับได้ 12 เท่ากันแต่เป็นคนละชุด — ดู D15

### ชุดที่ล็อกแล้ว

```
core   : F01  F03 (+F03b)  F05  F10                          = 4
vector : I01 I02 I03 I04 I05   Q01 Q02 Q03 Q04 Q06   V07 L02 = 12
```

**รวม 16 fault** — นิยามเต็ม อาการ วิธีฉีด ground truth และ assertion
ของทุกข้ออยู่ใน [`FAULTS.md`](FAULTS.md)

**ช่องว่างของเลข ID เป็นเรื่องตั้งใจ ห้ามเรียงใหม่ ห้ามใช้เลขซ้ำ**
เลขที่หายไปบอกว่าเคยพิจารณาแล้วตัดออก — ตารางเหตุผลอยู่ใน `FAULTS.md`
หัวข้อ "ที่พิจารณาแล้วไม่รับเข้าชุด" · `results/` และ `DECISIONS.md` อ้างเลขเดิมไปแล้ว

**Q01 คือตัวชูโรงของทั้งโปรเจค** เหตุผล:
- อธิบายง่ายที่สุด แต่ผลกระทบหนักที่สุด
- **ทำได้โดยไม่ต้องมีโมเดล AI เลย** recall เป็นตัวเลขล้วน ไม่สนใจว่า vector มีความหมายหรือไม่
- เป็นข้อที่คนทำ RAG แทบทุกคนไม่เคยวัด เพราะดูแต่ latency

**F03 คู่กับ F03b คือข้อที่พิสูจน์วิทยานิพนธ์ตรงที่สุด** — ข้อความ error
เหมือนกันทุกตัวอักษร แต่คนละสาเหตุ แยกได้ด้วย `pg_blocking_pids()` เท่านั้น

**I04 คือข้อที่แสดงว่า ANN index นำความไม่แน่นอนแบบใหม่เข้ามาในระบบฐานข้อมูล**
ระบบที่ควรจะ deterministic กลับสร้าง index ใหม่แล้วได้คำตอบไม่เหมือนเดิม
(สมมติฐานแรกโทษขั้นตอน k-means ของ IVFFlat — **การวัดหักล้างแล้ว** ดู E32:
HNSW ที่ไม่มี k-means ก็แกว่งเช่นกัน และปิด parallel build ก็ไม่หาย)

---

## 7. วิธีวัดผล — **ย้ายออกแล้ว 2026-07-27 (D18)**

> **หัวข้อนี้ถูกลบเนื้อหาทิ้ง แต่คงเลขข้อไว้**
> เพราะ `CLAUDE.md` · `DECISIONS.md` และ `sql/*.sql` อ้าง "`PROJECT.md` ข้อ 8 / 9 / 10"
> ถ้าเรียงเลขใหม่ การอ้างอิงทั้งหมดจะเลื่อน — หลักการเดียวกับที่ D15 ห้ามเรียงเลข fault ใหม่

**ทำไมถึงลบ:** เนื้อหาซ้ำกับที่อื่นเกือบทั้งหมด แล้ว**เคลื่อนออกจากกันจริง** —
ข้อนี้ยังสั่งวัด 4 ขนาด (100k / 500k / 2M / 5M) ซึ่งมีไว้รองรับ **L01 ที่ถูกตัดไปแล้วใน D15**
เป็นรูปแบบเดียวกับที่ D15 แก้ไปแล้วครั้งหนึ่ง (รายการ fault เคยอยู่สามที่แล้วเคลื่อนออกจากกัน)

**ตอนนี้อ่านที่ไหน**

| เรื่อง | อยู่ที่ |
|---|---|
| ฐานวัด recall ที่ล็อกแล้ว (salt · มิติ · กลุ่ม · noise · fingerprint) | `CLAUDE.md` หัวข้อ *ฐานวัด recall ที่ล็อกแล้ว* |
| โครงสร้างกลุ่มที่วัดได้ (ในกลุ่ม 0.3820 · ข้ามกลุ่ม 0.9997) | `CLAUDE.md` หัวข้อเดียวกัน · หลักฐาน `results/qf00_phase0_gate.txt` |
| ตัวชี้วัดและวิธีอ่าน (buffers · Heap Blocks · recheck) | `CLAUDE.md` กฎเหล็กข้อ **7** และ **7ก** |
| กฎการรันซ้ำ | `CLAUDE.md` หัวข้อ *กฎการรันซ้ำ* (ย้ายมาจากข้อนี้) |
| ขนาดข้อมูลที่วัดจริง | 100k และ 500k — `CLAUDE.md` หัวข้อ Q01 · เกณฑ์ผ่านเฟส 2 คือ **2 ระดับ** ไม่ใช่ 4 |
| ต้องบันทึกอะไรทุกครั้ง | `CLAUDE.md` กฎเหล็กข้อ 6 (ประทับเวอร์ชัน + วันที่) |

**เกณฑ์ผ่านของแต่ละเฟสไม่ได้เปลี่ยน** — ยังอยู่ที่ข้อ 9 ตามเดิม

---

## 8. สิ่งที่ส่งมอบ

| # | ของ | คำอธิบาย | จำเป็น |
|---|---|---|---|
| 1 | `quietfail-lab` | harness: docker compose + ตัวฉีด fault + ไฟล์เฉลย | ✅ |
| 2 | `quietfail-check` | ตัวตรวจที่รันได้จริง ออกเป็น GitHub Action ที่ block PR ได้ | ✅ |
| 3 | รายงานผล | ตัวเลขตอบ RQ1 + RQ2 พร้อมข้อจำกัด | ✅ |
| 4 | README | ตารางผลขึ้นก่อน วิธีติดตั้งทีหลัง | ✅ |
| 5 | ชุดโจทย์ AI + ผล | ตอบ RQ3 | ✅ |
| 6 | ร่างเล่มโครงงาน | จัดตามโครง CPE ของภาควิชา | ✅ |

> 📌 **สถานะจริงอยู่ที่ `CLAUDE.md` หัวข้อ *ของส่งมอบ* ซึ่งถูกตรวจด้วยเครื่อง**
> (`scripts/audit.py` เทียบกับ `scripts/audit_baseline.json` และเช็คไฟล์จริงสามทาง)
> ตารางนี้เป็น**ขอบเขตงาน** ถ้าขัดกัน **ให้เชื่อ `CLAUDE.md`**
>
> ข้อ 5 เคยติ๊ก ⬜ ค้างไว้ทั้งที่เก็บผลครบ 3 ครั้งแล้ว และข้อ 6 เพิ่มทีหลัง
> ไม่เคยถูกบันทึกที่นี่ — แก้แล้ว 2026-08-01 ตอนทวนเอกสารทั้งชุด

### `quietfail-check` ตรวจอะไรบ้าง

แบ่งตามระดับความมั่นใจ ตามหลัก 3 ชั้นที่ตกลงกันไว้:

**ยืนยันได้ → fail build ทันที**
- opclass ของ index ไม่ตรงกับ operator ที่โค้ดใช้ (I01)
- คอลัมน์ vector ที่ถูก query แบบ ORDER BY แต่ไม่มี index
- วัด recall จริงแล้วต่ำกว่าเกณฑ์ที่ตั้งไว้ (Q01)

**น่าจะใช่ → เตือน + บอกวิธีพิสูจน์**
- IVFFlat index ที่ถูกสร้างตอนตารางมีแถวน้อยกว่าเกณฑ์ (I02)
- `ef_search` / `probes` ยังเป็นค่า default (Q04)
- dead tuple ในตารางที่มี vector index เกินเกณฑ์ (L02)

**ห้ามแนะนำอัตโนมัติ**
- อะไรที่แตะ config ของ production หรือสั่งลบ/สร้าง index ใหม่ → บอกให้คนตัดสินใจ

---

## 9. เฟสและเกณฑ์ผ่าน

ทุกเฟสมีเกณฑ์ผ่านที่วัดได้ **ห้ามข้ามไปเฟสถัดไปถ้ายังไม่ผ่าน**

| เฟส | ชื่อ | ผ่านเมื่อ | สัปดาห์ |
|---|---|---|---|
| **0** | ตั้งหลัก | เอกสารนี้ + ชุด query + เกณฑ์วัดผล commit แล้ว | 1 |
| **1** | ปิดวงจร | **F01, F03, F05, F10** ฉีดซ้ำได้ 3 ครั้งติด มีไฟล์เฉลย มีสคริปต์นับคะแนน | 2 |
| **2** | ตัวชูโรง | **Q01** มีตัวเลข recall จริงที่ขนาดข้อมูล 2 ระดับ | 3–4 |
| **2.5** | ข้ามเครื่องมือ | **Q01 บน Qdrant** ได้ recall collapse เหมือนกัน + ตารางชั้น 3 | 5 |
| **3** | ครบชั้น | I01–I05, Q02–Q05 ครบ มีตารางผลรวม | 6–7 |
| **4** | เป็นซอฟต์แวร์ | `quietfail-check` รันใน CI แล้ว fail build ได้จริง | 8 |
| **5** | สอบทาน | L01, L02 + blind test จากเพื่อน | 9 |
| **6** | เล่าเรื่อง | README + รายงาน + บรรทัดเรซูเม่ | 10 |

> **เฟส 2.5 วางไว้ตรงนี้โดยตั้งใจ** — ทำทันทีหลัง Q01 สำเร็จ ขณะที่วิธีวัดยังสดอยู่ในหัว
> และทำ**ก่อน**ลงลึก fault ที่เหลือ เพราะถ้ารอไว้ท้าย จะไม่มีเวลาทำ
> ซึ่งแปลว่าเสียข้อสรุปที่สำคัญที่สุดของโปรเจคไป

**ปรับสัปดาห์ตามเวลาจริงได้ แต่ห้ามสลับลำดับ**

### ขั้นต่ำที่ส่งได้

**จบเฟส 2 = ส่งได้แล้ว** และยังเป็นโปรเจคที่ดี
ทุกอย่างหลังจากนั้นคือการเพิ่มน้ำหนัก ไม่ใช่การทำให้สมบูรณ์

นี่คือโครง "แกน 70% + ปีก 30%" ที่ตกลงกันไว้ — เฟส 0–2 คือแกน เฟส 3–6 คือปีก

---

## 10. ความเสี่ยง

| ความเสี่ยง | ระดับ | รับมือ |
|---|---|---|
| pgvector เวอร์ชันใหม่แก้ปัญหาบางข้อไปแล้ว โดยเฉพาะ Q03 | **สูง** | อ่าน CHANGELOG ของเวอร์ชันที่ใช้จริง**ก่อน**เขียนตัวฉีด ข้อไหนถูกแก้แล้วให้บันทึกว่า "แก้แล้วในเวอร์ชัน X" ซึ่งเป็นผลลัพธ์ที่มีค่าเช่นกัน |
| ขอบเขตบานไปหา vector DB ตัวอื่น | **สูง** | ตาราง "ไม่ทำ" ในข้อ 4 |
| vector สังเคราะห์ทำให้ recall ไม่มีความหมาย | กลาง | สร้างแบบมีกลุ่ม + ตรวจสอบว่า exact search ให้ผลที่สมเหตุสมผลก่อนวัดอะไร |
| ผลหมดอายุเพราะเวอร์ชันเปลี่ยน | กลาง | ประทับเวอร์ชัน+วันที่ทุกตาราง ออกแบบให้รันซ้ำได้ด้วยคำสั่งเดียว |
| ทำไม่ทัน | กลาง | เฟส 2 คือขั้นต่ำที่ส่งได้ |
| ถูกมองว่าต่อต้าน AI | ต่ำ | เขียนใน README ว่าใช้ AI ช่วยเขียนส่วนไหนของโปรเจคนี้เอง |
| ชื่อโปรเจคซ้ำกับของที่มีอยู่ | ต่ำ | ค้น GitHub/npm ก่อนตั้งจริง |

---

## 11. คำศัพท์ที่ต้องใช้ให้ถูก

ส่วนนี้สำคัญกว่าที่คิด เพราะการใช้คำผิดจะทำให้คนสัมภาษณ์ลดความเชื่อถือทันที

| ห้ามพูด | ให้พูด |
|---|---|
| "AI clustering" แบบเหมารวม | **ANN (approximate nearest neighbor) search** |
| "index ทำให้เร็วขึ้น" | "index ทำให้เร็วขึ้น**และเปลี่ยนคำตอบ**" |
| "AI หาไม่เจอ" | "recall@10 ลดลงเหลือ X%" |

**ข้อควรรู้:** IVFFlat ใช้ k-means ซึ่งเป็น clustering **จริงๆ** ดังนั้นคำว่า clustering
ถูกต้องเมื่อพูดถึง IVFFlat โดยเฉพาะ ส่วน HNSW เป็น**กราฟ** ไม่ใช่ clustering
การแยกสองอย่างนี้ออกจากกันได้ คือสัญญาณว่าเข้าใจจริง

**recall@k** = จากคำตอบที่ถูก k อันดับแรก ระบบหาเจอกี่อัน
เช่น เฉลยคือเอกสาร 10 ชิ้น ระบบเจอ 8 → recall@10 = 80%

---

## 12. บรรทัดเรซูเม่ (เขียนไว้ตั้งแต่ตอนนี้ เพื่อให้รู้ว่ากำลังเดินไปไหน)

> **QuietFail** — ชุดทดสอบความล้มเหลวเงียบของระบบค้นหาแบบเวกเตอร์
> ฉีด fault 12 รูปแบบที่รู้สาเหตุแน่นอน วัดด้วย exact search เป็นเฉลย
> พบว่าการตั้งค่า ANN index ที่ไม่มี error ใดๆ ทำให้ recall@10 ลดลงถึง **41%**
> และพบว่า HNSW กับ IVFFlat เสื่อมตามขนาดข้อมูลคนละแบบ เพราะค่าเริ่มต้นตัวหนึ่งเป็นค่าสัมบูรณ์ อีกตัวเป็นสัมพัทธ์
> **ยืนยันซ้ำบน 2 engine (pgvector, Qdrant) ด้วยเฉลยชุดเดียวกัน — recall loss เกิดทั้งคู่**
> **แต่ขนาดต่างกัน 2.5 เท่าเพราะค่าเริ่มต้นที่ผู้พัฒนาเลือก (ดู D16)**
> ส่งมอบเป็น GitHub Action ที่ block PR ได้

~~ช่องว่างจะถูกเติมหลังเฟส 2 ห้ามเดาตัวเลขใส่ก่อน~~
**เติมแล้ว 2026-07-27** จากการวัดจริงที่ 500,000 แถว (HNSW recall@10 = 0.5885)
หลักฐาน `results/q01_recall_collapse_500k.txt`

---

## 13. บันทึกสภาพแวดล้อม

```
PostgreSQL   : 17.10 (Debian 17.10-1.pgdg12+1)
pgvector     : 0.8.5
image        : pgvector/pgvector:0.8.5-pg17   ← ล็อกแล้ว ห้ามใช้ tag :pg17 เฉยๆ (ดู D14)
platform     : Windows 11 + Docker Desktop (WSL2)
dataset      : orders 200,000 แถว (23 MB)
config       : fragile (work_mem=64kB, max_connections=20)
วันที่ตั้งต้น   : 2026-07-24
ยืนยันเวอร์ชัน : 2026-07-26
```

คำสั่งที่ใช้ยืนยัน (รันซ้ำได้ทุกเมื่อ):

```powershell
docker compose exec db psql -U lab -d faultlab -c "SELECT extname, extversion FROM pg_extension WHERE extname='vector'"
```

> **ห้ามแก้ tag เป็น `:pg17`** — มันขยับตามเวอร์ชันใหม่โดยไม่มีอะไรเตือน
> ผลการทดลองทุกชุดในโปรเจคผูกกับ image ข้างบนเท่านั้น
