-- ============================================================
-- ตัวช่วยกลางของไฟล์ *_checker_states.sql — บังคับสูตรข้อ 6 ด้วยเครื่อง
--
-- ทำไมต้องมี (พบตอนทวน 2026-08-02):
--   ไฟล์ *_checker_states.sql ทั้ง 10 ตัวมีไว้ **พิสูจน์ว่าตัวนับคะแนนพลิกได้**
--   แต่ 9 ตัวไม่มี RAISE EXCEPTION หรือ ASSERT เลยสักบรรทัด — มันแค่ยิง
--   score.sql หลายรอบแล้วพิมพ์ผลออกมา
--
--   ผลคือ **ถ้าตัวนับคะแนนตอบเหมือนกันทั้งสามสถานะ ไฟล์ก็ยังจบด้วย exit 0**
--   แล้วผลถูก commit เข้า results/ เป็น "หลักฐานการพลิก" · audit ผ่านเพราะ
--   มันตรวจแค่ว่า**มีไฟล์** ไม่ได้อ่านว่าข้างในเขียนอะไร
--
--   = ความล้มเหลวเงียบชนิดเดียวกับที่โครงงานนี้ศึกษาอยู่ · รูปแบบเดียวกับ E30
--
-- ทำไมเป็นไฟล์กลาง ไม่ใช่ก๊อปไปทีละไฟล์:
--   บทเรียน E40 — สูตรที่เขียนซ้ำหลายที่จะเคลื่อนออกจากกัน เกิดขึ้นจริงกับ
--   fingerprint มาแล้ว จนต้องยุบเป็น qf_fingerprint() ตัวเดียว
--
-- วิธีใช้:
--   \i /sql/states_lib.sql                     -- ครั้งเดียวที่ต้นไฟล์
--   ... สร้างสถานะที่ 1 ...
--   \i /sql/score.sql
--   \set note 'มี fault'
--   \i /sql/states_capture.sql
--   ... สถานะถัดไป ...
--   \set expect 'DETECTED,NOT_DETECTED,CANNOT_CHECK'
--   \i /sql/states_assert.sql
--
-- ต้องตั้ง :fault ไว้ก่อนแล้ว (ตัวเดียวกับที่ score.sql ใช้)
-- ============================================================

DROP TABLE IF EXISTS qf_states_log;
CREATE TEMP TABLE qf_states_log (
    step    serial,
    fault   text,
    verdict text,
    note    text
);
